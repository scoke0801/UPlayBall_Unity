# 구단 / 시설 TODO

> 구현 완료. 이후 변경은 정규 지침 `docs/지침/Owner_Club_UI_Guidelines_UPlayBall.md`를 따른다.

## 디자인 정본

- 디자인 정본: [인벤 「첫화면 대기실! 하나하나 뜯어 보자」](https://m.inven.co.kr/webzine/wznews.php?idx=86747&searchwhere=writer&site=bm&svalue=%EA%B3%B5%EB%AF%BC%ED%99%98)
- 로컬 비교 이미지: docs/디자인/ref/구단경영_ref.png
- Route: Owner.Club.Facility
- 현재 상태: 구현됨
- 현재 View: UI_Scene_OwnerClubOperations
- 생성 배경: `Assets/Resources/UI/Generated/bg_owner_club_facilities_v1.png`
- 생성 프롬프트 요약: 텍스트·로고 없는 16:9 구장 단지, 훈련·회복·스카우팅·팬 시설을 한눈에 읽는 청회색/은색 아이소메트릭 Front Office 배경.

로컬 비교 이미지가 실제로 존재하면 구현 전에 반드시 view_image로 열어 화면 전체와 세부 비율을 확인한다. 인벤 기사와 로컬 이미지가 다르면 인벤 기사의 레이아웃 체계를 정본으로 삼고 로컬 이미지는 해당 세부 화면을 읽기 위한 보조 자료로 사용한다.

현재 UI는 보존 대상이 아니다. 정본과 차이가 크면 이 Route의 Presentation 계층과 레이아웃을 과감히 수정한다. 다만 현재 실제 Snapshot·Command와 공용 Shell/Simulation 경계는 유지한다.

## 현재 플로우

시설 → 구장/시설 Level·효과·비용 → Facility/Stadium Upgrade Command → 피드백.

## 레퍼런스 디자인 분석

기사의 정보 화면 문법으로 시설 목록과 선택 상세를 표현한다.

## 목표 레이아웃

좌측 시설 목록, 중앙 선택 시설 이미지/효과, 우측 다음 Level/비용/조건.

완료 기준은 분위기가 비슷한 화면이 아니다. 상단 탭 높이, 본문 여백, 목록/상세 폭, 카드 종횡비, CTA 위치와 정보 밀도를 정본에 가능한 동일하게 맞춘다. 16:9 확장분 때문에 원본 요소를 임의로 재배치하지 않는다.

## 목표 동작

시설 → 현재/다음 비교 → 비용/조건 → 확인 → Command → Level/자금 갱신.

- 진입 시 실제 Snapshot을 Bind하고 선택 변경은 View State로 관리한다.
- Command 전에 Preview/검증 결과를 표시하고 성공 후 Runtime Snapshot을 다시 Bind한다.
- Loading/Empty/Locked/Insufficient/Invalid/Error 상태를 같은 Content 영역에서 설명한다.
- Back은 진입 원점과 선택·필터·Scroll 상태를 복원한다.

## ImageGen 정의

화면 구현에 필요한 래스터 디자인은 built-in ImageGen으로 생성해 프로젝트 자산으로 연결한다.

구장·훈련장·분석실·의무실 삽화를 항목별 stylized-concept로 생성한다.

- 실행 방식은 built-in ImageGen이다.
- 입력 이미지는 reference image로 명시하며 원본 파일을 런타임 자산으로 복사하지 않는다.
- 결과물에는 텍스트, 실제 구단 로고, 상표, 워터마크를 넣지 않는다.
- Panel/Button/Table은 생성하지 않고 Native uGUI로 조립한다.
- 서로 다른 자산은 각각 별도 ImageGen 호출로 만든다.
- 구현 시 최종 프롬프트, 생성 결과, 프로젝트 저장 경로를 이 파일에 기록한다.

## 구현 TODO

- [x] 인벤 정본의 해당 영역을 다시 확인한다.
- [x] 존재하는 로컬 비교 이미지를 view_image로 실제 확인한다.
- [x] 현재 View 계층과 Snapshot/Command 연결을 확인한다.
- [x] 정본 기준 Layout을 정규 지침에 기록한다.
- [x] 시설 Route를 구장 요약과 시설 투자 목록의 2열 작업면으로 교체한다.
- [x] 공용 Shell Header/Navigation/Context 위치를 유지한다.
- [x] 시설 Campus 장식 배경을 별도 ImageGen 결과로 연결한다.
- [x] 현재/다음 Level·효과·비용 → 검증 → Upgrade Command → Rebind를 연결한다.
- [x] 최대 Level·리그 조건·부족 자금을 Resolver 사유와 비활성 CTA로 표시한다.
- [x] 실제 구현 상태와 남은 제한을 정규 지침에 갱신한다.

## 구현 결과와 제한

- `ClubUpgradeResolver`가 확정한 비용·조건·효과만 표시하고 `UpgradeFacility`/`UpgradeStadium` Command로 변경한다.
- 생성 배경은 `Assets/Resources/UI/Generated/bg_owner_club_facilities_v1.png`에 연결했다.
- 화면 해상도·문자 잘림·입력 육안 QA는 사용자가 수행한다.
