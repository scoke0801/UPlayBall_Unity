# 덕아웃 / 덕아웃 TODO

## 디자인 정본

- 디자인 정본: [인벤 「첫화면 대기실! 하나하나 뜯어 보자」](https://m.inven.co.kr/webzine/wznews.php?idx=86747&searchwhere=writer&site=bm&svalue=%EA%B3%B5%EB%AF%BC%ED%99%98)
- 로컬 비교 이미지: docs/디자인/ref/덕아웃_ref_1.png; docs/디자인/ref/덕아웃_감독_ref.png
- Route: Owner.Dugout.LineupNotes
- 현재 상태: 구현됨
- 현재 View: UI_Scene_OwnerDugout

로컬 비교 이미지가 실제로 존재하면 구현 전에 반드시 view_image로 열어 화면 전체와 세부 비율을 확인한다. 인벤 기사와 로컬 이미지가 다르면 인벤 기사의 레이아웃 체계를 정본으로 삼고 로컬 이미지는 해당 세부 화면을 읽기 위한 보조 자료로 사용한다.

현재 UI는 보존 대상이 아니다. 정본과 차이가 크면 이 Route의 Presentation 계층과 레이아웃을 과감히 수정한다. 다만 현재 실제 Snapshot·Command와 공용 Shell/Simulation 경계는 유지한다.

## 현재 플로우

덕아웃 → 6개 방침 Slider, 감독·수석코치, 요약 카드 → 임시 변경 → ConfigurationConfirmed.

## 레퍼런스 디자인 분석

기사의 덕아웃처럼 좌 40% 방침, 중 15% Staff, 우 45% 카드를 유지한다.

## 목표 레이아웃

현재 40:15:45 구조의 간격·카드 비율·선택 Popup을 원본에 더 가깝게 통일한다.

완료 기준은 분위기가 비슷한 화면이 아니다. 상단 탭 높이, 본문 여백, 목록/상세 폭, 카드 종횡비, CTA 위치와 정보 밀도를 정본에 가능한 동일하게 맞춘다. 16:9 확장분 때문에 원본 요소를 임의로 재배치하지 않는다.

## 목표 동작

방침/Staff 선택 → 적용값 Preview → 전술 요약 → 확정. 취소하면 Snapshot으로 복원한다.

- 진입 시 실제 Snapshot을 Bind하고 선택 변경은 View State로 관리한다.
- Command 전에 Preview/검증 결과를 표시하고 성공 후 Runtime Snapshot을 다시 Bind한다.
- Loading/Empty/Locked/Insufficient/Invalid/Error 상태를 같은 Content 영역에서 설명한다.
- Back은 진입 원점과 선택·필터·Scroll 상태를 복원한다.

## ImageGen 정의

화면 구현에 필요한 래스터 디자인은 built-in ImageGen으로 생성해 프로젝트 자산으로 연결한다.

감독·코치 초상, 전술 보드 질감, 카드 실루엣은 stylized-concept. Slider는 Native UI.

- 실행 방식은 built-in ImageGen이다.
- 입력 이미지는 reference image로 명시하며 원본 파일을 런타임 자산으로 복사하지 않는다.
- 결과물에는 텍스트, 실제 구단 로고, 상표, 워터마크를 넣지 않는다.
- Panel/Button/Table은 생성하지 않고 Native uGUI로 조립한다.
- 서로 다른 자산은 각각 별도 ImageGen 호출로 만든다.
- 구현 시 최종 프롬프트, 생성 결과, 프로젝트 저장 경로를 이 파일에 기록한다.

## 구현 TODO

- [x] 문서에 정의된 정본 구조와 로컬 비교 이미지를 확인했다.
- [x] 실제 Snapshot/Command와 공용 Shell Route 연결을 확인했다.
- [x] 좌 40% 방침 / 중 15% 인선 / 우 45% 요약의 상대 비율을 적용했다.
- [x] 감독·수석코치 실루엣과 후보 선택 Popup을 연결했다.
- [x] 진입 → 임시 편집 → 결정/되돌리기 → Runtime 재바인딩을 연결했다.
- [x] 후보 없음·Snapshot 없음·Command 오류 안내를 Content 안에서 처리했다.

## 실제 구현

- View: `UI_Scene_OwnerDugout`
- Snapshot/Command: `OwnerDugoutSnapshot`, `OwnerDugoutConfigurationCommand`
- 감독 자산: `Assets/Resources/UI/OwnerDugout/OwnerManager_Silhouette_V1.png`
- 수석코치 자산: `Assets/Resources/UI/OwnerDugout/OwnerHeadCoach_Silhouette_V1.png`
- 생성 기준: 실존 인물·구단 표식·문자 없이, 선수 카드와 같은 비식별 전신 실루엣으로 각각 생성했다.
- 적용 경계: 감독·코치·6축 방침을 한 Command로 적용하며 감독 변경 시 신뢰도 규칙은 Core 상태가 소유한다.
- 우측 2×2 영역은 빈 중앙 Text 카드가 아니라 제목 바, 세 지표 행, 0~100 Bar, 해석 문구를 가진 데이터 카드로 구성한다.
- 감독·수석코치는 판매 대상이 아니므로 하단 `판매` 버튼을 두지 않고 `결정`과 `취소`만 표시한다.
- 검증: Presentation 컴파일 성공. 사용자 요청에 따라 EditMode/PlayMode/시뮬레이션 테스트는 실행하지 않았다.
