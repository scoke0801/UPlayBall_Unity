# 상점 / 상점 TODO

## 디자인 정본

- 디자인 정본: [인벤 「첫화면 대기실! 하나하나 뜯어 보자」](https://m.inven.co.kr/webzine/wznews.php?idx=86747&searchwhere=writer&site=bm&svalue=%EA%B3%B5%EB%AF%BC%ED%99%98)
- 로컬 비교 이미지: docs/디자인/ref/상점_ref.png; docs/디자인/ref/스카우트_ref.png
- Route: Owner.Shop
- 현재 상태: 구현됨 — 전역 단일 Route, 상세/확인/Reveal/재구매 연결 완료
- 현재 View: UI_Scene_Shop

로컬 비교 이미지가 실제로 존재하면 구현 전에 반드시 view_image로 열어 화면 전체와 세부 비율을 확인한다. 인벤 기사와 로컬 이미지가 다르면 인벤 기사의 레이아웃 체계를 정본으로 삼고 로컬 이미지는 해당 세부 화면을 읽기 위한 보조 자료로 사용한다.

현재 UI는 보존 대상이 아니다. 정본과 차이가 크면 이 Route의 Presentation 계층과 레이아웃을 과감히 수정한다. 다만 현재 실제 Snapshot·Command와 공용 Shell/Simulation 경계는 유지한다.

## 현재 플로우

상점 → 추천/선수/스킬/작전 탭 → 2열 상품 Tile → 실제 확률 상세 → 최신 Quote 구매 확인 → 처리 중 입력 차단 → 구매 Command → Reveal → 재구매 또는 상품별 보관 화면.

## 레퍼런스 디자인 분석

기사의 상점처럼 추천/선수/스킬/작전 탭, 2열 Tile, 선물/구입 버튼 구조를 따른다.

## 목표 레이아웃

카테고리 전환 중 재화·가격·구매 제한 위치를 고정한다.

완료 기준은 분위기가 비슷한 화면이 아니다. 상단 탭 높이, 본문 여백, 목록/상세 폭, 카드 종횡비, CTA 위치와 정보 밀도를 정본에 가능한 동일하게 맞춘다. 16:9 확장분 때문에 원본 요소를 임의로 재배치하지 않는다.

### 구현 Layout 수치표

| 영역 | 구현값 | 정본 대응 |
|---|---:|---|
| Workspace 바깥 여백 | 16 px | 공용 Shell 안의 얇은 외곽 여백 |
| Panel Header | 46 px | 회색 제목 띠와 3 px Accent |
| 재화 요약 | 24 px | 카테고리가 바뀌어도 우측 고정 |
| 카테고리 탭 | 높이 40 px, 폭 116 px | 작은 직사각형 4개, 단일 선택 |
| 상태 안내 | 34 px | 잠금·부족·오류·긴 한글을 2줄까지 표시 |
| 상품 Grid | 2열, 간격 12 px | 원본 2열을 유지하고 16:9 폭만 Tile에 배분 |
| 상품 Tile | 높이 144 px, 열 폭 최소 280 px | 좌측 Pack / 우측 정보·CTA 비율 유지 |
| 상품 Artwork | 104 px 정사각 Frame | 원본 Pack 이미지 영역 |
| Tile CTA | 72×26 px | `상세`/`구입` 소형 버튼 |
| 상세 Overlay | 680×520 px | 구성·확률과 구매 조건을 한 화면에서 확인 |
| 구매 확인 Overlay | 520×310 px | 재화 차감 직전 최종 확인 |
| Reveal | 620×480 px | 결과명·신규/중복·재구매·보관 이동 |

Grid 열 폭은 `ContentSafeRect` 실제 폭에서 Padding과 간격을 뺀 뒤 2등분한다. 따라서 1280×720 이상에서 원본 요소의 순서는 유지하면서 가로 확장분이 상품 정보 영역에 배분된다.

## 목표 동작

카테고리 → 상품 → 상세 → 확인 → 처리 중 차단 → Reveal → 재구매/보관함.

- 진입 시 실제 Snapshot을 Bind하고 선택 변경은 View State로 관리한다.
- Command 전에 Preview/검증 결과를 표시하고 성공 후 Runtime Snapshot을 다시 Bind한다.
- Loading/Empty/Locked/Insufficient/Invalid/Error 상태를 같은 Content 영역에서 설명한다.
- Back은 진입 원점과 선택·필터·Scroll 상태를 복원한다.

## ImageGen 정의

화면 구현에 필요한 래스터 디자인은 built-in ImageGen으로 생성해 프로젝트 자산으로 연결한다.

Pack·티켓·Reveal 배경은 자산별 product-mockup/stylized-concept.

- 실행 방식은 built-in ImageGen이다.
- 입력 이미지는 reference image로 명시하며 원본 파일을 런타임 자산으로 복사하지 않는다.
- 결과물에는 텍스트, 실제 구단 로고, 상표, 워터마크를 넣지 않는다.
- Panel/Button/Table은 생성하지 않고 Native uGUI로 조립한다.
- 서로 다른 자산은 각각 별도 ImageGen 호출로 만든다.
- 구현 시 최종 프롬프트, 생성 결과, 프로젝트 저장 경로를 이 파일에 기록한다.

### 생성·연결 결과

- 선수 Pack
  - 프롬프트: `Create a single game UI product illustration for a Korean baseball management simulation shop... anonymous baseball player card pack... restrained silver and cool gray frame, navy and muted blue accents... No text, no letters, no numbers, no logos, no real team marks, no trademarks, no watermark, no buttons, no UI panel, no photoreal person.`
  - Reference: `docs/디자인/ref/상점_ref.png`
  - 저장: `Assets/10.Datas/Resources/UI/Shop/shop_player_pack.png`
- 스킬 Pack
  - 프롬프트: `Create a single game UI product illustration for a Korean baseball management simulation shop... sealed skill block pack... abstract interlocking training blocks and a subtle baseball diamond motif... No text, no letters, no numbers, no logos, no trademarks, no watermark, no buttons, no UI panel, no person.`
  - Reference: `docs/디자인/ref/상점_ref.png`
  - 저장: `Assets/10.Datas/Resources/UI/Shop/shop_skill_pack.png`
- Reveal 배경
  - 프롬프트: `Create a single decorative background asset for the purchase-result reveal... dark charcoal stage with restrained silver frame glow, soft stadium spotlights... empty center where native UI will place the acquired card... No text, no letters, no numbers, no player, no card, no logos, no trademarks, no watermark, no buttons, no UI labels.`
  - Reference: `docs/디자인/ref/스카우트_ref.png`
  - 저장: `Assets/10.Datas/Resources/UI/Shop/shop_reveal_background.png`
- 작전 상품은 기존 공용 자산 `Assets/10.Datas/Resources/UI/TacticCards/`의 타자·투수·공용 3종을 재사용한다.
- 티켓은 현재 `ShopProductKind`, Catalog, 지급 Command가 없으므로 장식만 발명하지 않았다. 실제 티켓 상품이 설계될 때 Product Definition과 Fulfillment를 먼저 추가한다.

## 구현 TODO

- [x] 인벤 정본의 해당 영역을 다시 확인한다.
- [x] 존재하는 로컬 비교 이미지를 view_image로 실제 확인한다.
- [x] 현재 View 계층과 Snapshot/Command 연결을 확인한다.
- [x] 정본 기준 Layout 수치표를 작성한다.
- [x] 현재 Presentation 레이아웃을 정본의 탭·2열 Tile·Reveal 구조로 보완한다.
- [x] 공용 Shell Header/Navigation/Context 위치를 유지한다.
- [x] 필요한 장식 자산을 자산별 ImageGen 호출로 생성하고 연결한다.
- [x] 진입 → 선택 → 상세/검증 → 구매 확인 → 확정 → Reveal → 재구매/보관 화면 플로우를 연결한다.
- [x] Loading/Empty/Locked/Insufficient/Invalid/Error와 긴 한글 문자열 상태를 처리한다.
- [x] 완료 후 실제 구현 상태와 남은 제한을 갱신한다.

## 구현 결과와 검증 범위

- `OwnerShopDetailsBuilder`가 Scout Joint Bucket과 Tactic Tier의 실제 후보군 재정규화 확률, Skill Gacha 공개 확률을 `ShopProductDetails`로 만든다. Presentation은 원본 가중치를 재계산하지 않는다.
- `UI_Scene_Shop`은 선택 탭과 탭별 Scroll을 보존하고, Back 입력 시 Reveal → 구매 확인 → 상세 순서로 Overlay 한 단계만 닫는다.
- 구매 확인 뒤 `SetProcessing`이 탭·Tile CTA·Overlay CTA를 잠가 같은 프레임의 중복 구매를 막는다. 구매가 끝나면 최신 Wallet/Quote Snapshot을 다시 Bind한다.
- Reveal의 보관 이동은 선수 카드 → 보유선수, 스킬 블록 → 카드훈련, 작전 카드 → 작전 설정 Route로 연결된다.
- Core, Game, Game.Unity 보조 컴파일은 상점 변경 반영 시점에 경고·오류 없이 통과했다. Presentation은 기존 하위 DLL을 참조한 독립 컴파일에서 경고·오류 없이 통과했다.
- Core/Game Test Assembly는 컴파일됐다. Unity Editor 밖에서는 TestRunner/UI 참조 경고가 남으며, Presentation Test Assembly 전체는 다른 UI Test들의 `UnityEngine.UI` 참조를 찾지 못해 실행하지 않았다. 상점 상세 표시 회귀 Test는 `ShopPresentationModelTests`에 추가했다.
- 별도 Console Smoke에서 `ShopService` 상세 조회와 `ShopPresentationModel`의 `62.50% · 후보 4장`, 잔여 구매 3회, Skill Pack Artwork 연결을 실제 호출해 통과했다.
- 최종 전체 의존 빌드는 동시에 진행 중인 구단주 선수시장·라인업 변경의 `ManagerModeMatchService`/`ManagerModeCoordinator` 오류에서 중단된다. 상점 범위 밖 파일이라 이 작업에서 수정하지 않았다.
- 사용자가 직접 확인하기로 한 화면 실행·Screenshot 비교와 PlayMode 테스트는 수행하지 않았다.
