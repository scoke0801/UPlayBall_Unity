# 덕아웃 / 감독방침 TODO

## 디자인 정본

- 디자인 정본: [인벤 「첫화면 대기실! 하나하나 뜯어 보자」](https://m.inven.co.kr/webzine/wznews.php?idx=86747&searchwhere=writer&site=bm&svalue=%EA%B3%B5%EB%AF%BC%ED%99%98)
- 로컬 비교 이미지: docs/디자인/ref/덕아웃_감독_ref.png; docs/디자인/ref/감독_ref.png
- Route: Owner.Dugout.ManagerPolicy
- 현재 상태: 구현됨 — 전용 6축 View/Command 연결
- 현재 View: UI_Scene_OwnerManagerPolicy

로컬 비교 이미지가 실제로 존재하면 구현 전에 반드시 view_image로 열어 화면 전체와 세부 비율을 확인한다. 인벤 기사와 로컬 이미지가 다르면 인벤 기사의 레이아웃 체계를 정본으로 삼고 로컬 이미지는 해당 세부 화면을 읽기 위한 보조 자료로 사용한다.

현재 UI는 보존 대상이 아니다. 정본과 차이가 크면 이 Route의 Presentation 계층과 레이아웃을 과감히 수정한다. 다만 현재 실제 Snapshot·Command와 공용 Shell/Simulation 경계는 유지한다.

## 현재 플로우

감독방침 → 전용 6축 편집 → 현재 인선·실제 경기 적용값 비교 → 결정/되돌리기 → 다음 경기 Snapshot 반영.

## 레퍼런스 디자인 분석

기사의 플레이방향 지시처럼 상반된 성향 축과 현재 설명을 같은 행에 둔다.

## 목표 레이아웃

방침 Panel을 확대하고 감독 성향·현재 Lineup 합성값을 우측 비교표로 표시한다.

완료 기준은 분위기가 비슷한 화면이 아니다. 상단 탭 높이, 본문 여백, 목록/상세 폭, 카드 종횡비, CTA 위치와 정보 밀도를 정본에 가능한 동일하게 맞춘다. 16:9 확장분 때문에 원본 요소를 임의로 재배치하지 않는다.

## 목표 동작

방침 조정 → 감독 성향 차이 → 판단 변화 설명 → 확정 → 다음 경기 Snapshot 반영.

- 진입 시 실제 Snapshot을 Bind하고 선택 변경은 View State로 관리한다.
- Command 전에 Preview/검증 결과를 표시하고 성공 후 Runtime Snapshot을 다시 Bind한다.
- Loading/Empty/Locked/Insufficient/Invalid/Error 상태를 같은 Content 영역에서 설명한다.
- Back은 진입 원점과 선택·필터·Scroll 상태를 복원한다.

## ImageGen 정의

화면 구현에 필요한 래스터 디자인은 built-in ImageGen으로 생성해 프로젝트 자산으로 연결한다.

성향 아이콘·노트 질감·선택 Marker는 stylized-concept. Slider/값은 Native UI.

- 실행 방식은 built-in ImageGen이다.
- 입력 이미지는 reference image로 명시하며 원본 파일을 런타임 자산으로 복사하지 않는다.
- 결과물에는 텍스트, 실제 구단 로고, 상표, 워터마크를 넣지 않는다.
- Panel/Button/Table은 생성하지 않고 Native uGUI로 조립한다.
- 서로 다른 자산은 각각 별도 ImageGen 호출로 만든다.
- 구현 시 최종 프롬프트, 생성 결과, 프로젝트 저장 경로를 이 파일에 기록한다.

## 구현 TODO

- [x] 존재하는 `덕아웃_감독_ref.png`를 실제 확인했다. 문서에 병기된 `감독_ref.png`는 저장소에 없다.
- [x] 좌 59% 6축 편집 / 우 35% 인선·적용값 / 하단 CTA를 적용했다.
- [x] 신뢰도에 따른 허용 범위, 중립 초기화, 저장 상태 되돌리기를 연결했다.
- [x] 감독·수석코치 인선은 덕아웃 탭에 남기고 이 화면은 방침만 책임지도록 분리했다.
- [x] Slider 변경 즉시 다음 경기의 Batting/Running/SmallBall/PinchHit/Hook/Bullpen 판단값 Preview와 능력치 직접 보정 없음 원칙을 표시한다.
- [x] Dugout View Route 변환을 제거하고 전용 Route/View로 연결했다.

## 실제 구현

- View: `UI_Scene_OwnerManagerPolicy`
- Snapshot/Command: `OwnerDugoutSnapshot`, `OwnerDugoutConfigurationCommand`
- Preview: `DugoutTacticalProfileResolver.PreviewPolicyChange()`가 현재 인선 합성값에 방침 변경분만 적용한다.
- 적용 경계: 선택된 감독·수석코치 ID는 보존하고 `DugoutPolicySettings` 여섯 값만 교체한다.
- 자산: 성향 표식과 Slider는 Native uGUI로 구성해 추가 래스터를 생성하지 않았다.
- 검증: Presentation 컴파일 성공. 사용자 요청에 따라 테스트와 대량 시뮬레이션은 실행하지 않았다.
