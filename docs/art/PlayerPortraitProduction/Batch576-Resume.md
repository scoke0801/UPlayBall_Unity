# 576명 생성 작업 재개 결과

중단 기록과 디스크를 대조한 결과 `sheet-002`~`sheet-036`의 크로마키 원본 35장과
프롬프트·외형 매핑이 이미 저장되어 있었다. 첫 시트는 이전 회색 배경 파일럿이었다.
내장 ImageGen과 고정 `sheet-576-v2` 마스터·기준 이미지를 사용해 첫 시트의
`face-0001`~`face-0016`을 크로마키 원본 `sheet-001__r02.png`로 새로 생성했다.
기존 파일럿과 이전 산출물은 보존했다.

작업 루트는 `output/imagegen/player-portraits/production-576-v1/`이다.

| 산출물 | 선택 경로 |
| --- | --- |
| 첫 시트 원본 | `sheets/raw/sheet-001__r02.png` |
| 나머지 원본 | `sheets/raw/sheet-002__r01.png`~`sheet-036__r01.png` |
| 새 실제 프롬프트 | `prompts/sheet-001__r02.txt` |
| 전체 검증·선택 목록 | `manifests/generation-576-resume-audit.json` |

36개 PNG를 디코딩하고 실제 크기와 제작 기록을 대조했다. 모두 1254×1254이며,
2048 요청보다 작게 반환된 원본이다. 원본·프롬프트 해시 72건 일치,
행 우선 셀 매핑과 `face-0001`~`face-0576`의 ID 누락·중복 0건을 확인했다.

새 첫 시트는 4×4 정면 피규어와 녹색 배경을 시각 확인했다. 셀 상하 여백은 요청보다 좁으므로
후속 분할에서 실제 좌표와 모자·상의 경계를 확인해야 한다.
**576명분의 생성 원본 확보 단계가 완료됐으며, 576개 고유 얼굴의 최종 승인을 의미하지 않는다.**
전체 얼굴 유사성 검수·유니폼 10종 결합·게임 적용은 미완료다. 상태는 `draft`로 유지한다.

이번 재개에서는 원본 생성·저장과 무결성 검증을 수행했다. 기존 160명 투명화 결과는
`Batch160-20260909.md`의 r06 기록을 따른다. 나머지 분할·투명화는 수행하지 않았다.
다음 작업에서 생성 ID를 추가 발급하기 전에 이 전체 선택 목록을 기준으로 남은 검수를 진행한다.
