# 구현 현황

이 문서는 2026-08-30 기준으로 저장소에 **실제로 존재하는 코드**를 조사해 정리한 현황이다. 기획 의도는 `BaseballManager_PROJECT.md`가 기준이고, 이 문서는 "그중 무엇이 이미 만들어져 있는가"만 다룬다. 새 게임 생성부터 Rookie~Galaxy 다단계 리그 승강, 정규시즌·포스트시즌·오프시즌 성장·계약-은퇴 통합 흐름까지 커리어 전체 루프가 구현되어 있으며, `CLAUDE.md`도 현재 단계를 Career Stabilization으로 갱신했다.

이 문서의 초판(2026-08-28 조사)은 새 게임~시즌~오프시즌 성장~계약 1주기가 처음 갖춰진 시점을 기록한 것이었다. 이후 다단계 리그 승강(Rookie~Galaxy), 구위 기반 정밀 경기 엔진(`DetailedMatchEngine`), 감독 AI 라인업 편성, 계약-은퇴 통합 흐름과 은퇴 회고 시스템, 시즌 서사(내러티브)·연출, 상황별 BGM 재생 등 다수의 기능이 추가되어 이번 조사에서 반영했다.

각 항목은 클래스/파일 경로 중심으로 요약한다. 세부 동작·수치 근거는 코드와 EditMode 테스트를 직접 참고할 것.

## 목차

- [1. 어셈블리 레이어 현황](#1-어셈블리-레이어-현황)
- [2. Baseball.Core](#2-baseballcore)
- [3. Baseball.Simulation](#3-baseballsimulation)
- [4. Baseball.Game](#4-baseballgame)
- [5. Baseball.Presentation](#5-baseballpresentation)
- [6. Baseball.Editor](#6-baseballeditor)
- [7. 정적 데이터 (Assets/10.Datas)](#7-정적-데이터-assets10datas)
- [8. 테스트 커버리지](#8-테스트-커버리지)
- [9. 확인된 갭 / 후속 확인 필요 사항](#9-확인된-갭--후속-확인-필요-사항)

## 1. 어셈블리 레이어 현황

`docs/지침/Simulation_Architecture_Guidelines_UPlayBall.md`가 정의한 5개 레이어가 실제로 asmdef로 분리되어 있고, 대응하는 테스트 asmdef도 존재한다.

| 어셈블리 | 경로 | 대응 테스트 |
|---|---|---|
| `Baseball.Core` | `Assets/02.Scripts/Core/` | `Assets/Tests/EditMode/Core/` |
| `Baseball.Simulation` | `Assets/02.Scripts/Simulation/` | `Assets/Tests/EditMode/Simulation/` |
| `Baseball.Game` | `Assets/02.Scripts/Game/` | `Assets/Tests/EditMode/Game/` |
| `Baseball.Presentation` | `Assets/02.Scripts/Presentation/` | `Assets/Tests/EditMode/Presentation/`, `Assets/Tests/PlayMode/Presentation/` |
| `Baseball.Editor` | `Assets/02.Scripts/Editor/` | (에디터 전용, 별도 테스트 없음) |

Core·Simulation은 `UnityEngine` 미참조 순수 C#로 실제 구성되어 있다(조사한 클래스 전부 POCO/struct/static class).

## 2. Baseball.Core

`Assets/02.Scripts/Core/` — 데이터 모델과 밸런스 계수. 전부 Unity 비의존.

### 2.1 선수/구단 데이터 모델 (`Players/`, `Teams/`)

- `Player.cs` — 경기 입력용 선수 신원 + 현재 능력치. `GetPositionProficiency(position)` 제공.
- `BatterAttributes.cs` / `PitcherAttributes.cs` — 0~100 범위 불변 `readonly struct` 능력치.
- `PositionProficiency.cs` — 포지션별 0~100 적응도.
- `PlayerPosition.cs` — `PlayerType`, 수비 포지션·투수 역할 enum, `Handedness`.
- `BattingApproach.cs` — 타석마다 선택하는 타격 접근 방식(균형/컨택/장타/신중).
- `CareerCreationRules.cs` — 실행 캐릭터 생성 규칙의 유일한 원장. 기본 50, 타자 자유 배분 60,
  투수 40, 항목 상한 75다. 구 `CharacterCreationBalance`와 `AttributeAllocation`은 삭제했고,
  테스트용 간편 입력은 이 규칙으로 정규화하는 호환 어댑터만 남겼다.
- `Team.cs` — 라인업 + 선발투수 + 지정 이닝부터 등판할 구원투수 보관. `GetCurrentPitcher(inning)`.
- `Lineup.cs` — 9인 타순 + 중복 없는 수비 배치 검증, 포지션 적응도 반영 팀 평균 수비력 계산.
- `LineupSlot.cs` — 타순 한 자리(선수+포지션).
- `PlayerGameRole.cs` — 감독 AI가 경기 중 실제로 부여한 기용 역할.
- `TeamArchetype.cs` / `TeamArchetypeLibrary.cs` — 재정/육성/선수층/스카우트 등급 축과 5개 기본 구단 아키타입.
- `TeamIdentityDefinition.cs` — 팀 이름 후보/색상(`TeamColor`), 계약 시 예상 출장 역할.

### 2.2 성장 시스템 런타임 상태 (`Growth/`)

새 게임부터 시즌 성장·부상·계약까지 이어지는 커리어 성장 시스템의 데이터가 이미 상세하게 갖춰져 있다.

- `PlayerAbility.cs` — 성장/노쇠를 개별 추적하는 능력치 식별자, 노쇠 계통, 훈련 활동 계통, 타자/투수별 카탈로그.
- `PlayerGrowthState.cs` / `AbilityRatings.cs` — Base Ability·Potential·Peak Bonus·능력치별
  DevelopmentProgress. 고정 순서 배열과 정수 진행도로 결정론을 보장한다.
- `GrowthResultRecord.cs` — 능력치·Potential·Peak 변화, 재현용 입력 스냅샷, Seed와
  `SimulationVersionStamp`를 포함한 세이브 가능 성장 결과.
- `CareerEvaluation.cs` — 스프링캠프 평가 입력/결과, 계약 시장 평가 입력, 감독 육성 스타일 enum.
- `InjuryState.cs` — 부상 기록(원인+예상 결장), 설명 가능한 부상 확률 입력, 심각도 enum.
- `OffseasonState.cs` — 12주 시간 예산 기반 오프시즌 활동 계획 + 조기 마감 처리.
- `SeasonUsageSummary.cs` — 시즌 자연 성장용 실제 출장 활용량 비율.
- `PlayerStudyState.cs` — 유학 프로그램 방문 이력.
- `TrainingProgramDefinition.cs` — 프로그램별 능력치 가중치/기간/비용/성장 채널.
- `SkillBlockDefinition.cs` / `SkillBoardState.cs` — 테트로미노 7종 스킬 블록 + 4×4 성장판 + Trait Socket 배치 상태(스킬 뽑기/보드 시스템 데이터 모델).
- `CareerEconomyState.cs` — 만원 단위 정수 Money + 수입지출 이력.

### 2.3 밸런스 테이블 (`Balance/`)

`BalanceTable.cs`가 아래 계수 묶음을 버전과 함께 최상위에서 컨테이닝한다. Game 레이어가 SO에서 이 구조체로 변환해 시뮬레이터에 주입한다.

| 구조체 | 조정 대상 |
|---|---|
| `PlateDisciplineBalance` | 투구별 Ball/Swing/Contact 확률 |
| `BattedBallBalance` | 인플레이 타구→안타/장타 변환(홈런·2루타·3루타 등) |
| `BaseRunningBalance` | 주자 추가 진루/득점/병살 확률 |
| `BattingApproachBalance` | 타격 접근 방식별 확률 보정 |
| `ContractOfferBalance` | 새 게임 계약 오퍼 여부/금액 |
| `ContractBonusBalance` | 계약 상여 조건 목표/보상 |
| `TeamGenerationBalance` | Rookie League 구단·포지션 경쟁자 생성 |
| `PlayerEvaluationBalance` | 신인 평가(포지션 적합도·구단 성향) |
| `CareerSeasonBalance` | 정규시즌 일정(기본 8구단 80경기), 기용 판단, 경기 후 상태 변화 |
| `RosterTurnoverBalance` | 시즌 전환 시 타 구단 로스터 변동 |
| `PostseasonBalance` | 포스트시즌 시리즈 길이(준결승 3전2선승/결승 5전3선승) |
| `SeasonAwardBalance` | 수상 후보 자격/평가 가중치 |
| `SeasonSettlementBalance` | 수상/포스트시즌 Money 배율 |
| `InjuryBalanceTable` | 경기/훈련 부상 위험 요인·심각도 분포 |
| `CareerGrowthEvaluationBalance` | 스프링캠프 평가 가중치, 계약 시장 가치 6요소 가중치 |
| `GrowthBalanceTable` | 나이-성장 곡선, 잠재력 격차, Work Ethic, 훈련 적합도, 컨디션, 반복, 자연 성장, 노쇠, 스킬 뽑기 확률 등 성장 관련 하위 테이블 묶음 |

`GrowthSkillContent.cs`는 기본 4×4 성장판 레이아웃과 타자/투수 계통별 스킬 뽑기 풀을 정적으로 제공한다.
`GrowthContentValidator.cs`는 프로그램·블록 ID, 기간, 티어 누락, Trait, 뽑기 풀과 판매가 정합성을 검사한다.

### 2.4 규칙 상수 (`Rules/`)

`BaseballRules.cs` — 3아웃, 4볼, 9이닝 등 시뮬레이션 전역이 공유하는 야구 규칙 상수.

## 3. Baseball.Simulation

`Assets/02.Scripts/Simulation/` — Core 데이터 + 주입된 밸런스/RNG로 실제 확률 계산과 판단 로직을 수행.

### 3.1 타석/경기 시뮬레이션 (`PlateAppearance/`, `Match/`)

- `PlateAppearanceSimulator` (+ `IPlateAppearanceSimulator`) — 밸런스와 RNG를 주입받아 투구 수부터 타구 결과까지 계산. 입력은 `PlateAppearanceMatchup`(타자/투수/수비), 출력은 `PlateAppearanceOutcome`.
- `MatchSimulator.cs`(+ `.Baserunning.cs` `.Events.cs` `.Fielding.cs` `.Results.cs`) — 두 팀 타순+선발투수로 결정론적 9이닝 경기 진행. 인플레이 결과를 실제 야수 수비 기회로 귀속.
- `DetailedMatchEngine.cs`(+ `.State.cs` `.Tactics.cs` `.Events.cs` `.Results.cs`) — 구위(Stuff)·투구수 부하·전술 판단을 반영한 실제 경기 엔진. 커리어 진행과 `MatchSimulator.Simulate` 공개 경로가 모두 이 엔진을 사용한다. 따라서 현재 대량 통계 테스트도 커리어 경기 공식을 직접 검증한다.
- `MatchEvent.cs` — `Pitch/Contact/Hit/RunnerAdvance/Score/Out/PlateAppearanceEnded/HalfInningEnded/MatchEnded` 이벤트 타입을 갖는 할당 없는 `readonly struct`. `IMatchEventSink`(`MatchEventSink.cs`)가 시뮬레이션→표현 전달 계약.
- `MatchInput.cs` / `MatchResult.cs` / `MatchDecision.cs` — SeasonId/GameId/RandomSeed 포함 입력, 완료 경기 결과(BoxScore+이벤트 스트림), 투구 전 플레이어 결정 요청.
- `BoxScore.cs` — 타격/투구/수비 라인 + 팀 이닝별 득점 누적.

`MatchSimulator` → `DetailedMatchEngine` → `MatchEvent` 스트림 → Presentation 계약이 실제 코드 구조로 구현되어 있다.

### 3.2 결정론적 RNG (`Random/`)

- `IRandomSource` — 시뮬레이터가 주입받는 난수 계약.
- `Pcg32Random` — 플랫폼 무관 PCG32 구현.
- `DeterministicSeed` — 기준 Seed에서 타석/성장/오프시즌 등 용도별 독립 Seed를 결정론적으로 파생.

### 3.3 성장 판정 로직 (`Growth/`)

- `PlayerGrowthFactory` — 캐릭터 생성 능력치에서 초기 Potential+성장 상태 생성.
- `GrowthResolver` — 훈련 프로그램 기대 성장값을 영구 능력치 변화로 변환.
- `NaturalDevelopmentResolver` — 시즌 활용 역할 기반 소폭 자연 성장.
- `AgingResolver` — Physical/Technical/Mental 계통별 시즌 노쇠 적용.
- `InjuryResolver` — 피로/나이/출장량/훈련강도/내구도 → 부상 결과.
- `ManagerRoleEvaluator` — 스프링캠프에서 현재 전력과 실제 경쟁자 차이로 역할 결정.
- `ContractMarketEvaluator` — 최근 성과+안정 전력 기반 0~100 시장 가치.
- `OffseasonScheduler` — 12주 시간 예산 겹침/유학/파트너/비용 제약 검증.
- `AiOffseasonPlanner` — AI 선수도 동일 공식으로 결정론적 오프시즌 계획 생성.
- `SkillBoardService` / `SkillGachaService` — 블록 배치 검증(회전/경계/겹침/Trait Socket),
  보너스 체감·상한, 리그 제한과 보장 카운트 있는 확률적 뽑기.
- `EffectiveAbilityResolver` — Base/Skill/Peak/Condition/Injury/Tactics를 한 경로에서 분해하고
  경기·역할·계약·UI가 사용할 Roster/Current/Match Ability를 제공한다.

### 3.4 커리어/시즌 시뮬레이션 (`Career/`)

- `TeamGenerator` / `NewGameSetup` / `GeneratedTeam` — 새 게임 시작 시 Rookie League 구단 결정론적 생성(포지션 필요도, 같은 포지션 경쟁자 포함).
- `SeasonScheduleGenerator` — 정규시즌 일정 생성.
- `PostseasonBracket` — 4강 토너먼트 대진/라운드.
- `ContractOfferEvaluator` / `ContractOfferBoard` / `ContractOffer.cs` — PlayerValue×PositionNeed×TeamBudget×TeamPreference×ScoutVariance 공식으로 오퍼 여부·조건 계산, 구단별 오퍼를 점수순(동점 시 TeamId 오름차순)으로 정렬해 실제 제시 목록 생성. 새 게임 입단과 재계약이 같은 로직을 공유.
- `RosterCompetitor` / `RosterTurnoverResolver` — 시즌 전환 시 타 구단 로스터 Overall을 포지션 필요도 기준선으로 회귀+변동.
- `PlayerValueEvaluator` / `PlayerBuildAdvisor` — 선수 가치 계산, 포지션-능력치 배분 어긋남 경고(생성 차단 아님).
- `ManagerUsageAi` — 계약 역할+경쟁자+컨디션+감독 평가로 한 경기의 실제 기용을 결정. "구단 로스터 편성은 감독 AI가 소유한다"는 프로젝트 경계를 구현한 핵심 클래스.
- `ManagerLineupAi` — 감독 AI가 매 경기 타순과 수비 위치를 결정(`8723702`). 플레이어가 직접 라인업을 편성하지 않는다는 경계를 실제로 지킨다.
- `PlayerRetirementResolver` — 나이·전력·시장가치·성향 계수로 은퇴 여부를 결정론적으로 판정(`9bcc015`). 계약 오퍼의 상위 리그 이적 조항·강등 시 이적요청권 판단과 함께 계약-은퇴 통합 흐름의 시뮬레이션 축을 이룬다.

## 4. Baseball.Game

`Assets/02.Scripts/Game/` — Unity 의존. 세이브 대상 런타임 상태 소유 + 서비스 계층 + 매니저 부트스트랩.

### 4.1 커리어 진행 (`Career/`)

**상태 클래스 (세이브 루트)**
- `CareerState` — 새 게임 계약 완료 후 선수/리그/계약 상태 묶음.
- `SeasonState` — 시즌 식별자, `LeagueLevel`, `SeasonPhase` 진행 상태.
- `LeagueState` / `TeamState` / `PlayerState` — 구단·선수 세이브 상태. 구단은 ID로만 참조.
- `PostseasonState` — 4강 토너먼트 대진/진행/우승 구단.
- `SeasonScheduleState` — 시즌 경기 Seed·대진·완료 결과·구단 순위 기록.
- `SeasonAwardState` 계열 — 수상 후보 점수 + 확정 수상.
- `SeasonReviewState` — 시즌 결산 화면 진행 순서.
- `LeagueSeasonStatisticsState` 계열 — 리그 전체 원본 타격/투구/수비/경기 기록.
- `PlayerSeasonStatisticsState` — 내 선수 시즌 누적 기록.
- `PlayerContractState` — 현재 계약.

**서비스 (진행 로직)**
`CareerManager`(+ `.Growth.cs` `.Schedule.cs` `.Retirement.cs` partial)가 진입점이고, 아래 서비스들이 각 단계를 담당한다: `CareerSeasonService`(정규시즌 라운드 진행), `CareerSeasonTransitionService`(오프시즌 전환/재계약 오퍼 대기), `CareerSeasonAutoCompletionService`(남은 시즌 자동 진행), `CareerPostseasonService`(포스트시즌), `CareerGrowthService`(시즌 결산+오프시즌 성장), `SeasonAwardService`(수상 결정), `SeasonSettlementService`(급여/수상/포스트시즌 보너스 지급, 원장 ID로 중복 지급 방지), `ContractBonusService`(계약 상여 조건/달성 계산), `ContractRenewalService`(재계약/타 구단 이적 협상), `LeagueStatisticsService`(경기 결과→리그 통계 누적), `LeagueSeasonRolloverService`(시즌 전환 시 리그 승강 반영), `LeagueHubService`(리그 순위/기록/일정 정렬), `CareerRecordsService`(+ `.Totals.cs`, 기록 화면 투영), `CareerScheduleViewBuilder`, `TeamOverviewBuilder`, `PlayerProfileViewBuilder`, `SeasonDateCalculator`, `CareerGameRunner`(정규/포스트시즌 공통 판단·집계 경로, `DetailedMatchEngine` 사용), `CareerMatchSession`(경기 준비/진행/커밋 단계와 재생), `CareerMatchPlayback`(타석 단위 이벤트 공개), `RetirementRecapService`(+ `.Recording.cs` `.Selection.cs` `.SnapshotBuilder.cs` `.Archive.cs`, 커리어 종료를 은퇴 회고·기록관 스냅샷으로 고정).

### 4.1.1 다단계 리그 월드 (`World*`, `CareerWorldFactory`)

`SeasonState.LeagueLevel` enum이 `Rookie`~`Galaxy`(다단계) 등급을 정의하고, `CareerWorldFactory`가 이 전 등급 리그와 전역 선수 레지스트리를 결정론적으로 생성한다(`ba7829a`, `a290b1b`). `WorldState`(세이브 루트) + `WorldSeasonService`/`WorldSeasonLifecycleService`(리그별 시즌 동시 진행/전환), `WorldPostseasonService`, `WorldOffseasonMarketService`(+ `WorldOffseasonMarketPlan`, 오프시즌 시장 개편), `WorldAiPlayerDevelopmentService`(AI 선수 성장), `WorldRecordState`(월드 전역 기록), `WorldGenerationConfiguration`이 다단계 리그를 지원한다. Rookie 단일 리그였던 최초 설계에서 Rookie/Minor/Major 동시 진행 → Rookie~Galaxy 다단계로 확장된 이력이 있다.

### 4.1.2 시즌 서사(내러티브) · 사운드

- `CareerNarrativeState` / `MatchNarrativeSnapshot` / `CareerNarrativeNewsEvaluators` — 경기·시즌 결과를 뉴스 피드·대시보드 서사 문구로 변환(`f2c9e0a`, `06adae7`). `UI_Scene_CareerDashboard.Narrative.cs`가 대시보드에 노출한다.
- `Game/Sound/` — `SoundManager`(매니저), `BgmDirector`(상황별 BGM 전환), `BgmSituation`(enum), `BgmPlaylistDefinition`(SO), `SoundConfiguration` — 정규시즌/포스트시즌/내 선수 타석 등 상황별 배경음악 재생 시스템(`1497258`). 설정 Popup이 열린 동안 중계 입력·BGM 전환을 멈추는 처리 포함.

### 4.2 새 게임 흐름

`NewGameFlow`(상태 전이) + `NewGameFlowState`(`NewGameStep` enum) + `NewGameManager`(영속 상태 소유, UI 읽기 모델 제공) + `NewGameConfiguration`(SO→순수 C# 변환) — 캐릭터 생성 → 구단 계약 → Rookie League 시작까지 이어진다.

### 4.3 씬 흐름 / 매니저 부트스트랩 / 입력

- `SceneId.cs`(`SceneId`, `SceneCatalog`, `SceneTransitionMode`), `SceneLoadManager`(+ `.Loading.cs`), `SceneContext`, `BootSceneController`, `SceneLoadRequest` — Boot/Loading/콘텐츠 씬 전환 상태.
- `GameManager`(`[DefaultExecutionOrder(-10000)]`) + `GameBootstrap` + `ManagerBehaviour<T>` + `IManager`/`IUpdatableManager`/`ISceneChangedManager` — 매니저 생명주기 계약.
- `InputManager` + `InputContext`/`InputContextLease` — Input System 액션맵과 화면별 입력 컨텍스트를 push/pop으로 안전하게 관리.

## 5. Baseball.Presentation

`Assets/02.Scripts/Presentation/` — Unity 의존 UI. Career 탭 8개 화면이 실제 구현되어 있다.

### 5.1 UI 프레임워크

`UIBase`(RectTransform+CanvasGroup 필수) → `UISceneBase`(화면 점유)/`UIHudBase`(경기 HUD)/`UIPopupBase`(팝업)/`UISystemBase`(로딩 등 최상위). `UILayer`(표시 우선순위), `UIManager`(등록/표시 스택/Cancel 입력), `UIRoot`, `PresentationBootstrap`(Scene 프리팹 유무와 무관하게 UI 기반 준비).

### 5.2 커리어 화면 (`Presentation/Career/`)

`CareerTabNavigation`(`CareerMainTab` enum, `ICareerTabScreen`)이 하단 8탭을 통합 라우팅한다.

| 화면 클래스 | 담당 |
|---|---|
| `UI_Scene_CareerDashboard` | 다음 경기 · 내 선수 역할/기록 중심 홈 |
| `UI_Scene_CareerGrowth`(+ Formatting/Rendering) | 4×4 성장판, 블록 상점, 오프시즌 주차 액션 |
| `UI_Scene_CareerMatch`(+ Playback) | 경기 준비→선수 중심 진행→결과 |
| `UI_Scene_CareerRecords`(+ Formatting) | 리그 순위+내 시즌/통산/수상 기록 |
| `UI_Scene_CareerSchedule`(+ Chrome/Content) | 월간 달력/목록/홈-원정 스플릿 전체 일정 |
| `UI_Scene_Contract` | 현재 계약/상여 진행/시장 가치/오퍼 선택 |
| `UI_Scene_League` | 리그 순위+기록 경쟁 비교 |
| `UI_Scene_NewGame` | 선수 생성→구단 계약→시작 마법사 |
| `UI_Scene_Player`(+ Formatting/Rendering/Helpers) | 내 선수 상태/능력/성장 근거/커리어 |
| `UI_Scene_Team`(+ Helpers/Roster) | 로스터/다음 경기 기용/포지션 경쟁/구단 정책 |

각 화면은 대응하는 `*PresentationBootstrap`(`CareerRecordsPresentationBootstrap`, `ContractPresentationBootstrap`, `GrowthPresentationBootstrap`, `LeaguePresentationBootstrap`, `NewGamePresentationBootstrap`, `PlayerPresentationBootstrap`, `SchedulePresentationBootstrap`, `TeamPresentationBootstrap`, `RetirementRecapPresentationBootstrap`)로 독립 생성/수명주기 관리된다.

`UI_Popup_RetirementDecision`/`UI_Popup_RetirementRecap`이 계약-은퇴 통합 흐름의 은퇴 확정·회고 화면을 담당하며, 모든 커리어 화면에 공용 설정 진입점(Popup)이 통합되어 있다(`ffa2101`).

## 6. Baseball.Editor

`Assets/02.Scripts/Editor/` — CLAUDE.md의 "통합 툴 런처" 지침대로 구성되어 있다.

- `BaseballToolsLauncher`(`EditorWindow`) — 프로젝트 에디터 도구를 자동 발견해 한곳에서 실행.
- `BaseballEditorToolAttribute` — 정적 도구를 런처가 발견하는 메타데이터.
- `NewGameDataAssetGenerator` — 새 게임 기본 정적 정의 생성.
- `SceneFlowSceneGenerator` — 표준 씬 4개 + Build Settings 순서 생성.
- `UIEditorTools` — UI 기반 프리팹 생성 + 정적 검증(4개 도구 등록).
- `UIScriptGeneratorWindow`(`EditorWindow`) — UI 클래스/폴더 구성 생성.
- `GrowthContentValidationTool` — 통합 툴 런처에서 성장 프로그램·Skill Block·뽑기 풀의 정적 정합성을 검사한다.

**주의:** 밸런스 대량 시뮬레이션 전용 에디터 메뉴 도구는 여전히 `[BaseballEditorTool]` 목록(Unity 에디터 내부)에는 없다. 대신 저장소 루트 `Tools/`에 Unity 에디터 밖에서 돌리는 독립 콘솔 프로젝트들이 추가되었다(`40a0dd1`, `c69b794`, `ffe1e26`, `8ad837f`):

- `Tools/SimulationDiagnostics` — 기본 10,000경기를 실제 `DetailedMatchEngine` 공개 경로로 실행해
  리그 평균 지표를 집계한다. `--growth-cohort 1000 20`은 실제 성장·노쇠·역할 평가기로 백업 복귀,
  Potential 도달과 프로그램 선택 집중도를 검증한다. 성장·역할 모드는 리그 월드·계약·경제를 생략한
  선행 코호트이므로 전체 커리어 분포로 해석하지 않는다.
- `Tools/PureSimulationTests` — Unity 없이 돌리는 콘솔 테스트 러너. `--testName` 필터, `[Explicit]` 테스트 건너뛰기, 상세 예외 출력 지원.
- `Tools/ContractFlowRetirementTests`, `Tools/RetirementRecapTests` — 리그 승강·계약-은퇴 통합 흐름, 은퇴 회고 스냅샷 전용 검증 콘솔 프로젝트.

CLAUDE.md·`Balance_Testing_Guidelines_UPlayBall.md`는 "EditMode 테스트나 콘솔 러너로도 돌 수 있어야 한다"고 명시하므로, Editor 메뉴가 아닌 콘솔 프로젝트로 존재하는 현재 형태는 지침 위반이 아니다.

## 7. 정적 데이터 (Assets/10.Datas)

실제로 존재하는 ScriptableObject 에셋은 `NewGameDefinition.asset`(`Assets/10.Datas/Resources/NewGame/`) 1개뿐이다. `NewGameDefinition.cs`(`Game/Data/`)가 정의를 선언한다.

CLAUDE.md가 예시로 든 `TeamDefinition`/`PlayerArchetype`/`LeagueDefinition`/`StadiumDefinition`/`NameDatabase` 등의 SO는 이 폴더에 없다. 대신 팀 생성·아키타입은 Core의 순수 C# 구조체(`TeamArchetypeLibrary`, `TeamGenerationBalance` 등)로 구현되어 있어, 밸런스/팀 데이터 대부분이 SO가 아니라 코드 내 정의로 대체된 상태다.

## 8. 테스트 커버리지

2026-08-30 소스의 `[Test]`/`[TestCase]`/`[UnityTest]` 선언은 381개다. TestCase 하나에 여러
인수가 붙는 경우 실제 발견 테스트 수는 더 많을 수 있으므로, 이 값은 자동 집계 가능한 선언 수로만 관리한다.

| 영역 | 테스트 선언 |
|---|---:|
| Core EditMode | 10 |
| Simulation EditMode | 140 |
| Game EditMode | 188 |
| Presentation EditMode | 28 |
| PlayMode | 15 |

Core 10개와 Simulation 140개는 Unity 비의존 실행으로 통과했다. Game은 Unity 네이티브 API를 쓰지 않는
핵심 성장·새 게임·리그 이동·계약 전환 40개를 별도 실행해 통과했고, 전체 asmdef 컴파일도 통과했다.
PlayMode 15개는 열린 Editor의 라이선스 채널과 충돌해 이번 작업에서 실행하지 못했으므로 통과로 기록하지 않는다.

결정론 테스트와 통계 테스트가 파일명(`*StatisticsTests`)으로 명확히 구분되어 있고, Core/Simulation 각각에 asmdef가 대응해 Unity 없이 검증 가능한 구조가 실제로 작동한다.

## 9. 확인된 갭 / 후속 확인 필요 사항

조사 중 발견했지만 이 문서 작성 범위를 벗어나 임의로 손대지 않은 항목들. (2026-08-28 초판의 항목 중 해소된 것은 갱신하고, 남은 것만 유지한다.)

1. ~~`SaveVersion` 필드 미확인~~ — **해소됨.** 현재 `CareerState`/`SeasonState`/`WorldState`/`LeagueState`/`TeamState`/`PlayerState` 등 주요 세이브 관련 클래스 대부분이 `SaveVersion`을 갖고 있다. 단 **디스크 세이브가 아직 없어** v7~v16 마이그레이션 체인(`CareerSaveMigrationService`)은 2026-09-02에 제거했다. 실제 세이브를 구현할 때 `SaveVersion = 1`부터 다시 시작한다(`BaseballManager_PROJECT.md` 41.12절).
2. ~~밸런스 대량 시뮬레이션 전용 툴 부재~~ — **부분 해소됨.** `BaseballToolsLauncher`(Unity 에디터 메뉴)에는 여전히 없지만, 저장소 루트 `Tools/SimulationDiagnostics` 콘솔 프로젝트가 N경기 자동 실행 + 통계 집계를 수행한다(6절 참고). Editor 메뉴로 통합할지는 판단 사항으로 남겨둔다.
3. **`Assets/10.Datas`에 정적 데이터 SO가 거의 없음** — `NewGameDefinition`, `BgmPlaylistDefinition` 등 소수뿐이며, CLAUDE.md가 예시로 든 `TeamDefinition`/`LeagueDefinition`/`StadiumDefinition`/`NameDatabase` 등은 아직 SO로 만들어지지 않았다. 현재 방식(코드 내 정의)이 의도적 설계인지, 아직 SO 저작이 밀린 것인지는 확인이 필요하다.
4. ~~`CLAUDE.md`의 현재 단계 드리프트~~ — **해소됨.** Career Stabilization 단계로 갱신했다.
5. **실제 1,000명×20시즌 월드 코호트 정기 실행 필요** — 경기와 성장 단위 대량 검증은 있으나,
   역할 복귀·리그 체류·경제·계약·은퇴를 실제 월드 상태로 한 번에 집계하는 정기 보고서는 아직 부족하다.
