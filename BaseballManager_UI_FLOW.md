# UI 플로우 가이드 — 선수 중심 대시보드

`BaseballManager_PROJECT.md` 40절(새 게임 플로우)로 계약을 맺은 선수가 커리어 내내 보게 될
메인 화면과 그 주변 화면 구조를 정의한다. `BaseballManager_CAREER_ECONOMY.md`(계약/오프시즌/
성장 경제)의 확정 사항을 화면으로 옮긴 문서다.

**상태: 정규 시즌 첫 Vertical Slice 구현 (2026-08-27).** 2절의 6구역 홈 대시보드, 다음 경기
기용 표시, 즉시 경기 진행, 시즌/개인 기록, 포지션 경쟁, 최근 결과, 향후 5경기까지 연결했다.
하단 8개 탭은 정보 구조만 표시하며 아직 전환되지 않는다. 내 선수 한 명의 벤치 대타 투입은
첫 Vertical Slice가 연결됐고, 복수 대타·대주자·대수비, 전체/핵심 장면 관전, 부상·오프시즌
변형 화면은 후속 범위다. 구현 상세와 대량 시뮬레이션 수치는 `BaseballManager_PROJECT.md`
40.8절과 40.14절을 따른다.

## 1. 목표와 원칙

구단 운영 게임과 달리 이 게임에서 플레이어가 화면을 열 때마다 가장 먼저 답을 원하는 질문은
네 가지뿐이다.

```text
나는 지금 주전인가?
다음 경기는 언제인가?
내 컨디션과 최근 성적은 어떤가?
지금 무엇을 하면 되는가?
```

메인 화면은 이 네 질문에 한 화면 안에서 답하는 **선수 중심 대시보드**로 잡는다. 구단 순위,
전체 로스터, 트레이드 같은 운영 정보는 메인이 아니라 하위 탭으로 내린다 — CLAUDE.md의
경계("플레이어가 팀을 운영하지 않는다")를 화면 우선순위에도 그대로 적용한다.

기준 해상도는 PC `2560×1440 / 16:9`. 다른 해상도 대응은 이 문서 범위 밖이다.

## 2. 레이아웃 개요 — 정규 시즌 메인

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ UPlayBall        2028 ROOKIE LEAGUE     5월 18일     보유금 3,850만원      │
├───────────────┬───────────────────────────────────────┬─────────────────────┤
│               │                                       │                     │
│   PLAYER      │              NEXT GAME                │      SEASON         │
│               │                                       │                     │
│ ┌───────────┐ │       부산 마리너스 VS 서울 블루윙스 │   팀 순위     4위   │
│ │  선수     │ │               5월 19일 / HOME         │   24승 21패         │
│ │  카드     │ │                                       │                     │
│ │           │ │       [ 예상 역할 : 선발 SS ]         │   개인 성적         │
│ └───────────┘ │   상대 선발  박정우  ERA 3.41         │   AVG .286 HR 7     │
│ 김도윤        │                                       │   RBI 31 OPS .812   │
│ SS / 21세     │            [ 경기 진행 ]              │                     │
│ OVR 67        │                                       │ 최근 5경기          │
│ 컨디션 ●●●●○  │                                       │ .333 / 1HR / 4RBI   │
│ 감독평가 72   │                                       │                     │
├───────────────┼───────────────────────────────────────┼─────────────────────┤
│ 팀 내 경쟁    │               NEWS / EVENT            │   UPCOMING          │
│ SS            │ "김도윤, 최근 5경기 타율 .333"        │ 05/19 서울   HOME   │
│ 김도윤    67  │ "주전 유격수 경쟁이 계속되고 있다."  │ 05/20 서울   HOME   │
│ 박재성    65  │                                       │ 05/22 대전   AWAY   │
│ 신인 최민석61 │                                       │ 05/23 대전   AWAY   │
├───────────────┴───────────────────────────────────────┴─────────────────────┤
│ 홈 │ 경기 │ 선수 │ 팀 │ 리그 │ 커리어                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

6구역(좌/중/우 × 상/하) + 상단 바 + Primary Navigation으로 고정한다. 시즌 상태(정규 시즌/부상/오프시즌)에
따라 각 구역의 **내용**은 바뀌지만 **구역 배치 자체는 유지**한다 (16절).

## 3. 좌측 — 내 선수 패널

화면의 기준점. 선수 카드 + 현재 커리어 상태를 보여준다.

```text
┌─────────────────┐
│   선수 카드     │
└─────────────────┘

김도윤
SS / 우투우타 / 21세
OVR 67

컨디션     86
감독 평가  72

현재 역할
● 주전
```

`현재 역할`을 이 패널에서 가장 크게 보여준다. 값은 39절/`BaseballManager_CAREER_ECONOMY.md`
11~12절이 정의하는 역할 집합과 정확히 일치시킨다.

```text
주전 / 주전 경쟁 / 플래툰 / 대타 / 백업 / 2군 / 부상자 명단
```

역할마다 색상/배지를 구분하되, 21절의 등급 컬러 규칙을 재사용하지 않는다 — 역할 배지와
카드 희귀도 배지는 서로 다른 색 체계를 쓴다(혼동 방지).

카드를 클릭하면 선수 상세 화면(24절 `Team > 선수 상세`에 대응, 새 구조에서는 `선수` 탭)으로
이동한다. 카드에는 앞면(이미지/능력치)과 뒷면(성적/4×4 성장판)이 있고 탭해서 전환한다.

## 4. 중앙 — 다음 경기 (메인 CTA)

정규 시즌은 경기 일정 중심으로 진행되므로(`BaseballManager_CAREER_ECONOMY.md` 20.1절)
메인 화면에서 가장 큰 영역이자 실질적 메인 CTA다.

```text
NEXT GAME

부산 마리너스 VS 서울 블루윙스
5월 19일 / HOME

예상 역할 : 선발 SS
```

`예상 역할`은 상대 선발 정보보다 우선순위가 높다 — 플레이어에게는 상대 전력보다 "내가
뛰는가"가 항상 먼저다 (22절 우선순위 참고). 표시 가능한 값:

```text
예상 역할 : 선발 SS
예상 역할 : 벤치
예상 역할 : 대타 대기
부상으로 결장 예정
```

### 4.1 경기 진행 버튼

가장 큰 버튼. 상황별 문구를 바꿀 수 있다.

```text
선발  → [ 경기 시작 ]
벤치  → [ 경기 관전 ]
결장  → [ 경기 결과 보기 ]
```

문구 분기가 구현 복잡도를 높이면 `[ 경기 진행 ]` 하나로 통일해도 된다 — 문구는 UX 다듬기
단계에서 결정할 사항이지, 기능 분기가 필요한 요구사항은 아니다.

### 4.2 벤치에서도 경기 진행에 의미를 준다

벤치/대기 상태에서도 버튼을 누를 이유가 있어야 한다.

```text
오늘의 역할: 벤치
예상 출전 상황: 대타 / 대주자
```

경기 진행 중 실제로 기회가 오면 즉시 게임 내 이벤트로 전환한다.

```text
8회말 1사 2루
감독이 당신을 대타로 호출합니다.
[출장]
```

이 흐름은 `BaseballManager_CAREER_ECONOMY.md` 13절(대타 출전도 완결된 경험)과 동일한
이벤트를 화면으로 옮긴 것이다. 시뮬레이션 쪽에서 이 대타 기회를 `MatchEvent` 스트림의 한
이벤트로 노출해야 Presentation이 별도 폴링 없이 반응할 수 있다 — Core/Simulation 문서
(아키텍처 절)의 이벤트 스트림 계약을 그대로 따른다.

첫 구현에서는 벤치 야수를 대타 후보로 경기 이벤트 스트림 안에 유지한다. 출전 전에는 자동
중계를 보거나 `경기 종료까지 진행`으로 즉시 완료할 수 있고, 감독의 `PlayerSubstitution`
이벤트가 발생하면 해당 타순에서 자동 진행을 멈추고 내 선수 타격 입력으로 전환한다.

## 5. 우측 상단 — 시즌 현황

리그 정보는 최소한만 노출한다.

```text
2028 시즌
팀 4위 / 24승 21패

김도윤
AVG .286 / HR 7 / RBI 31 / OPS .812
```

투수 선수라면 지표 세트를 교체한다.

```text
ERA 3.21 / W-L 5-2 / IP 62.1 / SO 58 / WHIP 1.18
```

지표 세트는 `PlayerRole`(타자/투수)에 따라 Presentation이 선택하되, 계산 자체는 Simulation
레이어의 누적 기록을 그대로 읽는다 — 화면 전용 재계산을 만들지 않는다.

### 5.1 최근 성적

```text
최근 5경기
.333 / 1 HR / 4 RBI
```

또는 폼 표시용 도트.

```text
최근 10경기
● ● ○ ● ○ ● ● ● ○ ●
```

목적은 정확한 수치 나열이 아니라 "최근 폼이 좋다/나쁘다"가 한눈에 들어오는 것이다.

## 6. 좌측 하단 — 팀 내 경쟁

이 게임의 메인 UI 중 가장 차별화된 구역이다. "내 자리가 안전하지 않다"는 긴장을 상시
노출한다.

```text
SS Depth
1. 김도윤   67   ▲
2. 박재성   65   -
3. 최민석   61   ▼

현재 역할: 주전
```

경쟁자보다 OVR이 낮아도 계속 주전일 수 있다 — UI에는 OVR만 간략 표시하지만, 내부 판단은
능력치·최근 성적·컨디션·감독 평가·수비 적합성·좌우 상성을 함께 쓰는
`ManagerAi`(39.6절)의 결과다. 화면이 "OVR 높은 순 = 주전"으로 오해되지 않도록, 경쟁 목록에
화살표(추세) 외에 숫자 정렬 자체를 OVR 순으로 강제하지 않는다 — 실제 역할 순서를 그대로
보여준다.

## 7. 중앙 하단 — 뉴스/이벤트 피드

별도 스토리 대신 리그가 만들어내는 사건을 보여주는 구역.

```text
"김도윤, 최근 5경기 타율 .333"
"서울 블루윙스가 FA 유격수 이진우를 영입했습니다."
"박재성이 햄스트링 부상으로 2주간 결장합니다."
감독: "김도윤에게 좀 더 많은 기회를 줄 생각이다."
```

이 피드는 장식이 아니라 게임 상태 변화 피드다 — 각 항목은 실제 게임 이벤트(부상, 영입,
역할 변경, 감독 코멘트)에서 생성하고, 화면 전용으로 지어낸 텍스트를 따로 만들지 않는다.

## 8. 우측 하단 — 일정

```text
05/19  서울   HOME
05/20  서울   HOME
05/22  대전   AWAY
05/23  대전   AWAY
05/24  대전   AWAY
```

현재 경기는 강조 표시. 부상 중에는 결장 예정 경기를 함께 보여준다.

```text
05/19   결장
05/20   결장
05/22   복귀 예상
```

## 9. 상단 바

최대한 얇게 유지한다.

```text
UPlayBall   2028 ROOKIE LEAGUE   5월 18일   보유 자금 3,850만원   [설정] [저장]
```

Money는 성장의 핵심 자원(`BaseballManager_CAREER_ECONOMY.md` 2절)이므로 항상 노출한다.

## 10. 공용 Shell Navigation

Global Navigation은 6개 업무 영역으로 고정하고 실제 기능은 Local Navigation으로 연다.
메뉴와 업무 패널의 시각 기준은 `docs/디자인/ref/`이며, 작은 직사각형 탭·밝은 회백색 패널·얇은
청회색 구분선·조밀한 표 형태를 공통 Shell에 적용한다.

```text
홈 / 경기 / 선수 / 팀 / 리그 / 커리어
```

| 업무 영역 | Local Navigation |
|---|---|
| 홈 | 단일 선수 중심 Dashboard |
| 경기 | 다음 경기 / 일정 / 경기 결과 |
| 선수 | 선수 정보 / 능력치 / 성장 / 스킬 |
| 팀 | 선수단 / 선발 라인업 / 투수진 / 팀 정보 (편성 불가) |
| 리그 | 순위 / 타자 순위 / 투수 순위 / 리그 기록 |
| 커리어 | 계약 / 시즌 기록 / 통산 기록 / 수상·하이라이트 |

Navigation Route는 Local 단계에서 끝난다. 기록의 타격/투수/수비/주루, 정규/포스트시즌,
핵심/전체 표시는 화면 내부 Filter/Selector로 처리한다. 선수 모드의 팀 화면은 감독 AI 판단을
설명하는 읽기 전용 화면이며 Owner 편성 Command를 노출하지 않는다.

### 10.1 `커리어 > 시즌/통산 기록` — 핵심 수치에서 상세 기록표로 확장

기록 화면은 야구 시뮬레이션 게임의 선수 기록표처럼 `개인 / 시즌 / 역대 / 수상 / 경기 로그`를
한 흐름에서 오가며, `정규시즌 / 포스트시즌` 기록을 절대 합산하지 않고 범위 선택으로 분리한다.
타격은 `G / GS / PA / AB / R / H / 2B / 3B / HR / RBI / BB / HBP / SO / SF / GIDP /
TB / AVG / OBP / SLG / OPS / BB-K`, 투구는 `G / GS / W / L / SV / HLD / BS / IP / H / R /
ER / HR / BB / HBP / SO / BF / QS / ERA / WHIP / K-BB / HR9`까지 원본에 존재하는 지표를 노출한다.
수비와 주루 역시 성공률만 보여 주지 않고 기회·처리·아웃·보살·실책·병살·어려운 타구 처리와
도루 시도 결과를 함께 보여 결과를 설명할 수 있게 한다.

첫 진입은 `전체 지표`이며 `핵심` 보기로 정보량을 줄일 수 있다. 전체 지표는 글자를 축소해 한 화면에
억지로 넣지 않고 표 하단 가로 스크롤을 사용한다. 시즌·수상·상세 지표 목록은 행을 자르지 않고
세로 스크롤로 끝까지 확인한다. 최근 경기 로그는 선수 역할에 맞춰 타자는 `AB/H/HR/RBI/BB/HBP`,
투수는 `IP/ER/SO/BB/HBP`를 보여 주며, 쓰이지 않는 반대 역할 열을 0으로 채우지 않는다.

`구단` 탭은 열람 전용이다. 로스터·라인업·전술을 플레이어가 직접 편성하는 화면을 여기에
만들지 않는다 — CLAUDE.md가 명시한 "플레이어가 팀을 운영하는 기능은 만들지 않는다"는 경계를
UI 탭 설계에도 그대로 적용한다.

## 11. `선수 > 성장` — 성장 보드 / 오프시즌 액션

정규 시즌 중에는 열람만 가능하다. 실질적인 재배치는 오프시즌에만 연다
(`BaseballManager_PROJECT.md` 39.5절 "배치는 휴식일과 오프시즌에 무료로 바꿀 수 있다").

```text
GROWTH
┌──┬──┬──┬──┐
│██│  │  │  │
├──┼──┼──┼──┤
│██│██│  │  │
├──┼──┼──┼──┤
│  │██│  │  │
├──┼──┼──┼──┤
│  │██│██│██│
└──┴──┴──┴──┘

장착 효과: 교타 +5 / 수비 +3 / 정신 +2
다음 블록 변경 가능 시점: 오프시즌
```

성장 내부는 `[성장 보드] [오프시즌 액션]` 두 서브 탭으로 분리한다. 기본 진입은 성장 보드이고,
훈련·유학처럼 Money와 주차를 함께 쓰는 행동은 오프시즌 액션 탭에만 둔다.

성장 보드는 `선수 요약 18% / 보드 42% / 보관함 40%`의 3열 구조를 사용한다. 우측 전체는
동일 정의 블록을 스택한 고정 카드 그리드이며, 계통·N/R/E/U/L·현재 보드 배치 가능 필터를
제공한다. 카테고리는 블록 내부 색상, 희귀도는 외곽 프레임과 문자 배지로 구분한다.

상점은 우측 상단의 `[블록 뽑기]` 하나로 열리는 별도 오버레이다. 오버레이는 N/R/E/U/L 단계형
선택기, 전체/계통 선택, 최소 보장, 상세 확률, 결제 전후 금액, 1회/5회 구매, 잠금·부족·잔여 횟수
사유를 한 화면에 보여 준다. 구매 결과는 결제 명령에서 즉시 결정해 보관함에 저장하고, 오버레이를
닫은 뒤 새 블록을 선택 상태로 이어 준다.

보드 조작은 임시 편집 후 확정한다. 배치 가능한 기준 칸만 청록색으로 강조하고, 실제 장착 블록은
카테고리 내부색과 희귀도 외곽선으로 경계를 표시한다. 하단에서 현재 효과/변경 후 효과와 안전 회수
비용을 비교한 뒤 `[편집 취소] [초기화] [변경 적용]`으로 확정한다.

## 12. `커리어 > 계약`

```text
부산 마리너스
계약 기간 2028~2030 / 연봉 4,500만원 / 잔여 기간 2년

상여 조건
100경기 출장     +500만원   [███████████-------] 67/100
20홈런            +800만원
OPS .850          +500만원
올스타            +700만원
리그 우승         +1,000만원
```

상여 조건의 진행도 바는 시즌 중에도 갱신되어 동기부여 요소로 작동한다. 상단 바의 보유 자금을
클릭해도 이 탭으로 진입할 수 있게 한다.

## 13. 부상 상태에서의 메인 화면

부상은 경기 정보보다 우선순위가 높다 — 중앙 영역을 경기 CTA 대신 부상 정보로 교체한다.

```text
INJURY
햄스트링 염좌
예상 복귀 12일 / 예상 결장 8경기
[재활 상태 확인]
```

일정 구역의 다음 경기 표시는 `결장`으로 바뀌고, 경기 진행 버튼은 `[ 경기 결과 진행 ]`처럼
관전 전용 문구로 바뀐다. `BaseballManager_CAREER_ECONOMY.md` 9절(부상 시 선택: 계속 출전/
치료/전문 치료)의 선택 UI는 이 부상 정보 카드 안에서 진입한다.

## 14. 오프시즌 전환 — 메인 화면 자체가 바뀐다

시즌이 끝나면 같은 `홈` 탭이지만 레이아웃 내용이 오프시즌 대시보드로 전환된다. 구역 배치
틀(2절)은 유지하되 각 구역의 내용이 바뀐다.

```text
┌──────────────────────────────────────────────────────────────────┐
│ 2028 OFF-SEASON                         보유금 8,720만원          │
├─────────────────┬───────────────────────────────┬────────────────┤
│ 시즌 결과       │        OFF-SEASON             │ 계약 상태      │
│ 126경기         │       남은 기간 12주          │ 부산 / 1년 남음│
│ AVG .301        │                               │ 연봉 4,500만원 │
│ HR 18 / RBI 74  │       [훈련 선택]             │                │
│ ALL STAR        │                               │                │
├─────────────────┤                               ├────────────────┤
│ 획득 상여금     │                               │ 구단 뉴스      │
│ +2,700만원      │                               │ FA 영입...     │
└─────────────────┴───────────────────────────────┴────────────────┘
```

### 14.1 오프시즌 액션 카드

중앙에는 남은 주차가 가장 크게 보인다.

```text
OFF-SEASON
12주 남음
```

행동은 카드 형태로 나열한다. `BaseballManager_CAREER_ECONOMY.md` 20.2절의 예산제(주+Money)
값을 그대로 카드에 노출한다.

```text
┌──────────────────┐   ┌──────────────────┐
│ 개인 타격 훈련  │   │ 일본 유학       │
│ 3주 / 300만원    │   │ 6주 / 3,000만원  │
│ 교타 ▲▲          │   │ 변화구 ▲▲▲▲     │
└──────────────────┘   └──────────────────┘
```

액션 카드를 선택한 뒤 실행을 한 번 누르면 해당 활동의 전체 기간을 즉시 진행하고, 잔여 주차와
보유금·성장 결과를 바로 반영한다. 활동 기간을 1주씩 반복 클릭하게 만들지 않는다.

```text
남은 기간   12주 → 6주
보유금     8,720만원 → 5,720만원
```

훈련·유학 카드는 서로 다른 화면으로 바로 실행하지 않고 공통 `선수 성장 활동 확정 팝업`을 연다.
팝업은 선수 정보 아래에 `남은 주차 전→후 / 보유금 전→후 / 컨디션 전→후 / 완료 주차`를 먼저
보여주고, 왼쪽 프로그램 목록과 오른쪽 능력치 현재값→예상 범위·적합도·반복 효율·부상 위험을
배치한다. 하단에는 12주 전체 타임라인과 결과 요약을 유지한다. 훈련만 강도 탭을 노출하고 유학은
같은 골격에서 특별 성장 가능성과 넓은 결과 범위를 강조한다. 실행 불가 이유는 해당 요약 카드와
CTA 바로 위에 표시하며 버튼은 `3주 훈련 시작`, `8주 유학 확정`처럼 행동과 기간을 함께 쓴다.

`성장 계획에 담기`를 누른 활동은 팝업을 닫아도 12주 타임라인에 누적된다. 다음 활동의 기간·비용·
Condition 미리보기는 이미 담긴 계획 이후를 기준으로 계산하며, 확정 CTA는 `3개 성장 계획 실행`처럼
누적 개수를 표시하고 계획 전체를 순차 실행한다. 타임라인의 계획 시작 칸에서 아직 시작하지 않은
활동을 삭제할 수 있고, 삭제한 기간은 뒤 계획을 당겨 즉시 회수한다. 앞 활동 후 Condition이 다음
활동의 최소 조건에 못 미치면 실행 전에 차단하고 회복 활동을 먼저 담도록 설명한다.

### 14.2 구단 활동 피드 (우측)

```text
TEAM ACTIVITY
FA 유격수 박성현 영입 (OVR 72, SS)
당신과 포지션이 겹칩니다.
```

오프시즌에도 AI 구단이 함께 움직인다는 것(`BaseballManager_CAREER_ECONOMY.md` 10절)을
화면으로 계속 상기시키는 구역이다.

## 15. 스프링캠프 화면

오프시즌 마지막 단계, 시즌 개막 직전에 별도 화면으로 진입한다
(`BaseballManager_CAREER_ECONOMY.md` 20.4절).

```text
2029 SPRING CAMP
SS 경쟁

박성현   72        감독 평가  74
김도윤   70        감독 평가  71
최민석   63        감독 평가  55

2029 ROLE: 플래툰
```

결과 확인 후 정규 시즌 메인(2절)으로 복귀하며 시즌이 개막한다.

## 16. 시각적 방향

프로야구매니저류 UI의 정보 밀도는 가져오되 그대로 복제하지 않는다.

**가져올 것:** 작은 선수 카드, 능력치 바, 구단 로고, 숫자 중심 표기, 업무 영역과 표의 명확한 경계.

**바꿀 것:** 화면 전체를 테두리 박스로 꽉 채우지 않는다. 넓은 여백, 큰 숫자, 얇은 패널,
강한 정보 계층을 쓴다.

### 16.1 등급 컬러

카드 외곽선/등급 배지 같은 국소 강조에만 쓴다. 화면 전체를 등급 색으로 물들이지 않는다.

```text
NORMAL   Blue Gray
RARE     Blue
SPECIAL  Gold
LEGEND   Red / Gold
```

역할 배지(3절의 주전/백업 등)는 이 등급 컬러 팔레트를 재사용하지 않는다 — 등급(카드
희귀도)과 역할(경기 출전 상태)은 서로 다른 축이라 같은 색으로 표시하면 혼동을 만든다.

## 17. 시각적 우선순위

화면 요소의 크기/강조 순서를 다음과 같이 강제한다.

```text
1순위   다음 경기 / 경기 진행
2순위   내 현재 역할
3순위   내 선수
4순위   최근 성적 / 감독 평가
5순위   포지션 경쟁
6순위   리그 순위 / 일정 / 뉴스
```

구단 순위보다 "내가 다음 경기에 나가는가"가 항상 더 크게 보여야 한다 — 이 순서가 이
게임을 구단 운영 게임이 아니라 선수 개인 커리어 게임으로 만드는 핵심 UI 규칙이다.

## 18. 한 문장 정의

> 다음 경기에서 내가 어떤 위치에 있는지 확인하고 바로 커리어를 진행하는 화면.

## 19. 기존 24절(UI 주요 화면)과의 관계 — 확정됨 (2026-08-26)

`BaseballManager_PROJECT.md` 24절은 원래 `Team`(라인업/투수진/벤치 편성), `Front Office`
(트레이드/FA/드래프트를 플레이어가 직접 수행)처럼 구단 전체를 플레이어가 운영한다는 전제의
화면 목록이었다 — `BaseballManager_GAME_SYSTEM.md`와 같은 시기의 구단 운영 게임 설계.

**이 문서가 정본이다.** 24절은 이 문서의 10절 구조(홈/경기/선수/팀/리그/커리어)로
교체했다. `팀` 영역은 열람 전용으로 확정했고, 트레이드/드래프트/FA 판단은 감독 AI와 구단
소유로 확정했다.

## 20. 홈 → 경기 진행 → 결과 플로우

경기는 구단 전체를 계속 조작하는 화면이 아니라, 빠르게 흐르다가 내 선수에게 의미 있는 순간에
멈추는 선수 커리어 화면이다.

```text
홈
→ 경기 준비(확정 역할·상대·컨디션 확인)
→ 자동 진행
→ 내 선수 입력 상황에서 정지
→ 선택과 결과
→ 자동 진행 재개
→ 경기 결과·개인 변화
→ 다음 날 홈
```

홈의 `경기 진행`은 곧바로 결과를 만들지 않고 경기 준비를 연다. 여기서 예상 역할과 실제
확정 역할의 차이, 선발/벤치/투수 역할, 진행 방식을 확인한다. `결과만 보기`는 보조 행동으로
유지한다.

경기 화면의 고정 우선순위는 `점수·이닝·B/S/O·주자` → `입력 대기 이유` → `현재 매치업` →
`오늘 개인 기록`이다. 보유금·우편·글로벌 메뉴는 숨기고, 우측에는 현재 사용할 수 있는 행동만
표시한다. 화면이 멈춘 모든 순간에는 `입력 대기 — 내 타석`처럼 이유를 명시한다.

타자 1차 선택은 `Balanced`/`Contact`/`Power`/`Patient` 네 가지다. 매 투구마다 다시 고르도록
강제하지 않고 마지막 선택을 유지하며, `다음 투구`와 `현재 타석 자동 진행`을 분리한다. 선택은
UI 장식이 아니라 시뮬레이션 명령이며 결과 확률에 실제로 영향을 줘야 한다.

내 선수 타석이 끝나면 안타·삼진·병살타·희생플라이 등 최종 결과를 현재 상황과 함께 큰 패널로
먼저 보여 주고, 설정된 확인 시간이 지난 뒤 자동 진행을 재개한다. `느리게`/`보통`/`빠르게`는
중계 이벤트 간격과 결과 확인 시간만 바꾸며 경기 Seed와 시뮬레이션 결과를 바꾸지 않는다.
`경기 종료까지 진행`은 선발·벤치·결장 여부와 무관하게 진행 화면에서 항상 접근 가능해야 한다.

경기 종료는 `최종 점수` → `개인 결과` → `감독 평가·컨디션·시즌 기록 변화` 순으로 정리한다.
대표 행동은 `다음 날로`이며, 이때 날짜·순위·다음 경기·뉴스 후보를 갱신한 홈으로 돌아간다.

현재 1차 구현은 내 선수 타석의 투구별 정지, 결과 확인, 재생 속도, 벤치 야수 한 명의 대타 포착,
결과만 보기를 지원한다. 주루 판단, 감독 지시, 복수 대타·대주자·대수비, 부상, 수비 선택,
중요 상황 관전 모드는 `BaseballManager_PROJECT.md` 40.10과 40.14의 후속 범위로 관리한다.

### 20.1 반복 진행 키보드 동선

```text
홈                 Space / Enter  경기 준비
경기 준비           Space / Enter  내 선수 중심 시작
경기 준비           R              결과만 보기
내 타석             1~4            타격 방식 선택
내 타석             Space          다음 투구
내 타석             A              현재 타석 자동 진행
경기 결과           Space / Enter  다음 날로
홈                 S              시즌 자동 완료 확인
자동 완료 확인     Enter          실행
자동 완료 확인     Esc            취소
포스트시즌 홈       Space / Enter  내 구단 다음 경기 준비 (미진출·탈락 시 남은 결과 진행)
시즌 결산 홈        Space / Enter  성장·수입 결산
오프시즌 홈         Space / Enter  성장 또는 계약 화면 열기
```

정규시즌의 남은 시즌 자동 완료는 확인 패널에서 남은 정규시즌 경기 수와 포스트시즌 자동 진행을
명시한다. 이 명시적 단축 기능을 실행하면 정규시즌·포스트시즌·수상 평가까지 진행하고
`SeasonReview`에서 멈춘다. 일반 진행에서는 플레이어 구단이 포스트시즌에 살아 있는 동안 내 구단
경기를 한 경기씩 경기 화면에서 진행하며, 미진출하거나 탈락한 뒤에만 남은 경기를 자동 진행한다.
오프시즌 계약·이적·성장 선택은 플레이어의 주요 의사결정이므로 자동 완료 범위에 포함하지 않는다.
정규시즌이 이미 끝난 뒤에는 같은 중앙 CTA가 `Postseason → SeasonReview → Offseason` 상태에 맞춰
바뀐다. 오프시즌이 끝났을 때 계약이 만료됐다면 계약 탭에서 선택을 마치기 전에는 다음 시즌으로
넘어가지 않는다.
