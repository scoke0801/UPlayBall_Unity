# Single Baseball Manager

> **구단주 수집 경제·우승 상여:** 연습 보상을 제외한 145,200회 수집 비교 후 경기 SP를 15→35로 적용했다.
> 승리 +10 SP는 유지하고 페넌트 우승 600 SP·포스트시즌 우승 1,200 SP를 각각 한 번 지급한다.
> 시설·우승 없이 .500에서 25인 수집 중앙 2.00·p90 2.81리그다. 5리그 최적 주전·10리그 특수 카드·
> 30리그 성장·50리그 갤럭시 완료를 검증한 결과는 아니다. [측정·채택 범위](docs/reports/owner-economy-progression.md)를 따른다.

> **카드 훈련 배치 선수 우선:** 목록 수량 옆에 기본 활성인 배치 우선 버튼을 제공한다.
> 로스터 등록 선수부터 보여 주고 그룹 안에서는 선택한 정렬을 유지하며, 필터 입력은 공용 네이비 스타일을 쓴다.
> [변경과 검증 범위](docs/reports/owner-training-roster-priority.md)를 따른다.

> **코칭스태프 운영실 개선:** 역할별 후보 필터와 공석·연봉 요약, 현재 담당자 대비 효과·계약 비교,
> 검토·취소·확정을 제공한다. 교체 위약금과 자금 부족 후보의 효과 미리보기 표시를 바로잡았다.
> 콘솔 14건·스킨 자원 29건·외부 컴파일 통과. 에디터 테스트·실제 화면 검수는 미실행이다.
> [변경 및 검증 범위](docs/reports/owner-staff-office-redesign.md)를 따른다.

> **서포트 카드 목록·리그 해금 확장:** 보관함에 세로 스크롤과 팀/개인·타자/투수·해금/보유/잠김 필터를 추가했다.
> 기본 6종에서 리그마다 2종씩 갤럭시 24종까지 해금하며, 최고 진출 리그를 기준으로 강등 후에도 유지한다.
> 구단주 저장 버전은 31이다. 상세 경기 24,000건을 실행했으며 전체 UI 컴파일은 별도 경기 준비 화면 오류로 막혔다.
> 에디터 테스트·실제 화면 검수는 미실행이다. [변경·밸런스·검수 범위](docs/reports/owner-support-catalog-unlocks.md)를 따른다.

> **경기 준비·공개 라인업 Front Office V2:** 구단·선발 비교와 경기 계획을 네이비 작업면으로 정리하고,
> 공개 라인업에 보직별 제목·팀컬러 요약·카드 기본 클릭 상세 보기를 연결했다. 외부 컴파일은 오류·경고 0이다.
> 에디터 테스트·실제 화면 검수는 미실행이며 [변경 범위와 남은 검증](docs/reports/owner-pregame-lineup-front-office-v2.md)을 따른다.

> **특수 영입·덕아웃 V2 작업 영역:** 레전드·커리어하이 영입과 덕아웃의 흰 외부 작업면을 V2 네이비로 연결하고
> 제목·설명·상태 글자 대비와 덕아웃 스태프 카드의 보조 색상을 정리했다. 자원 계약 29/29는 통과했으며,
> 외부 컴파일은 별도 서포트 카드 변경의 미해결 참조 3건으로 실패했다. 사용자 요청으로 에디터 테스트는 미실행이다.
> [변경·검증 제한](docs/reports/owner-recruit-dugout-v2-surfaces.md)을 따른다.

> **구단주 특성훈련 구현:** 원본 v1.0의 C/B/A/S 성장·TP 경제·후보 선택/저장·오프시즌 게이트와
> 14종 경기 효과, Front Office V2 전용 훈련 작업면을 연결했다. 구단주 저장 버전은 30이다.
> 외부 규칙/저장/경기 테스트 68건과 상세 엔진 29,000경기 대조를 통과했다.
> 실제 렌더·입력 검수는 미실행이며 경제 초깃값과 기획 해석은
> [특성훈련 구현 보고서](docs/reports/owner-trait-training-implementation.md)를 따른다.

> **구단주 매니저 안내 실용성 개선:** 매 경기 생성하던 무근거 준비·투수진 점검 안내를 제거하고
> 실제 편성 문제와 대상 자리를 안내한다. 홈·리포트의 모두 읽음과 읽고 보관을 제공하며,
> 읽은 동일 문제는 경기 전환·저장 복원으로 재알림하지 않는다. 콘솔 11건 통과,
> 로드 후 패널 누락은 실제 뷰 활성 상태·런타임 바인딩까지 확인해 복원하도록 수정했다.
> 후속 참조 포함 UI 컴파일은 통과했다. 에디터 테스트·실제 화면 검수는 미실행이다.
> [변경·검증 범위](docs/reports/owner-manager-actionable-guidance.md)를 따른다.


> **구단주 업무·관전 정보 계층 개선:** 코칭스태프·시즌 전적·구단 정보·스킬 배치·유학·역대 강팀·관전의
> 제목, 지표, 목록과 행동 영역을 구분했다. 시즌 전적은 리그 필터에 연동된 성과 요약을 제공한다.
> 자원 정적 검증은 29/29 통과했으며 최종 빌드는 동시 변경 코드의 참조 오류로 실패했다.
> 에디터 테스트·실제 화면 검수는 미실행이다. [변경 및 검증 범위](docs/reports/owner-information-hierarchy-20260913.md)를 따른다.

> **연습경기 특수 구원투수 배치:** 커리어하이·레전드 구원투수는 최대 2명이며 불펜 1·2번으로 배치한다.
> `practice-v6` 카탈로그는 363팀·262,812경기 재생성, 650경기 도전 완주, Fast/Full 실제 불펜·해시 검증을 통과했다.
> 에디터 테스트는 사용자 요청으로 미실행이다. [수정·검증 범위](docs/reports/legendary-practice-bullpen-placement-20260913.md)를 따른다.

> **서포트 카드 선수 보드 개선:** 구단주 서포트 화면을 보관함·4×2 선수 Minicard 보드·실행 패널로 재배치했다.
> 선수 원화·포지션·연도와 능력치 전후 값을 함께 표시하며, 보유 여부에 따라 구매 또는 적용을 대표 행동으로 안내한다.
> 에디터 테스트는 사용자 지시로 실행하지 않는다. 변경 및 실제 화면 검수 제한은
> [서포트 카드 UI 개선 기록](docs/reports/owner-support-card-redesign.md)을 따른다.

> **연습경기 특수 카드 선발 배치:** 커리어하이·레전드 타자는 선발 9자리만 보강하며,
> 같은 인물이 벤치에 있으면 적법한 선발·벤치 교환으로 배치한다. `practice-v5` 카탈로그는
> 363팀·262,812경기 재생성, 668경기 도전 완주, Fast/Full 편성·해시 검증을 통과했다.
> 에디터 테스트는 사용자 요청으로 미실행이다. [수정·검증 범위](docs/reports/legendary-practice-starter-placement-20260913.md)를 따른다.

> **역대 강팀 역사 승률 우선:** 연습경기 역사구단 상위 100팀은 실제 정규시즌 승률 75%와 상세 경기 승률 25%로 선정한다.
> 전체 363팀의 기존 262,812경기를 활용했고 변경된 목록의 실제 620경기·300승 완주, 관련 테스트 12건과 콘텐츠 검증을 통과했다.
> [선정 기준과 검증 범위](docs/reports/legendary-practice-historical-priority-20260913.md)를 따른다.

> **도감·위시·팀 컬러 내부 패널 개선:** 공용 `OwnerDashboardStyle.ApplyInset`으로 목록·효과·상세 본문과
> 검색 입력에 네이비 세로 음영·1px 가장자리 반사선을 적용하고 도감 하단은 V2 행동 바로 연결했다.
> Presentation 단독 컴파일과 V2 자원 정적 검증은 통과했다. 에디터 테스트·실제 화면 검수는 미실행이다.
> [변경과 검증 제한](docs/reports/owner-collection-inset-panels.md)을 따른다.

> **Front Office V2 데이터 작업면 확장:** 일정·선수 기록·역사/구단 기록실·재정·부가시설·코칭스태프,
> 작전카드·위시 빈 상태·특수 영입과 스킬 블록 배치에 네이비 내부 면과 선택·입력 상태를 적용했다.
> 최종 Presentation 단독 보조 컴파일은 통과했다. 전체 참조 빌드는 동시 변경된 Core 타입 누락으로 실패했으며
> 에디터 테스트·실제 렌더·입력 검수는 미실행이다. [적용 범위와 검증 제한](docs/reports/owner-v2-data-surfaces-extension.md)을 따른다.

> **성장 관리 팝업 개선:** 연구·합성·S 상자 사용을 나누고 획득 모양·효과·재료·실행 비용을 함께 표시한다.
> 선수별 성장에는 이름·포지션·등록 상태·연도·구단 필터와 정렬을 제공하며, 블록은 공용 원형·별 타일을 사용한다.
> 외부 컴파일은 통과했다. 에디터 테스트는 사용자 지시로 미실행이며 실제 화면·입력 검수는 남아 있다.
> [변경 및 검증 범위](docs/reports/owner-development-popup-redesign.md)를 따른다.

> **경기 관전 V2 데이터 작업면:** 구단주 정규시즌·포스트시즌·연습경기의 관전 및 결과에
> 네이비 기록표·공용 V2 버튼·실제 Medium/Light 서체와 기록 탭별 스크롤·복귀 포커스를 연결했다.
> 외부 컴파일은 오류·경고 0이며, 에디터 테스트·실제 렌더·입력 검수는 미실행이다.
> [적용 범위와 검증 제한](docs/reports/owner-match-v2-data-surfaces.md)을 따른다.

> **구단주 홈 안내 크기 안정화:** 매니저 안내의 접기·펼치기는 동일한 카드 높이를 사용한다.
> 초상·본문·추천 행동·리포트 버튼 위치를 유지하고, 리포트 전환에도 같은 기준으로 홈 배율을 계산한다.
> 변경 및 검증 범위는 [홈 안내 크기 안정화](docs/reports/owner-home-layout-stability.md)를 따른다.

> **리그 V2 데이터 작업면:** 순위표·구단 성적·대전 결과·순위 변화의 내부 표를 네이비로 맞추고
> 내 구단 배지·성적 요약·선택 및 포커스 표시를 구분했다. 구단주 하위 탭 띠와 V2 선택선도 정리했다.
> 외부 컴파일·정적 검사는 통과했으며 사용자 지시로 에디터 테스트·실제 렌더·입력 검수는 하지 않았다.
> [적용 범위와 검증 제한](docs/reports/owner-league-v2-data-surfaces.md)을 따른다.

> **타이틀 Front Office V2 연결:** 모드 선택 패널·모드 카드·하단 버튼·안내 및 종료 확인창에
> 기존 V2 공용 PNG와 입력 상태를 연결했다. 타이틀 설정의 본문·저장 카드·확인창도 V2를 사용한다.
> 진입 및 저장 흐름은 유지한다. 설정 후속 보조 컴파일은 통과했으며 실제 화면·입력 검수는 미실행이다.
> [적용 범위와 검증 제한](docs/reports/title-front-office-v2.md)을 따른다.

> **포스트시즌 Front Office V2:** 실제 시즌 결과 팝업의 헤더·대진·기록 카드를 V2로 통일하고
> 네 라운드 카드 높이를 고정했다. 컴파일과 자원 정적 검증을 통과했으며 에디터 테스트·화면 검수는
> 실행하지 않았다. [연결·검증 범위](docs/reports/owner-postseason-front-office-v2.md)를 따른다.

> **Front Office V2 미연결 화면:** 작전 카드·스카우트·합성·스킬 배치·유학과 구단·리그 하위 메뉴,
> 상점 목록·상세 패널에 기존 V2 스킨을 연결하고 덕아웃 글자 대비를 수정했다.
> 외부 컴파일·자원 정적 검사는 통과했으며, 요청에 따라 에디터 테스트·렌더 검수는 하지 않았다.
> [적용 범위와 검수 제한](docs/reports/owner-front-office-v2-remaining-screens.md)을 따른다.

> **구단주 프런트 매니저 리포트:** 문제별 제목·설명 30종과 3개 카드 목록, 동일 패널 안의 상세 보기를 제공한다.
> 읽음과 문제 해결을 구분하고 목록 복귀 포커스·빈 보관함·종료된 제안의 이동 제한을 적용한다.
> 화면·검증 범위는 [리포트 개선 기록](docs/reports/owner-manager-report-redesign.md)을 따른다.

> **구단주 시즌 중 성장 이용:** 스킬블록 구매·배치·해제는 정규시즌과 포스트시즌에도 허용한다.
> 유학은 시즌 중 1군 해제 후 신청하고 경기 주간 정산에 따라 진행한다. 전체 조 포스트시즌 종료 후에는
> 1군 등록을 유지한 채 신청할 수 있으며, 훈련 일정의 `유학 모두 완료`로 모든 파견 선수의 귀환까지
> 필요한 주차만 한 번에 정산한다. 귀환 결과 확인 후 다음 시즌을 시작한다. 기존 기간·비용·성장량과
> 시즌당 1회 제한은 유지한다. [변경·검증 범위](docs/reports/owner-season-growth-access.md)를 따른다.

> **타격·투구 반영 및 연습 Bake 재검증:** 정신력의 중복 타격 실행 보조를 줄이고 상위 제구의 오차 하한을 완화했다.
> 연습 로스터에도 역사 투수의 투구 용량·회복력을 반영하며 엔진 버전은 13이다.
> 전체 363구단 × 팀당 1,448경기를 두 번 재현한 목록으로 갱신했다. 44년 × 16회 역사 대조에서 승률 오차는 소폭 감소했지만,
> 실제 시즌 순위와 연습 보강 구단 순위의 괴리는 해결되지 않았다. 수치·검증·남은 문제는
> [투구 반영 개선 기록](docs/reports/pitching-valuation-20260912.md)을 따른다. 아래의 엔진 12 유지 기록은 이전 검토 결과다.

> **선수 오더 자동 배치:** 연도·구단을 선택해 덱 인원을 우선 확보하고 부족한 포지션을 고 코스트로 보완한다.
> 동일 인물 중복, 수비 위치·선발/구원 구분, 백업 포수, 커리어하이·레전드 합계 4명과 역할별 3명 상한을 지킨다.
> 자동 편성은 기존 변경안 미리보기와 배치 저장으로 확정한다. [동작·검증 범위](docs/reports/owner-auto-lineup.md)를 따른다.

> **스킬 슬롯 잠금 제거:** 구단주 성장판은 처음부터 4×4 전체 16칸을 사용한다.
> 잠금 마스크·칸 해제 경험치·전용 전지훈련과 관련 UI·저장 상태를 제거했다. 블록의 경계·겹침·회전 규칙은 공용 서비스를 따른다.
> `SkillBlockPlaced`를 가이드 정본과 스키마에 등록하고 최초 배치 안내를 저장당 한 번 제공한다. 구단주 저장 버전은 29다.
> 적용·검증 범위는 [슬롯 잠금 제거 기록](docs/reports/owner-skill-unlocked-board-20260912.md)을 따른다.

> **스킬 슬롯 잠금 제거:** 구단주 성장판은 처음부터 4×4 전체 16칸을 사용한다.
> 잠금 마스크·칸 해제 경험치·전용 전지훈련과 관련 UI·저장 상태를 제거했다. 블록의 경계·겹침·회전 규칙은 공용 서비스를 따른다.
> `SkillBlockPlaced`를 가이드 정본과 스키마에 등록하고 최초 배치 안내를 저장당 한 번 제공한다. 구단주 저장 버전은 29다.
> 적용·검증 범위는 [슬롯 잠금 제거 기록](docs/reports/owner-skill-unlocked-board-20260912.md)을 따른다.

> **연습경기 대표 선수·연속 경기:** 커리어하이·레전드를 제외한 원 시즌 코스트 상위 3명을 표시한다.
> 도전 목록은 순위·진행 상태·승리 표시와 시대 드롭다운을 사용하며, 결과 화면에서 같은 상대와
> 다음 경기를 이어간다. 3승 직후에는 최초 보상 수령으로 안내한다.
> [변경 및 검증 제한](docs/reports/legendary-practice-featured-and-continuation-20260912.md)을 따른다.

> **연습경기 전체 구단 Bake:** 부포지션 포수를 누락하던 검사와 보강 시 포수 자격 유실을 수정했다.
> `practice-v4`는 363팀 전체가 262,812경기(팀당 1,448경기)에 참가하며, 같은 Seed 재베이크 결과가 일치한다.
> 전체 참가 명단을 카탈로그 해시에 포함하고 실제 로딩에서도 정본과 대조한다. 편성 실패 팀을 제외하고 계속하지 않는다.
> 능력치 분석의 Stamina·투수 Mental 무효 판정은 재측정으로 정정했고 엔진 계수는 유지했다.
> [수정·검증 기록](docs/reports/match-engine-ability-sensitivity-review-20260912.md)이 아래의 이전 295팀 Bake 기록보다 우선한다.

> **구단주 포스트시즌 대진표:** 하단 정규시즌 시드에서 상단 우승 구단으로 올라가는 계단식 대진표를 사용한다.
> 구단 엠블럼·명판과 승자 경로를 표시하고 우측에 라운드별 규칙·승수를 모은다.
> 구단 선택과 선수단 복귀 포커스는 유지한다. [변경·검증 기록](docs/reports/owner-postseason-bracket-redesign.md)을 따른다.

> **구단주 유학 선수 탐색:** 이름·포지션·등록 상태·연도·구단 필터와 정렬을 제공한다.
> 14장씩 탐색하며 선택 카드와 과정 조건을 오른쪽에서 확인한 뒤 적용한다. 취소는 원래 선수를 유지한다.
> 적용 및 검증 범위는 [유학 선수 선택 개선 기록](docs/reports/owner-study-picker-redesign.md)을 따른다.

> **역대 강팀 표시 정보:** 실제/가상 Identity 설정을 대표 선수·선발·상대 라인업·관전에 일관되게 적용한다.
> 상대 정보는 기존 25인 공개 카드 보드로 제공하며 팀컬러 내부 ID와 중복 연도를 제거한다.
> 적용 및 검증은 [역대 강팀 UI 수정 기록](docs/reports/legendary-practice-identity-ui-20260912.md)을 따른다.

> **카드 합성 목록 필터:** 이름·연도·구단·포지션·중복 보유·잠금 표시를 교차 검색하고 초기화한다.
> 필터 변경 시 등록한 합성 대상을 유지하며, 잠금은 사각형 문자 대신 한국어 상태와 선택 스타일로 표시한다.
> 적용과 검증 범위는 [합성 필터 개선 기록](docs/reports/owner-fusion-filters-20260912.md)을 따른다.

> **경기·커리어 통계 회귀 수정:** 합성 투수의 구종 선택·실행 숙련도를 공유하고 AI 타이밍 오차를
> 55→61ms, 커리어 기용 컨디션 가중치를 0.30→0.45로 조정했다. 만 경기 타율 .295·삼진율 18.3%,
> 45시즌 타자 선발률 80.5%이며 엔진 버전은 12다. 연습경기는 173,460경기로 순위를 재베이크했다.
> 근거·세이브 초기화·전체 검증 범위는 [통계 회귀 수정 기록](docs/reports/statistics-regression-fix-20260912.md)을 따른다.

> **역대 강팀 연습경기:** 구단주 상단 메인 메뉴의 상점 다음 `연습경기`에서 공용 셸·오더·상세 경기 엔진으로 100팀에 도전한다.
> 팀별 누적 3승 해금, 독립 5선발 순환, 최초 보상 원자 저장과 기존 관전 화면을 연결했다.
> 295개 유효 후보의 173,460경기 순위를 두 번 재현했으며 실제 300승 완주를 검증했다.
> 적용 범위와 Unity 검수 제한은 [연습경기 구현 기록](docs/reports/legendary-practice-implementation.md)을 따른다.

> **커리어하이·레전드 발급 분리:** 커리어하이 발급 자격을 충족하는 Person은 연도·구단 계보와 무관하게
> 레전드 발급에서 제외한다. 사용자 보유 여부가 아닌 전체 발급 자격 기준이며 부분 Bake에도 적용한다.
> 커리어하이 116장 유지·레전드 105→19장, 특수 카드 총 571장·레시피 135개다.
> 연습경기 100팀은 기존 커리어하이 우선 선택으로 편성이 동일하며 순위 Bake를 유지한다.
> 검증 범위와 제한은 [특수 카드 중복 제외 기록](docs/reports/special-card-exclusivity-20260912.md)을 따른다.

> **구단주 덕아웃 개편:** 감독 6명·수석코치 7명의 운영 조합과 인물별 피규어 초상화를 제공한다.
> 기존 4종을 기본 인물에 고정하고 ImageGen으로 9종을 추가해 13명의 얼굴을 구분한다.
> 자산·프롬프트·검증 범위는 [인물별 초상화 기록](docs/art/Dugout/individual-portraits.md)을 따른다.
> 인선·방침의 임시 변경은 실제 경기와 같은 Resolver로 미리 보고 확정한다. 효과는
> `DugoutStaffBalance.json`에서 저작하며 상대 맞춤 성향은 좌우 타석 유불리를 대타 평가에 반영한다.
> 적용·아트·대량 통계·화면 검증 상태는 [덕아웃 개선 기록](docs/reports/dugout-redesign-20260912.md)을 따른다.

> **구단주 스카우트 화면:** 파견 범위·전담 스카우터·영입 리포트를 구분하고 유학 메뉴의 Native 버튼 스타일을 사용한다.
> 구단·연도·방침 검색, 청색 선택 상태, 취소·포커스 복원과 신규·중복 영입 표시를 제공한다.
> ImageGen 스카우터 남녀·연령별 4종과 파견 전 구장 배경을 연결했다. 외형 선택은 화면 전용이다.
> 적용 및 검증 범위는 [스카우트 UI 개선 기록](docs/reports/owner-scout-ui-redesign-20260912.md)을 따른다.

> **프야매 기획서 M0~M5:** 구단주 성장 원장, 2경기 서포트, 6→16칸 보드·인접 세트·연구·합성,
> 전지훈련, 해외훈련 3등급·취소·대성공, 능력치 교정, 훈련 파트너, 슬로건을 실제 UI·저장에 연결했다.
> 유학은 시즌 중에는 1군 해제 후 주간 정산으로, 전체 조 포스트시즌 종료 후에는 별도 4주 일정에서 진행한다.
> 비시즌에는 1군도 참가하며 빠른 귀환을 지원한다. 과정은 1/2/3주와 120,000/350,000/750,000 PT다.
> 기존 24개 목적지와 스태프 운영을 재사용하며 캠프 XP에는 현재 코치 효율을 반영한다. 저장 버전은 28이다.
> 제작용 수치·현재 리그 대응·테스트와 실제 검증 범위는 [구현 대조 및 검증 기록](docs/reports/owner-reference-systems-20260912.md)을 따른다.

> **구단주 홈·프런트 매니저 통합:** `docs/todo/UPlayBall_Main_UI_Revision_Plan.html`을 따라
> 상태·내비게이션 두 밴드와 우측 경기 → 추천 → 시즌 대시보드를 사용한다. 저장은 전역 헤더에 둔다.
> 추천은 카드 안에서 펼치고 매니저 리포트는 같은 위치에서 전체·중요·보관 목록과 상세를 제공한다.
> 열람·보관은 원본 로스터 문제의 해결과 구분하며 기존 선수·편성 슬롯 딥링크를 재사용한다.
> 리포트 누적 저장 추가로 저장 버전은 **19**이며 마이그레이션은 만들지 않는다.
> 적용 범위와 검증은 [홈 UI 개편 기록](docs/reports/owner-main-ui-revision-20260912.md)을 따른다.

> **구단주 유학 추가 확장·초기화 상품:** 야수·투수 각각 12개 과정(총 24개)을 제공한다.
> 상점 추천 상품의 유학 초기화권은 육성 포인트 200으로 구매하며, 구매 확인창에서 보유 선수를
> 지정해야 결제할 수 있다. 완료한 모든 유학의 누적 능력치와 시즌 참가 제한을 초기화하고,
> 일반 훈련·강화·스킬 블록은 유지한다. 진행 중인 유학은 완료 후 초기화할 수 있다.
> 기존 유학 비용은 반환하지 않는다. 가격 원본은 `NewGame/StudyResetShop.json`이며
> [변경·검증 기록](docs/reports/owner-study-reset-20260912.md)을 따른다.

> **구단주 포스트시즌 KBO 구조:** 각 조 상위 5팀이 와일드카드(4·5위, 최대 2경기) →
> 준플레이오프(3위 합류, 5전 3선승) → 플레이오프(2위 합류, 5전 3선승) →
> 한국시리즈(1위 합류, 7전 4선승)를 치른다. 4위는 WC 1승 또는 1무로 진출하고 5위는 2승이 필요하다.
> 실제 승수에 가산승을 섞지 않는다. 15회 동점은 무승부이며 WC 외 라운드는 승수 충족까지 추가 경기한다.
> WC 홈은 4위, 준PO/PO는 2-2-1, 한국시리즈는 2-3-2다. 2~4팀 조는 존재하는 하위 시드부터 시작한다.
> 선수 커리어·사전 역사 베이크는 기존 규칙을 유지한다. 구단주에 관한 과거 4강·결승 설명보다 이 규칙이 우선한다.
> 현재 구단주 저장 버전은 27이며 이전 포스트시즌 대진을 변환하는 마이그레이션은 만들지 않는다.
> 검증 범위는 [KBO 포스트시즌 변경 기록](docs/reports/owner-kbo-postseason-20260912.md)을 따른다.

> **카드 훈련 UI:** 구단주 보드 Theme로 선수 선택·능력치 비교·실행 영역을 구분한다.
> 선택 훈련의 성장량과 비용·훈련 후 잔액을 고정 표시하고 과정 변경 시 카드와 포커스를 유지한다.
> 보유 선수 이름 검색·포지션/연도/구단 필터와 코스트/이름/연도 정렬을 제공하며 검색 중 훈련 대상은 유지한다.
> [선수 탐색 개선 기록](docs/reports/owner-training-filters-20260912.md)을 함께 따른다.
> [화면 개편·검증 기록](docs/reports/owner-training-ui-redesign-20260912.md)을 따른다.

> **스킬 블록 공용 디자인:** 장착 화면의 성장판·목록·회전 미리보기와 선수 카드 뒷면이
> ImageGen 원형·별 문양 타일을 공유한다. 일반·레어는 원형, 엘리트·유니크·전설은 별이며
> 기존 등급 색상·배치 규칙을 유지한다. [자산 및 제작 기록](docs/art/SkillBlocks/README.md)을 따른다.

> **구단주 트로피룸 전시 구성:** 기존 트로피를 네이비 전시 프레임·누적 횟수·리그별 집계로 묶는다.
> 트로피 원화는 사용자 승인 v2 디자인(금색 컵·청색 우승 컵·월넛 기념패)에 선수 피규어 재질을 적용한다.
> 원본·프롬프트·투명 배경 검수는 [트로피 아트 기록](docs/art/ClubHistory/README.md)을 따른다.
> 수상 종류 선택 시 달성 시즌 네 열을 표시하고 기존 시즌 상세로 연결한다.
> 적용과 검증 범위는 [트로피룸 개선 기록](docs/reports/owner-trophy-room-redesign-20260912.md)을 따른다.

> **타자 카드 수비 필드:** ImageGen 피규어 화풍의 투명 야구장 원화를 적용하고 기존 포지션 공·한글 라벨을 겹쳐 표시한다.
> 자산·4개 해상도 렌더링·검증 결과는 [수비 필드 기록](docs/reports/hitter-card-defense-field-20260912.md)을 따른다.
>
> **투수 카드 뒷면:** 네이비 바탕의 중앙 변화 방향 도식을 얇은 구종 명판·원형 등급 배지가 둘러싼다.
> 초상 아래에 투타·역할·비용·최대구속을 모으고 스킬 블록 안내는 상태·개수·배치 경로로 구분한다.
> 기존 구종 데이터·변화 방향·기록 계약을 유지한다. [시각 정리 및 검증 기록](docs/reports/pitcher-card-back-redesign-20260912.md)을 따른다.

> **선수 오더 분석 서브탭:** 우측 분석 패널에 `컨디션 분석 / 선수비교`를 제공한다.
> 교체 변경안 생성 시 두 선수 카드·공통 6축 그래프·성장 보너스 포함 수치 표를 표시하며,
> 탭 전환으로 비교를 유지하고 기존 배치 저장·변경 취소 흐름을 사용한다.
> 구현과 검증은 [선수비교 UI 기록](docs/reports/owner-roster-comparison-tabs-20260912.md)을 따른다.

> **구단주 구단 기록실:** 구단 → 기록실과 리그 → 역사 기록에서 시즌 전적·통산 성적·최고 기록·트로피룸을 제공한다.
> 전체/10개 리그 등급 필터와 기록 행 → 달성 시즌 → 해당 시즌 선수 기록으로 이어지는 조회 경로를 공유한다.
> 통산 비율은 구단 소속 기간의 원본 합계, 최고 기록은 완료된 정규시즌, 수상은 확정된 실제 경기 결과를 사용한다.
> 사전 생성 역사와 운영 기록을 혼합하지 않는다. 구현·아트·검증 범위는
> [구단 기록실 확장 기록](docs/reports/owner-club-history-depth-20260912.md)을 따른다.

> **카드훈련·합성 진입 성능:** 보유 카드 전체의 UI·상세·훈련·수량별 판매 Preview 생성을 제거한다.
> Viewport 주변 카드 UI를 재사용하고 상세와 Preview는 사용 시 조회한다.
> 적용과 검증 범위는 [카드 메뉴 성능 기록](docs/reports/owner-powerup-entry-performance-20260912.md)을 따른다.

> **스킬 블록 배치 조작성:** 선수 목록은 코스트 내림차순이며 같은 코스트에서는 장착 우선·기존 순서를 유지한다.
> 선택 블록의 실제 회전 모양과 각도를 표시하고, 회전 시 성장판 슬롯을 재사용해 깜빡임과 포커스 유실을 막는다.
> 블록 선택·데이터 갱신에도 스킬 목록 스크롤 위치를 유지하고, 등급·선수 유형 필터 변경 시 맨 위로 이동한다.
> 적용 및 검증 범위는 [스킬 블록 UI 개선 기록](docs/reports/skill-block-ux-20260912.md)을 따른다.

> **구단주 유학 선택지 확장:** 야수·투수 각각 8개 과정으로 늘리고 기존 100포인트·4주·총 +3을 유지한다.
> 원형 핀에는 Imagegen으로 제작한 진주색·청색 피규어 비행기를 적용한다.
> 과정 목록·18,000경기 비교·화면 검증은 [유학 확장 기록](docs/reports/owner-study-expansion-20260912.md)을 따른다.

> **선수 카드 유학·특성 배지:** 유학 중/완료 이력을 공용 카드 오른쪽 아이콘과 설명으로 표시한다.
> 기획서의 C/B/A/S 특성 배지 자산과 공용 표시 계약을 준비했으며, 특성훈련 본체·확정 등급의 런타임 연결은
> 아직 없다. 표시 범위와 검증은 [성장 배지 기록](docs/reports/player-growth-badges-20260912.md)을 따른다.

> **구단주 순위표의 시즌 구분:** 리그 → 순위표에 `페넌트레이스 / 포스트시즌` 탭을 둔다.
> 정규시즌 표와 실제 포스트시즌 4강·결승 대진을 분리하며, 두 구단 참가 시 결승만 표시한다.
> 승수·진출·탈락·우승은 기존 시즌 보고 Snapshot을 읽고, 미확정 대진은 대기 상태로 남긴다.
> 적용 및 검증은 [순위표 시즌 구분 기록](docs/reports/owner-league-postseason-tabs-20260912.md)을 따른다.

> **구단주 선수 기록 상세 표시:** 리그 선수 기록은 기본 상위 30명을 대상으로 타격·투구·수비·주루 상세 지표를 한국어로 제공한다.
> 열 제목은 세로 고정·가로 동기화하며, 부문 탭은 고정 영역을 유지한다.
> 구현과 검증 제한은 [선수 기록 확장 기록](docs/reports/owner-season-records-expanded-20260912.md)을 따른다.

> **카드 합성·스킬 블록 선수 텍스트:** 공용 미니 카드의 명찰 중심을 유지하면서 한글 행 높이를
> 확보해 작은 카드에서 이름·연도가 생략되는 문제를 고쳤다. 포지션·코스트도 작은 카드에 맞춘다.
> 검증 범위는 [미니 카드 텍스트 복구 기록](docs/reports/owner-small-card-text-20260912.md)을 따른다.

> **구단주 포스트시즌 관전 복구:** 관전 전 시즌 보고는 진행 중에도 읽을 수 있으며,
> 연속 관전의 이전 경기 안내 저장은 다음 세션을 열기 전에 끝낸다. 관전 준비 실패 시 세션을
> 정리해 대진으로 복귀한다. [원인·검증 기록](docs/reports/owner-postseason-entry-fix-20260912.md)을 따른다.

> **공통 UI 글자 선명도:** uGUI 글자는 `UIProjectText`로 생성해 Canvas 배율뿐 아니라
> 부모 패널의 확대 배율도 동적 폰트 해상도에 반영한다. 기본 Medium은 Hinted Smooth로
> 렌더링한다. 유학·전력보강·카드 등에서 저해상도 글자를 확대하던 경로를 공통 보정한다.
> 최초 글리프 생성 중 폰트 갱신이 다른 Text를 중첩 렌더링할 수 있으므로, 공유 메시 버퍼는
> 글자 생성 이후에 비워 다른 문구가 섞이지 않게 한다.
> 원인과 회귀 검증은 [최초 진입 글자 중복 수정 기록](docs/reports/ui-first-entry-text-overlap-20260912.md)을 따른다.

> **저장 슬롯 사전 준비:** Loading 씬에서 선수·구단주 각 5개 슬롯 정보를 미리 읽고 준비가 끝난 뒤 진입한다.
> 선수 슬롯도 파일이 바뀌기 전까지 검증된 요약을 재사용하며 저장·백업 복구·삭제 시 갱신한다.
> **구단주 저장 슬롯 조회:** 슬롯 선택·확인창에서 전체 진행을 역직렬화하지 않고 표시용 필드만 읽는다.
> 파일 경로·크기·수정 시각이 같으면 요약을 재사용한다. 실제 불러오기와 저장 형식은 유지하며,
> 변경·검증 범위는 [저장 슬롯 성능 개선 기록](docs/reports/owner-save-slot-performance-20260912.md)을 따른다.

> **선수 카드 디자인 FX:** 광채 플립북은 개발용 ‘선수 카드 디자인’ 갤러리의 FX ON 미리보기에만
> 부착한다. 실제 게임의 공용 미니·상세 카드에는 적용하지 않는다. 동일 원화에 광량만 바꾸는
> v2 시트로 프레임별 형태 흔들림을 제거했다. [카드 FX 제작 기록](docs/art/PlayerCardFX/README.md)을 따른다.

> **구단주 유학 UI 복구:** 공용 카드의 CanvasGroup을 재사용해 선수 선택·유학 결정 버튼의 생성 중단을 고친다.
> 레퍼런스에 맞춰 지도는 원형 비행기 핀·흰색 목적지 이름표·금색 선택 테두리를 사용한다.
> 잠긴 목적지는 회색 물음표로 표시하며 항로·출발점·큰 비행기 표시는 제거한다.
> 변경과 검증 범위는 [유학 UI 복구 기록](docs/reports/owner-study-ui-fix-20260912.md)을 따른다.

> **구단주 선수 재계약 제거:** 구단의 계약 메뉴, 개별·일괄 갱신 명령과 계약 만료에 따른 다음 시즌
> 차단을 제거한다. 보유 선수·1군 등록은 시즌이 바뀌어도 유지하며 기존 연봉을 매 시즌 정산한다.
> 재계약금은 청구하지 않는다. 급여·과거 계약 조건의 저장 필드를 유지하므로 저장 버전은 변경하지 않는다.
> 선수 커리어의 입단·이적·계약 선택은 이 변경의 대상이 아니다. 아래 과거 구단주 계약 기록보다
> 이 규칙을 우선하며 검증은 docs/reports/owner-renewal-removal-20260912.md를 따른다.

> **구단주 정규시즌 속도:** 독립된 Runtime 복사본을 단일 작업 스레드에서 순차 계산하고,
> UI는 초당 10회 진행값만 받는다. 완료·중단 후 메인 스레드가 결과를 반영한다. AI의 불변 선수 입력을
> 재사용하고 내 조 진행률은 누적한다. 상세/간이 적용 범위·밸런스·Seed 순서는 유지한다.
> 디스크 저장·복원과 실행 복사 모두 로스터 배열 순서(AI 벤치 우선순위)를 보존한다.
> 로스터 순서 보존과 성능·결정론 검증은
> [정규시즌 성능 개선 기록](docs/reports/owner-season-performance-20260912.md)을 따른다.

> **구단주 시즌 결과 연출:** 정규시즌 순위가 확정된 1위 구단에만 ImageGen 페넌트 연출을 표시한다.
> 포스트시즌 우승은 우리 조 결승 종료로 판단하며, 관전 완료·복귀·시즌 보고에서 놓친 축하를 복구한다.
> 같은 세션의 중복 재생을 막고, 보고 UI는 성과·하단 기록·다음 행동 중심으로 정리한다.
> 적용 경로·아트·검증은 [시즌 결과 연출 개선 기록](docs/reports/owner-season-ceremonies-20260912.md)을 따른다.

> **구단주 프런트 매니저 교체:** 구단 → 구단주 화면의 매니저 사진 아래 `매니저 교체`에서
> 분석형·현장형·활력형의 기존 초상과 타입을 비교하고 즉시 선택한다. 선택은 기존 OwnerProfile의
> FrontManagerId에 저장하며 저장 실패 시 이전 매니저를 유지한다. 검증 범위는
> [매니저 교체 UI 기록](docs/reports/owner-front-manager-change-20260912.md)을 따른다.

> **선수 카드 앞면 정보:** 스탯·포지션·컨디션·카드 타입·구단명은 별도 사각 패널 없이
> 기존 카드 프레임 위에 표시한다. 구단주 카드의 구단명은 사용자 소속 구단명이 아닌 카드 원본
> 시즌의 실제·가상 Identity 이름이며, 연도는 이름 옆 기존 표기에만 남긴다.
> 적용·검증 범위는 `docs/reports/player-card-front-cleanup-20260912.md`를 따른다.

> **구단주 경기 결과 연속 진행:** 정규시즌·포스트시즌에 우리 구단의 다음 경기가 남아 있으면
> 결과 화면의 `다음 경기`로 이어서 관전한다. 결승 상대 결정에 필요한 앞선 대진은 기존 순서로 진행하며,
> 탈락·우승 후에는 버튼을 숨긴다. 연속 진행은 홈 복귀나 BGM 전환 없이 현재 오디오 상태를 유지한다.
> 검증 범위는 `docs/reports/owner-next-match-20260912.md`를 따른다.

> **구단주 스킬블록 카드 배율:** EX·레어는 2배, 커리어하이·레전드는 1.5배,
> 나머지는 1배다. 능력치별 블록 보너스 합계에 한 번 곱한 뒤 소수부를 버린다.
> 훈련·유학·강화·특성은 배율 대상이 아니다. 검증은
> `docs/reports/skill-block-multiplier-20260912.md`를 따른다.

> **선수 카드 플립북 광채:** 구단주 공용 미니 카드와 상세 앞면의 초상 뒤에 금빛 홀로그램
> 4×4 시트를 12fps 왕복 루프로 재생한다. 기존 프레임·사진 마스크·입력 경로를 재사용하며
> 경기 로직에는 영향을 주지 않는다. 자산·적용 범위·검증은 [카드 FX 제작 기록](docs/art/PlayerCardFX/README.md)을 따른다.

> **선수 카드 능력치 게이지:** 공용 구단주 카드 앞면은 0~120을 막대의 80%, 120~200을
> 나머지 20%로 연속 표시한다. 200 초과는 막대만 포화하며 실제 수치·경기 계산 상한은 유지한다.
> 최종 수치의 글자색은 기본 카드 능력치 기준으로 0~59 흰색, 60~69 노랑, 70~89 주황,
> 90 이상 빨강이다. 추가 성장 수치와 출처별 막대색은 별도로 유지한다.
> 표시 규칙과 검증 범위는 `docs/reports/player-card-stat-gauge-20260912.md`를 따른다.

> **공통 기본 폰트:** 선수·구단주 모드의 uGUI Text는 `UIProjectFonts.Default`를 통해
> `Assets/08.Fonts`의 이사만루 Medium을 공유한다. TMP 기본 에셋도 Medium SDF이며,
> Light·Bold SDF는 별도 굵기로 제공한다. 생성 설정은 `Tools/Fonts/README.md`를 따른다.

> **포스트시즌·하이라이트 의상:** 축하 일러스트에는 승리 구단, 경기 삽입 컷에는 장면 주체의
> 공격·수비 구단 색상을 같은 계보 규칙으로 적용한다. 배경 포함 그림은 의상 영역 마스크로
> 하늘·관중석을 보호한다. [적용·검증 기록](docs/reports/owner-illustration-uniforms-20260912.md)을 따른다.

> **구단주 경기 캐릭터 색상:** 카드 초상과 같은 구단 계보 발급표·유니폼 원색을 사용해
> 공격팀의 타자·주자와 수비팀의 투수·포수·야수를 구분한다. 원화의 청색 의상·장비 영역에
> 색상 교체 셰이더를 적용하며 같은 계보끼리는 같은 색을 유지한다. 적용 범위와 검증은
> [경기 캐릭터 색상 기록](docs/reports/owner-match-uniforms-20260912.md)을 따른다.

> **선수 오버 스탯·공격 과잉 개선:** 원시 능력치와 보너스 합산의 HardCap은 250이다.
> SoftCap 150까지 보존하고 초과분만 50% 반영한다(180→165, 250 이상→200). 이후 공통 경기 곡선을 적용한다.
> 실전 구종 품질의 100 절단을 제거하고 고구위의 정타·타구 속도 억제와 고품질 투구의 승부 비중을 보완한다.
> 컨디션·포지션 적응도·구종 원본 숙련도는 0~100을 유지하며, 기존 카드의 원본 능력치·훈련 천장은
> 일괄 변경하지 않는다. 경기 엔진 버전은 11이다. 검증은 `docs/reports/overstat-offense-20260912.md`를 따른다.

> **구단주 특수 영입 UI:** 레전드·커리어하이의 대상 카드는 공용 상세 앞면 렌더러를 사용하고,
> 재료는 4×2 공용 미니 카드 보드에서 선택한다. 개별 프레임·초상·텍스트 수동 합성은 제거한다.
> 재료의 보유·보호·등록 상태, 확정 취소와 팝업 포커스 복원을 제공한다.
> 구조·검수 범위는 [특수 영입 UI 개선 기록](docs/reports/special-recruit-ui-20260911.md)을 따른다.

> **구단주 팀 컬러 편성 보드:** 반복되던 세 열을 상단 두 슬롯·후보 컬렉션·효과 상세로 재구성한다.
> 선수 오더의 보드 Theme와 기존 카드·버튼 자산을 재사용하며, 장착 가능 여부와 저장된 발동 효과를 구분한다.
> 장착 충돌을 사전에 설명하고 목록 선택 시 스크롤을 유지한다. 경기 밸런스와 저장 구조는 변경하지 않는다.
> 구현과 검증 범위는 [팀 컬러 UI 개선 기록](docs/reports/team-color-ui-board-20260911.md)을 따른다.

> **구단주 팀 컬러 탐색·교체:** 후보 목록에서 이름·능력치 효과를 함께 비교하고 이름·효과 검색,
> 타자/투수 대상 필터와 등급·효과 합계·부족 인원·이름 정렬을 제공한다. 기본 순서는 장착 가능 여부와
> S→A→B→C 등급순이다. 상세의 슬롯 1·2 장착/교체 버튼을 직접 사용하며 슬롯 사전 선택은 필요 없다.
> 각 슬롯 해제와 적용 전 되돌리기를 제공한다. [탐색·교체 검증 기록](docs/reports/team-color-discovery-20260912.md)을 따른다.

> **유학 메뉴 진입 비용:** 성장 화면은 요청한 Route를 먼저 정해 유학 진입에서 스킬 화면을 만들지 않는다.
> 보유 선수는 요약으로 조회하고 선택한 선수의 상세·유학 과정만 계산한다. 선수 선택은 페이지 단위로
> 표시하며 Runtime 변경 시 조회를 갱신한다. 변경과 검증은
> [유학 진입 성능 기록](docs/reports/owner-study-entry-performance-20260911.md)을 따른다.

> **구단·전력보강 메뉴 진입 비용:** 구단 카드 집계는 요약 정보만 조회하고,
> 전력보강은 선택한 탭만 계산·Bind한다. 스카우트 진입에서 숨겨진 훈련·강화 카드 목록을
> 생성하지 않으며 Runtime 변경 시 조회 묶음을 무효화한다. 변경과 검증 범위는
> [메뉴 진입 성능 개선 기록](docs/reports/owner-menu-entry-performance-20260911.md)을 따른다.

> **2026-09-11 다른 게임의 가격 출처 제외:** MVP 베이스볼 온라인 기사를 프야매 가격으로 사용한
> 10장의 관측 덮어쓰기를 제거했다. Cost v21 / Balance v30은 기존 모델·전력 하한으로 7장 가격을
> 정정하며 김광현 2010은 C10, 레전드 기준도 실제 2010 Peak로 복원한다. 특수 카드는
> EX 73·레전드 105·커리어하이 116·레어 363, 총 657장·221레시피다.
> 근거와 검증은 [잘못된 게임 출처 가격 정정](docs/reports/rejected-source-cost-20260911.md)을 따른다.

> **2026-09-11 구단주 경기 UI 안내 정리:** 경기 결과에서 감독의 내부 성향 수치·판단 트레이스를 제거하고 기록표를 확장했다.
> 관전의 반복 AI 안내와 표현 방식 설명을 정리하며, 실제 플레이·교체 사유는 사건 발생 후 표시한다.
> 경기 준비는 구체적인 출전 불가 사유를 우선하고, 감독방침은 원시 임계값 대신 운영 방향을 설명한다.
> 오류 원문은 로그로 보내고 화면에는 복구 안내를 제공한다. 경기 규칙·밸런스·저장 구조는 변경하지 않는다.
> 조사 범위와 적용·검증 기록은 [구단주 경기 UI 안내 조사](docs/reports/owner-match-debug-ui-audit-20260911.md)를 따른다.

> **구단주 관전 헛스윙 타이밍:** 헛스윙 시트를 투수 준비 동작과 동시에 시작하던 경로를 수정했다.
> 투구 후반에 스윙 전체를 배치하고, 지켜본 공은 준비 자세를 유지한다. 경기 판정과 밸런스는 변경하지 않는다.
> 검증 범위는 `docs/reports/owner-swing-miss-timing-20260911.md`를 따른다.

> **구단주 번트 하이라이트:** `BuntAttempted` 공개에 번트 준비 전용 삽입 컷을 연결한다.
> 현재 경기 타자 스프라이트의 치비 비율·파란 모자·피규어 셰이딩을 참조한 이미지이며,
> 접촉·성공 결과는 선공개하지 않는다. 제작·검증은
> [번트 하이라이트 기록](docs/design/sprite_sheet_ingame/highlight-insets-v1/bunt-implementation.md)을 따른다.

> **2026-09-11 관측 코스트·재평가·특수 카드 정정:** Cost v20 / Balance v28은 확인된 일반 카드
> 가격을 최종 전력 하한보다 우선한다. 501장의 가격을 정정했으며, 재평가 전 2000년 김동주
> Cost 8은 사용자가 확인한 재평가 후 10으로 대체했다. 김동주 레전드 기준은 명시적 2000년이다.
> 레전드 원본 9~10·EX 원본 10 규약은 유지한다. 미달 EX 19장은 취소하여 EX 69·레전드 20·
> 커리어하이 114·레어 363, 566장·134레시피를 동기화했다. 전후 각각 정규시즌 13,824경기의
> 통계·순위·선수 기록은 동일하다. 타자 주 능력치는 번트 독립·송구의 수비 통합으로 확정했다.
> 후속 작업에서 번트 코드·44년 콘텐츠 전환과 역사 개인 타율 H/AB 집계를 반영했다.
> 실제 역사 성적 재현의 추가 보정·검증 상태는 [번트 독립 전환 기록](docs/reports/bunt-primary-stat-20260911.md)을 따른다.
> 가격 정정 당시의 근거와 범위는
> [관측 코스트 복원 기록](docs/reports/observed-cost-restoration-20260911.md)을 따른다.

> **2026-09-12 UI Identity 표시 계약:** 선수·구단주 모드의 구단명, 선수명, 엠블렘은 하나의
> Identity 모드로 함께 전환한다. 실제 모드는 원본 `PlayerPerson`과 해당 연도의 `TeamSeason` 브랜드·엠블렘을,
> 가상 모드는 Save에 보존된 가상 선수·프랜차이즈 이름과 가상 엠블렘을 사용한다. 모든 구단명은
> 플레이어 구단을 포함해 `원본연도 구단명`으로 표시하며, 입력한 구단주 별칭으로 이 쌍을 분리하지 않는다.

> **모드별 다중 저장 슬롯:** 선수 모드와 구단주 모드는 각각 5개 슬롯에 저장·불러오기한다.
> 슬롯 1의 기존 경로는 유지하며 선수 자동 백업은 슬롯별로 분리한다. 저장·불러오기 성공 시에만
> 후속 저장 대상이 바뀌며, 구단주 새 게임은 선택한 빈 슬롯에 첫 진행을 저장한다.
> 마지막 투구의 볼넷·삼진 아웃 표기와 결과 표시 시간도 보완했다. 저장 DTO와 경기 밸런스는 변경하지 않는다.
> 계약과 검증 범위는 [다중 슬롯·마지막 투구 표시 기록](docs/reports/save-slots-and-pitch-outcomes-20260911.md)을 따른다.

> **구단주 관전 표시 보완:** 공 최소 크기와 좌·우 타격 접점을 보정하고, 이닝별 득점표를 제목·구단별 행·고정 이닝 열로 분리했다.
> 상단 점수판·선수·중계·결과 기록은 실제 Identity 표시 설정을 함께 사용하며 일반 Out과 타석 종료의 중복 아웃 문구를 제거했다.
> 검증 범위는 [관전 표시 보완 기록](docs/reports/owner-match-visibility-20260911.md)을 따른다.

> **2026-09-11 구단주 미지급금 이월:** 시즌 종료 급여 부족은 다음 시즌을 차단하지 않는다.
> 현금으로 지급 가능한 만큼 정산하고 부족액은 `ManagerEconomyState.ContractArrears`에 보존한다.
> 이후 Money 수입은 미지급금을 우선 상환하고 잔액만 구매 가능한 현금이 된다. 이자·급여 면제는 없다.
> 시즌 전체 종료 후 만료 임박 선수의 갱신 계약금도 부족액 이월을 허용한다. 갱신 선택 자체는 유지하며,
> 시즌 중 갱신·만료 임박이 아닌 계약의 추가 연장·일반 구매·신규 스태프 영입에는 외상을 허용하지 않는다.
> 구단주 SaveVersion 21에 미지급금을 저장하고 홈·재정·계약 Preview에서 이월액과 상환 규칙을 설명한다.
> 검증 범위는 `docs/reports/owner-contract-arrears-20260911.md`를 따른다.

> **구단주 특수 카드 1군 배치 제한:** 플레이어 구단의 25인 1군에는 Legend·CareerHigh를
> 합산 최대 4장까지 등록하며, 타자와 투수는 각각 최대 3장까지 허용한다. 따라서 타자 3장+투수 1장,
> 타자 1장+투수 3장은 가능하지만 한쪽 4장 또는 합계 5장 이상은 불가능하다. 선수 오더 상단은
> 프리셋 이름·상태 대신 Legend/CareerHigh 사용 수와 현재 선택한 TeamColor를 표시한다.

> **구단주 새 게임 첫 시즌 연도:** 구단 선택 화면의 기준 연도와 관계없이, 메인 카드 10장 중
> 최고 Cost 카드의 원본 연도를 첫 운영 시즌으로 사용한다. 최고 Cost가 같으면 더 최신 연도,
> 연도도 같으면 `CardId` 오름차순으로 기준 카드를 고정한다. 플레이어 구단은 기준 카드의
> `OriginTeamSeasonKey`로 해당 연도 리그에 다시 연결하며, 스타터 로스터·일정·순위표·세이브가
> 모두 같은 연도를 사용한다.

> **선수 오더 사용성 보완:** 교체 Preview에서 카드·스크롤·페이지를 유지하며, 보유 목록의 필터·정렬·역할 탭·페이지 조작은 고정하고 카드만 스크롤한다.
> 같은 선수의 다른 등급·연도 카드가 배치된 경우 해당 선수 슬롯과 직접 교체할 수 있으며, 다른 슬롯의 동일 선수 중복 등록은 기존 검증으로 차단한다. 상단 카드 비율에 맞춰 패널 높이를 조절하고 외국인 인원 표시는 요약에서 제외한다.
> 구단 운영 안내는 홈에서만 열 수 있으며 다른 화면에는 안내 공간을 남기지 않는다. 검증 상태는 `docs/reports/roster-ui-usability-20260911.md`를 따른다.

> **2026-09-11 최종 발급 전력과 Cost 연결:** Cost v19 / Balance v27은 최종 능력치에 역할 가중치를
> 적용한 가격 하한을 기존 기록·연도 최고 성과·불펜 역할 평가와 함께 사용한다. 보정 전 가격이
> 보정 후 능력치에 그대로 남는 문제를 해결해 일반 카드 531장의 Cost만 변경했다.
> 김동주 2003년은 Cost 7→10이며 명시적 레전드 20명 전원과 커리어하이 148명이 발급된다.
> 전후 각 정규시즌 11,520경기의 팀·선수 기록·순위·선발 순환은 동일하다.
> 로컬 Editor/Runtime 정본·카탈로그를 동기화했고 실제 Fast/Full 로더가 619장·168레시피를 검증했다.
> 레전드 미달 대상을 자동 제외하던 직전 대응은 철회했다. 상세 근거와 검증 범위는
> `Tools/KBOImporter/FinalIssuedAbilityCost.md`를 따른다.

> **2026-09-11 프런트 매니저 기획 대조:** 구조 확인 전 작성된 안내 고도화 원안을 실제 Guide·Owner CTA·저장 경로와 대조했다.
> 기존 Guide를 확장해 선수단 검증·경기 준비·경기 후 점검의 세 흐름을 연결했다. 홈 수동 안내와 실제 대상 이동을 분리하고,
> 보류·유지·원본 해결 이력을 Owner 저장 버전 20에 저장한다. 범위·검증·남은 수동 검수는
> [프런트 매니저 단계별 구현 결과](docs/todo/매니저%20모드/프런트_매니저_단계별_구현_결과.md)를 따른다.

> **2026-09-11 스프라이트 경기 표현 연결:** 빈 경기용 카탈로그를 검수한 9클립 70프레임으로 채워
> 구단주 관전의 스프라이트 경로를 활성화했다. 포수·내야 수비·주루 원본을 보완하고,
> 주루 참조·진행 방향·타자 중복 표시·포수 크기·구장 배치를 수정했다.
> 사용자 검수 뒤 원근·공 잔류·주루 시간·베이스 대기 시트를 보완했다.
> 격리 Unity EditMode 50개와 4개 해상도 정적 합성을 검증했다(Python 8개는 이전 실행 기록).
> 실제 상세 경기 954이벤트를 1·2·4배속으로 재생했고, 주루 동시 출발·송구 수신·병살 중계를 연결했다.
> 중단 작업 재개 후 관전 세션 3개 모드 × 3개 배속의 전체 재생·공개 경계·최종 점수를 검증했다.
> 이닝 점수표의 EditMode 객체 삭제 오류를 수정하고 타격 카메라 전환 회귀를 추가했다.
> 타격·포구·홈런·슬라이딩·호수비·송구 삽입 이미지 6종을 제작하고 공개 경기 사건에 연결했다.
> 확장 기본 필터 EditMode 74/74, 실제 경기 6종 발생, 4개 해상도 24개 배치 합성을 검증했다.
> 제작·연결·검증 범위는 [하이라이트 적용 기록](docs/design/sprite_sheet_ingame/highlight-insets-v1/README.md)을 따른다.
> 메인 프로젝트 Play Mode 입력·공용 셸 통합과 최종 아트 검수는 남아 있다.
> 전체 경기 Play Mode와 선수 커리어 흐름은 별도이며,
> 상세 근거는 `docs/design/sprite_sheet_ingame/RuntimeConnectionReport.md`를 따른다.

> **전체 역사 포지션 결측 보완:** 1982~2025의 3,199건 중 3,121건에 시즌별 근거를 추가한다.
> 94LG의 결측 14명은 0명으로 줄고, 전체 78건은 미확인 상태를 유지하며 UI에서도 DH와 구분한다.
> 기존 능력치·Cost·원기록은 유지하고 기본 로스터 120개를 재구성한다. 포지션 전용 재저작에서는
> `prepare_position_revision.py`로 발급 가치 평가를 보존한다. 전후 각 정규시즌 52,272경기와
> 연도별 결정론 44건, 전체 Runtime 참조 검증의 근거는 `Tools/KBOImporter/SeasonPositionRevision.md`를 따른다.

> **2026-09-11 기존 테스트 실패 정리:** 카드 8종·144경기 일정·구단주 초기화/선수 연봉/전술 소비·뉴스 키를
> 현재 사양으로 검증하고, 고정 로테이션 반올림과 시즌별 통계 불확실성을 구분했다. 벤치 평가 선발과
> 실제 대타 누적도 각각 검사한다. 경기 엔진·밸런스 변경 없이 Headless EditMode 808 통과·0 실패·
> 11 Explicit 제외를 확인했다. 근거는 `docs/reports/test-repair-20260911.md`를 따른다.

> **역사 강팀 검증의 고정 5선발 계약:** 고정 5선발에서 과거 구단이 받는 로스터 깊이·기용 비중의
> 불리함은 허용한다. 실제 승률에 맞추기 위해 5선발을 강화하거나 에이스 등판을 늘리거나
> 팀 성적·순위를 보너스로 주입하지 않는다. 순번별 경기의 팀 승패와 전체 승률 오차를 함께
> 기록하고, 포지션 결측·능력치 변환 오류를 먼저 검토한다. 5선발의 불리함은 자동 면제 조건이 아니다.
> 개인 타격 기록 산식 v12·파생 밸런스 v26과 Mental 추격 억제 .0045를 채택했다. 44개 연도×128시드,
> 전후 각 3,345,408경기에서 선두 승률 오차는 4.47→3.65%p, 평균 상위 3위는 43/47이다.
> 전체 통계 채택 조건은 통과했지만 개별 선두의 승률 ±5%p·상위 3위 동시 통과는 30/47이며,
> 모든 연도를 정확히 재현한 것은 아니다. 적용·검증·잔여 문제는 `docs/reports/fixed-five-strength-20260911.md`를 따른다.

> **2026-09-11 사구·선구 판단 검증(위 타격 산식 개정 이전 결과):** 공통 상세 경기의 사구 영역·접촉 확률을 밸런스로 분리하고,
> 스위치 타자의 사구 방향을 상대 투수의 손으로 정정했다(엔진 7). 2021~2022년 전후 각
> 정규시즌 46,080경기에서 팀 경기당 사구는 .118→.217(실제 .525), 승률 MAE는 4.32→4.28%p다.
> 44개 연도와 오차가 큰 4개 연도의 총 267,840경기에서는 선두 47개 중 승률 범위 26개·상위 3위 44개로,
> 강팀 재현 전체 기준은 여전히 미달이다. Mental의 추격 억제 강화 후보는 승률 정확도와
> 상세/간이 통계 비교가 악화되어 채택하지 않았다. 선수 Mental 산식의 볼넷 성향 누락을 다음 진단 대상으로 삼는다.
> 검증 수치·잔여 오차는 `docs/reports/plate-discipline-20260911.md`, 연도별 결과는
> `docs/reports/historical-leaders-hbp-20260911.md`를 따른다.

> **2026-09-11 역사 강팀 재현 목표:** 연도별 정규시즌 선두의 우위와 실제에 가까운 반복 승률을
> 선수 구성·능력치·기용에서 재현한다. 실제 팀 승률·순위를 경기 결과나 팀 보너스로 주입하지 않는다.
> 잠정 진단 기준은 실제 승률 ±5%p·반복 평균 승률 상위 3위·32시드이며 매 시즌 우승 보장은 아니다.
> 44개 연도 8시드와 오차가 큰 4개 연도 32시드의 중복 제거 정규시즌 267,840경기에서
> 47개 선두 사례 중 승률 범위 26개·상위 3위 44개다. 전체 기준은 아직 미달이다.
> 상세 수치와 표본 제한은 `docs/reports/historical-leaders-20260911.md`를 따른다.
> Condition 없는 입력의 배치 비용 누락은 중립값 기준 비용만 적용하도록 수정했다(엔진 6).
> 전후 검증과 다음 진단 대상은 `docs/reports/condition-assignment-20260911.md`에 기록한다.

> **2026-09-11 경기 종료 주루 정합성:** 공통 `DetailedMatchEngine`의 단타·땅볼 처리에서
> 끝내기 득점 뒤 후속 주자의 선택 진루·주루사를 중단한다. 엔진 버전은 5로 구분하며
> 기존 미커밋 밸런스·기용 변경은 보존했다. 재현 테스트와 대량 통계의 검증 범위는
> `docs/reports/walkoff-baserunning-validation.md`를 따른다.

> **카드 초상·장식 레이어 정리:** Full/Mini 각각 등급 공통 초상 크기를 사용하고,
> 배경 → 초상 → 장식 → 정보 순서로 합성한다. EX·MVP도 공통 구단 명찰을 제공한다.
> 작은 Full 갤러리는 기준 크기로 작성 후 축소해 정보 잘림을 방지한다.
> 독립 Unity 프로젝트의 실제 16장 렌더와 검증 범위는
> `docs/reports/card-layer-validation/README.md`를 따른다.

> **2026-09-11 연구 보충 Runtime 검증 복구:** 보충 939명의 `RosterRole`을 출처 문자열 대신
> 기존 예비 번호 다음의 결정론적 순번으로 생성한다. Editor DTO에 출처·원기록 확보 상태를 읽고,
> `ResearchCardSupplement`는 `Unavailable`과 기록 0건을 요구하며 일반 선수는 기록 1건을 유지한다.
> 1982~2025 재굽기·Python 24개·Editor 검증 소스의 전체 검사(오류 0, 경고 34)·원기록 회귀 5건,
> 실제 Runtime 로더의 특수 카드 594장·레시피 143개 전체 참조 검증을 통과했다.
> C# 회귀는 Headless JSON 어댑터로 실행했으며 Unity Test Runner·파이프라인 UI 전체 실행은 별도다.
> Editor 정제본과 특수 카드를 재생성했으므로 Unity 컴파일 후 파이프라인 **2단계부터 재개**한다.

> **2026-09-10 역사 콘텐츠 내보내기 복구:** 파이프라인 2단계가 새 Editor Runtime 정제본으로
> 특수 카드·레시피를 재발급하고, 일반 파일 복사 전에 전체 로더·재료 참조를 검증한다.
> 실패 시 이전 특수 카드가 새 일반 콘텐츠에 붙는 순서 오류를 수정했다. 현재 정제본 기준
> EX 88·레전드 20·커리어하이 123·레어 363장, 레시피 143개를 배포 파일에 반영했다.
> C# Editor·Editor Tests 보조 컴파일, Python 18개 및 실제 로더의 전체 참조·해시 오류 검증 통과.
> 열린 Unity 에디터의 파이프라인 전체 실행·EditMode 실행은 이번 수정에서 재검증하지 않았다.
> 재개 시 2단계부터 실행하면 된다. 아래 최초 발급 수량보다 이 항목이 우선한다.

> **2026-09-10 고코스트 평가·특수 카드 연결:** 일반 카드 가격 하한을 정밀 보정해
> 연도 최고 성과 52장과 구원 역할 성과 137장, 총 189장의 Cost만 변경했다.
> 기본 능력치·신원·포지션·로스터·원기록은 보존한다. Cost v18 / Balance v25이며
> 동일 빌드로 전후 각 정규시즌 11,520경기와 결정론을 검증해 통계·순위 동일을 확인했다.
> EX 88·레전드 20·커리어하이 122·레어 363장 및 142개 영입 레시피를 Unity에 등록했다.
> 레전드·커리어하이는 실제 대상·재료 선택과 확정 영입, 레어는 일반 Scout에 연결한다.
> 이전 EX Cost Gate 차단·132장 부분 Bake·Unity 연결 미완료 기록보다 이 항목이 우선한다.
> 일반 5장 합성의 Production UI·확률 곡선과 특수 카드 팀의 대량 경제·경기 평가는 별도 남는다.
> 근거와 검증 범위는 `docs/reports/고코스트_평가_특수카드_연결.md`를 따른다.

> **2026-09-09 구단 계보별 유니폼 연결:** 완성된 `uniforms-v3`의 576외형 × 10의상 5,760개를
> 등록하고 18,326개 선수 시즌에 원본 구단 계보의 의상을 배정한다. MBC·LG, 쌍방울·SK·SSG,
> 삼미·청보·태평양·현대·우리·넥센·키움 등을 게임의 10개 유니폼 계보로 묶는다.
> 같은 선수의 얼굴은 유지하고 원본 시즌 구단이 달라지면 옷만 변경한다. 등급·강화·현재 보유 구단은
> 원본 시즌 의상을 바꾸지 않는다. 실제 이름을 조건 분기로 쓰지 않으며 Presentation 카탈로그가
> Franchise ID를 조회한다. 분류표·검증은 `Tools/PlayerUniforms/README.md`를 따른다.
> Unity EditMode 4개와 최신 Presentation 소스 보조 컴파일이 통과했다. 이 기록은 이전의
> 유니폼 결합·계보 연결 미완료 설명보다 우선하며, 계보 전체 얼굴 무중복은 별도 미완료다.

> **2026-09-09 구속 결측 평가 개선:** 실측 구속이 없는 투수는 일반 카드 자료로 검증한 표시 구속
> 추정을 사용하고 원기록을 실측값으로 채우지 않는다. 확보된 일반 카드 구속은 유지하며 Cost와
> 다른 기본 능력치는 변경하지 않는다. Ability v11 / DerivationBalance v24, 선수 신원 Seed는
> 평가식 버전과 분리한다. 변경 전후 각 11,520경기의 상세 정규시즌과 결정론을 검증했으며
> 적용 범위·모델 오차·경기 지표는 `Tools/KBOImporter/VelocityEstimation.md`를 따른다.

> **2026-09-09 선수 초상 전체 발급 재개:** 기존 576종을 역사 인물 3,565명과 시즌 18,326건에
> 연결했으며 얼굴당 6~7명으로 균등 사용한다. 동일 인물은 시즌·등급·이적 후에도 얼굴을 유지한다.
> 구단·시즌 363개 내 중복은 0건이다. 최대 역사 계보가 586명이므로 아래의 계보 전체 무중복
> 목표는 미충족이며, 이번에는 이전 세션의 구단·시즌 단위 배정을 유지한다. 생성 커리어 선수는
> 안정 ID 해시로 같은 초상 풀을 사용한다. 현재 감사 통과와 이전 EditMode 3개 통과,
> 이번 Unity 라이선스 및 전체 보조 빌드 제한은 `Tools/PlayerPortraits/README.md`에 기록한다.
> 이 항목은 아래의 대표 초상 임시 공유·일괄 배정 미완료 기록보다 우선한다.

> **2026-09-09 스프라이트 경기 표현 기반:** `docs/design/sprite_sheet_ingame/` 기획에 따라
> 원본 시트 감사·크로마키 가공·프레임 메타데이터·Unity 검수/가져오기 도구와 공통 2.5D 무대를 추가한다.
> 구단주 중계와 선수 커리어는 기존 공식 결과 및 공개 시점을 유지하며 승인 카탈로그가 있을 때만
> 스프라이트 표현을 사용한다. 미검수 손잡이·모션은 경기 카탈로그에서 차단하며 기존 표현을 유지한다.
> 이는 아트 승인이나 실제 경기 시각 검증 완료를 뜻하지 않는다. 범위·검증·남은 항목은
> `docs/design/sprite_sheet_ingame/ImplementationReport.md`를 따른다.
> 중단 후 재개 작업에서 포수 대체 표시, JSON 선택 접점, 모션 사건 순서 검증을 보강했다.
> 타격 A/B의 이전 손잡이 판독은 시안과 충돌해 미확정으로 되돌렸으며 검수 전 경기 사용을 차단한다.

> **2026-09-09 선수 대표 초상 연결:** `output/imagegen/player-portraits/production-576-v1/faces/transparent/face-0017__r01.png`
> 한 장을 Unity Resources의 `UI/Portraits/img_player_representative`로 등록한다. 선수 프로필·카드의
> 공통 기본 초상으로 임시 공유하며 카드 상세 앞면은 정면 상반신이 들어오도록 표시한다.
> 선수별 외형·계보·유니폼 배정과 일괄 등록은 이미지 제작 완료 후 별도 작업으로 진행한다.

> **2026-09-09 특수 카드 발급·획득 기반:** EX·레전드·커리어 하이의 Cost/계보/레시피 검증,
> 5장 합성 Resolver, 특수 영입 예약·확정 거래, 스카우트 제외, 스킬블록 배율·계보 팀컬러 경계를
> 추가했다. SaveVersion 19는 특수 거래 원장을 저장하며 이전 버전은 빈 원장으로 복원한다.
> 후속 작업에서 레전드 20명·재료 160그룹을 확정하고 커리어 하이 112장과 함께 132장·132개
> 레시피의 부분 Bake를 검증했다. EX 성적 1위 88건 중 Cost 10 미달 52건은 규칙 변경 결정 전까지
> 계속 차단한다. 레전드 선정·재료 및 EX 충돌 근거는 `docs/design/SpecialCardCuration.md`를 따른다.
> Unity 발급 파일 로드·UI 연결·Production E2E·경기 밸런스는 미완료다. 전체 구현 완료가 아니며
> 상세 진행 상태와 검증은 `docs/reports/특수카드_발급_획득_구현.md`를 따른다.

> **2026-09-09 이미지 생성 배경:** 향후 배경 제거용 원본은 균일한 단색 크로마키로 제작한다.
> 기본 녹색 `#00FF00`, 전경에 같은 색이 있으면 마젠타 `#FF00FF` 등 겹치지 않는 색을 사용한다.
> 배경 그라데이션·질감·그림자·체크무늬·전경 색 번짐을 금지한다. 투명 도구 고도화 중에는
> 생성 후 프로젝트 경로에 원본과 프롬프트를 저장하는 데서 마친다. 분할·투명화·후처리는 별도 요청을 따른다.

> **576종 선수 외형 양산 기획:** 계보별 최대 인원 576명이라는 사용자 집계를 기준으로
> 고유 외형 576종 × 유니폼 10종(5,760조합)을 계획한다. 동일 인물의 얼굴 유지와 계보 내
> 중복 방지를 함께 검증하며, 576개가 전역 배정에 충분한지는 계보 간 이적 관계 감사로 확인한다.
> 제작 프롬프트·시트 분할·의상 변환·검수는 `docs/art/PlayerPortraitProduction/Production576.md`를
> 따른다. 수량·배정의 데이터 감사와 생성·합성기 구현은 아직 완료되지 않았다.

> **선수 초상화 양산 기준:** 사용자가 선택한 둥근 피규어풍 정면 초상화를 `figurine-v1`의
> 기준으로 고정한다. 유니폼과 외형 변주는 별도로 제작하고, 고정 프롬프트·참조 이미지·
> 배경 제거·검수 규약은 `docs/art/PlayerPortraitProduction/README.md`를 따른다.
> 이번 범위는 제작 체계 문서화이며 대량 생성·게임 배정·자동 합성 구현을 의미하지 않는다.

> **생성 이미지 배경 제거 규약:** 이미지 생성 후 배경 제거가 필요하면
> `Tools/ImageBackground/Remove-ImageBackground.ps1`을 사용한다. 원본과 분리된 RGBA PNG를
> 만들고 실제 알파 채널 및 전경 보존을 검증한다. 무채색 배경·단일 피사체 범위와
> 이미지별 보호 영역 지정법은 `Tools/ImageBackground/README.md`를 따른다.

> **2026-09-08 구단주 트레이드 제거:** 구단주 모드의 1:1 트레이드 기능과 `Owner.Club.Trade`
> 메뉴를 제거한다. 선수 계약과 보유 카드 기반 1군 등록 변경은 유지하며, 선수 커리어 모드의
> 동일 리그 트레이드는 이 결정의 대상이 아니다. 기존 Save에 이미 반영된 로스터 결과는 유지하지만
> 구단주 트레이드 영수증은 SaveVersion 17부터 저장하지 않는다.

> **2026-09-08 투수 카드 보직 상한:** 같은 원본 구단·연도의 전체 선수 카드풀에서
> 마무리와 셋업은 각각 최대 2명까지만 발급한다. 기존 보직 판정 후보 중 마무리는 세이브,
> 셋업은 홀드 내림차순으로 남기며 동률은 PlayerSeasonId 오름차순으로 고정한다.
> 초과 후보는 중간계투로 분류하고 후보가 부족해도 정원을 채우지 않는다.
> 같은 선수 시즌의 모든 카드 등급이 같은 보직을 공유하며 이적·현재 배치로 재평가하지 않는다.
> 실제 1군 배치 슬롯은 기존 선발 5·불펜 4·셋업 1·마무리 1을 유지한다.

> **2026-09-08 선호 타순:** 구단주 선수 카드는 원본 구단·연도 Core25의 주전 9명을 기준으로
> 상위(1·2번) 2명, 클린업(3·4·5번) 3명, 하위(6~9번) 4명을 발급한다. 상위 주력 합과
> 클린업 장타 합을 최대화하는 중복 없는 배정을 사용하고, 동점은 PlayerSeasonId로 고정한다.
> 벤치는 주력·장타·컨택의 상대 우세로 평가하며 투수는 적용 대상이 아니다.
> 같은 선수 시즌의 모든 카드 등급은 같은 선호를 공유하며 강화·이적·라인업 변경으로 재평가하지 않는다.
> 선호 타순은 저장 컨디션과 경기 전 컨디션의 보통 하한을 보장하고, 부적합 타순은 추가 하락 추첨을 적용한다.
> 출전량 소모는 없으며 기존 독립 RNG 스트림·투수 피로는 유지한다. 조정값은 컨디션 밸런스 데이터가 소유한다.
> AI 경기·공개 조회와 새 게임 기본 타순은 같은 선호 배정으로 정렬한다. 기존 저장의 플레이어 타순은 유지한다.
> 원본 콘텐츠에서 카탈로그 생성·로드 때 파생하므로 저장 필드·버전 변경은 없으며 선수 커리어·역사 베이크에
> 컨디션 효과를 적용하지 않는다. 카드 뒷면에는 기존 프레임 위에 선호 타선 명판을 덧붙인다.
> 검증 결과는 `docs/reports/preferred-batting-order.md`를 따른다.

> 싱글 플레이 야구 **선수 커리어 시뮬레이션**

> **2026-08-26 방향 전환:** 이 프로젝트는 "구단을 운영하는 매니저 게임"에서 "선수 1명을 만들고 성장시키는 커리어 시뮬레이션"으로 장르를 확정했다.
> 플레이어는 로스터·라인업·전술을 편성하지 않는다. 그건 감독 AI가 한다. 플레이어는 **어느 구단과 계약할지**, **자신의 훈련·유학 등 개인 성장 방향**을 결정하고 자기 자신의 출전과 성적을 지켜본다.
> `BaseballManager_GAME_SYSTEM.md`는 "구단주 겸 감독이 전체 로스터를 편성한다"는 옛 전제로 쓰여 폐기 대상이다. 리그 승강 구조(`LeagueGrade`) 등 장르와 무관한 부분만 참고하고, 나머지(로스터 편성 `Rookie Tryout`, `TacticCard`, `ManagerAi`에게 플레이어가 지시하는 구조 등)는 따르지 않는다.
> 이 문서에서도 8·9·12·13·16·17절(라인업/투수 운영/경기 중 개입/경기 AI/드래프트/트레이드)은 "플레이어가 팀을 운영한다"는 옛 전제로 쓰인 절이라 재작성이 필요하다 — 해당 의사결정의 주체를 감독 AI로 바꾸고, 플레이어는 자신의 계약·이적·개인 훈련만 결정하는 것으로 재정의해야 한다. 새 게임 플로우는 40절을 따른다.

> **2026-08-31 방향 확정:** 사용자 지시로 **구단주 모드(구단 전체 운영)를 범위 안으로 재도입**한다. 이는 5~8행의 "구단주 모드 범위 밖" 결정, 그리고 23절(946~971행)의 "카드=UI 표현, 가챠 금지" 결정을 **구단주 모드에 한해서만** 뒤집는 확정 변경이다.
> 상세 설계는 `docs/todo/역사시뮬레이션_구단주모드/`에 있다. 핵심은 (1) 선수 커리어 모드는 기존 결정대로 그대로 유지하고 구단주 모드를 **별도의 두 번째 진입점**으로 신설하며, (2) 선수 카드 수집·스카우트 뽑기·중복 강화·전술카드 경제는 구단주 플레이어 전용이지만 **팀컬러 효과는 구단주 모드의 AI 구단에도 적용**하며, 선수 커리어에는 이 장착 정책을 적용하지 않고, (3) KBO Source Person/PlayerSeason/TeamSeason을 Offline Bake의 1:1 정본으로 유지하고 Runtime에는 실제 이름·Source ID·실제 구단명을 노출하지 않는다는 것이다. Core25는 해당 Source TeamSeason 내부 선수로만 구성하고, 원본 부족을 타 구단 선수나 생성 선수로 조용히 채우지 않는다. World에서는 표시 Identity와 Detailed Simulation History만 가상화하며 상세 계약은 42절과 구단주 모드 01~03/08절을 따른다.
> 5~8행·23절 본문은 "선수 커리어 모드에 한정된 결정"으로 좁혀 읽는다 — 별도 개정 없이 구단주 모드 문서(위 링크)가 우선한다.

> **2026-09-06 AI 팀컬러 적용:** 구단주 모드 AI는 현재 25인 로스터에서 발동한 후보의 대상자별
> 능력치 보너스 총합 내림차순, ID 오름차순으로 최대 두 슬롯을 선택한다. 공통 HighestOnly와
> Stackable 규칙을 지키고 일반·합성 참가팀에 같은 정책을 적용한다. 경기 직전 로스터로 계산해
> 1군 등록 변경·불러오기에 대응하며, 플레이어 상대 경기·AI 대 AI·시즌 일괄 진행과 상대 UI가 같은
> Query와 DetailedMatchEngine 입력을 사용한다. UI를 열어야 발동하는 효과가 아니다.
> 현재 로스터에서 파생하므로 저장 필드 추가나 재베이크는 필요 없다. 이미 완료한 경기와
> 새 게임 이전 역사 베이크는 변경하지 않는다. 검증은 `docs/reports/owner-ai-team-color.md`를 따른다.

> **2026-09-07 AI 전술·투수 휴식 정책:** 구단주 모드 AI는 챔피언리그 이하에서 전술 카드를
> 사용하지 않는다. 마스터·갤럭시는 기존 자동 선택을 유지하며 플레이어의 전술 장착·소모는 유지한다.
> 등록 투수 전원은 경기마다 최근 투구 부하를 갱신하고, 쉬는 선발은 0구로 기록한다.
> 경기 사이 휴식 라운드도 다음 경기 입력과 확정 상태에 반영하며 일정에서 파생해 불러오기·재시도 시
> 중복 반영하지 않는다. 플레이어와 AI 대 AI는 공통 경로를 사용한다.

> **2026-09-07 컨디션 방향 확정:** 구단주 모드는 주전 풀타임 출전을 기본으로 하며,
> 출전 경기 수·타석·투구 수·휴식 여부에 따라 저장 컨디션을 차감하지 않는다.
> 컨디션은 별도 결정론적 난수의 등락과 중립값 부근으로의 복원력으로 갱신한다.
> 경기 중 투구 수·최근 연투 부하에 따른 투수 피로는 컨디션과 별도로 유지한다.
> 선수단 컨디션 관리 수석코치의 경기 한정 보너스와 상점의 즉시 사용 컨디션 키트를 제공한다.
> 코치 효과·등락 폭·복원 계수·아이템 효과·가격은 밸런스 데이터가 소유하며, 아이템 효과는
> 기존 선수 상태와 구매 원장에 저장한다. 이 결정은 출전 소모·휴식 회복을 전제로 한 이전 설명보다 우선한다.
> 검증은 `docs/reports/owner-condition-full-time.md`를 따른다.

## 1. 프로젝트 개요

### 한 줄 정의

플레이어가 야구선수 캐릭터 1명을 생성해, 프로 구단과 계약하고 루키리그부터 시작해 경기 출전·개인 성장·이적을 거치며 자신만의 선수 커리어를 만드는 싱글 플레이 야구 시뮬레이션.

### 핵심 방향

- 온라인 서비스가 아닌 완전한 싱글 플레이 게임
- 실제 KBO 구단/선수 라이선스에 의존하지 않는 가상 리그
- 3D 물리 액션보다 캐릭터 성장과 시뮬레이션에 집중하되, 선수 관여 상황에는 선택형 2D 직접 입력을 제공
- 구단 로스터·라인업·전술은 감독 AI가 편성한다 — 플레이어는 자기 자신의 커리어만 관리한다
- 과거 엔트리브 `프로야구 매니저`의 선수 수집·자동 경기 관전·성장 재미를,
  "내가 만든 팀"이 아니라 **"내가 만든 선수"** 축으로 재해석
- 장기적으로 여러 시즌이 누적되며 한 선수의 커리어 역사(성적·수상·이적·은퇴)가 쌓이는 구조

---

## 2. 게임의 핵심 재미

### 2.1 내 선수가 경기하는 것을 지켜보는 재미

기본 진행은 자동 시뮬레이션이며, 선택형 직접 플레이에서는 자기 선수의 투구 의도와 스윙 실행만
입력한다. 라인업·교체·팀 전술 편성 권한은 계속 감독 AI가 가진다.

직접 플레이는 안타·홈런·삼진을 반환하는 별도 액션 게임이 아니다. 타자는 스윙 여부·배트 위치·
정규화된 입력 시점을, 투수는 구종·목표 위치를 공통 경기 시뮬레이션에 제출한다. 실제 제구, 컨택,
타구, 수비·주루와 공식 기록은 자동 진행과 같은 Resolver가 판정한다.

대신 경기 전에 결정한 다음 요소들이 **내 선수의 커리어**에 영향을 준다.

- 어느 구단과 계약했는가 (전력·육성 환경·포지션 경쟁)
- 개인 훈련·유학으로 어떤 능력치를 키웠는가
- 4×4 Skill Board를 어떻게 채웠는가
- 컨디션과 부상 관리

감독 AI가 내 선수를 출전시킬지, 어떤 타순·역할로 쓸지 결정한다. 플레이어는 자신이 키운 선수가 실제 경기에서 어떤 성적을 내는지 관찰하고, 왜 벤치에 앉았는지·왜 잘했는지를 이해한다.

### 2.2 선수에게 애착이 생기는 구조

선수는 단순한 카드가 아니라 여러 시즌 동안 커리어를 가지는 하나의 인물이다.

예:

```text
2032년
18세 신인 드래프트 1라운드 지명

2035년
주전 유격수 정착

2038년
리그 MVP

2043년
부상 이후 성적 하락

2048년
구단 프랜차이즈 스타로 은퇴
```

선수의 성장과 노쇠, 기록, 수상 경력, 포스트시즌 활약 등이 누적되도록 한다.

### 2.3 계속 다음 경기를 진행하고 싶은 짧은 루프

기본 플레이 루프:

```text
훈련 / 개인 컨디션 관리
↓
감독 AI가 출전 여부·기용 결정
↓
경기 진행 (감독 AI가 라인업/로테이션 운용)
↓
내 선수의 경기 결과 확인
↓
개인 기록 / 감독 평가 확인
↓
성장 블록 획득 / 4×4 배치 / 부상 / 이벤트
↓
다음 경기
```

정규 시즌 종료 후:

```text
시즌 개인 기록 결산
↓
수상 / 등급 변화
↓
은퇴 여부 판단 (선수 본인)
↓
FA / 이적 제안 검토
↓
잔류 또는 새 구단과 계약
↓
다음 시즌
```

---

## 3. 목표 플레이 경험

### 플레이 템포

한 경기의 플레이 시간은 약 1~3분을 목표로 한다.

플레이어는 다음 방식 중 하나를 선택할 수 있다.

1. 즉시 결과
2. 핵심 장면만 보기
3. 전체 경기 빠른 중계

전체 경기 관전도 실제 야구 경기처럼 길게 만들지 않는다.

### 게임 세션

짧게는:

```text
게임 실행
→ 라인업 확인
→ 3~5경기 진행
→ 종료
```

길게는:

```text
한 시즌을 연속해서 진행
→ 포스트시즌
→ 드래프트
→ 다음 시즌 준비
```

둘 다 자연스럽게 가능하도록 한다.

---

## 4. 세계관 / 리그

실제 프로야구 라이선스를 사용하지 않는다.

### 기본 공식 월드

- Rookie / Minor / Major 3개 독립 프로리그가 같은 월드에 동시에 존재한다.
- 리그별 8개 구단, 전체 24개 구단을 기본 공식 규모로 사용한다.
- 구단은 승격·강등하지 않고 선수만 오프시즌 계약으로 인접 단계 사이를 이동한다.
- 각 리그는 팀당 144경기 정규시즌과 상위 4개 팀 포스트시즌을 독립적으로 진행한다.
- 플레이어가 속하지 않은 리그도 같은 경기 규칙으로 승패·순위·선수 기록을 만든다.
- 상세 소유 구조와 이동 시장 규칙은 41절을 따른다.
- 역사 Source 구단의 연고지는 실제 계보에 고정한다. LG·두산·키움 계보는 서울, 롯데는 부산,
  SK/SSG는 인천, 삼성은 대구, 한화는 대전, 해태/KIA는 광주, KT와 현대 계보는 수원,
  NC는 창원, 쌍방울은 전주를 사용한다. 실제 근거가 없는 추가 지역은 후보에 넣지 않는다.
- 구단 표시 이름은 World 생성 시 한 번 확정해 저장한다. World Seed는 가상 별칭 선택에만 영향을
  주며 같은 Source Franchise의 연고지를 바꾸지 않는다.

예시:

```text
서울 블레이즈
인천 웨이브
수원 폭스
대전 스톰
광주 타이거즈
부산 마리너스
대구 크라운
창원 팔콘스
```

구단명은 임시이며 실제 제작 과정에서 별도 확정한다.

### 장기 확장

- 10개 구단
- 2군
- 고교 / 대학 선수 풀
- 해외 리그
- 외국인 선수
- 국제대회

초기 버전에서는 제외한다.

---

## 5. 선수 시스템

## 5.1 기본 정보

```text
이름
나이
생년
투타
포지션
소속 구단
등번호
연봉
프로 경력
계약 기간
```

## 5.2 타자 능력치

초기에는 지나치게 세분화하지 않는다.

```text
Contact     컨택
Power       장타
Eye         선구안
Speed       주루
Defense     수비
Arm         송구
```

선수 원시 능력치는 0~250 수치를 사용한다. 성장용 `AbilityRatings`는 1~250이며 백분율 상태와 구분한다. 경기 효과는 150 초과분만 50% 반영하고 원시 250 초과분은 버린다.

### Hidden / 내부 수치

```text
Potential       성장 가능성
Consistency     기복
Clutch          중요 상황
InjuryProne     부상 위험
WorkEthic       성장 성향
```

일부 수치는 플레이어에게 정확한 값 대신 등급으로 표시할 수 있다.

## 5.3 투수 능력치

```text
Stuff       구위
Control     제구
Movement    변화
Stamina     체력
Composure   위기 관리
```

### 추가 확장 후보

```text
Fastball
BreakingBall
OffSpeed
HoldRunner
Fielding
```

MVP 이후 필요할 경우 분리한다.

---

## 6. 선수 성장

선수의 능력치는 단순히 경험치를 채워 레벨업하는 구조가 아니다.

성장량은 다음 요소의 영향을 받는다.

```text
나이
Potential
출장 기회
현재 능력치
컨디션
부상
훈련
WorkEthic
랜덤 편차
```

### 성장 곡선 예시

```text
18~21세 : 빠른 성장
22~26세 : 안정적인 성장
27~30세 : 전성기
31~34세 : 완만한 하락
35세~   : 빠른 하락 가능
```

모든 선수가 동일한 곡선을 따르지는 않는다.

### 브레이크아웃

일부 선수는 특정 시즌 갑작스럽게 성장할 수 있다.

예:

```text
24세
Contact 58 → 67
Power   51 → 63
```

이를 통해 예상하지 못한 스타 선수가 등장하도록 한다.

---

## 7. 컨디션 / 피로도

경기마다 선수 상태가 달라진다.

```text
최상
좋음
보통
나쁨
최악
```

내부적으로는 숫자 값을 사용할 수 있다.

컨디션은 경기 능력치를 일시적으로 보정한다.

### 피로

특히 투수 운용에서 중요하게 사용한다.

```text
선발 투수
→ 등판 후 며칠간 회복

불펜 투수
→ 연투 시 피로 증가

야수
→ 연속 출장 시 소폭 피로 증가
```

---

## 8. 라인업

감독 AI가 선발 수비 위치와 타순을 설정한다. 플레이어는 라인업을 직접 편성하지 않고,
자신의 능력치·컨디션·감독 평가와 팀 내 경쟁 결과로 정해진 기용 계획을 확인한다.

### 타순

```text
1번
2번
3번
4번
5번
6번
7번
8번
9번
```

### 포지션

```text
C
1B
2B
3B
SS
LF
CF
RF
DH
```

### 벤치

감독 AI가 경기 상황에 따라 대타 / 대주자 / 수비 교체에 사용한다.

---

## 9. 투수 운영

### 선발 로테이션

초기 버전:

```text
1선발
2선발
3선발
4선발
5선발
```

### 불펜 역할

```text
Long Relief
Middle Relief
Setup
Closer
```

게임 AI는 지정된 역할과 컨디션을 기준으로 투수를 자동 기용한다.

---

## 10. 경기 시뮬레이션

게임의 가장 중요한 시스템.

실제 공의 강체 물리 시뮬레이션은 하지 않는다. 투구 궤적과 타격 평면은 정규화된 순수 데이터로
표현하며, 모든 최종 결과는 능력치·상황·결정론적 RNG를 사용하는 확률 모델로 처리한다.

자동 진행에서는 AI가, 직접 플레이에서는 사람이 같은 `PitchSelectionCommand`와 `SwingCommand`를
제출한다. 입력 공급자만 바뀌며 제구·컨택·타구·수비·주루 Resolver는 바뀌지 않는다.

기본 흐름:

```text
Pitcher
vs
Batter
↓
투수 우위 / 타자 우위 계산
↓
투구 결과
↓
Ball / Strike / Contact
↓
타구 결과 계산
↓
Out / Single / Double / Triple / HR
```

### 주요 입력값

타자:

```text
Contact
Power
Eye
Speed
Condition
Handedness
```

투수:

```text
Stuff
Control
Movement
Stamina
Condition
Handedness
```

상황:

```text
Pitcher Fatigue
Ball Count
Runner
Out Count
Inning
Stadium
Defense
```

### 결과

```text
Strikeout
Walk
Ground Out
Fly Out
Single
Double
Triple
Home Run
Error
Hit By Pitch
```

MVP에서는 희귀한 상황을 일부 생략할 수 있다.

---

## 11. 경기 화면

실제 3D 야구 게임 형태로 만들지 않는다.

### 기본 구성

```text
┌─────────────────────────────────┐
│              구장               │
│                                 │
│          간단한 선수 표시       │
│                                 │
├─────────────────────────────────┤
│  7회말 2사 1,3루               │
│                                 │
│  김도윤 vs 이재혁              │
│                                 │
│  초구 - 볼                     │
│  2구  - 스트라이크             │
│  3구  - 좌중간 깊숙한 타구!    │
│                                 │
│        2타점 2루타              │
└─────────────────────────────────┘
```

### 그래픽 방향

초기 버전에서는:

- 2D 야구장
- 선수 아이콘
- 공 이동
- 타구 궤적
- 주자 이동
- 간단한 이펙트

정도로 구성한다.

향후 필요하면 미니어처 형태의 3D 경기장으로 확장한다.

---

## 12. 경기 중 감독 개입

플레이어는 매 투구마다 조작하지 않는다.

중요 상황에서만 개입한다.

### 공격

```text
대타
대주자
번트
도루
히트앤런
적극 타격
기다리기
```

### 수비

```text
투수 교체
고의사구
수비 교체
수비 위치 조정
```

초기 MVP에서는 다음만 구현한다.

```text
대타
도루
번트
투수 교체
```

---

## 13. 경기 AI

상대 팀 감독 AI가 필요하다.

AI 판단 요소:

```text
점수 차
이닝
주자
아웃 카운트
투수 체력
투수 컨디션
좌우 상대
불펜 상태
선수 능력치
```

예:

```text
8회
1점 리드
선발 투수 체력 20%

→ Setup 투수 등판 확률 증가
```

완벽한 야구 AI보다 플레이어가 납득할 수 있는 의사결정을 우선한다.

---

## 14. 시즌 시스템

### 정규 시즌

MVP:

```text
8개 구단
팀당 144경기
```

모든 구단은 자동으로 시즌을 진행한다.

플레이어 경기 외의 경기는 즉시 시뮬레이션한다.

### 순위

```text
경기
승
패
무
승률
게임차
```

### 개인 기록

타자:

```text
AVG
OBP
SLG
OPS
HR
RBI
H
SB
WAR (후순위)
```

투수:

```text
ERA
W
L
SV
HLD
IP
SO
WHIP
WAR (후순위)
```

MVP에서는 WAR 같은 복잡한 지표는 제외 가능.

### 시즌 종료 파이프라인

```text
Preseason → RegularSeason → Postseason → SeasonReview → Offseason → Completed
```

`Completed`는 정규시즌 종료가 아니라 오프시즌까지 마친 시즌의 장기 보관 완료를 뜻한다.
정규시즌 마지막 경기에서는 순위와 신인 자격 스냅샷을 확정하고 정규시즌 통계를 동결한 뒤
반드시 `Postseason`으로 이동한다. 결산 내부 순서는 포스트시즌 결과 → 수상 → 내 선수 요약 →
수입 정산 → 계약 결과이며, 보상은 시즌·수상별 고유 ID 원장으로 한 번만 지급한다.

정규시즌 종료와 최종 시즌 결산은 같은 화면 명칭으로 합치지 않는다. 정규시즌 마지막 경기 뒤에는
`정규시즌 종료 → 최종 순위 → 포스트시즌 진입`을 먼저 공개하고, 우승 구단 확정 뒤에는
`포스트시즌 경기 요약 → 최종 결과 → 개인 수상/개인 성과 → 시즌 최종 요약 → 성장·수입 정산`을
확인 입력으로 진행한다. 이 동안 하단 탭과 뉴스 피드는 전체화면 리뷰 레이어 뒤에 숨긴다.

순위·포스트시즌·수상·정산 결과는 `SeasonReviewSnapshot` 한 인스턴스에 순서대로 확정한다.
결산 UI와 보상 계산은 이 값을 소비하며, 포스트시즌 우승·수상 뉴스는 각각 결과 장면을 확인한 뒤
`AfterPostseasonReveal` / `AfterAwardReveal` 관문에서 발행한다. 연출을 건너뛰어도 최종 시즌 요약에서
한 번 멈추고 두 뉴스 관문과 보상 원장을 동일하게 처리한다.

리그 전체 선수 통계가 원본이며 기존 `PlayerSeasonStatisticsState`는 내 선수 항목을 읽는 파사드다.
정규시즌·포스트시즌은 별도 `CompetitionStatisticsState`에 저장하고 투구 이닝은 아웃 카운트
정수로 누적한다. 트레이드 전후의 정규시즌 기록은 시즌 합계와 구단별 분할 기록을 함께 보관한다.
`StatisticsSchemaVersion`은 3이다. 시즌 리뷰 스냅샷 도입으로 세이브 `SaveVersion`은 9이다.

---

## 15. 포스트시즌

초기 확정 구조는 정규시즌 상위 4팀 토너먼트다.

```text
준결승 A: 1위 vs 4위 / 3전 2선승
준결승 B: 2위 vs 3위 / 3전 2선승
결승: 두 준결승 승자 / 5전 3선승
```

- 준결승 홈 배정은 상위/하위/상위 시드, 결승은 상위/상위/하위/하위/상위 시드 순서다.
- 포스트시즌에는 무승부가 없다. 최대 연장 뒤에도 동점이면 저장된 경기 RNG로 반드시 승자를 확정한다.
- 정규시즌과 포스트시즌 선수 기록은 `CompetitionScope`로 분리한다.
- 플레이어 구단이 살아 있는 동안에는 내 구단 경기를 한 경기씩 준비·관전·결과 확인 흐름으로 진행한다.
  미진출하거나 탈락한 뒤에만 남은 타 구단 경기를 자동 진행해 우승팀을 확정한다.
- 플레이어 팀이 미진출하거나 탈락해도 남은 경기를 진행해 우승팀과 `PostseasonMvp`를 결정한다.
- 계단식 포스트시즌은 후속 `PostseasonFormatDefinition` 확장으로만 남긴다.

정규시즌 동률은 상대 전적 → 득실차 → 총 득점 → 최소 실점 → 시즌 시작 시 저장한 고정
타이브레이커 순으로 시드를 정한다. 정규시즌 종료와 준결승 종료 뒤에는 각 1일 휴식을 적용하고,
피로·부상은 라운드 사이에 초기화하지 않는다.

---

## 16. 드래프트

매 시즌 종료 후 신인 선수가 생성된다.

### 신인 정보

```text
이름
나이
포지션
투타
현재 능력
Potential
스카우팅 평가
```

예:

```text
박태준
18세 / SS

타격      ★★★★☆
장타      ★★★☆☆
수비      ★★★★★
주루      ★★★★☆

Potential : S
```

### 드래프트

초기에는 전년도 순위 역순으로 진행한다.

```text
8위 → 7위 → ... → 1위
```

AI 구단 역시 필요 포지션과 잠재력을 고려하여 지명한다.

---

## 17. 트레이드

트레이드는 구단 운영 메뉴가 아니라, 현재 구단의 로스터에 막힌 내 선수가 다른 구단에서 더 많은
출장 기회를 얻는 커리어 이동 경로다. 선수와 AI 팀을 플레이어가 직접 골라 거래하지 않는다.

MVP 범위는 **동일 리그 내 선수 1명 ↔ AI 경쟁자 1명**의 추상화다. 내 선수가 목표 구단으로
이동하면 해당 구단에서 포지션과 가치가 가장 비슷한 AI 경쟁자 한 명이 이전 구단으로 이동한다.
지명권·현금·다대다 거래, 리그 간 트레이드는 월드 전체 리그와 모든 AI 선수 커리어를 영속화한
뒤의 후속 범위다.

### 17.1 시즌 내 시점과 안전장치

- 기본 80경기 시즌에서 41경기부터 56경기 마감일까지 4경기 간격으로 시장을 평가한다.
- 루키 첫 20경기에는 거래하지 않으며 한 시즌 최대 1회다.
- 이적 후 25경기 보호 기간을 데이터에 보관한다. MVP는 시즌 1회 제한이 더 강한 안전장치다.
- 관심 점수가 기준을 넘어도 별도의 결정론적 성사 확률을 적용한다.
- 마감 뒤 미결 관심은 `Failed`로 닫으며 다음 시즌에 그대로 이어지지 않는다.

### 17.2 양 구단 가치 평가

영입 구단은 선수 가치, 포지션 필요, 기존 경쟁자 대비 상승 폭, 포스트시즌 경쟁 긴급도,
계약 가성비를 평가한다. 현재 구단은 포지션 중복, 계약 만료 위험, 리빌딩 성향, 예산 압박,
선수 태도에서 현재 역할 중요도와 포스트시즌 경쟁도를 감점한다. **양쪽 점수가 모두 기준을
넘어야** 협상이 진행된다. 이름·구단별 특수 분기 없이 `CareerMovementBalance`로 기준을 주입한다.

### 17.3 선수 권한과 단계

선수는 거래 승인권 대신 에이전트에게 다음 태도를 전달한다.

```text
PreferToStay / Neutral / OpenToTrade / RequestTrade
```

잔류 선호는 매각 의향을 낮추고, 요청은 거래 가능성을 높이는 대신 감독 신뢰를 낮춘다. 관심은
`Interest → Rumor → Negotiating → Completed/Failed`로 진행하며, 팀 화면에는 관심 구단·예상 역할·
예상 출장 비율을 보여 준다. 뉴스 시스템은 이 확정 상태를 전달할 뿐 거래를 결정하지 않는다.

### 17.4 이동 원자성 및 기록

`PlayerMovementService`가 양 구단 로스터, 선수·계약의 `TeamId`, 감독 평가, 현재 팀 예상 역할,
리그 통계의 선수 소속을 한 번에 바꾼다. 기존 계약 기간·연봉·계약상 역할·성장·부상 상태와
시즌 합계는 유지한다. 정규시즌 통계는 구단별 `PlayerTeamStatisticsSplitState`도 누적하며 기록 화면에는
트레이드 날짜, 이전/새 구단, 이동 전/예상 역할과 팀별 분할 성적을 함께 표시한다.

---

## 18. 계약 / FA

계약은 금액만 고르는 화면이 아니라 **안정적인 역할과 더 높은 단계의 도전 사이의 선택**이어야
한다. 새 게임 입단, 기존 구단 재계약, 시즌 중 연장, 공개 시장은 평가 계수를 재사용하되
`ContractOfferChannel`로 경로와 공개 시점을 분리한다.

### 18.1 시즌 중 연장

기본 21~40경기 구간에서 계약 잔여 1시즌 이하, `StartingCompetition`, 감독 평가 60 이상,
시장 급여가 현재 계약의 120% 이상인 선수만 연장 후보가 된다. 기존 구단은 공개 시장보다 낮은
92% 급여 계수와 작은 계약금을 제안하는 대신 1~2년 안정성과 예상 역할·출장 비율을 제시한다.
수락하면 기존 계약 종료 뒤에 연장 기간을 붙이고, 거절하면 같은 시즌에 반복 제안하지 않는다.

### 18.2 시즌 종료 우선 협상

계약 만료 뒤에는 기존 구단이 공개 시장보다 먼저 한 번 평가한다. 선수 가치 25%, 포지션 필요
20%, 현재 역할 15%, 최근 성적 15%, 나이·Potential 10%, 가성비 10%, 감독 관계 5%에서
경쟁자와 예산 압박을 감점한다. 35점 미만이면 기존 구단 오퍼가 **0개**일 수 있다.

플레이어는 수락하거나, 거절하고 공개 시장을 열거나, 오퍼를 보류한 채 시장을 확인할 수 있다.
보류 시 기본 20%의 결정론적 철회 위험이 있다. 기존 구단 오퍼는 외부 오퍼 수와 별도로 평가하되
화면의 전체 최대 오퍼 수에는 포함하고, 공개 시장에는 현재 구단을 중복 포함하지 않는다.

### 18.3 공개 시장과 실패 상태

공개 시장은 현재 구단을 제외한 같은 리그 구단과 인접 리그 구단을 함께 평가하며 최소 정규 오퍼를
강제로 보장하지 않는다. 정식 외부 오퍼가 0개면 최후 수단으로 현재 리그의 1년
`BenchCompetition` 육성 계약 하나를 제시한다. 각 오퍼는 리그 단계, 기간, 연봉·계약금, 계약상 역할,
예상 출장 비율, 경로를 함께 보여 준다.

인접 상위 리그 정식 계약은 `Promotion`, 인접 하위 리그 재기 계약은 `Rehabilitation` 채널로
구분한다. 세이브 v8에서는 실제 대상 리그 구단의 오프시즌 로스터 스냅샷을 기준으로 선수의 리그
환산 전력, 기록 표본 신뢰도, Potential, 포지션의 최약·최강 경쟁자, 수요, 예산, 육성 등급을 평가한
구단만 오퍼를 만든다. 화면 최대 오퍼 수 안에서는 이 자격을 통과한 인접 리그 선택지를 우선 보존해
같은 리그의 높은 점수 오퍼에 가려지지 않게 한다. 상위 리그 자격을 통과한 구단이 0개면
`Promotion` 오퍼도 0개이며 최소 개수 보정은 하지 않는다.
트레이드 거부권·선수/구단 옵션·바이아웃·에이전트 협상도 후속 범위다.

---

## 19. 부상

선수는 경기 또는 시즌 진행 중 부상을 당할 수 있다.

```text
경미
1~3경기

보통
1~3주

중상
1~3개월

시즌 아웃
```

부상 확률은 다음의 영향을 받는다.

```text
InjuryProne
피로
나이
출장량
랜덤
```

---

## 20. 은퇴

선수는 나이와 능력치를 기준으로 은퇴를 결정한다.

예:

```text
35세
능력치 유지
→ 현역 지속 가능

34세
큰 부상
능력 급락
→ 은퇴 가능성 증가
```

프랜차이즈 선수의 은퇴는 별도 연출을 제공할 수 있다.

---

## 21. 선수 기록 / 역사

장기 플레이의 핵심 콘텐츠.

선수마다 커리어 기록을 저장한다.

```text
시즌별 기록
통산 기록
수상 기록
소속 구단
트레이드 기록
드래프트 순번
포스트시즌 기록
```

구단 역시 역사 정보를 가진다.

```text
역대 순위
우승 횟수
역대 감독
프랜차이즈 선수
팀 기록
```

---

## 22. 수상

`MVP`라는 이름은 개발 범위와 수상을 혼동하지 않도록 코드에서 사용하지 않는다. 수상은
`RegularSeasonMvp`와 `PostseasonMvp`로 명시한다. 초기 확정 수상은 다음과 같다.

```text
정규시즌 MVP
포스트시즌 MVP
신인왕
타격왕
홈런왕
타점왕
도루왕
평균자책점 1위
다승왕
탈삼진왕
세이브왕
골든글러브
```

모든 수상은 무작위 투표가 아니라 동결된 리그 전체 선수 통계로 결정한다.

- 정규시즌 MVP·신인왕·기록상·골든글러브는 정규시즌 기록만 사용한다.
- 포스트시즌 MVP는 우승팀 선수 중 포스트시즌 라운드 가중 기여도 1위다.
- 역할별 MVP 점수는 후보 집단 백분위로 0~100 정규화하고 팀 성적은 5%만 반영한다.
- 골든글러브는 실제 수비 기회·성공·난이도·기대 실점 억제만 사용하며 타격과 능력치 원값을 보지 않는다.
- 기록상은 동일 기록 공동 수상을 허용하고, MVP·신인왕·골든글러브는 고정 동점 규칙으로 한 명을 뽑는다.
- 결과에는 상위 3명과 지표별 점수 근거를 함께 보관한다.

---

## 23. 선수 획득과 카드 시스템

과거 온라인 매니저 게임처럼 과금형 카드 뽑기를 사용하지 않는다.

대신 선수 확보 수단을 게임 플레이 안에 둔다.

```text
신인 드래프트
트레이드
FA
자유계약
스카우팅
```

필요하다면 선수 UI 표현만 카드 형태로 사용한다.

즉:

```text
카드 = UI 표현
```

이지,

```text
카드 = 가챠
```

가 되지 않도록 한다.

---

## 24. UI 주요 화면

**2026-08-26 개정.** 아래는 구단 전체를 플레이어가 운영한다는 전제의 옛 화면 목록이었다
(`BaseballManager_GAME_SYSTEM.md`와 같은 시기). 40.7절에서 확정한 `BaseballManager_UI_FLOW.md`
설계로 교체한다. 메인은 선수 중심 대시보드이며 Global Navigation은 6개 업무 영역
(홈/경기/선수/팀/리그/커리어)으로 고정한다. 성장·기록·계약처럼 같은 상태와 시간 축을 공유하는
기능은 업무 영역의 Local Navigation으로 묶는다. 각 영역의 세부 화면 구조와 레이아웃은
`BaseballManager_UI_FLOW.md`를 기준 문서로 삼는다. 여기서는 화면 책임만 요약한다.

플레이어에게 보이는 리그명과 기록 지표는 한글을 정본으로 사용한다. 리그는 `루키 리그`부터
`갤럭시 리그`까지 전체 이름으로 표시하고, 공간이 좁은 곳에서만 `루키`·`올스타`·`갤럭시` 같은
한글 축약형을 쓴다. 기록 표시는 `AVG` 대신 `타율`, `ERA` 대신 `평균자책점`처럼 한국어 설명형
명칭을 우선한다. enum·저장 ID·테이블 열 Key 같은 내부 식별자는 영문을 유지하며 화면에 직접
노출하지 않는다. `MVP`·`FA`와 좁은 카드의 포지션 코드처럼 한국 야구에서 통용되는 표기만 예외다.

### 홈

선수 중심 메인 대시보드. 좌측 내 선수 패널(현재 역할), 중앙 다음 경기/경기 진행 CTA, 우측
시즌 현황/최근 성적, 좌측 하단 팀 내 포지션 경쟁, 중앙 하단 뉴스/이벤트 피드, 우측 하단 일정
6구역으로 고정한다 (`BaseballManager_UI_FLOW.md` 2절). 시즌 상태(정규 시즌/부상/오프시즌/
스프링캠프)에 따라 구역 내용이 바뀐다.

### 선수

선수 카드 상세, 프로필, 능력치.

### 성장 (`선수 > 성장`)

4×4 성장판과 스킬 블록 상점(가챠) 진입점. 정규 시즌 중에는 열람만, 배치 변경은 오프시즌에만
연다 (`BaseballManager_UI_FLOW.md` 11절, `BaseballManager_PROJECT.md` 39.5절).

### 경기

다음 경기, 전체 시즌 일정, 경기 결과.

### 리그

순위, 리그 선수 기록.

### 팀

로스터 열람, 포지션 경쟁, 감독 정보. 라인업·투수진·전술은 **열람 전용**이며 플레이어가
직접 편성하지 않는다 — 그 판단은 `ManagerAi`(39.6절) 소유다. 플레이어는 자신의 트레이드
태도만 설정하고, 관심 구단·예상 역할·출장 비율·마감일을 확인할 수 있다. 거래 대상·대가는
구단 AI가 결정하며 뉴스 피드는 관심·루머·확정 상태를 전달한다.

### 기록 (`커리어 > 시즌 기록 / 통산 기록 / 수상·하이라이트`)

커리어 기록, 시즌 기록, 수상, 명예의 전당.

### 계약 (`커리어 > 계약`)

현재 계약, 연봉, 상여 조건과 진행도, 과거 계약 (`BaseballManager_UI_FLOW.md` 12절).

### 경기 화면 (Match)

경기 진행 중 스코어보드, 구장 연출, 중계, 선수 정보를 보여준다. 감독 명령 화면은 없다 —
경기 중 작전·교체는 `ManagerAi`가 판단하고, 대타 기회 같은 플레이어 개입은
`BaseballManager_UI_FLOW.md` 4.2절의 이벤트 호출(`[출장]`)로만 발생한다.

---

## 25. 데이터 구조 방향

게임 데이터와 런타임 세이브 데이터를 분리한다.

### 정적 데이터

```text
TeamDefinition
PlayerArchetype
LeagueDefinition
StadiumDefinition
NameDatabase
BalanceTable
```

### 런타임 데이터

```text
LeagueState
TeamState
PlayerState
SeasonState
ScheduleState
MatchState
```

예:

```csharp
public sealed class PlayerState
{
    public int PlayerId;

    public int Age;
    public int TeamId;

    public BatterAttributes Batter;
    public PitcherAttributes Pitcher;

    public float Condition;
    public float Fatigue;

    public PlayerCareer Career;
    public PlayerContract Contract;
}
```

ScriptableObject 자체를 세이브 데이터로 사용하지 않는다.

---

## 26. 경기 시뮬레이션 아키텍처

> `docs/지침/Simulation_Architecture_Guidelines_UPlayBall.md`로 이동 (2026-08-28). 경기 시뮬레이션/표현 분리, 결정론적 시뮬레이션 지침은 해당 문서를 따른다.

---

## 27. 결정론적 시뮬레이션

> `docs/지침/Simulation_Architecture_Guidelines_UPlayBall.md`로 이동 (2026-08-28).

---

## 28. 밸런스 테스트 도구

> `docs/지침/Balance_Testing_Guidelines_UPlayBall.md`로 이동 (2026-08-28).

---

## 29. 세이브

싱글 플레이이므로 전체 리그 상태를 저장한다.

```text
현재 날짜
현재 시즌
모든 구단
모든 선수
선수 기록
경기 결과
리그 순위
스케줄
드래프트 정보
계약
부상
역사
Random Seed
```

세이브 파일은 버전 정보를 반드시 가진다.

```text
SaveVersion
```

---

## 30. MVP

첫 플레이 가능한 버전의 범위를 강하게 제한한다.

### League

- 8개 가상 구단
- 구단당 25명
- 팀당 144경기 시즌
- 4팀 포스트시즌

### Player

- 타자 능력치 6개
- 투수 능력치 5개
- 나이
- 성장
- 컨디션
- 피로
- 간단한 부상

### MyPlayer (신규)

- 캐릭터 생성 (이름/투타/포지션/초기 능력치 배분)
- 구단 계약 오퍼 비교와 입단 선택
- 개인 기록 누적

### Team (감독 AI 소관 — 플레이어가 직접 편성하지 않음)

- 라인업
- 타순
- 선발 로테이션
- 불펜 역할

### Match

- 자동 경기 시뮬레이션
- 텍스트 중계
- 타구 / 주자 간단 표현
- 대타
- 투수 교체
- 번트
- 도루

### Season

- 일정
- 순위
- 개인 기록
- 포스트시즌

### Offseason

- 선수 성장 / 노쇠
- 은퇴
- 신인 생성
- 드래프트

### Save

- 저장
- 불러오기

---

## 31. MVP에서 제외할 기능

아래 시스템은 선수 커리어 MVP에 한해 초기에 구현하지 않는다. 2026-08-31에 별도 진입점으로
재도입한 구단주 모드에서는 `docs/todo/역사시뮬레이션_구단주모드/`의 단계와 데이터 계약이 이 목록보다
우선한다. 두 모드 사이에 카드 소유 경제나 구단 운영 상태를 공유한다는 뜻은 아니다.

```text
온라인 멀티플레이
실제 KBO 라이선스
실제 선수
실시간 서비스
가챠
과금
2군
외국인 선수 제도
복잡한 FA 협상
에이전트
코칭스태프
스카우터 직원
구단 시설
구장 건설
티켓 가격
굿즈
재정 시뮬레이션
팬 충성도
미디어
선수 성격 관계
국가대표
해외 리그
육성 선수
고교 야구
대학 야구
실제 3D 야구 플레이
```

여기서 제외하는 것은 실시간 3D 물리·수비·주루 액션 게임이다. 정규화된 2D 타격 평면에서 자기 선수의
투구 의도와 스윙 실행만 입력하고 기존 시뮬레이션이 결과를 판정하는 직접 플레이는 40.24의 정식 범위다.

핵심 재미가 검증된 이후 추가한다.

---

## 32. 개발 순서

### Phase 1 — Simulation Prototype

UI 없이 경기 결과를 계산한다.

구현:

```text
Player
Team
Lineup
MatchSimulator
BaseRunner
Score
BoxScore
```

목표:

```text
두 팀 데이터를 넣으면
9이닝 경기 결과가 정상적으로 생성된다.
```

---

### Phase 2 — League Simulation

```text
8개 팀
일정 생성
AI 라인업
전체 경기 자동 진행
순위 계산
```

목표:

```text
144경기 시즌을 처음부터 끝까지
자동 시뮬레이션할 수 있다.
```

---

### Phase 3 — Balance Tool

수천 시즌을 자동 시뮬레이션한다.

```text
AVG
OBP
SLG
ERA
HR
BB
SO
득점
```

분포를 확인한다.

---

### Phase 4 — Management UI

```text
Home
Roster
Lineup
Player Detail
League Standing
Schedule
```

구현.

이 단계부터 실제 게임처럼 플레이 가능해진다.

---

### Phase 5 — Match Presentation

```text
스코어보드
텍스트 중계
야구장
타구 표현
주자 표현
감독 개입
```

구현.

---

### Phase 6 — Career Loop

```text
시즌 종료
성장
노쇠
은퇴
드래프트
다음 시즌
```

구현.

이 단계에서 장기 플레이 게임이 완성된다.

---

### Phase 7 — Roster Management

```text
트레이드
FA
계약
부상자 관리
```

추가.

---

### Phase 8 — Core Growth Content

시뮬레이션과 여러 시즌 커리어 루프가 검증된 뒤 다음 순서로 추가한다.

```text
감독 운영 성향
선수 유학
4×4 Skill Board
연도 팀 컬러
```

연도 팀 컬러는 실제 플레이로 만들어진 시즌 기록을 재료로 삼기 때문에 가장 마지막에 구현한다.
각 시스템의 상세 원칙은 39절을 따른다.

---

## 33. 최초 프로토타입 목표

처음부터 완성된 야구 게임을 만들지 않는다.

첫 목표는 콘솔 또는 개발 UI에서 다음 결과가 나오는 것이다.

```text
서울 블레이즈 5
부산 마리너스 3

WP 김민우
LP 박재혁
SV 최진수

HR
김도윤 2호
이성준 1호
```

그리고 선수 시즌 기록에 결과가 정상 반영되면 성공이다.

그 다음:

```text
1경기
↓
10경기
↓
144경기
↓
1시즌
↓
10시즌
```

순서로 확장한다.

---

## 34. 기술 방향

> `docs/지침/Simulation_Architecture_Guidelines_UPlayBall.md`로 이동 (2026-08-28). 어셈블리 레이어 구조(Baseball.Core/Simulation/Game/Presentation/Editor)와 Simulation 레이어의 Unity 비의존 원칙은 해당 문서를 따른다.

---

## 35. 프로젝트 원칙

> `docs/지침/Project_Principles_UPlayBall.md`로 이동 (2026-08-28).

---

## 36. 프로젝트의 최종 지향점

이 프로젝트가 장기적으로 지향하는 경험은 다음과 같다.

> 무명 신인을 드래프트에서 뽑고,
> 몇 년 동안 기회를 주며 성장시키고,
> 팀의 중심 선수로 우승을 함께한 뒤,
> 어느 순간 노쇠한 그 선수를 은퇴시키면서
> 지난 15년의 기록을 다시 돌아보게 되는 게임.

단순히 한 경기의 승패가 아니라,

```text
선수의 커리어
구단의 역사
리그의 역사
```

가 플레이 결과로 남는 것을 핵심 목표로 한다.

---

## 37. 임시 프로젝트 체크리스트

- [ ] 프로젝트명 확정
- [ ] 가상 리그 설정
- [ ] 선수 능력치 정의
- [ ] 타석 결과 모델 설계
- [ ] 주루 모델 설계
- [ ] 수비 모델 설계
- [ ] 투수 교체 AI 설계
- [ ] MatchSimulator 프로토타입
- [ ] BoxScore 생성
- [ ] 선수 기록 누적
- [ ] 일정 생성
- [ ] 시즌 순위 계산
- [ ] 대량 시뮬레이션 테스트 툴
- [ ] 기본 관리 UI
- [ ] 경기 중계 UI
- [ ] 성장 / 노쇠
- [ ] 신인 생성
- [ ] 드래프트
- [ ] 포스트시즌
- [ ] 세이브 / 로드

---

## 38. 현재 우선순위

2026-08-30 기준 최초 프로토타입 목표와 커리어 전체 루프는 구현되어 있다. 현재 단계는
**Career Stabilization**이며, 신규 기능 확장보다 다음 네 가지를 우선한다.

1. 실제 커리어 경기 경로의 대량 통계와 능력치 민감도 검증
2. 백업·부상 선수의 성장 진행도 보존과 역할 복귀 가능성 검증
3. Rookie~Galaxy 전 구간의 성장 선택 지속성 확보
4. 스킬 블록·자금력이 일반 훈련과 역할 평가를 독점하지 않도록 통합 검증

`MatchSimulator`의 공통 경기 규칙은 `DetailedMatchEngine`을 사용한다. 선수 커리어와 역사 베이크는
상세 투구 계산만 사용한다. 구단주 모드의 다른 조는 아래 「구단주 다른 조 간이 타석 시뮬레이션」의
승인된 예외로 타석 단위 계산을 사용한다. 과거 경량 경기 구현은 활성화하지 않는다.

시즌 자동완료 성능 개선은 먼저 canonical 상태 checksum, 재개 가능한 동기 세션, 무관전자 Detailed
출력 프로필을 마련하고, Unity Player 실측이 목표를 만족하지 못할 때만 작업 스레드나 별도 Fast
엔진을 검토한다.

이 단계의 성공 기준은 다음과 같다.

> **같은 Seed·입력·규칙 버전에서 결과가 재현되고, 20시즌 동안 역할·성장·계약·리그 이동이 한쪽으로
> 고착되지 않으며, 플레이어가 결과의 주요 원인을 확인할 수 있는 것.**

---

## 39. 선수 성장 시스템

성장 시스템의 정본은 `BaseballManager_CAREER_ECONOMY.md`다. 이 절은 프로젝트 전체에서 지켜야 할
요약과 구현 진입 조건만 유지한다. 세부 가격·확률·공식·데이터 구조는 해당 문서를 따른다.

### 39.1 최종 자원 계약

플레이어가 계획하고 소비하는 성장 자원은 정확히 두 개다.

| 자원 | 역할 |
|---|---|
| `Money` | 훈련, 유학, 파트너, 치료, 스킬 블록 구매 |
| `Offseason Time` | 고정 12주의 활동 스케줄링 |

다음 값은 소비 자원이 아니다.

- `Potential`: 능력치별 장기 성장 한계
- `Condition`: 훈련 효율과 부상 위험 상태
- `Opportunity`: 감독 AI와 팀 내 경쟁으로 정해지는 역할 상태
- `WorkEthic` / `TrainingFit`: 성장 기대값 보정

`DevelopmentCapacity`와 `SkillPoint`는 사용하지 않는다. 유학 남발은 오프시즌당 한 번, 활동량은
12주, 무한 성장은 Potential·나이 효율·노쇠, 반복은 계통별 효율 감소로 막는다.
런타임과 세이브의 `Money`는 계약금·연봉과 같은 **원 단위 정수**로 저장하고, UI에서만 만원·억원으로
환산한다. BalanceTable에 600만원을 `600`으로 넣어 600원만 차감하는 단위 혼용을 허용하지 않는다.

### 39.2 성장 경로

| 경로 | 시기 | Base Ability | 블록 | 비용 |
|---|---|---:|---:|---|
| 시즌 자연 성장 | 시즌 결산 | 소폭 상승 | 없음 | 없음 |
| 개인 훈련 | 오프시즌 | 상승 | 없음 | Time + 소액 Money |
| 훈련 파트너 | 오프시즌 | 상승 | 고유 Trait 가능 | Time + Money + 조건 |
| 유학 | 오프시즌 | 큰 상승 | 고유 Trait 가능 | 6~8주 + 많은 Money |
| 스킬 블록 상점 | 언제나 | 변화 없음 | 랜덤 획득 | Money |
| 노쇠·중대 부상 | 결산·발생 시 | 감소 가능 | 없음 | 치료 선택에 따라 Money |

개인 훈련은 `TrainingIntensity.Safe / Standard / Intensive` 세 강도를 지원한다. 강도는 별도
프로그램 특수 분기가 아니라 `GrowthBalanceTable.TrainingIntensities`의 기간·비용·성장력·컨디션·
부상 위험 변환 규칙으로 계산한다. `Standard`는 프로그램 원본값을 그대로 사용하며, 강도와 변환된
실행값은 활동 계획과 `GrowthResultRecord`에 함께 저장한다.

훈련 프로그램은 `Foundation / Advanced / Professional / International / Elite /
Championship / Legacy` 접근 등급을 가진다. Rookie부터 Galaxy까지 리그 진행에 맞춰 새 선택이
열리되, 상위 등급은 성장 총량의 단순 상위호환이 아니라 Potential·Peak·노쇠·반복 페널티·Trait처럼
서로 다른 병목을 해결한다. Game 레이어는 리그와 접근 등급을 매핑하고 Core의 프로그램 정의는
Game의 `LeagueLevel`을 참조하지 않는다.

접근 권한은 커리어 최고 도달 리그의 `TrainingKnowledgeTier`와 현재 소속 리그의
`FacilitySupportTier`로 나눈다. 강등 후에도 배운 프로그램은 유지하지만 시설 지원이 부족하면 비용,
최소 보장 성장과 Potential 돌파에 불이익을 받는다.

`성장 계획에 담기`는 마지막 선택을 교체하는 임시 선택이 아니라 `OffseasonState.Activities`에
복수 활동을 순서대로 배치하는 명시적 계획 명령이다. 계획 실행은 주차·활동 ID 순서로 모든 활동을
완료하며, 앞 활동의 비용·기간·예상 Condition을 다음 활동의 실행 가능성과 미리보기에 반영한다.
시작 전 활동은 취소할 수 있고, 취소 후 뒤 활동은 빈 주차가 생기지 않도록 앞으로 당긴다.

경기 결과는 Base Ability를 즉시 올리지 않는다. 성적은 감독 평가·출장 기회·수입·계약을 거쳐 다음
성장 선택에 간접 영향을 준다.

### 39.3 능력치 적용 순서

```text
Base Ability
→ Skill Board Bonus
→ Peak Bonus
→ Condition / Fatigue / Injury / Tactics
→ PlateAppearanceSimulator / MatchSimulator
```

`Base Ability ≤ Potential`을 항상 지킨다. Potential 초과 기량은 영구 Base가 아니라 능력치별
0~3의 시즌형 `Peak Bonus`로 저장한다. 성장판 보너스는 장착 중에만 적용하며 Potential 여유 계산에는
포함하지 않는다. 경기·감독 평가·계약·UI는 `EffectiveAbilityResolver`가 반환하는 동일한
`AbilityBreakdown`을 사용하고, `OVR`은 저장 자원이 아니라 해당 시점 능력치에서 다시 계산한다.

### 39.4 유학

- 오프시즌 전용, 오프시즌당 한 번, 6~8주와 Money를 소비한다.
- 시즌 중 경기 이탈과 빠지는 경기 수는 사용하지 않는다.
- `DevelopmentCapacity`를 소비하지 않는다.
- 같은 계통 연속 선택은 100% → 90% → 75% → 60%로 효율을 낮춘다.
- 결과 Seed는 활동 시작 시 저장한다.
- Base Ability 감소 결과는 없고 성장 여유가 있으면 최소 성장을 보장한다.
- 프로그램에 따라 고유 Trait 블록이나 드문 Potential +1 사건을 제공할 수 있다.
- 최상위 성장 프로그램은 대상 능력이 Potential에 막혔을 때 공개된 규칙에 따라 Potential 돌파를
  먼저 적용해, 절대 상한 99 이전의 고가 프로그램 무성장 결과를 막는다. 일반 훈련은 Potential을
  넘는 Base를 만들지 않는다.

### 39.5 스킬 블록과 4×4 성장판

- 스킬 블록은 `SkillPoint`가 아니라 게임 내부 Money 상점의 랜덤 뽑기와 성장 활동 보상으로 얻는다.
- 뽑기는 Normal/Rare/Elite/Unique/Legendary 다섯 상품이며 가격은 600만원/1,500만원/
  4,000만원/1억원/2억 5,000만원이다. 상품 이름은 최소 보장 등급이며 확률은
  N `55/28/13/4/0`, R `0/70/22/7/1`, E `0/0/75/20/5`, U `0/0/0/85/15`,
  L `0/0/0/0/100`을 1차값으로 둔다.
- Normal~Elite 5회 구매는 5% 할인한다. Elite 10회·Unique 30회·Legendary 60회 미획득 후
  다음 뽑기에 각각 보장을 둔다.
- 뽑기 전 전체 또는 계통을 선택하고 모든 결과는 블록 하나를 지급한다.
- Unique는 오프시즌 2회, Legendary는 오프시즌 1회로 제한한다. Legendary는 Champion 이상과
  개인 수상 1회 이후 해금한다.
- 블록 기본 보너스는 Normal/Rare/Elite/Unique/Legendary 순서로 +1/+2/+3/+4/+5다.
- 같은 능력치의 첫 번째/두 번째/세 번째/네 번째 블록은 100%/60%/30%/0%로 적용한다.
  능력치 하나의 유효 Skill Bonus는 +9, 보드 전체는 +18을 넘지 않는다.
- 상품의 최소 리그는 Normal/Rookie, Rare/Minor, Elite/Major, Unique/Classic,
  Legendary/Champion이다. Legendary는 개인 수상 1회 조건도 함께 요구한다.
- Unique·Legendary는 단순 수치보다 Trait을 제공한다. Trait은 실제 경기 상황에서 발동하고
  계약 안정 전력에 직접 중복 합산하지 않는다.
- 개막일에 `ActiveSkillBoardSnapshot`을 고정하고 경기·역할·계약은 항상 같은 스냅샷을 사용한다.
- 시즌 중에도 보드를 교체할 수 있다. 편집은 자유롭고 확정만 비용을 받으며, 확정 전까지 스냅샷은
  바뀌지 않는다. 확정 시점 이후의 경기와 역할 평가부터 새 스냅샷을 쓰고 이미 치른 기록은 불변이다.
  횟수 제한과 적응 페널티는 두지 않는다.
- 보장 카운트와 확률을 공개한다. 중복은 강화 재료가 아니라 소액 Money 판매만 허용한다.
- 보드는 4×4이며 모든 등급의 블록은 `I/O/T/S/Z/J/L` 7종 중 하나인 4칸 표준 테트로미노다.
- 선수·구단주 모드 모두 같은 모양 계약을 따른다. 구단주 전용 1~3칸 블록 생성과 검증 우회는 제거하며, 인접 세트 효과는 표준 테트로미노에 적용한다.
  등급은 칸 수가 아니라 보너스와 프레임으로 구분하며, 회전·경계·겹침을 검증한다.
  특성 소켓과 지정 칸 강조는 제거한다. 장착된 특성 블록은 모든 합법적인 위치에서 같은 특성을 활성화한다.
- **모양은 계통·등급에 종속되지 않는다.** 어떤 계통을 골라도, 어떤 등급이 나와도 7종 모양이
  모두 후보이며 같은 확률로 나온다. 배치 퍼즐의 변수는 계통 선택이 아니라 뽑기 결과여야 한다.
- 블록 정의 변경으로 기존 장착이 경계나 겹침 조건을 위반하면 해당 블록만 손실·비용 없이 보관함으로 돌린다.
- 실제 보드와 임시 편집 보드를 분리한다. 신규 블록 추가는 무료이고, 기존 배치 이동·회수는
  1,500만원 안전 회수를 오프시즌당 한 번 사용한 뒤 일괄 확정한다.

### 39.6 결정론과 데이터 레이어

Core의 순수 C# 상태·정의:

```text
PlayerGrowthState        CareerEconomyState
OffseasonState           PlayerStudyState
SkillBoardState          TrainingProgramDefinition
SkillBlockDefinition     GrowthResultRecord
InjuryRecord
```

Simulation의 순수 C# 계산:

```text
GrowthResolver                 NaturalDevelopmentResolver
AgingResolver                  OffseasonScheduler
SkillGachaService              SkillBoardService
InjuryResolver
```

Game은 일정·세이브와 정적 데이터 변환을 소유하고 Presentation은 결과와 근거만 표시한다. Core와
Simulation은 `UnityEngine`을 참조하지 않는다. 같은 입력과 Seed에서는 성장·뽑기·부상 결과가 같다.

### 39.7 구현 순서와 진입 조건

Phase 1의 `Player / Team / Lineup`, `PlateAppearanceSimulator`, `MatchSimulator` 통계 검증 이전에는
성장 UI를 확장하지 않는다.

1. Base Ability / Potential / Age / Condition 상태
2. 성장·자연 성장·노쇠 계산과 대량 커리어 시뮬레이션
3. 오프시즌 12주 스케줄러, 개인 훈련, 유학
4. 훈련 파트너
5. 스킬 블록 상점과 4×4 성장판
6. 부상·치료·재활
7. AI 오프시즌 성장과 스프링캠프 역할 평가
8. 계약 시장·수입 루프
9. UI와 연출

현재 완료 상태와 남은 작업의 우선순위·완료 조건은 `BaseballManager_GROWTH_FOLLOWUP.md`에서 추적한다.
성장 규칙이 충돌하면 `BaseballManager_CAREER_ECONOMY.md`가 우선한다.

### 39.8 필수 테스트

- 같은 입력과 Seed의 성장 기록 완전 일치
- Base≤Potential, Peak 0~3, 나이·컨디션·반복 보정 경계
- 12주 초과·활동 겹침·두 번째 유학·같은 파트너 반복 거부
- 스킬 블록 회전·겹침·경계·Socket·제거·재설계
- 뽑기 최소 등급·5회 할인·Elite/Unique/Legendary 보장 카운트·구매 제한·판매 잠금
- 부상 위험 요인의 방향성과 치료 비용
- 나이·소득·Opportunity·전략별 수천~수만 커리어 성장 분포
- 플레이어와 AI 리그의 장기 OVR 분포 및 32세 이후 노쇠 속도
- 같은 Seed·입력·`SimulationVersionStamp`의 경기·성장·역할 판정 완전 일치
- 출장 0의 성장 진행도 보존, 백업의 역할 복귀율, 프로그램 선택 집중도

### 39.9 만들지 않을 것

- 경기 경험치로 즉시 능력치를 올리는 RPG 성장
- `DevelopmentCapacity`, `SkillPoint`, 블록 조각 같은 추가 소비 재화
- 현금성 확률 상품, 프리미엄 재화, 유료 시간 단축, 즉시 부상 회복 판매
- 실패 시 능력치 하락 또는 블록 파괴형 강화
- 유학 반복으로 Potential을 넘는 무한 성장
- 경기마다 보드를 갈아 끼우는 상대별 장착 노동
- 이유를 기록하지 않는 숨은 승률·성장 보정

---

## 40. 새 게임 플로우 (캐릭터 생성 → 구단 계약)

2026-08-26 장르 전환에 따라 확정한 새 게임 진입 플로우다. 기존 39절 성장 콘텐츠(유학, Skill Board, 감독 효과, 연도 팀 컬러)는 이 플로우로 생성된 선수 위에 그대로 얹는다.

### 40.1 전체 흐름

```text
선수 이름 / 국적 입력
↓
투수 / 타자 선택
↓
포지션 선택 (투수는 SP/RP 역할도 함께)
↓
투타 선택
↓
초기 능력치 배분 (합계 상한 내에서 6개 배분)
↓
선수 카드 생성 (무소속)
↓
Rookie League와 소속 구단 생성 (Seed 기반)
↓
각 구단이 선수를 평가해 일부만 계약 오퍼
↓
구단 비교 (계약금 / 육성 환경 / 포지션 경쟁 / 예상 출장)
↓
입단 구단 선택 → 계약
↓
Rookie League 시즌 시작
```

새 게임 시작부터 첫 경기 진입까지 5분을 넘기지 않는 것을 목표로 한다.

### 40.2 능력치 배분

타자: `Contact`, `Power`, `Speed`, `Arm`, `Defense`, `Mental`
투수: `Stamina`, `Velocity`, `Stuff`, `Breaking`, `Control`, `Mental`

- 초기값은 전 능력치 40, 추가로 분배 가능한 포인트를 별도로 지급한다. 구체적인 총합·상한(예: 개당 40~65)은 `BalanceTable`에 정의하고 코드에 하드코딩하지 않는다 — 밸런스 조정 시 재컴파일이 필요해지면 안 된다.
- 포지션별 권장 능력치와 실제 배분이 어긋나도 생성을 막지 않는다. 대신 경고만 표시한다. 잘못된 빌드도 플레이어의 선택으로 인정한다.
- 프리셋(교타형/장타형/준족형/수비형/균형형 등)은 추천 배분값을 채워줄 뿐이며 이후 자유 수정을 막지 않는다.
- 생성 화면의 `Eye`는 내부 `Mental`에 대응하며 선구·삼진·볼넷 판단에 쓰인다. `Arm`은 실제 송구와
  추가 진루 억제에 쓰인다. 번트는 생성 시 독립 투자 항목으로 두지 않고 `Contact` 60% + `Mental` 40%의
  파생값으로 계산한다. 커리어의 핵심 선택인 포지션 수비와 송구를 우선하고, 낮은 빈도의 번트만을 위해
  6개 슬롯 중 하나를 고정 소비하지 않기 위함이다.
- 선구안(`PlateDiscipline`) 같은 7번째 능력치를 추가하지 않는다. `Core.Players`의 6능력치 계약을 유지한다.

### 40.3 구단 생성과 평가

- 새 게임마다 Rookie League 구단 집합을 Seed로 새로 생성한다 (이름/색상/성향/재정/육성시설/기존 로스터).
- 구단 성향은 `TeamArchetype`(육성형/강팀형/타격 육성형/투수 육성형/약체형 등) + 무작위 변주로 만든다. 완전 무작위 생성은 피한다 — 특징 없는 팀이 나온다.
- 구단이 내 선수를 평가하는 공식은 `PlayerValue × PositionNeed × TeamBudget × TeamPreference × ScoutVariance` 형태의 데이터화된 가중치로 구성하고 `BalanceTable`에 둔다.
- 전 구단이 오퍼를 주지 않는다. 일부(예: 8개 중 3~5개)만 제안해 "어디서 나를 원하는가"라는 긴장을 만든다.
- 계약 오퍼는 계약금·연봉만 비교하게 하지 않는다. 예상 역할(주전 경쟁/백업), 같은 포지션 경쟁자 명단, 육성 환경 등급을 함께 보여줘 계약금과 출장 기회 사이의 트레이드오프를 만든다.

### 40.4 아키텍처 배치

- 능력치 배분 규칙·구단 평가 공식·계약 오퍼 확률은 `Baseball.Simulation`(순수 C#)에 둔다. Unity 의존 코드(입력 UI, 카드 연출)는 `Baseball.Game`/`Baseball.Presentation`에서 이 결과를 소비만 한다.
- 구단 정적 정의(`TeamArchetype`, 이름 후보 풀 등)는 SO(`Assets/10.Datas/`)로 두되, 새 게임에서 생성된 실제 `TeamState`/`PlayerState`(런타임 상태)는 순수 C# 클래스로 세이브 대상이 된다. SO를 세이브에 직접 참조하지 않는다.
- 새 게임 생성 전체가 `RandomSeed` 하나로 결정론적이어야 한다 — 같은 Seed로 새 게임을 두 번 시작하면 동일한 구단 목록·동일한 오퍼가 나와야 한다.

### 40.5 이 절의 구현 순서

Phase 1(Player/Team/Lineup, PlateAppearanceSimulator, MatchSimulator)이 통계적으로 검증된 뒤 진행한다. `BaseballManager_GAME_SYSTEM.md`의 리그 승강 구조(`LeagueGrade`)를 재사용할 계획이면 그 절만 이 문서 4절(세계관/리그)로 옮겨 정리한다.

### 40.6 최종 설계 — 커리어 경제와 오프시즌 성장

2026-08-27 기준 성장 시스템의 최종 설계는 `BaseballManager_CAREER_ECONOMY.md`에 통합했다.
40절 플로우로 입단한 선수는 정규 시즌을 경기 일정 중심으로 진행하고, 시즌 결산 뒤 고정 12주의
오프시즌에 성장 활동을 배치한다.

- 소비 성장 자원은 `Money`와 `Offseason Time` 두 개뿐이다.
- `Opportunity`는 소비 자원이 아니라 감독 AI와 팀 내 경쟁이 정하는 역할 상태다.
- `Potential`은 능력치별 장기 성장 한계이며 소비하지 않는다.
- `DevelopmentCapacity`와 `SkillPoint`는 폐기한다.
- 개인 훈련·훈련 파트너·유학·재활은 주 수와 Money를 소비한다.
- 유학은 오프시즌당 한 번, 6~8주를 사용한다.
- 스킬 블록은 게임 내부 Money 상점과 성장 활동 보상으로 획득한다.
- 스프링캠프에서 성장 결과와 신규 경쟁자를 함께 비교해 감독 AI가 다음 시즌 역할을 정한다.

구현 진입 조건은 39.7/40.5와 동일하게 Phase 1 통계 검증 이후이며, 성장 UI는 Core·Simulation의
대량 커리어 검증보다 먼저 확장하지 않는다.

### 40.7 UI 플로우 가이드 — 선수 중심 대시보드

40.6절 커리어 경제를 실제 화면으로 옮긴 UI/UX 설계는 `BaseballManager_UI_FLOW.md`에
정리했다. 다루는 범위:

- 메인 화면을 "선수 중심 대시보드"로 잡는 원칙과 6구역 레이아웃(2560×1440 기준)
- 좌측 내 선수 패널(현재 역할 배지), 중앙 다음 경기·경기 진행 CTA, 우측 시즌 현황·최근 성적
- 좌측 하단 팀 내 포지션 경쟁, 중앙 하단 뉴스/이벤트 피드, 우측 하단 일정
- 공용 Shell의 6개 업무 영역(홈/경기/선수/팀/리그/커리어)과 Local Navigation,
  `선수 > 성장`(4×4 성장판)·`커리어 > 계약`(상여 조건 진행바) 세부
- 부상 상태·오프시즌 전환·스프링캠프 시점의 화면 전환 규칙
- 시각적 우선순위(다음 경기 > 현재 역할 > 내 선수 > 최근 성적 > 포지션 경쟁 > 리그 정보)와
  등급 컬러를 국소 강조로만 쓰는 시각 방향

**24절(UI 주요 화면)을 이 설계로 교체했다 (2026-08-26).** 기존 24절은 `Team`(라인업/투수진
편성), `Front Office`(트레이드/드래프트/FA를 플레이어가 직접 수행)처럼 구단 전체를 플레이어가
운영한다는 전제의 옛 화면 목록이었다. `BaseballManager_UI_FLOW.md` 기준 8탭 구조로 재작성해
`팀` 영역을 열람 전용으로, 트레이드/드래프트/FA 판단을 감독 AI와 구단 소유로 확정했다.

### 40.8 구현 상태 — 정규 시즌 첫 Vertical Slice

2026-08-27 기준 계약 완료 뒤 멈추던 흐름을 다음 범위까지 연결했다.

- 8구단·구단당 80경기의 결정론적 Round-robin 일정. 각 경기는 `SeasonId`/`GameId`/
  `RandomSeed`를 상태에 저장한다.
- `ManagerUsageAi`가 선수 기량·계약 예상 역할·포지션 경쟁자·컨디션·감독 평가로 다음 경기의
  `StartingBatter`/`StartingPitcher`/`ReliefPitcher`/`Bench`/`PitcherRest`를 결정하고, 화면에 표시한 판단을
  경기 상태에 고정해 실제 시뮬레이션과 일치시킨다.
- 같은 라운드의 리그 4경기를 함께 진행해 구단 승패·득실점·순위를 누적한다.
- `MatchSimulator`에 선발/구원 투수 기록 분리를 추가했다. 첫 Vertical Slice에서는 7회부터
  한 명의 지정 구원투수가 끝까지 던지는 단순 모델이며, 불펜 다중 교체는 후속 범위다.
- 내 선수의 타격/투구 시즌 기록, 최근 5경기, 컨디션, 감독 평가를 누적하고 선수 중심
  6구역 대시보드의 다음 경기 CTA와 연결했다.
- 세이브 구조 계약 변경에 따라 `SaveVersion`을 2로 올렸다. 아직 디스크 Save/Load가 없어
  Version 1 실사용 세이브는 존재하지 않으며, 저장 구현 시 Version 1은 새 게임 재생성 대상으로
  명시적으로 처리해야 한다.

기용 계수는 45개 완주 시즌·리그 전체 14,400경기로 검증했다. 팀 경기당 득점 4.60, 타자
선발률 23.5%(AVG .272 / OPS .796), SP 등판률 10.7%(경기당 6.0이닝 / ERA 5.05), RP
등판률 45.0%(경기당 2.9이닝 / ERA 5.48)였다. 벤치 대타가 아직 없는 상태에서 첫 시즌이
지나치게 수동적으로 흐르지 않도록 계약 역할별 기회 가산점을 9/4/-1로 조정한 결과다.

2026-08-28에는 낮은 OVR과 `BenchCompetition`이 결합하면 무출장 → 감독 평가 정체 →
무성장으로 이어지던 고착을 교정했다. 평소 기용은 기존의 기량·컨디션·감독 평가 경쟁으로
결정하되, 2군과 대타가 아직 없는 MVP 정규시즌에서는 계약 역할에 따라 1/2/3 로테이션 주기마다
최소 평가 기회를 부여한다. 기본 5인 로테이션에서 타자·RP는 약 5/10/15경기마다, SP는
자신의 로테이션 차례 중 매 1/2/3번째에 해당한다. 컨디션 70 미만에서는 평가 기회도
강행하지 않고, 포스트시즌에는 평가 목적의 강제 기회를 적용하지 않는다. 계약 화면에는 역할과 함께 이 조건·주기를 표시해 희소한 출장 가능성을
계약 전에 판단할 수 있게 한다.

역할별 40개씩 총 120커리어·360시즌(정규시즌 리그 115,200경기)을 3시즌 연속 실행한 결과,
1년차 타수 중앙값은 `StartingCompetition` 156, `RosterCompetition` 98,
`BenchCompetition` 19였고 세 역할 모두 0타수는 0건이었다. 3시즌 누적 타수 중앙값은
각각 529/317/59, 관측 최소는 397/105/54였으며 30타수 미만 커리어는 0건이었다. 기존 고착
보고 Seed 1548/1822/1685/2096의 3시즌 누적도 63/61/65/81타수로 회복했고, 같은 Seed의
3시즌 지문은 일치했다. 별도 동급 팀 10,000경기에서는 AVG .262, ERA 4.39, 팀 경기당
1.27홈런·4.37득점, BB% 8.7%, SO% 18.5%, BB/K 0.47이었으며, 전력 56 대 48의 5,000경기
강팀 승률은 66.4%였다. 이 최소 평가 기회는 MVP 안전망이며, 당시 미구현이던 벤치의 실제 대타
출전은 40.14절의 첫 선수 교체 Vertical Slice에서 별도로 연결했다.

후속 선수 교체 범위는 복수 대타·대주자·대수비와 투수 교체를 `MatchEvent`로 노출하는 것이다.
전체/핵심 장면 관전, 하단 8개 탭의 실제 화면, 부상·오프시즌 전환도 후속 범위다. 현재 하단
탭은 정보 구조만 표시하며 상호작용하지 않는다.

### 40.9 구현 상태 — 시즌 종료·포스트시즌·수상 파이프라인

2026-08-28 기준 정규시즌 Vertical Slice 뒤를 다음 범위까지 연결했다.

- 리그 전체 선수의 타격·투구·포지션별 수비 기록을 `LeagueSeasonStatisticsState` 한 곳에 누적하고,
  내 선수 기존 통계는 같은 원본을 읽는 파사드로 전환했다. 정규시즌 종료 시 원본을 동결한다.
- 정규시즌 순위 상위 4팀을 1-4/2-3 준결승 3전제와 결승 5전제로 진행한다. 포스트시즌
  `MatchInput.RequiresWinner`는 최대 연장 뒤에도 무승부를 남기지 않는다.
- 상대 전적 → 득실차 → 득점 → 최소 실점 → 저장된 고정값 순으로 동률 시드를 결정한다.
- `RegularSeasonMvp`, `PostseasonMvp`, 신인왕, 타격·투구 기록상, 투수 포함 포지션별
  골든글러브를 통계로 평가한다. 상위 후보와 지표별 점수 근거도 결과 상태에 저장한다.
- 신인 자격은 로스터 선수별 통산 타석·투구 아웃·등록 시즌을 이어서 저장하고, 시즌 시작 시
  `120 PA 미만 / 135 outs 미만 / 등록 1시즌 이하` 조건으로 고정한다. 시즌 도중 기준을
  넘겨도 현재 시즌 자격은 바뀌지 않으며 새로 유입된 로스터 선수는 통산 0부터 시작한다.
- 정산은 급여·수상·포스트시즌·우승 보상 ID를 원장에 남겨 재진입 시 중복 지급을 막고,
  수상·우승 계약 가산점을 최대 30점으로 다음 계약 오퍼에 반영한다.
- `SeasonPhase.Completed`를 장기 보관 완료로 재정의하고 `SeasonReview`와 내부 결산 단계를 추가했다.
  통계 스키마 변경에 따라 당시 `StatisticsSchemaVersion`은 2, `SaveVersion`은 6으로 올렸다.
  이후 40.15절의 구단별 분할 기록과 커리어 이동 상태 추가로 각각 3과 7이 되었다.

현재 수비 모델은 인플레이 결과를 실제 야수에게 귀속해 기대 대비 처리 결과를 쌓는 초기 모델이다.
포수 도루 저지·블로킹, 내야 병살 연결, 외야 보살 같은 포지션 특화 10% 데이터는 아직 없으므로
골든글러브 계산에서 해당 비중을 나머지 실제 지표에 비례 재분배한다. 도루 시도·몸에 맞는 공도
현재 경기 시뮬레이터가 만들지 않으므로 원본 필드는 유지하되, 0인 기록상은 수상자를 만들지 않는다.

30개 완주 시즌의 정규시즌 9,600경기와 포스트시즌 272경기를 대량 실행한 결과 팀 경기당 득점은
4.530점, 시즌당 수상은 19개, 골든글러브는 투수 포함 9개였고 포스트시즌 무승부·누락 MVP·
80경기 미완주 구단은 없었다. 같은 Seed 완주 2회의 우승팀·정규시즌 MVP·포스트시즌 MVP와
시즌 지문도 일치했다. 수비 기록 추가는 기존 타석 결과 확률을 바꾸지 않아 종전 경기 밸런스
검증 범위를 유지한다.

### 40.10 구현 상태 — 홈에서 선수 중심 경기 진행

2026-08-28 기준 홈의 다음 경기 CTA를 즉시 결과 처리에서 다음 흐름으로 교체했다.

```text
홈 → 경기 준비 → 내 선수 중심 진행 또는 결과만 보기 → 경기 결과 → 다음 날 홈
```

- 경기 준비에서 실제 확정 역할, 대진, 경기일, 컨디션, 감독 평가를 먼저 확인한다. 투수 역할은
  아직 개입 선택이 없으므로 관전 결과로 진행한다. `Bench` 야수는 대타 후보로 경기 안에 남아
  있으며, 감독이 투입하면 그 시점부터 내 선수 입력을 받는다.
- `StartingBatter`는 다른 타자의 타석 결과를 빠른 자동 중계로 순서대로 보여 주고 내 선수의 매
  투구 직전에 정지한다. 화면은 정지 이유, 이닝·점수·B/S/O·주자, 오늘 기록, 현재 매치업,
  최근 이벤트를 함께 표시한다.
- 타격 방식은 `Balanced`/`Contact`/`Power`/`Patient` 네 가지다. 선택은 Presentation 상태가
  아니라 순수 C# `BattingApproach` 명령으로 저장되며 실제 Swing·Chase·Contact·타구 결과
  확률에 적용된다. 조정값은 `BattingApproachBalance`에 모았다.
- 경기 세션은 저장된 선택 목록을 같은 `RandomSeed` 위에 다시 재생해 다음 입력 지점을 찾는다.
  따라서 UI 프레임이나 입력 시점과 무관하게 `Seed + 경기 입력 + 선택 목록`이 같으면 이벤트와
  BoxScore가 완전히 같다. 경기 완료 결과는 같은 라운드의 다른 경기와 함께 한 번만 누적한다.
- 경기 후에는 승패, 개인 기록, 감독 평가와 컨디션 전후 값을 확인한 뒤 갱신된 홈으로 돌아간다.

타격 방식별 동급 매치업 20,000타석 결과는 `Balanced` SO 18.37% / BB 8.60% / HR 3.19%,
`Contact` SO 12.68% / HR 1.57%, `Power` SO 23.87% / HR 5.34%, `Patient` BB 15.37%였다.
선택의 장단점이 방향대로 나타났고, 모든 선수가 기존 `Balanced`를 쓰는 동급 팀 10,000경기는
AVG .262, 팀 경기당 1.269홈런·4.381득점, BB% 8.78%, SO% 18.51%로 기존 기준선을 유지했다.
동일 경기의 `Balanced` 선택 9개를 명령 재생한 461개 이벤트도 일반 자동 경기와 전부 일치했다.

아직 구현하지 않은 범위는 플레이어 주루 판단, 감독 지시, 복수 대타·대주자·대수비 호출,
부상 선택, 수비 개입, 중요 상황만 보기, 타석·이닝 단위 경기 중 저장이다. 이 항목은 기존
`MatchEvent` 스트림과 선택 명령 목록을 확장해 구현하며, Presentation을 위해 시뮬레이션을
분기하지 않는다.

### 40.11 구현 상태 — 키보드 진행과 시즌 자동 완료

2026-08-28 기준 반복 경기에서 마우스 이동이 플레이 템포를 끊지 않도록 홈에서 결과까지의
기본 확정 입력을 `Space`/`Enter`로 통일했다. 경기 준비의 `R`은 결과만 보기,
홈의 `S`는 시즌 자동 완료 확인을 연다. 기존 타석 내부의 `1~4`/`Space`/`A`는 유지한다.

자동 완료는 호출 시점의 시즌 단계만 처리한다. 정규시즌에서는 기존
`CareerSeasonService.AdvanceNextRound`로 남은 정규시즌을 진행한 뒤 `Postseason` 진입 화면에서
멈추고, 포스트시즌에서는 별도의 확인을 거쳐 `CareerPostseasonService.AdvanceToChampion`으로
남은 대진만 진행한 뒤 `SeasonReview`에서 멈춘다. 따라서 단일 경기 진행과 같은 난수·기용·기록·
순위·수상 경로를 쓰며, 계약과 성장 같은 커리어 의사결정은 자동으로 건너뛰지 않는다. 잘못된
대량 진행을 막기 위해 현재 단계의 남은 범위와 정지 지점을 알리는 확인 패널을 반드시 거친다.

### 40.12 구현 상태 — 시즌 종료 뒤 다음 시즌 연결

2026-08-28 기준 정규시즌 마지막 경기 뒤 멈추던 홈 CTA를 시즌 상태별로 연결했다.

```text
Postseason → 포스트시즌 결과 진행
SeasonReview → 성장·수입 결산
Offseason → 성장 계획
계약 만료 → 계약 오퍼 선택
계약 체결 또는 유효 계약 → 다음 시즌 RegularSeason
```

- 포스트시즌 결과 진행은 기존 경기·기록·수상 경로를 그대로 사용하며 결산에서 멈춘다.
- 결산은 실제 출장량으로 `SeasonUsageSummary`를 만들고 자연 성장·노쇠·연봉·상여를 한 번만
  적용한다. 타자는 출장 경기 비율, SP는 5인 로테이션 기회 대비 선발 수, RP는 팀 경기 대비
  등판 수로 활용량을 계산한다. 능력치 배분은 새 계수를 만들지 않고 기존 포지션 평가 가중치를
  정규화해 재사용한다.
- 오프시즌 종료 시 계약이 만료됐으면 최고점 오퍼를 자동 선택하지 않는다. 계약 화면에서 구단을
  직접 고른 뒤에만 다음 시즌을 시작한다.
- Seed `280828` 통합 검증에서 포스트시즌 8경기 뒤 `SeasonReview → Offseason → 2029
  RegularSeason` 전환과 수입 지급을 확인했다.

현재 포스트시즌은 결과 진행 방식이다. 내 선수의 포스트시즌 경기를 정규시즌과 같은 준비·타석
관전 화면으로 진행하는 것은 기존 `CareerMatchSession`을 포스트시즌 입력에도 연결하는 후속 범위다.

### 40.13 구현 상태 — 타석 단위 빠른 자동 중계

2026-08-28 기준 선수 중심 경기 진행에서 계산된 이벤트를 한꺼번에 표시하던 방식을 타석 단위
시간 재생으로 교체했다.

- 순수 C# `CareerMatchSession`이 미결정인 내 투구 직전에서 이벤트 생성을 멈추고,
  `CareerMatchPlayback`이 이미 계산된 이벤트의 공개 위치와 그 시점의 이닝·점수·B/S/O·주자 상태를
  소유한다. 투수 교체·수비 전술 같은 타석 준비 이벤트는 먼저 공개하되 아직 선택하지 않은 투구는
  생성하지 않으므로, 표시 계층이 `BatterId`로 정지점을 다시 추측하지 않는다.
- 기본 0.42초 간격으로 각 타자의 투구·타구·주자 진루·득점을 한 타석 묶음으로 공개한다.
  `느리게`/`보통`/`빠르게`는 이 표현 간격과 결과 확인 시간만 각각 1.75/1.0/0.5배로 바꾸며,
  `Time.timeScale`이나 시뮬레이션 결과에는 관여하지 않는다. 화면은 내 구단 기준 `공격 중`/
  `수비 중`, 현재 매치업, 베이스 점유와 내 선수 주루 여부, 타자별 결과 로그를 함께 갱신한다.
- 내 선수 타격 이벤트 앞에서는 자동 소비를 멈춘다. 플레이어가 `다음 투구`를 누르면 그 투구와
  연결된 타구·주자 결과만 즉시 공개한다. 타석이 끝나면 안타·삼진·병살타·희생플라이처럼
  플레이 결과를 별도 패널에 크게 표시하고 보통 속도 기준 2초간 유지한 뒤 자동 중계를 재개한다.
- 모든 진행 화면에 `경기 종료까지 진행`을 두어 선발·벤치 여부와 무관하게 남은 이벤트를 즉시
  소비할 수 있다. `결과만 보기`는 기존 즉시 결과 경로를 유지한다.
- Unity Test Runner에서 타석 묶음·내 선수 정지·주자 상태 복원·병살 묶음·교체 이벤트 공개를
  포함한 EditMode 159건이 통과했다. PlayMode에서는 1회부터의 자동 중계와 벤치 상태의 속도
  전환·즉시 완료 2건이 통과했다.

이 구현은 전체 경기 빠른 중계의 첫 완성 단위다. 중요 상황만 보기, 실제 아이콘 이동 보간·타구
궤적 연출, 플레이어의 주루·수비 선택은 후속 범위다.

### 40.14 구현 상태 — 벤치 야수 대타 투입 포착

2026-08-28 기준 `Bench` 야수도 경기 시작 시 즉시 완료하지 않고, 감독의 대타 후보로 경기
이벤트 스트림 안에 남는다.

- `CareerGameRunner`는 경기 Seed에서 분리한 결정론적 RNG로 벤치 야수의 대타 계획을 만든다.
  기본 후보 선정 확률은 35%이며, 7회 이후 점수 차 3점 이내에서 기존 선발의 타순과 수비 위치를
  이어받는다. 세 값은 `CareerSeasonBalance`와 `NewGameDefinition`에서 조정한다.
- `MatchSimulator`는 투입 시 `PlayerSubstitution` `MatchEvent`를 먼저 기록하고 활성 타자·수비수와
  BoxScore 기록 대상을 교체한다. 이후 타석이 내 선수라면 `CareerMatchSession`이 정확히 그
  입력 지점에서 멈춘다. 투입되지 않으면 관전 중 언제든 `경기 종료까지 진행`할 수 있다.
- 교체 선수는 선발과 별도 타격·수비 기록을 가지며, 실제 타석 또는 수비 아웃이 있을 때만 출장으로
  누적된다. 벤치 경쟁 20시즌 검증에서는 선발 107경기, 교체 출전 359경기, 876타수를 기록했다.
- 같은 검증 실행의 동급 팀 10,000경기는 AVG .264, OBP .327, SLG .428, 팀 경기당 4.40득점,
  BB% 8.7%, SO% 18.0%, 전력 우위 팀 승률 66.1%로 기존 통계 기준을 유지했다.

이 단계는 내 선수 한 명의 야수 대타 투입을 검증하는 Vertical Slice다. 복수 교체, 대주자,
대수비 전용 투입, 투수 교체와 교체 후 재교체는 후속 선수 교체 시스템 범위다.

### 40.15 구현 상태 — 기존 구단 재계약과 동일 리그 트레이드

2026-08-28 기준 시즌 종료 뒤 모든 구단 오퍼를 한 번에 보여 주던 구조를 기존 구단 우선 협상과
외부 공개 시장으로 분리하고, 정규시즌 중 로스터에 막힌 선수가 다른 구단에서 기회를 얻는 이동
경로를 연결했다.

- `ContractRenewalEvaluator`가 기존 구단의 선수 가치·포지션 필요·역할·최근 성적·Potential·
  가성비·감독 관계를 평가한다. 기준 미달이면 재계약하지 않으며, 기준 이상이면 1~3년과 계약상
  역할·예상 출장 비율을 제시한다.
- 21~40경기에는 조건을 충족한 계약 잔여 1년 이하 선수에게 시즌 중 연장 오퍼가 생긴다.
  시즌 종료에는 기존 구단 단독 협상을 먼저 열고 수락/거절/보류 후 외부 시장으로 이동한다.
  공개 시장은 현재 구단을 제외하고, 정식 오퍼가 없을 때만 1년 육성 계약을 최후 수단으로 둔다.
- `TradeValuationAi`와 `TradeMarketService`가 41~56경기 사이 양 구단 필요를 평가한다. 플레이어는
  잔류 선호/중립/트레이드 가능/요청 태도를 선택하고, 관심·루머·협상 단계와 예상 역할·출장
  비율을 팀 화면에서 확인한다.
- 성사 시 `PlayerMovementService`가 계약을 승계한 채 양 팀 로스터와 선수 소속을 원자적으로
  바꾸고 계약상 역할과 분리된 새 팀 예상 역할·감독 평가·다음 경기 기용을 다시 계산한다. 한 시즌 최대 1회, 루키 보호, 마감일,
  이적 후 보호 규칙을 `CareerMovementBalance`에 모았다.
- 시즌 합계와 함께 구단별 타격·투구 분할을 누적한다. 기록 화면은 이동 이력과 이전/새 구단별
  성적을 보여 준다. 통계 스키마는 3, 세이브 버전은 7이다.

결정론적 합성 커리어 10,000시즌에서 트레이드는 2,623회(26.23%) 성사되어 평균 약 3.8시즌에
한 번 체감되는 목표 범위에 들어왔다. 최신 합본 Unity EditMode 전체 186건, 이동 서비스 5건, 계약 우선
협상 9건이 통과했고 `Baseball.Core`/`Simulation`의 Unity 비의존 경계를 유지했다.

상위 리그 월드와 플레이어 대상 실제 인접 리그 계약은 41절의 동시 진행·오프시즌 시장 구조로
구현했다. 아직 구현하지 않은 범위는 복수 선수·현금·지명권 거래, 트레이드 거부권과 옵션 조항,
역할 약속 위반에 따른 불만·구단 신뢰도다. 리그 간 트레이드는 정식 규칙상 허용하지 않는다. 뉴스는
이 시스템의 확정 이벤트를 표현하는 별도 계층이며 커리어 이동 판단이나 계약 결과를 소유하지 않는다.

### 40.16 구현 상태 — 결정론적 커리어 뉴스

2026-08-28 기준 홈의 임시 LastGame 문자열을 확정 도메인 사건에서 생성된 커리어 뉴스로
교체했다.

- CareerNewsState가 대기 이벤트, 현재 시즌 기사, 커리어 연표, 처리한 EventId, 주제 쿨다운,
  최대 3개의 활성 스토리라인을 세이브 루트에서 소유한다. 기사에는 템플릿 ID·변형 번호뿐 아니라
  생성 당시 제목·부제·본문과 구조화된 NewsFactSet도 고정해 이후 템플릿 개정이 기존 세이브의
  문장을 바꾸지 않는다.
- 정규시즌은 내 경기와 같은 라운드의 다른 리그 경기, 선수 기록, 구단 순위, 연속 기록이 모두
  확정된 뒤 EndOfScheduleDate 관문에서만 발행한다. 같은 GameId의 승패·개인 활약·3/5/8
  연승·데뷔/첫 안타/첫 홈런/시즌 홈런 단계는 원본 사건을 유지한 채 기사 한 건으로 병합한다.
- 포스트시즌은 AfterGameResult → AfterSeriesResult → AfterPostseasonReveal →
  AfterAwardReveal 관문을 분리했다. 경기·시리즈 종료·탈락·우승·MVP/신인왕과 내 선수 수상은
  전용 결과가 확정된 뒤에만 뉴스가 되며, 우승과 내 선수의 주요 수상은 커리어 연표에 보관한다.
- 우선순위는 기본 중요도, 내 선수 +40, 내 구단 +20, 커리어·경기 영향, 희귀성, 스토리 후속,
  반복 패널티를 합쳐 S/A/B/C/D로 분류한다. 한 주기에는 톱뉴스 1건, 관련 기사 최대 2건,
  리그 브리핑 1건으로 최대 4건만 발행하며 D 사건도 중복 생성을 막기 위해 처리 이력에는 남긴다.
- 문장 변형은 SeasonId + 정렬된 EventId + TemplateId의 고정 FNV-1a 해시로 고른다.
  UnityEngine.Random과 시스템 시간을 쓰지 않는다. 한국어 템플릿은 완성 문장 단위로 두고,
  이름 뒤 조사만 KoreanPostpositionFormatter가 받침과 으로/로의 ㄹ 예외를 처리한다.
- CareerNewsDefinition SO가 점수, 활약 기준, 마일스톤, 문장 템플릿을 데이터로 주입할 수
  있게 하고 에셋이 없을 때는 테스트된 한국어 기본 정의를 쓴다.
- 홈은 최신 기사 3건만 표시하고 다음 경기 기용 계획 같은 비공개 알림을 뉴스에서 제거했다.
  전체 뉴스 팝업은 최신/내 커리어/구단/리그/이적·계약/기록·수상/커리어 연표 필터,
  읽음 상태, 기사 상세를 제공한다. 고정된 하단 8탭은 늘리지 않는다.
- 부상 확정·복귀, 장기 역할 변화, 계약 서명, 공개 가치가 있는 오프시즌 활동은 각각 전용
  evaluator가 확정 결과를 NewsEvent로 바꾸는 연결점을 제공한다. 뉴스가 부상 기간·역할·
  계약·성장 결과를 계산하거나 바꾸지는 않는다. 진행 중인 커리어 이동 작업은 해당 도메인
  서비스의 최종 커밋 지점에서 이 연결점을 호출해야 한다.

Unity 6000.3.21f1 EditMode에서 중복 방지, 같은 입력의 동일 문장, 경기 사건 병합, 계약 공개
관문, 비출장 경기 제목, 한국어 조사, 정규시즌·포스트시즌·수상 통합을 포함한 뉴스 테스트 11건이 통과했다.
Core·Simulation·Game·Presentation EditMode 전체 회귀 테스트 186건도 모두 통과했다.

### 40.17 구현 상태 — 경기 결과와 오늘의 리그 내러티브

2026-08-29 기준 경기 결과를 일회성 기록 출력이 아니라 `사실 → 해석 → 반응 → 변화`로 읽을 수
있는 1차 내러티브 파이프라인으로 교체했다. 경기 결과와 일일 브리핑의 납득 가능성을 먼저 확보했고,
40.18에서 구간 리포트·반응 선택·장기 계약 및 트레이드 스토리까지 같은 구조로 확장했다.

- 경기 시작 시점의 팀·상대·순위·시즌 기록·감독 신뢰·컨디션·최근 출전 기록을
  `MatchNarrativeBaseline`으로 고정한다. 경기 종료 뒤 확정된 개인 기록과 변화만 합쳐
  `MatchNarrativeSnapshot`을 한 번 만들고 `SeasonState`에 `GameId`별로 보관한다. 결과 화면과
  뉴스는 이후 라이브 상태가 아니라 이 스냅샷을 읽는다.
- `NarrativeTag`는 승패와 한 점 차 경기, 선발·대타·비출장, 홈런·멀티히트·무안타·사사구 출루·
  무삼진·다삼진, 최근 연속 기록, 감독 신뢰·컨디션 변화를 사실 단위로 분리한다. 기록하지 않은
  구종, 타구 속도, 타석 접근법은 문장 근거로 사용하지 않는다.
- 경기 결과 화면은 상단 한 문장 결론, 개인 기록, 수행 평가, 수행 근거, 경기 영향, 최근 흐름,
  감독 코멘트 순으로 보여 준다. 경기 평점과 장기 감독 신뢰를 분리하고, 신뢰·컨디션은 실제
  전후 수치와 변화 이유를 표시한다. 값이 같으면 화살표와 상승색을 쓰지 않고 `유지`로 표시하며
  역할도 `반영 완료`가 아니라 실제 역할의 유지 상태로 표시한다.
- 최근 흐름은 비출장 경기를 제외한 최근 5경기 표본과 연속 안타·무안타를 사용한다. 3경기 연속
  무안타에서 `Slump` 스토리라인을 시작하고 이후 안타가 나오면 같은 스레드를 `Recovered`로
  해소한다. 기사에 사용된 경기 수와 기록은 생성 시점의 사실로 고정한다.
- 일일 리그 브리핑은 시스템 안내문 대신 같은 라운드의 전체 스코어, 실제 전후 순위 변동, 내 팀과
  내 선수 결과를 모듈로 조합한다. `오늘 경기 종료 · 순위 반영 완료`는 기사 본문이 아니라 상세
  화면의 상태 배지로만 표시한다.
- 뉴스 문구는 제목·리드·본문 블록·인용문·매체·톤을 하나로 묶은 `NewsArticleVariant` 단위로
  선택한다. 필수·금지 사실 조건, 가중치, 쿨다운 그룹을 지원하며 선택된 Variant ID와 완성 문장을
  기사 상태에 저장한다. 최근 5주기 안의 같은 문구 그룹은 우선 제외하되 모든 후보가 제외되면
  결정론적 후보로 복귀한다.
- 리그 공식 외에 지역 스포츠와 전국 스포츠 매체 타입을 추가했다. 별도 코칭스태프 시뮬레이션은
  만들지 않고 구단의 `TeamArchetype`에서 성과·육성·분석·보수 관점을 파생한다. 어투가 달라도
  확정 기록과 실제 신뢰 변화 밖의 구종·타구 질·선수 심리를 만들어내지 않는다.

`Baseball.Game`, `Baseball.Presentation`, `Baseball.Game.Tests` 보조 빌드는 경고·오류 없이 통과했다.
순수 C#으로 실행 가능한 내러티브 스냅샷 4건과 뉴스 결정론·병합·공개 관문·비출장·일일 브리핑
및 직렬화 호환성 7건도 통과했다. 열려 있는 Unity 에디터가 프로젝트 잠금을 소유해 별도 BatchMode 전체 EditMode
회귀 테스트는 이번 변경 뒤 다시 실행하지 못했으므로, 이 절의 검증 결과를 기존 186건 전체 통과로
간주하지 않는다. 밸런스 계수는 변경하지 않았으므로 대량 경기 시뮬레이션 재측정 대상은 아니다.

### 40.18 구현 상태 — 구간 리포트와 커리어 스토리 스레드

2026-08-29 기준 경기 내러티브의 남은 범위를 선수 커리어 관점에서 연결했다. 미디어·팬·동료를
독립 경영 시뮬레이션으로 확장하지 않고, 다음 경기를 누르게 만드는 원인과 후속 결과를 저장하는
최소 서사 상태로 제한한다.

- 7경기마다 주간 리포트, 20경기마다 월간 리포트를 발행한다. 해당 구간의 저장된
  `MatchNarrativeSnapshot`만 합산해 개인 타수·안타·홈런·타점과 팀 승패를 고정하며, 라이브 시즌
  누적을 과거 기사에 다시 대입하지 않는다. 지역 스포츠·구단 소식·전국 스포츠 관점은 제목·리드·
  본문이 묶인 `NewsArticleVariant`로 선택된다.
- `RisingForm`, `Slump`, `RosterCompetition`, `RecordChase`, `ContractSeason`, `TradeRumor`는 시작·진행·
  해소 상태를 `NewsStorylineState`에 저장한다. 상승세는 4경기 연속 안타에서 시작하고 무안타 경기에서
  안정화되며, 부진은 반등 안타에서 해소된다. 주전 경쟁은 조정 가능한 감독 신뢰 기준을 실제로
  통과할 때만 시작·해소한다.
- 통산 안타·홈런 기록은 조정 가능한 마일스톤 배열과 접근 범위를 사용한다. 기록 도전 스레드는
  `기록 종류 + 목표값`을 Topic Key로 구분해, 다른 첫 기록이나 별도 마일스톤이 엉뚱한 도전을
  해소하지 못하게 한다.
- 기존 구단의 실제 연장 제안이 생성되면 계약 논의 기사를 한 번만 발행하고, 수락·거절 결과를
  확정 관문 뒤에 공개한다. 트레이드 시장은 실제 `Interest → Rumor → Negotiating → PlayerTraded`
  단계가 바뀔 때만 기사를 만들며, 확정 이적은 이전·새 구단과 예상 역할을 커리어 연표에 남긴다.
- 데뷔·첫 안타·첫 홈런·연속 부진 시작/해소·주전 위험·중요한 한 점 승부·계약 제안·트레이드설만
  짧은 반응 이벤트 후보가 된다. 최근 6라운드 쿨다운과 단일 대기 이벤트 규칙으로 반복 피로를 막고,
  세 답변은 감독 신뢰·자신감·미디어 평가·팬 반응·팀워크에 작은 효과를 정확히 한 번 적용한다.
  미해결 반응이 있으면 다음 경기·자동 진행을 막아 선택 결과가 건너뛰어지지 않는다.
- 세이브 스키마는 v10으로 올렸다. `CareerNarrativeState`가 선택 이력과 네 관계 지표를 소유하며,
  v9→v10 마이그레이션은 월드·선수·계약·시즌 상태를 그대로 보존하고 내러티브 상태만 초기화한다.

`Baseball.Game`, `Baseball.Presentation`, `Baseball.Game.Tests` 보조 빌드가 통과했고, 경기 해석·뉴스
결정론·구간 리포트·상승세/주전/트레이드 스레드·반응 단일 적용·v10 마이그레이션을 포함한 순수 C#
집중 테스트 19건이 통과했다. Unity BatchMode 전체 EditMode 회귀 테스트도 시도했지만 Licensing
Client가 headless entitlement를 찾지 못해 테스트 시작 전에 중단됐으므로, 이 결과를 기존 전체 테스트
통과로 간주하지 않는다. 경기 확률과 밸런스 계수는 바꾸지 않았으므로 대량 시뮬레이션 재측정
대상은 아니다.

### 40.19 구현 상태 — 커리어 챕터 컷 이미지 연출

2026-08-29 기준 `Assets/04.Images/UI`의 세로 이미지를 정규시즌·포스트시즌·수상·오프시즌 성장
결과에 연결했다. 연출은 시뮬레이션 결과를 만들거나 보상을 적용하지 않고, 이미 확정·저장된 사실을
선수 커리어의 기념사진으로 보여 주는 Presentation 소비자로 제한한다.

- `UI_CareerPresentation` 공통 Popup 하나가 같은 Sprite를 어두운 Blur 배경과 선명한 세로 전경에
  동시에 배치한다. 제목·설명·확정 기록은 Unity UI로 분리해 이미지에 리그명·선수명·로고를 넣지 않는다.
- 이미지 7장을 SO 정의 8개에 연결했다. 정규시즌 1위, 포스트시즌 우승, 골든글러브, 훈련, 유학,
  휴식은 전용 이미지를 쓰고 정규시즌 MVP와 포스트시즌 MVP는 공용 수상 이미지를 공유한다.
- `RankUp`, `Award`, `Championship`, `Training`, `Travel`, `Rest` 모션 프리셋과 기본 재생 시간은
  `CareerPresentationData`가 소유한다. DOTween은 비네트 배경 줌, 전경 25~40px 이동, 텍스트·기록
  순차 등장, 우승 미세 흔들림, UI 색종이, 수상 광택, 유학 패닝, 휴식 호흡 루프를 unscaled time으로 재생한다.
- 시즌 리뷰의 저장 가능한 `SeasonReviewState`를 발생·확인 원본으로 사용한다. 정규시즌 1위,
  포스트시즌 우승, 포스트시즌 MVP, 골든글러브, 정규시즌 MVP만 조건을 만족할 때 큐에 들어가며,
  연출 완료 뒤 기존 `AdvanceSeasonReview`를 호출한다. 따라서 연출 도중 종료하면 같은 리뷰 단계에서
  재개되고, 이미 진행한 단계는 같은 시즌에 다시 보이지 않는다.
- 오프시즌은 새 `GrowthHistory` 결과만 관찰한다. 유학은 `Study`, 휴식은 `rest`, 그 밖의 개인·파트너
  훈련은 Training으로 분류하며 실제 주차·능력 변화·Potential 변화·컨디션·비용을 표시한다. 같은
  시즌의 동일 활동을 다시 실행하면 1.4초 Compact Cut을 사용한다.
- 주요 연출은 1초 뒤부터 건너뛸 수 있고 활동 연출은 결과 표시 뒤 자동 종료한다. `Full`,
  `Simplified`, `ResultOnly` 세 밀도는 PlayerPrefs 기반 설정 계약으로 분리했으며 별도 설정 화면은
  공통 설정 UI가 추가될 때 이 계약에 연결한다.
- 수상과 우승 뉴스는 문구가 아니라 고정 `SourceEventId`에서 이미지 종류를 복원해 같은 Sprite를
  상세 썸네일로 사용한다. 병합 기사에서는 우승·MVP·수상·활동 순의 명시적 우선순위를 적용한다.
- 커리어 기록은 별도 중복 저장소를 만들지 않고 기존 `SeasonHistory`와 `Awards`를 원본으로 쓴다.
  기록 화면의 골든글러브·정규시즌 MVP·포스트시즌 MVP 행을 선택하면 보상 콜백이 없는 Major Cut으로
  다시 재생한다.

`Baseball.Core`, `Baseball.Simulation`, `Baseball.Game`, `Baseball.Presentation` 보조 빌드는 경고·오류
없이 통과했다. 큐 중복 방지, 유학 주차, 반복 Compact Cut, 휴식 결과, 수상 재감상의 무보상 계약,
뉴스 이미지 ID와 병합 우선순위를 포함한 집중 테스트 12건도 통과했다. Unity BatchMode 검증은
Licensing Client가 headless entitlement를 찾지 못해 에셋 Import와 화면 캡처 전에 중단됐다.
따라서 최종 완료 전 Unity 6000.3.21f1에서 Sprite/SO/Shader Import, 16:9·16:10 레이아웃, 입력 스택,
각 프리셋의 실제 속도와 색 대비를 Play Mode로 확인해야 한다. 확률·밸런스 수치는 변경하지 않았다.

### 40.20 구현 상태 — 감독 AI 타순 편성과 기용 정보 일치

2026-08-29 기준 포지션 번호를 그대로 타순으로 사용하던 임시 규칙을 제거했다. 감독 AI가 먼저
포지션별 선발을 확정한 뒤 `Contact`, `Power`, `Speed`, `Mental`을 타순 역할별로 평가해 실제 아홉 명
타순을 만든다. 따라서 3루수라는 이유만으로 4번에 고정되지 않는다.

- `ManagerLineupAi`는 4번 중심타자, 1번 출루형, 2번 테이블세터, 3·5번 해결사, 6~9번 하위 타선
  순으로 후보를 배정한다. 동점은 `PlayerId` 오름차순으로 해소해 입력 배열 순서와 무관하게 결정론적이다.
- 역할별 가중치는 `ManagerLineupBalance`와 `NewGameDefinition`의 `Manager Lineup` 항목에 둔다.
  `Bunt`는 전술 실행 능력, `Defense`는 수비 가치이므로 이번 타순 점수에서는 제외했다.
- 홈의 다음 경기, 구단 로스터·브리핑, 실제 `MatchInput`이 모두 같은 `CareerLineupPlan`을 사용한다.
  현재 역할인 `벤치 경쟁`과 그날의 기용 결과인 `선발 3B · N번 타자`를 서로 덮어쓰지 않는다.
- 벤치 선수가 교체 출전할 때도 포지션 번호가 아니라 완성된 라인업에서 해당 수비수의 실제 타순을
  찾아 승계한다.

이질적인 능력치의 두 팀을 기본 밸런스로 10,000경기 시뮬레이션한 결과는 AVG .268, OBP .337,
SLG .435, 팀 경기당 4.720득점·1.304홈런, BB 8.63%, SO 17.62%, ERA 4.75였다. 같은 선수들을 기존
포지션 순서로 배치했을 때보다 팀 경기당 득점은 0.038점 증가해 기존 득점 환경을 유지했다.
`Baseball.Simulation.Tests`, `Baseball.Game.Tests`, `Baseball.Presentation` 보조 빌드는 경고·오류 없이
통과했고, 3루수 비고정·역할별 배치·결정론·선수/수비 위치 보존·화면/경기 일치를 포함한 집중 테스트
6건도 통과했다. 열려 있는 Unity Editor의 전체 Script Assembly 컴파일과 Domain Reload도 성공했다.
Editor가 현재 프로젝트를 사용 중이어서 별도 BatchMode EditMode 실행은 하지 못했다.

### 40.21 구현 상태 — 타이틀 및 5단계 선수 커리어 생성

2026-08-29 기준 기존 즉시 입력형 새 게임 화면을 타이틀에서 시작하는 선수 커리어 생성 흐름으로
교체했다. 이 절의 수치와 단계는 40.1~40.2의 과거 생성 UI 세부안보다 우선한다.

```text
타이틀 → 기본 정보 → 포지션/희망 보직 → 초기 능력치 → 세부 설정
→ 경기 운영 설정 → 최종 확인 → 신인 계약 오퍼 → 커리어 홈
```

- 이 항목의 최초 구현 당시 타이틀은 `img_title`을 배경으로 선수 커리어만 활성화했고 구단주 모드는
  `준비 중`이었다. 2026-08-31 방향 변경 이후 타이틀은 선수 모드와 구단주 모드를 별도 진입점으로
  제공하며, 각 모드의 실제 Runtime·Save가 준비된 경우에만 이어하기를 활성화한다. 명시적 세션 없이
  두 Runtime이 함께 존재하면 한쪽 화면을 임의 노출하지 않고 모드 선택으로 돌아간다.
- 생성 중 입력은 `CareerCreationDraft`에만 보관하고 최종 확인 뒤 `CareerCreationProfile`로 확정한다.
  이전 단계로 돌아가도 draft와 화면 선택을 유지하며 타이틀 복귀 시 draft만 폐기한다.
- 이름은 공백 정리 후 2~12자의 한글·영문·숫자를 허용한다. 타자/투수, 투타를 1단계에서 함께
  확정하며 `img_hitter_default`와 실제 리소스명 `img_pitcher_default`를 프리뷰에 사용한다.
  유형이 바뀔 때만 0.22초 확대와 0.10초 안정화 Tween을 재생한다.
- 타자는 C/1B/2B/3B/SS/LF/CF/RF/DH를 필수 선택한다. 투수는 Starter/LongRelief/
  MiddleRelief/Setup/Closer를 확정 보직이 아닌 희망 보직으로 저장하고 실제 기용은 감독 AI가 정한다.
- 생성 전용 능력치는 1~250 계약에서 기본 50, 생성 상한 75를 쓴다. 타자는 6개에 60포인트,
  투수는 4개에 40포인트를 정확히 모두 사용해야 하므로 균형형의 평균은 Rookie 기준 60이다.
  이전 규칙의 항목별 배분 폭 25는 유지해 특화와 약점 선택을 보존한다. 프리셋은 같은 총점을
  자동 분배할 뿐 보너스를 만들지 않으며, 6능력치 경기 모델에는 draft 원본을 보존한 어댑터로 연결한다.
- 투수는 포심을 포함한 정확히 3개 구종과 주무기 하나를 고른다. 주무기는 숙련도 55, 나머지는
  45로 저장한다. 타자는 보너스 없는 타격 스타일을 고른다. 신구종 습득과 숙련도 성장은 후속
  오프시즌 훈련 범위이며 초기 생성 화면에서는 만들지 않는다.
- `CareerGameSettings`가 타격/투구 방침, 전체 관전/내 선수 개입/내 선수 자동/즉시 결과,
  1×/2×/3×/5×, 내 선수 장면 자동 감속을 소유한다. 기본값은 내 선수 개입·2×·자동 감속 ON이다.
  방침은 `MatchDecisionCoordinator`에 주입되어 플레이어 타자와 투수의 실제 확률 모델에 적용된다.
- 네 진행 방식은 같은 `MatchSimulator` 이벤트 스트림을 소비한다. 전체 관전과 내 선수 자동은
  끝까지 계산한 이벤트를 재생하고, 즉시 결과는 연출을 생략한다. 내 선수 개입은 타자와 투수 모두
  상대 타석 시작 전에 한 번 멈추며, 투스트라이크 보호와 3볼 신중 승부는 자동 오버레이로 처리한다.
- 커리어 설정 Popup은 경기·화면·사운드·조작·게임 종료 탭을 가진다. 경기 탭은 선수 유형에 맞는
  방침만 보여 주며 배속·자동 감속·진행 방식을 바꾼다. 진행 중 즉시 결과와 타이틀 복귀는 별도
  확인을 거치며 안전한 취소 버튼을 기본 포커스로 둔다. 홈/선수/성장/일정/리그/구단/기록/계약은
  `CareerNavigationChrome`의 같은 위치에 있는 설정 버튼을 공유한다. 경기 중 설정 버튼은 자동
  중계가 화면을 다시 그려도 파괴되지 않는 지속 UI 계층에 두며, Popup이 열린 동안 중계를 멈춘다.
- 내 선수 장면 자동 감속은 선택 배속 표시만 바꾸는 옵션이 아니라 실제 중계 대기 시간에 1×를
  적용한다. 내 선수 중심 자동에서는 다른 선수 장면을 5×로 처리하고 내 선수 타격·투구 장면은
  옵션이 켜져 있을 때 1×로 전환하며, 상단 상태에도 현재 적용 중인 유효 배속을 표시한다.
- 타이틀 복귀는 `Time.timeScale`을 1로 복구하고 경기·성장·커리어 런타임 상태와 생성 draft를
  제거한 뒤 Management의 타이틀 상태로 돌아간다. 디스크 저장이 없으므로 복귀 뒤 이어하기는 없다.
- 생성 프로필 추가로 세이브 스키마를 v11로 올렸다. v10→v11 마이그레이션은 월드·선수·계약·
  내러티브를 보존하고 기존 선수 포지션에서 기본 생성 프로필과 경기 설정을 복원한다.

전체 솔루션 보조 빌드는 경고·오류 없이 통과했다. 동일 입력 결정론 검사를 포함한 10,000경기
진단은 AVG .271, OBP .344, SLG .451, 팀 경기당 4.181득점·1.316홈런, BB 8.98%, SO 16.76%였다.
초기 능력치 상향 뒤 별도 10,000개 Rookie 로스터 생성 진단에서 균형형 타자·투수의 포지션 평가는
각각 60, AI 개별 경쟁자 평균은 59.97, 같은 포지션 최고 경쟁자 평균은 61.64였다. 따라서 플레이어는
리그 평균 전력으로 시작하되 주전 자리는 구단 선택·특화 배분·감독 판단을 통해 경쟁한다.
Unity BatchMode는 Licensing Client가 headless entitlement를 찾지 못해 EditMode/PlayMode 실행과
실제 화면 캡처를 완료하지 못했다. 따라서 Unity 6000.3.21f1에서 타이틀 Sprite Import, 16:9/16:10
레이아웃, 키보드·게임패드 포커스, Tween 속도와 타이틀 복귀를 Play Mode로 확인해야 한다.

### 40.22 구현 상태 — 세부 경기 시뮬레이션 Detailed V2

2026-08-29 기준 `DetailedV2`를 유일한 실제 경기 실행 경로로 전환했다. 과거 고정 이닝 교체와
연장 승자 추첨 결과는 더 이상 사용하지 않으며, 기존 `Team` 입력은 V2 경기 로스터로 바꾸는 호출부
어댑터로만 남긴다. 리플레이 결과 호환은 지원하지 않고 동일 V2 입력의 결정론만 보장한다.

```text
투구 → Stamina 피로·이닝 Stress·타순 대면 → 실효 투수 능력
     → PullScore와 불펜 역할/최근 부하 → 동적 교체

인플레이 → 범주형 타구 → 수비수 도달 → 포구 → 송구
         → 공식 안타/실책/야수선택/병살 → 상황별 주루
```

- `MatchRosterSnapshot`은 선발 라인업, 벤치, 선발투수, 다인 불펜, 역할, 컨디션, 최근 3일 부하와
  감독 전술 성향을 한 경기 입력으로 잠근다. `PlayerState`는 날짜별 최근 투구 수를 저장하고 정규시즌,
  배경 리그, 포스트시즌이 경기 확정 시 `PitcherUsageReport`를 같은 경로로 반영한다.
- `PitcherGameState`와 `PitcherFatigueResolver`가 역할·Stamina·최근 부하로 실효 투구 용량을 만들고
  55% 이후 비선형 능력 하락을 적용한다. 이닝 Stress와 Mental 완화, 타자별 대면 횟수 보정은 물리
  피로와 분리한다. `PitcherManagementAi`는 피로, 현재 위험, 세 번째 타순, 성과, 레버리지, 상성,
  감독 성향과 불펜 보존 비용을 점수화한다.
- 불펜은 Starter/Swingman/LongRelief/MiddleRelief/Setup/Closer 역할을 사용한다. 최고 구원투수는
  9회 고정이 아니라 레버리지와 미래 사용 비용으로 선택한다. Save/Hold/BlownSave와 inherited runner,
  투구 수를 BoxScore와 시즌 통계에 분리 기록한다. 몸풀기는 이 단계에서 자동 즉시 교체로 유지한다.
- `BattedBallResolver`, `FieldingPlayResolver`, `BaserunningResolver`가 타구 생성과 수비·주루를 분리한다.
  Range는 도달과 BABIP, Hands는 포구/송구 실책, Arm은 추가 진루와 병살에 서로 다르게 작동한다.
  도달 실패는 안타이고 루틴 타구 처리 실패만 ROE이며, 잠재 아웃 기반 자책점 추적을 적용한다.
- 여러 대타·대주자·수비 강화와 퇴장 선수 재출전 금지를 `SubstitutionLedger`가 검증한다. 도루, 번트,
  고의사구, PullShift/DoublePlayDepth/InfieldIn/GuardLines는 `RunExpectancy24`와 휴리스틱
  `WinExpectancyModel`의 기대값 차이로 선택하며 AI 판단에는 RNG를 사용하지 않는다.
- `SituationalBattingDecisionProvider`와 `SituationalPitchingDecisionProvider`가 타자/투수의 전략을
  같은 `DecisionContext`로 결정한다. `MatchSession.Advance()`는 이벤트 1건, 의사결정 요청, 공수 교대,
  경기 종료 경계만 반환하므로 Simulation은 Unity UI를 모른다. 선수 입력은 타석당 한 번이며 Auto,
  KeyMoments, FullControl 권한을 지원한다. 구단주 모드도 같은 이벤트 스트림과 공용 Match HUD를
  소비하되, 선수 직접 입력이나 존재하지 않는 운영 Command를 가짜로 노출하지 않고 실제 Owner
  Runtime이 제공하는 권한만 별도 Overlay로 연결한다.
- 경기 RNG는 PitchOutcome/SwingDecision/Contact/BattedBall/Fielding/Baserunning/Injury 서브스트림으로
  나뉜다. 같은 Seed·입력·판단의 이벤트 스트림은 필드 전체 비교 테스트로 고정한다.
- 정규 시즌은 12회 동점 무승부, 포스트시즌은 승자가 날 때까지 진행한다. 자동 주자는 `MatchRules`의
  선택 규칙이며 어떠한 이닝 상한에서도 승자를 추첨하지 않는다.
- 이벤트 스트림은 투수 교체 이유, 피로 단계, 대타·대주자, 도루, 번트, 고의사구, 수비 위치,
  실책·야수선택·병살을 포함한다. 경기 화면은 `ReasonCode`를 “피로 누적”, “세 번째 타순 대면”,
  “승부처 대응”처럼 표시하며 모든 불펜·벤치 선수 이름을 V2 로스터에서 찾는다.

순수 C# NUnit 우회 러너에서 규칙·결정론·접근법·10,000경기 회귀를 포함한 Simulation 테스트
30건이 모두 통과했다. 최종 별도 10,000경기 진단은 AVG .268, OBP .342, SLG .446,
팀 경기당 4.094득점·1.294홈런, BB 9.04%, SO 17.41%, HBP 1.09%, 경기당 실책 .529,
팀당 2.682명 투수 등판, 선발 89.26구, 도루 성공률 66.24%, 무승부 2.45%였다.
Unity Script Assembly와 Core/Simulation/Game/Presentation 보조 빌드는 경고·오류 없이 통과했다.
Unity BatchMode EditMode는 `Unity.PerformanceTesting.Editor.TestRunBuilder` 사전 단계에서 정지하므로
패키지 훅을 정리한 뒤 에디터 Test Runner에서 동일 테스트를 다시 확인해야 한다.

### 40.23 구현 상태 — 포지션·송구·희망 보직의 실제 경기 연결

2026-08-30 기준 생성 화면이 약속하는 포지션별 빌드와 실제 커리어 경기 입력을 같은 능력치 계약으로
연결했다.

- 타자 6능력치는 `Contact / Power / Speed / Arm / Defense / Mental`을 정본으로 삼는다. 생성 화면의
  `Eye`는 `Mental`로, `Arm`은 같은 이름의 능력치로 저장한다. 기존 enum 순서와 Skill Block 내부 ID는
  유지해 세이브 배열과 이미 생성된 블록 ID를 깨지 않는다.
- `FieldingPlayResolver`의 Range와 Hands는 `Defense`, Arm은 실제 `Arm`에서 파생한다. Arm은 추가 진루,
  병살 송구, 도루 저지에 영향을 주며 포구·송구 실책은 Hands가 담당한다. `Bunt`는 독립 저장값 대신
  `Contact`와 `Mental`의 파생값이다.
- `PlayerValueEvaluator`와 시즌 성장 가중치는 포지션별 수비 기여 차이를 반영하되 모든 능력치를
  양의 가중치로 평가한다. 생성 화면에는 일부 능력치만 정답처럼 보이는 "핵심 능력" 목록을 노출하지
  않고, 포지션이 수비 위치와 팀 내 경쟁군을 결정한다는 계약만 안내한다.
- NPC 로스터는 더 이상 모든 능력치를 같은 OVR로 복제하지 않는다. Seed, 포지션, 목표 OVR에서
  결정론적으로 프로필과 개인 편차를 만들고 포지션 가중 OVR은 원래 경쟁자 OVR에 맞춘다. 실제 경기
  라인업도 축약 경쟁자 수치로 선수를 다시 만들지 않고 `WorldState.PlayerState`의 능력치와 성장 결과를
  사용한다.
- 구원투수의 `LongRelief / MiddleRelief / Setup / Closer` 희망 보직은 경기 불펜 등록 역할까지 전달한다.
  `PitcherManagementAi`는 이 역할과 이닝, 레버리지, 피로를 함께 사용하므로 세부 희망 보직이 실제
  등판 시점과 기회에 영향을 준다.
- 부 포지션은 이번 범위에서 자동 생성하지 않는다. 현재 캐릭터 생성은 주 포지션 하나를 선택하고 감독
  AI도 그 포지션 경쟁에만 등록하므로, 멀티 포지션은 별도 기용·성장·UI 규칙 없이 숨은 데이터만
  추가하지 않는다.

수정 후 별도 10,000경기 진단은 AVG .263, OBP .338, SLG .435, 팀 경기당 3.920득점·1.252홈런,
BB 9.03%, SO 17.42%, HBP 1.09%, 경기당 실책 .541, 팀당 2.656명 투수 등판, 선발 89.45구,
도루 성공률 66.81%, 무승부 2.50%였다. 같은 진단에서 균형형 타자·투수의 포지션 가치가 각각 60,
Rookie 경쟁자 평균 OVR 59.97, 포지션 최강 경쟁자 평균 61.64로 유지됐다.

### 40.24 구현 상태 — 선수 직접 플레이 미니게임 1차 정식 통합

2026-08-30 기준 기존의 "직접 타격·투구 없음" 계약을 선택형 선수 행동 입력으로 확장했다. 이 변경은
실시간 3D 야구로 방향을 바꾼 것이 아니라, 같은 확률 시뮬레이션의 `PitchIntent`와 `SwingExecution`
결정 지점에 사람 입력 공급자를 추가한 것이다. 감독의 출전·교체 권한과 기존 기록·성장·계약·뉴스
연결은 그대로 유지한다.

- `MiniGameBalance`가 정규화 목표 범위, 제구 분산, 배트 유효 범위, 35/80/140ms 타이밍 기준,
  AI 입력 오차, 타구 품질과 장타 조건을 한곳에 보관한다. Core와 Simulation에는 Unity 타입이 없다.
- `PitchSelectionRequest/Command`, `BatterMiniGameRequest`, `SwingCommand`, `PitchFlightDescriptor`,
  `ContactProfile`, `PitchPlayData`를 순수 데이터 계약으로 추가했다. Unity `Vector2`나 프레임 시간 대신
  정규화 좌표와 0~1 입력 시간을 저장한다.
- `PitchExecutionResolver`는 보유 구종·숙련도·Velocity/Breaking/Control·피로가 반영된 실제 위치,
  구속, 변화량과 제구 예상 타원을 만든다. 목표점은 의도일 뿐 실제 통과점과 분리된다.
- `SwingContactResolver`는 지켜보기, 헛스윙, 파울, 번트와 인플레이를 위치·타이밍으로 판정한다.
  인플레이는 안타가 아니라 타구 속도·발사각·좌우 방향·회전·품질을 만들고 기존
  `BattedBallResolver → FieldingPlayResolver → BaserunningResolver`로 전달한다.
- AI 투수와 AI 타자도 각각 `PitchSelectionCommand`와 `SwingCommand`를 생성한다. 자동 진행과 직접
  플레이는 같은 Resolver를 사용하며, 추천 입력을 그대로 제출한 직접 플레이는 자동 진행과 공식
  이벤트·BoxScore가 일치한다. 같은 구종을 반복하면 타자의 Mental에 따라 유인구 추격이 줄고 위치·
  타이밍 예측 오차가 작아진다.
- `MatchSession`과 `CareerMatchSession`은 `Advance → DecisionRequired → SubmitPitchSelection /`
  `SubmitSwingExecution → replay-from-seed` 경계를 제공한다. 같은 Seed·같은 명령은 같은 실제 투구와
  이벤트를 만들며 입력 재생, 디버깅, 향후 리플레이 기반을 공유한다.
- `CareerGameSettings`에 Simulation/MiniGame 진행, 전체 관여·중요 상황·타석 선택 범위, 입문·표준·
  프로 보조를 추가했다. 역할별 권장값은 타자 전 타석, 선발투수 타자 단위 선택, 구원투수 전 타자다.
  자동 전환은 현재 타석, 현재 투수 이닝, 남은 경기 단위로만 실행한다.
- Presentation은 3×3 존과 외곽 자유 목표, 구종 목록, 제구 예상 타원, 목표 좌표, 타격 평면, 타원형
  배트 커서, 실제 궤적 재생, 타격 의도·번트, 목표/실제 위치와 컨택 피드백을 런타임 UI로 그린다.
  별도 비트맵이 필요하지 않아 진행 중인 공용 UI Skin 작업이나 ImageGen 자산을 덮어쓰지 않았다.
- `PitchEvent`에는 구종·목표/실제 위치·구속·변화량·스윙 위치/시점·컨택 프로필을 기록한다. 공식
  `MatchEvent`와 BoxScore를 그대로 사용하므로 시즌 기록, 수상, 계약 평가, 뉴스와 은퇴 대표 경기의
  입력 경로를 분기하지 않는다.

최종 보정값으로 평균 팀 10,000경기 진단을 실행한 결과 AVG .275, OBP .350, SLG .426, 팀 경기당
3.79득점, BB 9.2%, SO 17.4%, HR/PA 1.54%, HBP 1.10%, 강팀 승률 73.2%였다. 40.23 기준 대비 AVG,
OBP, SLG, 득점, BB, SO, HBP가 모두 약 ±5% 안이며 통계 범위 테스트를 통과했다. 직접 플레이 핵심
테스트 8개와 기존 경기 규칙·결정론 회귀 테스트도 통과했다.

현재 정식 1차 범위 밖은 체크 스윙, 낫아웃, 폭투/포일의 주자 진루, 고급 릴리스 게이지, 수비·주루
직접 조작, 장면 리플레이와 전용 오디오다. Unity BatchMode는 라이선스 초기화 단계에서 진행되지 않아
에디터 Play Mode의 16:9/16:10 레이아웃과 실제 입력 장치 포커스는 별도 시각 검증이 남아 있다.

### 40.25 구현 상태 — 투수 미니게임 2D 궤적·제구 피드백

투수 직접 플레이는 기존 확률 판정과 `PitchSelectionCommand` 입력 계약을 유지하면서, 선택 결과가
포수 미트까지 도달하는 과정을 읽을 수 있도록 표현 단계를 분리했다. 타석 시작 즉시 투구하지 않고
`PrePitchReady → PitchSelection/TargetAiming/StrategySelection → PitchConfirmed → Windup →`
`BallInFlight → PlateArrival/BatterReaction → PitchResult → NextPitchReady` 순서로 진행한다.

- `PitchTypeProfileCatalog`는 `PitchExecutionResolver` 내부 구종 상수를 값 변경 없이 순수 C#으로
  분리한다. Resolver와 대표 궤적이 같은 변화 방향·시작 시점을 사용하지만, 실제 제구 난수는 미리보기로
  공개하지 않는다.
- `PitchFlightDescriptor.Evaluate(t)`는 포인트 배열이나 Unity 타입 없이 정규화 시간의
  `PitchTrajectoryPoint`를 계산한다. `Evaluate(0)`은 릴리스, `Evaluate(1)`은 실제 `PlatePoint`이며,
  투수·타자 표현 모두 이 평가기를 실제 궤적의 단일 원천으로 사용한다.
- Presentation은 공의 2D 크기 증가, 늦은 변화, 고정 5~8개 잔상으로 원근과 구종 차이를 표현한다.
  화면상 변화 강조는 도착점을 보존하는 Presentation 설정이고 판정 좌표·구속·컨택 결과를 바꾸지 않는다.
- 사람 투수는 구종 대표 궤적, 목표 십자, 기존 Resolver가 제공한 예상 제구 타원을 투구 전 확인한다.
  도착 뒤에는 목표점과 실제 통과점, 연결선, 제구 오차와 기존 `ContactProfile` 결과를 확인한다.
- 사람 타자와 CPU 관전에는 투수 목표점·예상 제구 타원·대표 궤적을 사전 노출하지 않는다. 실제로
  확정된 Descriptor의 공 이동만 공유하며 Instant Simulation에는 Presentation 객체를 만들지 않는다.
- 투구 확정 뒤 입력과 중복 제출을 잠그고, 실제 `PitchEvent` 공개를 궤적 재생 완료까지 보류한다.
  `CareerMatchPlayback.RevealThroughEvent`가 해당 투구까지만 공개하므로 뒤의 타구·주자·타석 종료 이벤트는
  기존 중계 흐름으로 계속 소비된다.
- 공·잔상·대표 궤적·목표/실제 마커는 화면 초기화 때 한 번 생성해 재사용한다. 투구마다 경로 배열을
  만들지 않고 현재 `t`에서 직접 평가하며, 설정 팝업으로 Update가 정지하면 전용 누적 시계도 멈춘다.

Simulation 경계·구종 방향·이벤트 부분 공개와 PlayMode 흐름 테스트를 추가했다. 2026-08-30 이번
수정에서는 사용자 요청에 따라 빌드, 결정론 회귀 실행, 10,000경기 통계 회귀 실행은 보류했으므로
실행 통과를 완료 조건으로 보고하지 않는다.

### 40.26 구현 상태 — 타자·투수 공통 Play Resolution 연출

타자와 투수 미니게임의 입력 뒤 결과를 곧바로 텍스트로 압축하지 않고, 같은 경기 이벤트를 소비하는
공통 `PlayResolutionSequence`로 연결한다.

```text
Pitch / Contact / Fielding / Runner MatchEvent
→ PlayResolutionSequenceBuilder
→ PresentationCue 타임라인
→ Plate View 또는 Field View
→ 판정 Cue 직후 HUD·점수·로그 공개
```

- Plate View는 Take, Swing, Swing & Miss, Foul, Contact를 실제 투구 도착과 이어서 표현한다.
  인플레이가 아니면 포수 미트 도착과 판정 뒤 다음 투구로 돌아간다.
- 인플레이는 Impact Hold 뒤 2D Field View로 전환한다. 타구 종류별 궤적, 실제 담당 야수 이동,
  포구·공 회수·송구, 주자의 동시 이동, 베이스 도착의 `OUT/SAFE/SCORE`를 한 시퀀스로 보여 준다.
- Simulation이 확정한 `BattedBallDescriptor`와 `FieldingPlayOutcome`을 `BallInPlayEventData`로 이벤트에
  실어 보낸다. Presentation은 6-3 땅볼 같은 결과를 역추측하거나 별도 RNG로 야수를 선택하지 않는다.
- 공·야수·주자·송구선은 화면 초기화 때 고정 생성하고 플레이마다 위치와 표시 상태만 초기화한다.
  타구 경로 포인트 배열, Rigidbody, Collider, Unity 난수는 사용하지 않는다.
- `CareerMatchPlayback` 이벤트 공개 위치는 Cue 타임라인이 소유한다. Contact, Field 전환,
  RunnerAdvance, Out, Score, PlateAppearanceEnded는 해당 화면 사건이 발생한 직후까지 순차 공개되며,
  경기 로그는 이미 본 플레이를 기록하는 역할만 한다.
- 타자와 투수는 서로 다른 결과 판정기를 두지 않는다. 입력 평가와 야구 결과는 구분해 표시하며,
  좋은 컨택의 정면 타구 아웃과 좋은 제구 뒤 타자의 대응도 기존 확률 결과 그대로 설명한다.

이번 변경은 결과 이벤트의 표현 계약을 보강한 것이며 판정 확률, RNG 호출, 기록 집계 순서를 바꾸지
않는다. 2026-08-30 작업에서는 사용자 요청에 따라 빌드, 결정론 검증, 10,000경기 회귀를 실행하지
않았으므로 Unity Play Mode의 레이아웃·실제 재생과 함께 별도 검증이 남아 있다.

---

## 41. 다중 리그 월드 및 선수 커리어 승강 시스템

### 41.1 핵심 계약

Rookie, Minor, Major는 다음 난이도를 해금할 때 교체되는 배경이 아니라 같은 월드에서 동시에
시즌과 역사를 만드는 독립 리그다. 기본 공식 월드는 `3개 리그 × 리그당 8구단 = 24구단`이다.
Major 구단이 Minor/Rookie 구단을 산하로 소유하지 않으며 구단 승강도 없다. 선수만 계약 시장을
통해 `Rookie ↔ Minor ↔ Major` 인접 단계로 이동한다.

- 시즌 중 이동은 같은 리그 트레이드만 허용한다.
- 리그 간 이동은 오프시즌 신규 계약으로만 허용한다.
- 다년 계약 선수를 구단이 일방적으로 다른 단계에 보내지 않는다.
- Rookie → Major, Major → Rookie 직접 이동은 기본 규칙에서 금지한다.
- 상위 리그 진입은 능력치 해금 보상이 아니라 역할·출장·연봉·육성·우승 가능성을 비교하는 선택이다.
- 자격이 없는 선수에게 최소 오퍼 수를 맞추기 위한 가짜 승격 오퍼를 만들지 않는다.

### 41.2 상태 소유 구조

```text
CareerState
├─ MyPlayerId
├─ CareerEconomyState / PlayerGrowthState / PlayerDecisionState
└─ WorldState
   ├─ WorldSeed / GlobalCalendarState
   ├─ LeagueRegistry     LeagueId → LeagueState
   ├─ TeamRegistry       TeamId → TeamState
   ├─ PlayerRegistry     PlayerId → PlayerState
   ├─ ContractRegistry   ContractId → PlayerContractState
   ├─ PlayerMovementLedger
   ├─ WorldRecordState
   └─ DomainEventJournal
```

`LeagueId`는 `Rookie.Main` 같은 영구 식별자이고 `LeagueLevel`은 Rookie/Minor/Major 경쟁 단계다.
같은 단계에 East/West 리그가 추가될 수 있으므로 두 값을 합치지 않는다. `CurrentLeague`는 별도
저장 포인터가 아니라 `MyPlayer.CurrentLeagueId`로 `WorldState`에서 계산한다. 리그는 `TeamId`
목록을, 구단은 `RosterPlayerIds`를 보유하며 실제 객체 소유권은 전역 레지스트리에 둔다.

### 41.3 결정론적 월드 진행

플레이어가 다음 경기 날짜로 진행하면 모든 리그를 같은 월드 날짜까지 진행한다. 처리 순서는
`LeagueId` 오름차순 → `GameId` 오름차순이며, 리그와 사건마다 독립 Seed를 파생한다.

```text
EventSeed = Hash(
    WorldSeed, SeasonYear, LeagueId, WorldDate,
    EventType, PrimaryEntityId, SecondaryEntityId)
```

Rookie 일정 한 건이 추가되어도 Major 경기 결과가 바뀌어서는 안 된다. 경기는 같은
`MatchSimulator`를 사용하고 현재 리그와 배경 리그의 차이는 장기 보관할 이벤트·박스스코어
상세도뿐이다. 뉴스는 `DomainEventJournal`에 확정된 사건을 소비하며 월드 판단이나 이동을 수행하지 않는다.

### 41.4 오프시즌 시장과 리그 이동

오프시즌은 8주를 기본으로 시즌 종료 처리 → 기존 구단 우선 협상 → 관심 공개 → 공개 시장
3라운드 → 최종 시장 → 캠프/역할 확정 순서로 진행한다. 조정 가능한 주 수와 철회 확률은
BalanceTable에 둔다. Major부터 Minor, Rookie 순서로 시장을 처리해 상위 리그 영입으로 생긴
공석이 아래 단계로 전파되게 한다.

한 시장 라운드는 모든 구단이 같은 로스터·예산 스냅샷으로 오퍼 생성 → 선수별 오퍼 확정 →
선호 판단 → `OfferId` 순서 일괄 커밋 → 수요 재계산 순서다. 상위 리그 오퍼는 실제 포지션 공백,
기존 후보 대비 기여도, 환산 성적과 표본 신뢰도, 역할, 예산, 경쟁 방향, 나이·부상 위험을 모두
통과해야 한다. 정식 오퍼가 0개일 수 있으며 최후 수단은 같은/하위 리그 단기 계약이나 Rookie
테스트 계약으로만 제공한다.

### 41.5 계약과 역할

`PlayerContractState`는 `ContractId`, 선수·체결/현재 구단과 리그, 기간·연봉·계약금,
`PromisedRole`, 예상 출장률, 옵션·바이아웃·거부권·인센티브를 장기적으로 소유한다.
`PromisedRole`, 현재 로스터의 `ProjectedRole`, 실제 `ActualUsage`는 서로 다른 값이다. 시즌 25%,
50%, 75% 지점에서 부상 제외 출장 기회를 기준으로 약속 이행과 성적 정당화 정도를 함께 평가한다.

### 41.6 선수 생태계와 기록

모든 현역·FA 선수는 영구 `PlayerId`를 가지며 계약·성장·부상·이동·은퇴 상태가 시즌을 넘어
유지된다. 매 시즌 Rookie 신규 선수 클래스가 유입되고 미계약·노쇠 선수는 은퇴할 수 있다.
장기 평균 전력은 `Major > Minor > Rookie`를 유지하되 단계 사이 분포가 겹쳐 실제 승격 경쟁이
발생해야 한다.

순위·개인상·포스트시즌·리그 기록은 `LeagueId`별로 분리한다. 커리어 합계는 전체/Rookie/Minor/
Major/구단/시즌/정규시즌/포스트시즌 필터를 제공한다. 모든 이동은 날짜, 시즌, 이전/대상 리그와
구단, 역할 3종, 계약, 사유를 `PlayerMovementRecord`에 남긴다.

### 41.7 세이브 v8 마이그레이션

v7 단일 리그는 현재 리그의 구단·선수·시즌·성장·부상·계약·통계·팀별 분할 기록과 영구 ID를
우선 보존한다. 해당 `LeagueLevel`의 영구 `LeagueId`를 부여하고 누락된 두 리그만 별도
Migration Seed로 결정론적으로 생성한다. 생성된 리그에 가짜 과거 우승·수상 기록을 넣지 않으며
`WorldRecordState.HistoryStartYear`부터 월드 역사를 제공한다. 실제 디스크 직렬화가 추가될 때는
저장 후 재로드 결과 해시까지 마이그레이션 완료 조건에 포함한다.

v9는 v8 월드·기록을 그대로 유지하고 현재 시즌의 동결된 순위, 포스트시즌 결과, 수상, 정산 원장을
`SeasonReviewSnapshot`으로 복원한다. v8에 없던 성장 전 능력치는 현재 값과 같게 두어 마이그레이션이
허위 성장량을 만들지 않으며, 진행 중인 시즌은 현재 `SeasonPhase`에 맞는 리뷰 장면에서 재개한다.

v10은 v9 월드·시즌·계약을 보존하고 `CareerNarrativeState`를 추가한다. 이전 세이브에는 반응 선택
이력이 없으므로 자신감·미디어 평가·팬 반응·팀워크를 중립값 50에서 시작하며, 마이그레이션 과정에서
과거 인터뷰나 관계 변화를 소급 생성하지 않는다.

v11은 v10 월드·선수·계약·내러티브를 보존하고 `CareerCreationProfile`과 `CareerGameSettings`를
추가한다. 이전 세이브는 현재 선수 유형과 포지션을 기준으로 기본 프로필을 만들고 경기 진행은
내 선수 개입·2×·내 선수 장면 자동 감속 ON으로 시작한다.

v12는 `CareerMemoryLog`, 시즌별 `CareerSeasonArchive`, 은퇴 시 한 번 고정되는
`RetirementRecapSnapshot`을 추가한다. v11 이전에 존재하지 않았던 훈련 선택·경기 방침·계약 거절은
소급 추정하지 않는다. 기존 시즌 기록에서 확인 가능한 시즌 카드·커리어 하이·마지막 출전만
`derived_fact`로 구성하며, v12 이후의 선택부터 실제 호출 시점에 누적한다. 은퇴 회고와 기록관은
이후 밸런스·칭호 계산식이 바뀌어도 과거 결과가 달라지지 않도록 라이브 상태가 아닌 이 스냅샷만 읽는다.

### 41.8 구현 순서와 완료 조건

1. 월드 기반: 영구 ID, 전역 레지스트리, 계산된 `CurrentLeague`, v8 구조 마이그레이션
2. 동시 시즌: 글로벌 캘린더, 배경 리그 경기, 독립 포스트시즌·시상, 사건별 Seed
3. AI 선수 영속화: 계약·성장·부상·은퇴·신규 선수·FA 풀
4. 통합 시장: 실제 수요, 승격/잔류/하락, 동시 입찰, 오퍼 예약·철회
5. 계약 깊이: 옵션·바이아웃·역할 보장·약속 위반·방출 잔여 연봉
6. 기록·뉴스·UI: 월드 허브, 계약 비교, 관심도, 커리어 타임라인
7. 장기 확장: 동일 단계 복수 리그·지역/국제 리그·리그 간 대회

필수 불변 조건은 선수 중복 소속 0, 활성 계약 최대 1, 구단의 단일 리그 소속, 같은 리그 팀끼리만
경기, 시즌 중 리그 간 이동 0, 같은 리그 트레이드, 이동 전후 기록 합 보존, 뉴스의 월드 상태 변경 0,
월드 날짜 역행 0이다. 최종 검증은 10,000개 이상 15~20시즌 커리어, 같은 Seed 해시 일치,
세이브/로드 일치, 20시즌 예산·연봉 안정성까지 포함한다.

### 41.9 구현 상태 — 월드 수명주기, 1차 AI 선수 생태계, 플레이어 인접 리그 시장

2026-08-28 기준 다음 기반을 세이브 v8로 연결했다.

- 새 게임 계약 시 `Rookie.Main`, `Minor.Main`, `Major.Main`과 24개 독립 구단을 동시에 생성한다.
- `WorldState`가 정렬된 리그·구단·선수·계약 레지스트리와 글로벌 캘린더, 이동 원장, 월드 기록,
  도메인 이벤트 저널을 소유한다. `CareerState.CurrentLeague`는 내 선수 소속으로 계산한다.
- 모든 핵심 로스터 선수에게 전역 고유 `PlayerId`를 부여하고 구단은 `RosterPlayerIds`를 보관한다.
- 리그별 독립 Seed로 일정과 경기를 생성한다. 내 선수의 한 라운드를 진행할 때 다른 두 리그도
  같은 라운드까지 동일 `MatchSimulator`로 처리해 승패·순위·선수 누적 기록을 만든다.
- 내 선수 리그의 포스트시즌 진행도에 맞춰 배경 두 리그도 독립 대진과 경기 Seed로 포스트시즌을
  진행한다. 세 리그의 우승·시상·최종 순위를 확정한 뒤에만 공통 오프시즌으로 진입한다.
- 시즌 전환은 모든 리그를 같은 다음 연도로 원자적으로 넘기며, 리그별 우승팀·준우승팀·최종 순위·
  수상을 `CompletedSeasonSummaries`에 영속한다. 글로벌 캘린더도 공통 개막일로만 이동한다.
- AI 로스터 선수도 영구 `PlayerState`와 `PlayerGrowthState`를 가진다. 리그별 초기 연령 분포를 적용하고,
  실제 시즌 출장량으로 `NaturalDevelopmentResolver`와 `AgingResolver`를 실행해 능력·나이·타석·투구
  이닝·등록 시즌을 다음 시즌 로스터에 투영한다. 구단 로스터의 Overall은 더 이상 별도 임시 능력치를
  성장시키지 않고 글로벌 선수 상태에서 계산한다.
- 새 월드는 내 선수뿐 아니라 AI 528명에게도 활성 계약을 부여한다. `PlayerCareerStatus`가 현역·FA·
  은퇴를 구분하고, 현역이 아닌 선수는 로스터와 활성 계약을 가질 수 없도록 불변 조건을 검사한다.
- 오프시즌에는 현재 월드를 바꾸지 않는 `WorldOffseasonMarketPlan`을 먼저 만든다. Major 은퇴 공석을
  Minor 선수로, 그 결과 생긴 Minor 공석을 Rookie 선수로 채운 뒤 Rookie 공석만 18~22세 신규 선수로
  보충한다. 한 오프시즌에 한 단계만 이동하며 모든 신규 선수는 영구 `PlayerId`와 성장 상태를 얻는다.
- 계약 선택이 끝나면 세 리그의 다음 시즌 상태, 선수 소속, 계약 종료·신규 계약, 이동 원장과
  `PlayerRetired`/`PlayerPromoted`/`RookiePlayerSigned` 도메인 사건을 한 월드 커밋 경로로 반영한다.
  따라서 리그를 하나씩 바꾸는 중간 상태가 외부에 노출되지 않는다.
- 시즌 중 내 선수 트레이드는 맞교환 AI의 활성 계약도 함께 승계한다. 트레이드로 구단별 포지션
  경쟁자 수가 달라지면 다음 오프시즌 시장이 같은 리그의 잉여 선수를 재배치해 포지션당 기본 2명의
  깊이를 복구한 뒤 은퇴와 승격 시장을 처리한다.
- 동일 Seed로 AI 528명을 20시즌 진행한 10,560 player-season 회귀 검증에서 모든 선수의 성장·노쇠
  기록 40건과 등록 시즌 20회를 보존했다. 20시즌 뒤 평균 Overall은 Rookie 58.01, Minor 65.31,
  Major 74.55로 단계 순서를 유지했다. 이는 단일 월드 선수 수명주기 검증이며 최종 10,000커리어
  밸런스 검증을 대체하지 않는다.
- 시장 도입 후 동일 Seed 월드 두 개를 각각 12시즌 진행한 순수 C# 회귀 검증에서 결과 지문이 매 시즌
  일치했다. 월드당 은퇴 229명, 승격 414건, Rookie 신규 계약 229건이 발생했고 현역 529명과 활성 계약
  529건이 유지됐다. 12시즌 뒤 평균 Overall은 Rookie 45.74, Minor 56.45, Major 70.97이었다.
- 추가로 8개 Seed를 각각 20시즌, 총 84,480 AI player-season 진행했다. 은퇴 4,306명, 평균 은퇴 나이
  37.45세, 승격 7,760건, Rookie 유입 4,306명이었고 모든 월드에서 현역/활성 계약 529건과 소속 불변
  조건을 유지했다. 20시즌 시점 리그 평균 Overall은 Rookie 42.89, Minor 48.37, Major 55.54,
  활성 계약 최고 연봉은 362,400,000으로 단계 순서와 경제 상한이 유지됐다. 장기 전력 절대치는
  통합 시장의 구단 수요·육성 계수를 붙일 때 다시 튜닝한다.
- 동일 리그 트레이드는 두 구단 로스터, 두 선수 소속, 계약, 전역 레지스트리, 이동 원장과 도메인
  사건을 한 커밋 경로로 갱신한다.
- 플레이어 공개 시장은 같은 리그 오퍼와 함께 실제 인접 리그 오퍼를 만든다.
  `LeagueMovementEvaluator`가 현재 Overall에 상위 리그 수준 페널티를 적용하고, 시즌 성적은
  타석/투구 아웃 표본 신뢰도만큼만 반영한다. 이후 Potential, 대상 구단의 포지션 경쟁자, 동적 수요,
  예산, 육성 등급을 모두 통과해야 `Promotion` 또는 `Rehabilitation` 오퍼가 된다.
- 인접 리그 계약을 체결하면 기존 리그 로스터에서 내 선수를 제거하고 대상 구단에 등록한 뒤 계약의
  `CurrentLeagueId`, 이동 원장의 이전/대상 리그, `PlayerPromoted`/`PlayerRehabilitationSigned`
  도메인 사건과 세 리그 다음 시즌을 한 전환 경로로 확정한다. 계약 화면 View 데이터에도 대상
  `LeagueLevel`, 역할, 예상 출장률, 연봉과 계약 기간을 함께 전달한다.
- 플레이어 시장 밸런스 변경 뒤 40개 독립 월드를 각각 3시즌, 총 120시즌·63,360 AI player-season으로
  검증했다. 상위 도전용 강한 Rookie 표본은 20개 중 5개 월드에서 실제 Minor 공석과 평가 기준을
  통과했고, 최소 전력 미달 표본은 20개 중 승격 오퍼 0건이었다. 오퍼가 발생한 월드는 실제 계약까지
  체결한 뒤 소속·계약·로스터 불변 조건을 모두 통과했다. 이는 초기 회귀 표본이며 최종 커리어 속도
  밸런스는 포지션·선수 유형별 장기 대량 시뮬레이션에서 다시 조정한다.
- `CareerSaveMigrationService`가 v7 현재 리그를 보존하고 누락된 두 리그를 Migration Seed로 생성한다.

아직 완료되지 않은 범위는 AI의 다년 계약 선택·방출·FA 대기·하위 리그 재기 계약과 시즌 부상,
구단 예산·포지션 수요 점수를 반영한 AI 동시 입찰 시장, 계약 옵션·약속 위반, 월드 리그 허브 UI,
실제 디스크 세이브 직렬화와 재로드 마이그레이션, 10,000커리어 대량 검증이다. 이 항목이 끝나기
전에는 다중 리그 시스템 전체를 완료로 판정하지 않는다.

### 41.10 구현 상태 — 은퇴 회고 `한 선수의 기록`

2026-08-29 기준 선수 커리어 종료를 라이브 상태 삭제가 아니라 고정 기록 생성 과정으로 연결했다.

구종 사용량, 경기·뉴스 원문 연결, 통산 순위, 디스크 기록관, 전용 오디오처럼 추가 기반이 필요한
범위의 데이터 계약·구현 순서·완료 조건은 `BaseballManager_RETIREMENT_RECAP_FOUNDATIONS.md`를 따른다.

- 경기 확정 시 실제 기용 역할, 타격·투구 방침, 최초 기록 후보, 대표 경기와 마지막 공식 출전
  `GameId`를 누적한다. 훈련·유학·성장 비용, 훈련 중 부상, 계약 수락·거절, 이적과 트레이드 의사도
  각 기능의 커밋 지점에서 `CareerMemoryLog`에 기록한다.
- 시즌 전환 직전에 소속팀·나이·역할·정규/포스트시즌 기록·Overall 변화·수상·계약·성장·부상·
  플레이 방침·스킬 보드를 `CareerSeasonArchive`로 고정한다. 현재 시뮬레이션 결과에 실제 구종별
  사용량이 없으므로 해당 배열은 준비하되 값을 추측하지 않고 화면에서도 숨긴다.
- 대표 순간은 기록 중요도 30%, 커리어 변화 25%, 플레이어 선택 20%, 희소성 15%, 감정 맥락 10%로
  점수화한다. 데뷔와 실제 마지막 출전을 우선하고 직접 선택·역경을 가능한 경우 한 건 이상 포함하며,
  동일 유형은 최대 2건, 전체는 최대 7건으로 제한한 뒤 시간순으로 표시한다.
- 은퇴 확정 직전에 통산 기록, 커리어 하이, 시즌 카드, 플레이 경험 요약, 최대 2개 칭호, 리그·구단
  유산, 대표 순간을 `RetirementRecapSnapshot`으로 한 번만 고정한다. 이후 회고와 기록관은 이
  스냅샷만 읽는다. v11 이력은 기존 시즌 사실만 복원하고 당시 저장되지 않은 선택·능력치 변화를
  `기록 없음`으로 남긴다.
- 설정의 게임 종료 탭에서 마지막 시즌 선언 또는 시즌 결산/오프시즌 즉시 은퇴를 두 단계 확인으로
  선택한다. 선언한 시즌은 정규시즌 탈락 시 마지막 정규 경기, 포스트시즌 진출 시 팀의 마지막
  포스트시즌 경기 뒤 자동 은퇴한다.
- 은퇴의 주 진입점은 설정 메뉴가 아니라 **계약 만료 후 오퍼 비교 화면**이다. 다음 시즌 조건을
  눈앞에 두고 `한 시즌 더 뛸 값어치가 있는가`를 저울질하는 순간이 은퇴 결정의 자연스러운 자리이기
  때문이다. `PlayerLifecycleBalance.RetirementMinimumAge` 이상이면 계약 버튼 옆에 은퇴가 함께
  제시되고, 확정하면 남은 제안은 모두 `ContractDeclined` 기억으로 남는다. 이 단계의
  `CareerSeasonTransitionService`는 아직 커리어 상태를 바꾸지 않았으므로 전환을 버리고 그 시즌
  그대로 은퇴를 확정한다. 서명 시 `GuaranteedRetirementAge`에 걸리는 나이면 계약이 마지막 시즌이
  된다는 사실을 같은 화면에서 미리 알린다.
- 플레이어와 AI 선수의 은퇴 나이 기준은 모두 `PlayerLifecycleBalance` 하나에서 온다. 화면·규칙
  어디에도 나이 상수를 직접 쓰지 않는다.
- 은퇴 커밋은 마지막 구단 로스터와 활성 계약을 제거하고 `PlayerMovementLedger`와
  `DomainEventJournal`에 은퇴를 기록한 뒤 월드 소속 불변 조건을 다시 검사한다.
- Presentation은 5막 회고, 시즌별 강조 카드, 플레이어 선택, 대표 순간, 순차 통산 기록, 칭호·마지막
  인사·최종 커리어 카드, 1초 길게 눌러 전체 건너뛰기와 8개 탭 기록관을 같은 스냅샷으로 렌더링한다.
  사건별 `PresentationAssetKey`는 라커룸·경기장·전광판·라인업·계약·재활의 6종 공용 일러스트를
  선택한다. Sprite를 불러오지 못한 환경에서는 라커룸·전광판형 런타임 그래픽과 사건별 색조로
  폴백해 회고 진행이 중단되지 않는다.
- 순수 C# 회귀 테스트는 가중치, MemoryId/시간 불변성, 대표 순간 제약, 월드 은퇴 커밋, 5막·8개 탭
  View 생성을 검사한다. 전체 솔루션 보조 빌드는 경고·오류 없이 통과했다.

현재 남은 외부 자산/기반 범위는 은퇴 전용 음악/효과음, 실제 투구 결과의 `PitchType` 방출,
뉴스·과거 경기 원문 영구 보관과 링크 이동, 실제 리그/구단 통산 순위 스냅샷, 디스크 저장 기반
커리어 기록관 누적이다. 계산 원본이 없는 세계 기록 버튼은 허위 순위를 만들지 않고 비활성 상태로
표시한다. Unity Play Mode에서 16:9/16:10 레이아웃, Tween 속도, 입력 포커스, 여러 Popup이 겹친
상태의 전환은 별도 시각 검증이 필요하다.

### 41.11 구현 상태 — Rookie~Galaxy 10단계 영속 리그 (세이브 v13)

2026-08-30 기준 41.9의 3리그 프로토타입을 다음 10단계 월드로 확장했다.

```text
Rookie → Minor → Major → World → AllStar
→ Classic → Winners → Champion → Master → Galaxy
```

- 새 월드는 10개 영속 `LeagueId`, 80개 구단, 구단당 25명, 현역 2,000명을 생성한다. 모든 리그는
  팀당 144경기 정규시즌과 상위 4팀 포스트시즌을 같은 시뮬레이터로 진행한다.
- `LeagueDefinition`이 목표 OVR·분포·연봉/상금/평판 계수·승강 슬롯·포스트시즌·훈련 해금·신인
  품질·AI 전술 수준·최초 도달 보상을 보관한다. 리그 이름에 따른 경기 확률 보정은 없다.
- 하루 진행 순서는 `LeagueLevel → LeagueId → GameId`로 고정하고 World Seed·연도·일자·리그·경기·
  목적에서 사건 Seed를 파생한다. 동일 Seed 월드 두 개의 전 경기·성장·은퇴·계약·승강·뉴스·다음
  일정 지문이 일치해야 한다.
- 정규시즌 1~2위와 인접 하위 리그 7~8위를 경계마다 동시에 맞교환한다. Rookie 아래와 Galaxy 위
  이동은 없다. 모든 리그의 계획을 먼저 만든 뒤 36건(승격 18·강등 18)을 일괄 적용한다.
- 승률·상대 전적·득실차가 모두 같은 2/3위, 4/5위, 6/7위 경계는 결정론적 단판 결정전으로 정한다.
  결정전은 정규시즌 기록에 합산하지 않고 `LeagueTiebreakGameState`로 별도 보관한다.
- 포스트시즌 우승과 정규시즌 승격은 독립 업적이다. 과거 시즌 요약은 당시 `LeagueId`, 단계,
  최종 순위, 우승·준우승, 수상과 결정전 결과를 고정한다.
- 구단 승강 시 선수·계약은 같은 `TeamId`를 따라가며 역할은 새 로스터에서 다시 평가한다. 개인
  리그 이동은 실제 대상 구단 경쟁자·포지션 수요·조정 성과·Overall 적합도·나이/Potential·성장·
  내구도·평판을 평가한 오퍼로만 발생한다. Classic 미만의 27세 이하·상위 10%급 선수만 최대 두
  단계를 건너뛸 수 있다.
- 개인 계약 이동은 대상 구단의 같은 포지션 최약 선수를 이전 구단으로 보내 두 구단의 25인
  로스터와 포지션 깊이를 유지한다. 맞교환 AI의 소속·계약 결정도 같은 시장 계획에서 교체한다.
  내 선수는 AI 시장 결정 대상에서 제외하고 선택한 계약만 별도 커밋해 계약·이동 원장의 중복을 막는다.
- Rookie·Minor 계약에는 상위 리그 이적 허용 조항과 보상금을, 그보다 높은 단계 계약에는 강등 시
  이적 요청 조항을 저장한다. 계약 화면은 대상 리그, 예상 역할·출장률, 실제 경쟁자와 조항을 함께
  보여 준다. 잔여 계약 중 조항이 발동하면 `현 계약 유지`와 적격 외부 오퍼를 함께 제시한다. 상위
  리그 이적을 택하면 기존 계약을 종료하고 보상금을 선수 수입이 아닌 구단 간 이동 원장에 기록한다.
  강등 시 이적 요청권은 구단의 다음 시즌 단계가 낮아졌을 때만 공개 시장을 연다.
- 매 시즌 리그 평균과 선수 백분위·조정 성과를 동결한다. 커리어 시즌 기록은 `SeasonId`, `LeagueId`,
  당시 단계/강도, `TeamId`, `PlayerId`, 역할, 원시 기록과 조정 기록을 함께 보존한다.
- `CareerReputationState`는 기대 순위 대비 팀 성적, 역할 기대 대비 개인 성적, 포스트시즌·수상·출장
  안정성으로 S~D 평가와 평판을 누적한다. 승격 하드 게이트로 쓰지 않으며 단계별 최초 도달 보상은
  커리어당 한 번만 지급한다.
- 훈련·스킬 뽑기 전용 경로는 Major/Classic/Champion에서 Elite/Unique/Legendary 순으로 해금한다.
  Master·Galaxy는 새 등급 대신 기존 최상위 조합의 선택권을 강화한다.
- 플레이어는 34세부터 은퇴·마지막 시즌을 선택할 수 있고 43세 시즌을 마지막 시즌으로 선언한다.
  AI는 34~42세에 나이·능력 하락·출전 감소·장기 부상과 남은 계약·마일스톤·우승 가능성·프랜차이즈
  관계·베테랑 수요를 평가한다. `Ambitious / PlayingObsessed / FranchiseLoyal / ChampionshipSeeker`
  성향은 같은 상황의 은퇴 의도를 다르게 만들며 43세 상한은 공통이다. 은퇴 공석은 상위부터 실제
  하위 리그 선수로 채우고 마지막 Rookie 공석만 신인으로 보충한다.
- 계약 만료 뒤 정식 오퍼가 0개면 동일 리그 보장 계약을 만들지 않는다. 실제 Rookie 구단의 경쟁자·
  포지션 수요·Overall·나이/Potential·내구도·최근 성과를 사용해 최대 두 곳의 테스트 입단을 평가한다.
  통과하면 1년 백업 경쟁 계약을 제시하고, 두 곳에 연속 실패하면 나이와 무관하게 `Unsigned` 커리어
  종료와 은퇴 회고로 이어진다. 테스트 통과점은 `PlayerLifecycleBalance`에서 조정한다.
- 리그 허브는 10단계 사다리, 현재/이전/다음/최고 도달 리그와 승격·포스트시즌·잔류·강등 구역을
  표시한다. 뉴스는 승격 경쟁·확정, 강등 위험·확정, 구단 이동, 상위 리그 관심·계약, Galaxy 데뷔,
  마지막 시즌과 은퇴 사건을 공개 관문 이후 기사로 변환한다.
- v12→v13 마이그레이션은 기존 Rookie~Major 구단·선수·계약·시즌·원장·뉴스 이력을 그대로 두고
  World~Galaxy 7개 리그와 56개 구단·1,400명만 Migration Seed로 추가한다. 과거 도달 리그를
  평판 상태에 표시해 마이그레이션 직후 최초 보상을 중복 지급하지 않는다.

순수 C# 검증에서 한 월드를 10시즌 연속 진행한 결과 현역/활성 계약은 각각 2,000건으로 유지됐고,
은퇴 1,001명과 같은 수의 Rookie 신인이 유입됐다. 평균 은퇴 나이는 38.31세이며 43세 초과 현역은
없었다. 10년 뒤 평균 OVR은 Rookie 43.95, Minor 44.88, Major 46.95, World 49.56, AllStar 51.63,
Classic 54.35, Winners 57.38, Champion 63.32, Master 72.12, Galaxy 82.20이었다. 모든 인접 평균이
오름차순이며 전체 상승 폭은 38.25다. 같은 Seed의 전체 시즌
지문, 경계 결정전, 36건 승강, 개인 Minor 계약 이동, 뉴스 공개 관문과 v12→v13 마이그레이션도
독립 회귀 테스트를 통과했다.

`MultiLeagueWorldTests.AdvanceIndependentWorldSeasons_10000개월드의승강소속기록불변조건을유지한다`는
10,000개 독립 월드 시즌용 `[Explicit]` 장시간 soak test로 제공한다. 현재 수동 검증에서는 이
장시간 항목을 실행하지 않았으므로 10,000 월드 시즌 결과를 완료 수치로 인용하지 않는다.

계약 기간 중 이동 조항, AI 은퇴 성향, 무오퍼 뒤 Rookie 테스트 입단과 미계약 종료는 행위·화면·
회귀 테스트까지 연결했다. 남은 통합 항목은 실제 디스크 Save/Load 서비스와 저장 후 재로드 해시
검증이다. 현재 타이틀도 디스크 저장이 없을 때 이어하기·불러오기를 숨기는 계약이므로, 저장 DTO와
객체 참조 복원 정책을 먼저 확정한 뒤 v13 전체 월드 직렬화를 별도 단계로 구현한다. 이 항목과 위의
10,000 월드 시즌 soak test가 끝나기 전에는 다중 리그 시스템 전체를 최종 완료로 판정하지 않는다.

### 41.12 결정 — 커리어 세이브 마이그레이션 체인 제거 (2026-09-02)

**커리어 모드에는 디스크 세이브가 아직 없다.** `CareerState`를 직렬화하는 코드도, 파일 I/O도,
불러오기 UI도 없다. 그런데 `CareerSaveMigrationService`(360줄)와 `CareerWorldFactory`의
`CreateMigratedWorld`·`ExpandV12World`·`AddMigratedLeague`(약 250줄)는 v7~v16 업그레이드 경로를
유지하고 있었다. **한 번도 디스크에 쓰인 적 없는 파일 포맷의 마이그레이션이다.** 프로덕션 호출처는
0개였고 생성되는 곳은 `MultiLeagueWorldTests` 6곳뿐이었다.

유지 비용이 음수라고 판단해 전부 제거한다. 근거는 "안 쓰니까"가 아니다.

- 이 코드가 **실패하는 테스트 3건을 만들어내고 있었다.** 테스트가 현재 v16 월드의 Rookie 리그를
  v7 세이브인 척 주입해서, 엠블럼이 붙은 리그와 엠블럼 없이 새로 생성된 리그가 한 월드에 섞였다.
  실제 v7 세이브라면 전 구단 `EmblemId = 0`이라 통과하는, **존재할 수 없는 상태**였다.
- 그 실패를 통과시키려고 `EnsureValidTeamEmblems`에 둔 "아무도 배정받지 않았으면 넘어간다"는
  예외가, 일부만 배정된 월드까지 통과시켜 진짜 결함을 늦게 드러냈다.

제거 후 41.7·41.9·41.11절의 v8·v12→v13 마이그레이션 서술은 **당시 작업 기록**으로만 남는다.
해당 코드는 더 이상 없다. 필요하면 `git show db00508` 등으로 복원할 수 있다.

**`SaveVersion` 필드는 유지한다.** 34절 계약이고 상태 생성자 100곳을 통과한다. 걷어내는 것은
별개의 대형 변경이며 얻는 것이 없다. 실제 디스크 세이브를 구현하는 시점에 `SaveVersion = 1`부터
다시 시작하고, 그때부터 진짜 마이그레이션 경로를 만든다. **로드 경로가 없는 지금 버전 가드만
남기면 그것도 죽은 코드이므로 코드로는 아무것도 남기지 않는다.**

같이 `WorldState.EnsureValidTeamEmblems`의 예외를 없애 **"모든 구단은 유일한 양수 엠블럼을 가진다"**
를 무조건 불변식으로 만든다. 월드를 만든 쪽이 배정 책임을 진다.

`CareerWorldFactory.CreateNewWorld(bakedContent, ...)`는
`CareerBakedTeamRuntimeDefinition.EmblemId`를 그대로 사용하므로, Baked 입력 경계에서 모든 엠블럼이
양수이고 월드 전체에서 유일한지 검증한다. 프로덕션 `ICareerBakedContentProvider`를 연결할 때도
임의 기본값 0을 허용하지 않는다.

표시 이미지의 의미는 `Assets/10.Datas/Resources/TeamEmblems/TeamEmblemIdentities.json`을 정본으로
삼는다. 저장된 `EmblemId`의 월드 내 유일성 계약은 유지하되, 표현 레이어는 구단 별칭에 연결된
이미지를 우선 사용한다. 동일 프랜차이즈가 여러 리그·연도에 등장해도 상징은 같아야 한다.
지역·리그 접두사는 이미지 선택에 영향을 주지 않으며, 새 별칭과 표기 변형은 카탈로그에 등록한다.
미등록 사용자 구단은 명시된 기존 ID를 사용하고, 유효한 ID도 없으면 이미지 없는 상태로 표시한다.
순위표·구단 정보·계약·경기·선수 카드는 공통 `TeamEmblemSprites` 경로를 사용한다.

### 41.13 결정 — 실제 4인 불펜 표시와 기용 통계 재정렬 (2026-09-03)

`DetailedMatchEngine` 전환 뒤에도 팀 화면은 과거의 단일 `ReliefPitcher` 규칙을 사용했고,
호환용 `Team` 어댑터는 실제 불펜 첫 슬롯인 Swingman을 `ReliefPitcher`로 축약했다. 화면과 테스트가
이 손실 값을 정답으로 삼아 실제 경기의 `MatchRosterSnapshot.Bullpen`과 다른 선수를 보여 줬다.

- `TeamOverviewBuilder`는 다음 경기의 실제 `CareerGameRunner` 입력을 만들고 그
  `MatchRosterSnapshot`의 선발·불펜을 그대로 표시한다. 호환용 `Team.ReliefPitcher`를 UI 계약으로
  사용하지 않는다.
- 선발 강판이 사실상 작동하지 않던 수치를 바로잡기 위해 `BullpenManagementBalance`의 기본
  PullThreshold를 60에서 40으로 낮췄다.
- Condition이 주전 경쟁에 충분히 반영되도록 `CareerSeasonBalance.ConditionDecisionWeight`를
  0.12에서 0.30으로 높였다. Serialized `NewGameDefinition`과 코드 기본값은 같은 값이다.
- 4인 불펜에서 구원 등판당 2.5~3.8이닝을 요구하던 옛 단일 구원 모델 테스트는 0.8~2.2로
  바꿨다. 실측값에 맞춰 무작정 넓힌 것이 아니라 역할 분산 뒤 등판당 약 1~2이닝을 검증한다.

같은 45시즌/14,400경기 회귀에서 R/G 4.02, 타자 선발 출장률 86.1%, 선발 6.3 IP/App,
구원 1.6 IP/App를 기록했다. 변경 전 98.5%, 7.3, 1.6에서 주전 경쟁과 선발 강판이 실제로
작동하는 방향으로 이동했으며 기존 득점 환경은 유지됐다.

동시에 역사 콘텐츠는 Editor 실명 검수본과 Runtime 실명 제거 Canonical 정제본을 분리한다. Runtime
정제본은 Person별 고정 가명을 저장하지 않고 World Identity 이름 후보 Pool만 함께 제공하며, 검증된
정제본만 `HistoricalRuntimeContentCatalog`를 통해 Player Build에 포함한다. 구단주 모드 공통 Runtime 경로는
44시즌 World Record와 Save/Load까지 연결했지만, 선수 커리어의 10단계 동시 리그에 440개
TeamSeason을 모두 Rookie부터 진입시키는 정책은 아직 없다. 이 정책 없이 최근 10개 연도를 등급에
임의 배치하지 않으며, Production Career 새 게임은 Synthetic으로 대체하지 않고 명시적으로 실패한다.

## 42. Historical World 정본 계약 (2026-09-04)

이 절은 선수·구단 Historical Content와 World 생성에 관한 최상위 Source of Truth다. 41.13까지의
기록이나 별도 문서가 이 절과 충돌하면 이 절을 따른다.

```text
Source PlayerPerson / PlayerSeason / TeamSeason
        ↓ 1:1 Runtime-safe Canonical Bake
BaseAttributes / Cost / TrainingCeiling / Origin / Core25
        ↓
WorldIdentityRegistry
        ↓
Detailed Match / Season Simulation
        ↓
Season Statistics / Team Standings / Postseason / Awards
        ↓
WorldHistorySnapshot / WorldCardCatalog / SpecialCompositeTeam
```

### 42.1 선수와 구단은 Source와 1:1이다

- **2026-09-09 연구 카드 보충 승인:** KBO 정규시즌 원본에 없는 선수 시즌도 research 일반 카드의
  인물·연도·소속·수치 근거를 보존한 별도 Source로 추가한다. `research_roster_supplement.json`이
  보충 정본이며 기존 KBO Person 연결, 출처·원본 행·해시와 미관측 능력 필드를 보존한다.
  Runtime은 `SourceBacked` 안의 `sourceDataKind=ResearchCardSupplement`로 구분하고 실명은
  개발용 표시 카탈로그에서만 제공한다. 동일인 연결이 모호하거나 같은 인물·연도가 중복되면 보류한다.
  `sourceRecordAvailability=Unavailable`인 보충 시즌은 원기록 행을 만들지 않는다. 실제 0경기로
  위장하지 않으며 게임 내 기록은 출전 후 공통 시뮬레이션에서 생성한다.
  이번 보충은 기존 Core25·능력치·원기록을 유지한 전체 Normal 카드풀 확장이다. 120개 구단·연도에
  939시즌을 추가해 17,387 → 18,326시즌이 됐다. 1994 LG는 42 → 58명으로 늘었으며 research
  54명이 전부 연결된다. 신원·소속·타입 재검토 693건은 보류한다. 이전 ContentHash의 WorldHistory
  캐시는 재사용하지 않으며 Unity 도구에서 재베이크한다. 결과는 `docs/reports/historical-roster-coverage/`를 따른다.
- **1985 삼성 Reference 보정:** `annual_reference_1985_samsung.json`의 일반 카드18명과 기사 Cost1명을
  추가 기준으로 사용한다. 김일융 Cost8의 타 게임 출처를 프야매 Cost10으로 교체하고 김시진의
  타 게임 Cost 근거는 제외한다. 미확보 일반 카드에는 EX·육성 후 수치를 대입하지 않는다.
  적용 후 Core25 Cost144→155, 상세 경기 전후 각24시즌에서 삼성 합산 승률.50887→.52575를 확인했다.
  다른43개 연도·다른 구단·원기록은 그대로이며 실제.706 시즌의 완전 복원은 아니다.
  출처·판본·남은 자료와 통계는 `Research/PyaMaeCardDb/1985-Samsung/README.md`를 따른다.

- 한 Source Person은 하나의 안정적인 `PlayerPersonId`가 되고 그 인물의 모든 연도는 같은 ID 아래
  연결한다. 서로 다른 Source Person의 시즌을 이어 인공 커리어를 만들지 않는다.
- 한 Source PlayerSeason은 정확히 하나의 `PlayerSeasonDefinition`이 된다. 시대·포지션·역할
  모집단의 평균·표준편차·백분위·Z-Score는 정규화 기준일 뿐 다른 선수의 개별 기록을 능력치에
  혼입하는 재료가 아니다.
- 한 Source TeamSeason은 정확히 하나의 `TeamSeasonDefinition`이 된다. Core25는 그 Source
  TeamSeason 소속 선수만으로 구성하며 다른 Source Team에서 조용히 보충하지 않는다. 원본 부족은
  Bake 오류와 Validation Report로 드러낸다. 실제 1982~2025 데이터의 투수 quota 부족 54석은
  `ReplacementGenerated`로 명시하고, 개별 선수 혼합이나 공분산 표본 없이 동일 연도·역할 모집단의
  고정 20백분위 aggregate baseline으로만 채운다. 이는 Source PlayerSeason이 아니라 결손 슬롯
  fallback이며 해당 Source TeamSeason 밖의 선수를 가져오지 않는다.
- 여러 Reference의 Contact/Power/Speed를 조합하는 Synthetic Player Mixing과 여러 구단의
  장타·선발·수비·불펜을 조합하는 Franchise Fingerprint Mixing은 Production 경로에 두지 않는다.
- Career 리그 배치는 `NewGameDefinition`의 명시적 연도 목록(현재 2016~2025)을 LeagueGrade 순서로
  소비한다. 누락·중복·10구단 미달이면 실패하며 코드가 임의로 최근 연도를 선택하지 않는다.

### 42.2 Canonical 값과 World 값의 경계

Canonical Bake에는 `PlayerPersonId`, `PlayerSeasonId`, `BaseAttributes`, `Cost`,
`TrainingCeiling`, `Position`, `PitcherRole`, `RegistrationType`, `OriginYear`,
`OriginFranchiseId`, `OriginTeamSeasonKey`, `TeamSeasonDefinition`, `Core25`가 들어간다. 같은 Source
Data/Normalization/Balance Version이면 이 값은 동일하며 World Seed, World 성적, 수상, 표시 이름으로
다시 계산하지 않는다.

선수와 구단의 표시 이름은 Canonical Definition에 두지 않는다. 새 World는 `PlayerPersonId`와
`FranchiseId`를 키로 `WorldPlayerIdentity`와 `WorldFranchiseIdentity`를 만들고
`WorldIdentityRegistry`에 확정한다. 생성 이름은 프로젝트가 보유한 실제 선수·구단명 exact match와
World 내부 중복을 거부한다. 한 Person의 모든 시즌과 한 Franchise의 모든 연도 TeamSeason은 같은
World 이름을 공유한다. 표시 문자열은 Presentation 전용이며 Simulation, TeamColor, Scout, Award,
Contract, Roster, AI의 판정 키로 사용하지 않는다.

`WorldIdentityRegistry`, `WorldHistorySnapshot`, 통계·순위·포스트시즌·수상·특수 합성팀 상태는
Save에 확정 값을 기록한다. Load에서 Name Generator, Historical Simulation, Award Resolver를 다시
실행하지 않는다.

### 42.3 가상 역사는 실제 경기 Simulation 결과다

Source 시즌 기록과 팀 순위·수상은 능력치 변환 및 Offline 검증의 근거이지 정식 World History가
아니다. 고정된 Canonical TeamSeason을 `DetailedMatchEngine`과 동일한 야구 판정 모델에 투입해
개인 기록, 팀 기록, Standings, Postseason을 만든다. 이어 실제 World Statistics를 입력으로
All-Star, Golden Glove, Regular Season MVP, All-Star Game MVP, Postseason MVP를 결정한다.
BaseAttributes 순위나 Source Award를 정식 World 수상 결과로 복사하지 않는다.

`OriginalHistory`는 과거 데이터 비교·회귀 검증이 필요한 동안에만 Legacy/Debug/Validation 경계에
격리한다. 사용자 선택 가능한 정식 새 게임은 `SimulatedHistory` 하나이며, 해당 Legacy 경로가
Runtime의 통계·순위·수상을 Source 결과로 되돌리는 우회로가 되어서는 안 된다.

`AllStarComposite`, `GoldenGloveComposite`, `YearSelectComposite`는 선수를 합성하는 파이프라인이
아니다. World Statistics와 Award가 확정된 뒤 기존 `PlayerSeasonId`를 참조해 구성하는 게임용 특수
로스터다. 세 특수팀 사이 중복 금지를 유지하며 Historical Award 계산에는 역으로 참여하지 않는다.

### 42.4 필수 검증

- Source Person/Season/TeamSeason의 1:1 provenance와 타 선수·타 구단 개별 기록 Mixing 0건
- `ReplacementGenerated`는 보고된 Core25 결손 54석에만 존재하고 deterministic aggregate baseline 사용
- World Seed와 무관한 `BaseAttributes`, `Cost`, `TrainingCeiling`, `Origin`
- 다른 Seed에서 달라질 수 있는 개인·팀 성적, Standings, Postseason, Award
- 실제 선수·구단명 exact match 0건, World 내 이름 중복 0건, Person/Franchise 다년도 이름 일치
- 표시 이름만 바꿔도 Match Event/Statistics 결과가 동일한지 검증
- Save/Load 전후 Identity, Statistics, Standings, Award의 완전 일치와 재생성 호출 0회
- Source 통계와 Baked Rating의 의도된 상관 및 다수 World Seed 대량 Simulation의 리그 분포 검증

### 42.5 구현 검증 상태 (2026-09-05)

Canonical Bake부터 World Identity, Detailed Historical Simulation, Statistics/Standings, Award,
WorldCardCatalog, 특수 합성팀과 Save DTO까지 연결했다. 다른 Seed 검증이 Seed 값 자체를 Hash에 포함해
false-positive가 되지 않도록 Player/Team Statistics, Standings/Postseason, Awards, Identity, 전체
History의 Seed 제외 Fingerprint를 분리했다. Simulation Seed와 Identity Seed도 독립 입력으로 검증해
Identity만 바꿀 때 경기 역사와 수상이 변하지 않아야 한다.

Python Importer/Bake는 56/56, 순수 C# Headless Core/Simulation/Game은 538 passed, 0 failed,
Explicit 장기 Fixture 1 skipped로 통과했다. Detailed 2024 Historical smoke는 동일 Simulation/Identity
반복과 Identity-only 변경을 포함해 3회, 실행당 409경기를 완료했고 동일 Seed Result Hash는
`564e68ae44d943ff`였다. Save DTO round-trip과 Identity-only History 불변도 통과했다.

실제 schema 5 Archive 1982~2025 전체 44시즌 4회 검증, Runtime Content Provider와 Historical Database
Browser Unity 실행은 테스트가 준비됐으나 현재 Unity Licensing Client가
`com.unity.editor.headless` 권한을 얻지 못해 Test Runner 진입 전에 중단됐다. 따라서 이 항목은 아직
통과로 기록하지 않는다. Browser의 worker thread `JsonUtility` crash 경로는 main thread load로
교체했으며 라이선스가 정상인 Unity Editor에서 실제 Archive load 재검증이 필요하다.

### 42.6 Canonical 능력치와 Cost의 일관성

> 이 절은 Cost v7 구현 이력이다. 현행 Cost 계약과 검증 결과는 42.8의 v8 정의가 대체한다.

Cost는 출전량이나 그 해의 카드 희소도가 아니라, 확정된 BaseAttributes의 역할별 종합 능력치에
대응한다. 같은 종합 능력치에는 같은 Cost를 부여하며 연도별 백분위와 출전량은 진단으로 남긴다.
Source와 Replacement는 같은 고정 Cost 경계를 사용한다. 능력치에서 표본 신뢰도를 보정한 뒤
가격에서 출전량 상한을 다시 적용하지 않는다.

타격·투구 효율은 같은 연도 전체 유형과 비교하고 수비만 실제 포지션, 투수 Stamina는 선발/구원
역할군과 비교한다. 극소표본 성과를 평균 전력으로 복원하지 않도록 보수적 사전 Z(-1)를 적용하되,
수비 근거 부족은 중립값으로 구분한다. 역사 시즌의 길이는 당시 구단 경기 수로 반영한다.
타격 출전에 비해 수비 이닝이 적은 시즌은 DH 중심 역할로 추론하고 근거를 Trace에 보존한다.
공식 DH 출전 기록이나 실측 구속·구종을 복원한 것으로 해석하지 않는다.

주요 성과 지표의 Rating 폭과 역할 가중치를 조정하며, 최종 Runtime TrainingCeiling은 Cost와
무관하게 BaseAttributes +3(상한 99)으로 통일한다. 현재 계수는 버전 관리되는 밸런스 초안이다.
모든 개별 능력치의 우열이나 인접 Cost 경계의 최소 성능차까지 보장하는 계약은 아니다.

Ability v5 / Cost v7 / PositionRole v5 / DerivationBalance v9를 적용했다. 1982~2025의 44개 연도,
Source 17,333개 선수 시즌과 Replacement 54개의 JSON 재생성·데이터 대조를 수행했다. 사용자 요청에
따라 테스트·컴파일·Unity·대량 경기 시뮬레이션은 이번 변경에서 실행하지 않았다. 실제 경기 성능,
리그 평균, AI 기용과 경제 밸런스의 완료 선언이 아니다. 상세 근거와 잔여 검증 항목은
`docs/reports/선수_Cost_능력치_개편_데이터확인.md`를 따른다.

### 42.7 대표 로스터의 능력치·적합성 분리

> 이 절은 Core25 v3 구현 이력이다. 현행 v4의 제한적 주전 재검토는 42.8을 따른다.

Canonical Core25 배치는 `ability-fit-core25-v3`, DerivationBalance v10을 사용한다.
이 변경은 BaseAttributes·Cost·TrainingCeiling·Natural Position/Role을 바꾸지 않고 Assigned Role과
Core25 선택을 개선한다. 경기 중 감독 AI나 타순 최적화의 대체 구현이 아니다.

- 타자는 수비 8자리와 DH를 함께 최대 가중 매칭한다. 주포지션 고정 가산 100을 제거하고,
  적격 부포지션은 소폭 감점(-4)해 능력치와 수비 적합성을 함께 비교한다.
- 실제 주수비 포지션 외의 부포지션은 5경기 또는 45아웃 이상의 수비 기록이 있어야 인정한다.
  무경험 포지션은 적격 매칭이 불가능할 때만 경고를 남기고 fallback한다.
- 벤치는 남은 적격 포수가 있으면 백업 포수 한 명을 먼저 확보한다. 나머지는 교체 능력치와
  아직 확보하지 못한 백업 포지션당 6점의 보완 가치를 합쳐 순차 선택한다.
- 선발 자격을 충족한 투수끼리는 선발용 능력치만 비교해 5명을 정한다. 선발 등판 비율 등
  역할 분류 점수를 전력 가산점으로 재사용하지 않는다. 셋업·마무리는 전문 보직 후보를 우선하고,
  일반 불펜은 남은 선발까지 포함해 구원용 능력치로 경쟁한다.
- 동점은 Stable ID로 결정하며 원본 소속·중복 금지·Core25 역할별 정원 계약을 유지한다.

현재 계수는 데이터 기반 배치 개선안이며 경기 성능이나 경제 밸런스 검증 결과가 아니다.
사용자 요청에 따라 이번 변경도 JSON 재생성·배치 대조까지만 수행하고 테스트·컴파일·경기
시뮬레이션은 별도 실행 대상으로 남긴다.

### 42.8 프야매 Cost 역공학 Research Gate

Cost v8 재보정은 Source 1:1·Offline Canonical·World/Edition 독립성을 유지하면서,
능력치 종합합과 시즌 기여·출전량·포지션/역할 문맥을 분리해 평가하도록 한다.
Ability 오류를 Cost 경계로 가리거나 실제 선수/팀/연도별 Cost override를 두지 않는다.

연구 자료는 Runtime과 분리된 `Tools/PMReference/`에 보관한다. 출시·재평가·개별 후속 수정과
Normal/특수 카드의 비용 동등성을 구분한다. 2012 SK 사용자 Fixture는 학습 가중치0의 별도 회귀
자료다. 유효 후기 Normal 자료가 부족한 상태에서 사용자가 모델 진행을 명시해, 해당 한계를 보고서에
남기는 조건으로 B 역사 Anchor + C 일관 확장 정책을 채택했다.

현재628개 중복 제거 관측을 수집했으나 후기 Normal·Source 연결 확인 후보는1장이라 원작 최종판
Calibration Gate는 미통과다. Production은 Ability v5를 유지하고 Cost v8/Balance v11/Core25 v4로
갱신했다. Cost v8은 Source 성과 quality, workload, 수비 기회, 역할 집단 내 최대 ±0.5 보정과
Cost9/10의 workload·reliability·quality 자격을 독립 구성 요소로 기록한다. Core25 v4는 기존 최대
적합 매칭을 유지하며 같은 Natural Position의 벤치 선수가 Cost2·평균 능력4 이상 높고 슬롯 손실2
이하이며 수비 안전선을 지킬 때만 주전을 재검토한다.

44시즌 Source17,333개와 명시적 보충54개를 재Bake했고, 현재 2012 SK Builder20은60+79=139다.
동일 입력 두 번의 ContentHash와 ArchiveHash가 일치했고 Unity Runtime Export 및 Source/Runtime
계약 감사 오류는0건이다. GS 결측 연도는 역할 분류기의 inferred Starter 비율을 재사용한다.
subtype·최종 버전 불명인 2011 관측491장의 탐색 지표는 ±1 69.25%, MAE1.134라 PM 원본 동일성
완료 조건은 충족하지 않는다. 상세 수치와 잔여 한계는
`Tools/PMReference/reports/PM_REFERENCE_RESEARCH.md`를 따른다.

### 42.9 기록 중심 평가와 결측 제외 (v6 이력, 현행은 42.12)

Ability v6 / Cost v9 / DerivationBalance v12가 42.6·42.8의 평가 지표를 대체한다.
체력은 KBO Source 1982~2025 단일 시즌 최다 1282아웃(427⅓이닝)을 100으로 두고
`25 + 75 × 시즌 이닝 / 427⅓`로 분배한다. 실측 구속은 110km/h=25, 170km/h=100의
선형 척도를 사용한다. 두 기준은 시즌별 Z-score와 신뢰도 축소를 적용하지 않는다.
실측 `fastballVelocityKph`가 없으면 구속은 중립 55이며 삼진율로 구속을 추정하지 않는다.

제구는 BB/9 역방향, 구위·변화구·투수 멘탈은 ERA 역방향이다. 교타는 타율,
장타는 홈런 수·장타율 각 50%, 주력은 도루 수, 수비는 수비율·9이닝당 실책 역방향이다.
추가 타격 평가축을 만들지 않도록 타자 멘탈도 타율을 사용하고, 송구는 기존 보살·도루저지율을
유지한다. 절대 기준 이외에는 해당 시즌 비교 집단과 당시 시즌 길이 기반 표본 보정을 유지한다.

결측은 실제 0과 구분하고 Ability와 Cost에서 해당 지표의 가중치를 제외한 뒤 남은 가중치를
재정규화한다. 한 능력치의 근거가 전부 없으면 중립 55를 쓰고 Trace에 결측임을 남긴다.
Cost quality는 타율·장타율·홈런·도루 또는 ERA·BB/9만 사용한다. 기존 출전량·역할·elite
자격 구조는 유지하며, 수비 출전 기록 자체에 주던 정액 가산은 제거한다.
특정 구단·이름·연도에 능력치 가산이나 Cost override를 적용하지 않는다.

최대 100 능력치가 Runtime에서 거부되지 않도록 AbilityRatings와 관련 범위 검사를 100으로
맞추며 TrainingCeiling은 BaseAttributes +3, 최대 100이다. Save 구조는 바뀌지 않는다.
1982~2025 Source 17,333개와 Replacement 54개, 구단 363개를 재Bake하고 Runtime JSON 46개를
동기화했다. Core25 유형별 6능력 평균은 과거(1982~2000) 정규 1위 구단 52.093,
최근(2016~2025) 정규 2·3위 구단 52.167이다. 개별 구단의 승률이나 순위 보장은 아니다.

사용자 지시에 따라 테스트·컴파일·Unity·대량 경기 시뮬레이션은 실행하지 않았다.
기존 WorldHistory Bake는 이전 ContentHash에 묶여 있으므로 새 능력치의 경기 결과로 재사용할 수
없다. 이번 작업은 Canonical 평가 Bake까지만 수행했다. 상세 결과는
`docs/reports/선수_기록평가_v6_Bake_결과.md`를 따른다.

### 42.10 구단 기본 전력과 편성 Cost의 분리

구단의 기본 전력은 현재 등록된 25인 선수단에서 타자는 타자 6능력, 투수는 투수 6능력을
각각 평균한 뒤 선수별 동일 가중으로 집계한다. `RosterStrengthResolver`가 순수 C#으로
계산하며, Core25 원본 구성에서는 기존 `ReferenceStrength`와 반올림 정밀도 내에서 일치한다.
벤치를 포함하되 타자의 투수 능력처럼 해당 유형과 무관한 값은 포함하지 않는다.

Cost는 기존 시즌 가치 가격과 편성 비용으로 유지한다. `RosterCostResolver`의 주전 타자 9명+
투수 11명 합계에 벤치 비용을 추가하지 않는다. 능력치 평균과 Cost를 혼합한 전력 공식을 만들거나
Cost 차이에서 예상 승률을 산출하지 않는다. 카드 Edition·훈련·팀컬러·당일 컨디션은 기본 전력에
포함하지 않으므로 UI에서 기본 능력 기준임을 표시한다. 빈 선수단은 0점 대신 미평가로 표시한다.

구단주 홈은 내 구단과 다음 상대의 기본 전력을 우선 표시하고 편성 비용을 별도로 표시한다.
선수단 화면은 야수·투수 평균과 평가 대상을 함께 표시한다. Game의 읽기 전용 Query가 현재
로스터를 평가하고 Presentation은 그 결과만 표시한다. 저장 필드와 경기 확률 모델은 변경하지 않는다.
커리어 구단 화면의 기존 능력치 평가와 Editor의 `ReferenceStrength` 표시는 그대로 유지한다.

1982~2025년 363개 구단에서 실제 새 C# Resolver를 실행해 Cost 불일치 0건을 확인했다.
기본 전력과 저장 `ReferenceStrength`의 최대 차이는 소수점 저장 반올림에 따른 0.00003334이며,
실제 승률과 연도 평균 제거 Pearson 상관은 0.871933이다(Cost는 0.746317).
이는 역사 기록의 사후 설명력이고 경기 시뮬레이션의 재현력 검증은 아니다. 세부 분석은
`docs/reports/구단_Cost_실제_KBO_성적_상관분석.md`를 따른다.

### 42.11 프야매 관측 기반 평가 보정 (v7 이력, 현행은 42.12)

Ability v7 / Cost v10 / DerivationBalance v13은 42.9의 기본 구조를 유지하면서 아래 평가축을
대체한다. 목표는 당시 관측에 가까운 일반 산식이며 선수·구단·특정 연도별 보정표는 사용하지 않는다.

- Cost의 유형별 기본점을 야수 2.5·투수 1.25로 설정한다. 2011 관측의 학습 연도에서 작은 공통
  이동량을 탐색했고, 검증에서 악화된 야수 이동은 채택하지 않았다. 출전량·품질·elite 자격은 유지한다.
- 주력은 도루 시도율 70%·성공률 30%, 기준점 67로 평가한다. 타자 정신은 출루율 70%·타율 30%,
  기준점 63이다. 가중치는 신뢰도 보정한 시즌 지표에 적용한다. 단순 도루 수·타율 한 축보다
  행동 성향과 출루 기여를 구분하며, 모든 근거가 결측이면 조정 기준점 대신 중립 55를 유지한다.
- Cost는 독립된 `costMetricEvidence`에서 원기록을 읽는다. 주력 등 표시 능력치의 구성을 바꿔도
  Cost의 도루 수 근거가 사라지지 않으며, 이 자료는 Editor 전용으로 Runtime에서 제거한다.
- 원작 야수의 네 번째 능력은 Bunt다. 현재 Arm과 대응시키지 않는다. 수비·구종의 `+N`은 상승
  임계값이므로 기본 능력치로 학습하지 않는다. 투수 능력·체력·실측 구속 기준은 유지한다.

1982~2025 Source 17,333개와 Replacement 54개를 재Bake했다. 다른 생성 Seed의 JSON 93개가
전부 바이트 단위로 같고 Runtime 46개도 동기화했다. 원작 관측 491건에서 Cost MAE는
1.200→0.996, ±1 일치율은 66.4%→74.1%다. 일부 보류 표본의 정확 일치율은 악화됐으며,
시점 충돌·고Cost 편중·소수 Normal 이미지 때문에 최종판 공식 복원 완료로 해석하지 않는다.

Importer 80건과 역사 데이터 관련 Headless EditMode 78건이 통과했다. 변경 전후 각각
DetailedMatchEngine 10,000경기를 측정했고, 타율 0.269→0.273·ERA 3.396→3.496·
구단 경기당 득점 3.509→3.616·성공 도루 0.773→1.167이다. 도루 증가는 추가 관찰 대상이다.
이 실험은 피로를 초기화한 중립 로스터 비교이며 커리어 전체 시즌·구단 경제의 수용성 검증은 아니다.
Unity Test Runner/Play Mode는 미실행이다. 기존 WorldHistory Bake는 새 ContentHash와 맞지 않아
기존 실제 시뮬레이션 fallback을 사용한다. 근거·재현 명령·제약은
`docs/reports/pm_calibration_20260906/결과.md`에 기록한다.

### 42.12 수비·투수 원문 카드 확장 보정 (v8/v11 이력, 현행 코드는 42.14)

Ability v8 / Cost v11 / DerivationBalance v14는 인벤 86864/86866의 원문 HTML과 연결된 카드
452장을 대조한 공통 평가 척도를 사용한다. 42.11의 지표 조합은 유지하되 표시 기준점·변환 폭을
조정한다. 교타 67/16, 장타 67/16, 주력 69/12, 수비 73/8, 타자 정신 65/8,
구위 69/24, 변화구 71/20, 제구 77/8, 투수 정신 67/20이다(기준점/Adjusted Z 변환 폭).
체력·실측 구속의 절대 기준, 결측 55, 송구 평가, 100 상한은 유지한다.

선수별·구단별·특정 연도별 예외는 없다. 438개 유일 Source 연결을 선수 인물 ID 단위로
학습/검증/보류 분할했고, 같은 인물의 다른 시즌을 학습과 검증에 나눠 넣지 않는다.
수비 기사 593행은 Normal 400·All Star 185·EX 7·Rare 1로 분리했다. 투수 기사에는
추가 저변화구 투수를 포함해 카드 52장과 목표 등급 445건이 있다.
`+N`은 선수·포지션·구종별 상승 필요치다. 공통 등급 경계로 기본 수비/변화구를 역산하지 않는다.

Cost의 기본점·공격/투구 품질·출전량·elite 자격은 유지한다. 수비 가산은 표시 Defense/Arm 대신
독립 `defensiveQualityProfiles`와 원기록 근거로 계산한다. 표시 척도 조정 때문에 가격이 같이
상승하지 않는다. Source 가격은 정수 경계 주변 야수 20개만 바뀌었고, 기존 참조 491건의 Cost
MAE 0.996은 유지된다. 새 438건의 Cost MAE는 1.034→1.032다.

새 표본의 수비 MAE는 18.101→5.454(388장), 변화구는 15.160→4.320(50장)이다.
체력·구속 오차와 기존 소표본의 타자 정신 악화는 남는다. 원문이 B 이상 수비와 선별 투수를
대상으로 하므로 전 리그 분포의 정답으로 보지 않는다. 현재 연결 이미지가 기사 게시 당시와
완전히 같은 파일이라는 증거도 없으므로 원작 최종판 복원 완료로 선언하지 않는다.

Importer 88건, 다른 Seed의 Bake JSON 93개 일치, Editor/Runtime 데이터 동기화를 확인했다.
기존 빌드의 동일 DetailedMatchEngine 러너로 전후 각각 10,000경기를 비교했다.
타율 .273→.272, ERA 3.496→3.330, 구단 경기당 득점 3.616→3.434이다.
최신 소스의 보조 빌드는 별도 변경 중인 TeamColorDefinitions의 컴파일 오류로 실패했으며,
이 수치를 최신 소스 전체의 검증으로 해석하지 않는다. Unity Test Runner/Play Mode 미실행이다.
WorldHistory 캐시는 사용자가 별도로 Bake한다. 근거·선수별 비교·검증 제약은
`docs/reports/pm_threshold_review_20260906/결과.md`를 따른다.

### 42.13 Cost와 기본 주전 배치 추가 보정 (v12 공식 베이크 이력)

평가 코드는 Ability v8 / Cost v12 / Balance v15 / Roster v5다. 선수·팀·특정 연도별 예외 없이
타자 Cost 기본점·출전량·상위 진입 경계를 조정했다. 투수 후보는 별도 2013 관측에서 악화되어
적용하지 않았다. 모든 기본 능력치와 투수 Cost는 기존 값과 같다.

주전 점수는 해당 시즌 PA와 포지션 수비 이닝의 제한된 가산을 사용한다. 9자리 동시 최대 가중
매칭을 유지하고, Cost와 단순 능력치 평균에 따른 사후 교체는 제거한다. 구원 후보 중 최다 세이브
선수는 최소 세이브 요건을 충족하면 Natural Role과 함께 마무리 후보 자격으로 인정한다.

공식 데이터의 Cost MAE는 438장 기준 1.032→0.742, 야수 주전 명단 불일치는 60명 중 9→6,
수비 근거가 있는 52명의 위치 불일치는 10→7이다. 전체 팀 시즌의 10타석 미만 주전은 20→0명이다.
엑셀 2011 표본 MAE 0.510→0.519와 일부 팀의 배치 악화는 남는다. 참조의 강화·판본 차이와
과거 수비 원자료 결측을 이름별 강제 배치로 메우지 않는다.

후보 검증에서 Importer 93건과 서로 다른 Seed의 JSON 93개 일치를 확인했다. 사용자 베이크 후
공식 Editor 93개·Runtime 46개 JSON이 후보와 모두 일치하고 Runtime 카탈로그 연결도 통과했다.
현재 공식 데이터의 10,000경기는 타율 .272, ERA 3.325, 구단 경기당 득점 3.431이며 이전 후보와
결과 체크섬까지 같다. Game 포함 Headless 빌드 오류 0개, 관련 EditMode 101건 통과를 확인했다.
Unity 실행은 미검증이다. 사용자 생성 WorldHistory 5개의 헤더도 현재 Runtime 원본을 참조한다.

현재 적용 상태·남은 오차는 `docs/reports/pm_live_review_20260906/검토.md`를 따른다.
94 LG 수비 자료 결측과 중간 성적 투수의 Cost 과대평가는 남아 있다. 판독 근거·보정 설계·
재실행 도구는 `docs/reports/pm_final_review_20260906/결과.md`에 보존한다.

### 42.14 SK 2007~2009 선발 Cost와 과거 포지션 근거 보강

현행 코드는 Ability v8 / Cost v13 / Balance v16 / Roster v6다. 공식 데이터는 42.13절의
사용자 베이크 상태이며, 이번 후보는 `.tmp/pm-sk-calibration/final`에만 생성했다. 새 평가를 공식
데이터와 WorldHistory에 반영하는 재베이크는 사용자가 수행한다.

2010년 인벤 SK 팀덱 이미지의 선발 15명을 기준으로 출전량만으로 Cost 8에 몰리는 현상과,
좋은 성적에도 출전량 자격 때문에 9/10에 도달하지 못하는 현상을 함께 수정했다. 투수 선발 역할군의
공통 성적·출전량 계수, 양의 성적과 출전량의 곱에 대한 가산, 가격 경계·상위 자격을 JSON에서
조정한다. 선수·팀 이름별 Cost 예외는 없으며 기본 능력치와 경기 확률 공식은 바꾸지 않았다.
구원 공식은 유지하지만 전체 투수 내 역할 백분위 변화로 일부 구원 가격도 달라질 수 있다.

SK 15명의 평균 절대 오차는 1.000→0.333, 정확 일치는 3→10명이며 모두 ±1이다. 08 김광현은
9→10, 09 김광현은 8→10, 08 송은범은 8→6으로 참조와 일치한다. 선발 명단·등판 순서는 그대로다.
기존 438장 전체 Cost 오차도 0.742→0.687로 감소하지만, 별도 2013 투수 62건은 1.032→1.274로
악화된다. SK는 후보 선택에 사용한 표본이며 독립 검증으로 보고하지 않는다. 판본 차이와 잔여 오차는
`docs/reports/pm_pitcher_position_review_20260906/결과.md`에 명시한다.

94 LG의 시즌 수비 자료 결측은 KBO 수상 기록·당사자 인터뷰와 당시 라인업 사진을 근거로
`season_position_evidence.json`에 보강했다. 실제 수비 기록이 있으면 이를 우선하며 결측일 때만
시즌+Source ID+구단이 일치하는 포지션을 사용한다. 수비 경기·이닝과 능력치는 생성하지 않는다.
Editor와 Runtime 모두 김동수 C·류지현 SS·한대화 3B·노찬엽 CF 배치를 확인했다.

Importer 101건, Game 포함 관련 Headless EditMode 101건을 통과했다. 두 Seed의 선수·시즌
데이터 파일 90개가 일치하고 메타데이터 3개의 차이는 중간에 수정한 원본 해시 보존 처리 때문이다.
최종 후보의 파일 해시·콘텐츠 재조립 검증과 44개 연도 결정론 검사를 통과했다. 동일 조건 10,000경기
전후 타율 .272→.272, ERA 3.325→3.323, 구단 경기당 득점 3.431→3.429,
홈런 0.682→0.680이다. Unity Test Runner·Play Mode·Player Build는 수행하지 않았다.

### 42.15 역사 강팀의 가격과 실제 경기 우위 검토

현재 사용자 베이크는 Cost v13 / Balance v16 / Roster v6다. 85 삼성·00 현대·08 SK·16 두산·
94 LG·98 현대·82 OB·88 해태·91 해태·10 SK를 조사했으며 9팀의 Core25 Cost가 연도 내 1위다.
그러나 기존 WorldHistory 5개와 별도 중립 대결 185,400경기에서 당시 리그 지배력은 충분히
재현되지 않았다. 가격 일치를 팀 경기력의 검증으로 대신하지 않는다.

과거 6팀의 주전 54명에 수비 위치 근거가 없고, 정식 경기에서는 결측 DH가 비주포지션 패널티로
연결될 수 있다. 82 박철순의 팀 투구 비중은 원기록 31.0%에서 현재 구단주 캐시 11.5%로 낮아진다.
10 SK는 Natural Setup인 정우람이 5선발에 배치된다. 포지션·실제 기용 근거와 5인 선발 고정의
영향을 먼저 검토하고, 이후 능력치 차이와 득실점·승률 차이를 평가한다. 팀·선수 이름별 보너스는
이 검토의 해법으로 사용하지 않는다.

`historical-strength` 진단은 같은 대진마다 홈·원정 및 25개 선발 조합을 균등하게 실행한다.
기존 중립 Reader를 쓰므로 원포지션 보정·손잡이·누적 피로까지 포함한 정식 역사 경로와 구분한다.
현재 역사 캐시는 읽기만 했으며 이번 검토에서 밸런스·공식 데이터·캐시를 수정하지 않았다.
정규시즌 78~80경기인 게임 역사와 실제 시즌의 경기 수·승률 계산 차이도 분리해 해석한다.
상세 수치와 웹 근거는 `docs/reports/team_strength_review_20260906/검토.md`에 보존한다.

### 42.16 역사 강팀 재현 개선과 5인 선발 순환 검증

후속 정밀 검증에서 부포지션의 경기 입력 전달 누락을 수정하고 1985·1992 시즌 포지션 근거를
보강했다. 9개 후보 464,917경기에서 85 삼성·92 빙그레·10 SK의 실제 유사 승률과 동년 1위를
함께 충족한 후보는 없었다. 곡선 기본값·5인 순환·공식 베이크는 유지한다. 전체 상위 30개 통과는
미완료이며 코드/Research 수정과 생산용 게시를 구분한다. 근거·후보 해시·회귀 결과 및 기용 계약
재검토 사항은 `Tools/KBOImporter/PositionRepair.md`를 따른다.

선발 등판은 **팀별 실제 경기마다 1→2→3→4→5→1** 순서다. 과거 GS나 이닝 비중으로 등판
횟수를 가중 배분하지 않는다. 휴식일·올스타는 순번을 소비하지 않고 포스트시즌도 순번을 이어간다.
검토 중 추가했던 가중 기용 계획과 스케줄러는 제거했다. 이닝 비중은 경기당 소화 이닝과
교체 판단의 결과로 평가하며 원기록 비중을 맞추기 위해 순환 규칙을 바꾸지 않는다.

DerivationBalance v18 / RosterBuilder v8은 선발 결손을 실제 GS 양수 → GS 결측 → GS=0 순서로
보충한다. GS는 후보 선정 근거다. 수비 위치 웹 근거는 11→236건으로 보강했으며 수비 이닝이나
능력치를 만들지 않는다. 결측 임시 DH와 실제 DH 전담을 IsPositionEvidenceMissing으로 구분하고
결측 배치 비용을 적용해 공동 최적화한다. 경기의 결측 위치 패널티만 중립으로 처리한다.

MatchRatingCurve는 center 45 / slope .45로 변경했고 기본값과 런타임 JSON을 함께 갱신했다.
Source 17,333명의 기본 능력치와 모든 투수 Cost는 유지된다. 포지션 비교 모집단이 바뀐 타자
49명의 Cost가 변하며 2010 프야매 438장 MAE는 .6872→.6895다. 가격 일치 개선으로 주장하지 않는다.

실제 역사 시즌 경로의 최종 400시즌·3,144개 팀 시즌에서 모든 경기의 1~5선발 순서를 검증했다.
80경기는 16·16·16·16·16회, 78경기는 16·16·16·15·15회였다. 주요 10개 연도 각각 32시드의
전후 합계 203,109경기, 별도 10개 연도 각각 8시드의 합계 55,798경기를 비교했다.
연도 내 실제 승률 상관은 주요 .782→.840, 별도 .653→.765다. 08 SK .551→.596, 00 현대
.547→.572로 개선됐지만 85 삼성 3위·10 SK 4위가 남는다. 시대 간 순위 복원 완료는 아니다.

Python 107개, Headless NUnit 68개를 통과했다. 공통 15,000경기 통계를 포함하며 10,000경기
표본의 AVG .261 / 팀 경기당 3.40득점 / 강팀 승률 58.3%는 기존 허용 범위다.
Unity Test Runner·PlayMode·Player Build는 미실행이다. 공식 Archive·역사 캐시는 덮어쓰지 않았으며
사용자의 동시 베이크를 별도 기록했다. 최종 v18 데이터·코드의 재베이크가 필요하다.
전체 조건과 잔여 문제는 `docs/reports/team_strength_improvement_20260906/결과.md`를 따른다.

### 42.17 정규시즌 경기 수 144경기 통일

선수 모드·구단주 모드·역사 기록 생성·시즌 진단 도구는 다음 새 시즌 생성부터 팀당 144경기를
사용한다. Core 기본값과 NewGameDefinition 직렬화 자산을 함께 변경하며 역사 실행기도 같은
CareerSeason.RegularSeasonGamesPerTeam 설정을 읽는다. 10구단 리그는 정규시즌 총 720경기,
상대 구단별 16경기다. 7·9구단 역사 시즌도 현재 일정 생성 규칙에서 팀당 144경기로 편성된다.

1~5선발 순환은 유지한다. 144경기를 완주하면 선발 등판은 29·29·29·29·28회가 된다.
진행 중인 세이브의 확정 일정과 기존 베이크 파일은 소급 수정하지 않는다. 신규·다음 시즌 및
다음 역사 생성 실행부터 적용한다. 경기 수를 Unity Balance ContentHash에 포함하고 Core 기본
ContentHash를 v5로 갱신해 80경기 캐시를 새 설정과 일치하는 결과로 재사용하지 않는다.

사용자 지시에 따라 경기·시즌 재시뮬레이션과 캐시 베이크는 실행하지 않는다. 앞 절의 400시즌
수치는 변경 전 78~80경기 검증 이력이며 144경기의 통계 검증 결과로 사용하지 않는다.

### 42.18 역사 베이크 병렬 실행과 완료 산출물 재사용

World History Bake는 순수 데이터 로딩을 메인 스레드에서 완료한 뒤 백그라운드에서 실행한다.
기본 동시 연도 수는 CPU 여유를 고려한 최대 4개이며 통합 도구 설정과 CLI에서 조정한다.
각 역사 연도는 독립 실행기·Seed·피로 상태를 소유하고, 최종 기록은 연도 순서로 합친다.
한 시즌 내부의 경기 순서와 144경기·1~5선발 순환·DetailedMatchEngine 공식은 그대로 사용한다.
일반 런타임 Builder는 기본 직렬이며 베이크 도구가 병렬 실행 옵션을 명시적으로 전달한다.

연도별 완료량·완료 경기 수를 표시하고 경기 사이에 취소를 확인한다. 콘텐츠 로딩·기록 매핑·
바이너리 인코딩·파일 쓰기·Unity 자산 등록 시간을 로그로 구분한다. Assembly Reload는 실행 중
잠시 잠그고 성공·실패·취소 경로 모두에서 해제한다. Unity API는 작업 스레드에서 호출하지 않는다.

같은 콘텐츠·밸런스·Seed Key와 복원 가능한 정규형 본문을 가진 완료 파일은 다시 계산하지 않는다.
완성된 파일만 원자적으로 교체하고 모든 요청이 완료된 뒤 카탈로그를 등록한다. 중단한 작업의
완료 Seed는 다음 실행에서 재사용하며 강제 재베이크 진입점도 제공한다. Seed만 같은 서로 다른
모드의 결과를 공유하지 않는다. 중간 연도 체크포인트는 이번 범위에 포함하지 않는다.

역사 기록은 경기 종료 직후 기존 규칙으로 누적해 전반기 재집계를 제거한다. World 생성은
전체 경기 객체 보관을 생략하고, 경기별 진단 요청은 원본 경기 보관을 유지한다. 불변 기대 득점
표를 공유해 반복 초기화 할당을 줄인다. 검증 결과와 실행 환경의 한계는
`docs/reports/world_history_bake_performance_20260906/결과.md`에 기록한다.

### 42.19 캐시 기록 기반 연도 카드 평가 보정

현행 **Ability v10 / Cost v17 / DerivationBalance v23**을 공식 Editor Source·Runtime과 게임 Runtime에
반영했다. 레퍼런스가 있는 연도 일반 카드는 canonical reference의 Cost와 기본 능력치를 stable Source
ID로 동기화하고, 미수록 카드는 기록 기반 공통 산식으로 생성한다. 선수명·팀별 코드 분기와 수치 예외는
없으며 원본 성적은 보존한다.

canonical reference는 `annual_reference_overrides.json` 생성 데이터다. 카드 ID, 필드별 출처, 값,
버전, SHA-256을 가지며 bake manifest가 파일 무결성을 검증한다. Editor Source에는 동기화 전 산식값과
최종값을 함께 남기고 Runtime에는 최종값만 전달한다. 따라서 동기화와 산식 성능을 별도로 감사할 수 있다.

카드 연도는 2013년 이하이고 월별 카드는 제외한다. 1989~1999·2010~2013 웹 아카이브 일반 카드와
2000~2009 DB 일반 카드를 포함해 7,526장을 연결했다. 일반/AS 387쌍의 Cost 동등성을 확인한 뒤 일반
판본이 없는 13장만 AS Cost로 보충한다. 동명이인·개명은 원본 기록과 포지션으로 구분한다.

최종 bake는 Cost 7,526장과 기본 능력치 42,554필드, 합계 50,080필드가 레퍼런스와 전부 일치한다.
Cost 과소·과대평가는 각각 0건이며 10Cost 129장도 전부 일치한다. 1994 LG 이상훈은 Cost 10과 투수
능력치 89/72/85/87/87/85로 반영되며, 동명이인 1994 삼성 이상훈 Cost 5와 별도 Source ID를 쓴다.
전체 18개 구단 표기에서도 연결된 Cost·능력치 불일치는 각각 0건이다.

미수록 Cost 산식은 성적·출전량·역할·신뢰도·수비 기여를 입력으로 쓰는 Gradient Boosting 회귀 나무다.
동일 선수를 한 그룹으로 둔 Validation+Holdout 2,924장에서 MAE 0.519, 정확 일치 53.08%, ±1 이내
95.28%, bias -0.026이다. 능력치 ridge 산식의 Holdout MAE는 실측 없는 Velocity를 제외한 7,388필드에서
5.151이다. 이 수치는 미수록 카드 일반화 성능이며 연결 레퍼런스의 최종 정확 일치와 구분한다.

Python 135건과 카드 경로 Headless EditMode 64건은 모두 통과했다. 전체 Headless EditMode는 743 통과,
9 실패, 11 skip이며 실패는 카드 경로 밖의 진행 중인 PitchArsenal, CareerSchedule/FastForward,
시즌 통계 허용 범위, ManagerHistoricalSave, Owner E2E, CareerNews enum 작업이다. Release 빌드 4개는
경고·오류 0이다. 중립 10,000경기와 44개 역사 연도 균형 10,000경기 결정론을 확인했다. 실제 bake를
읽는 시즌 경로는 1994·2000·2008·2009·2012·2013을 각 32회 실행해 총 114,838경기와 연도별 결정론
6건을 통과했다. 자료별 출처·전체 구단 오차·경기 지표·재현 명령은
`Research/PyaMaeCardDb/Calibration/README.md`를 따른다.

## 43. Player/Owner 공용 UI Shell 계약 (2026-09-05)

구단주 화면 이동은 현재 Runtime에서 이미 바인딩한 화면을 재사용한다. 최초 진입한 화면의
Snapshot만 만들며, `RuntimeChanged`와 명시적 `Refresh`에서 조회 상태를 무효화한다.
변경 직후에는 현재 화면만 갱신하고 나머지는 다음 진입 시 갱신한다. 홈 복귀로 전체 화면을
재구성하지 않으며, 같은 Primary 안의 Local 전환은 탭 버튼을 재사용한다.
검증 근거와 최초 진입 비용은 `docs/reports/owner-ui-transition-performance.md`를 따른다.

전력보강의 스카우트 상품 목록은 실제 확률을 선계산하지 않는다. 선택한 상품의 확률을 처음
조회할 때 계산하고 해당 Snapshot 수명 동안 재사용한다. 전체 연도·구단 조합이 늘어나도
지도는 6개, 방침 창은 10개 상품씩 페이지로 표시한다. 상세 근거와 검증 범위는
`docs/reports/owner-power-up-latency.md`를 따른다.

구단주 버튼은 `OwnerUiButtonSkin`과 `Resources/UI/OwnerSkin`의 전용 ImageGen 프레임을 사용한다.
메뉴·보조 동작·주요 실행의 시각 위계를 분리하며 선수 모드 프레임을 재사용하지 않는다.
선택 상태는 포인터 포커스와 독립적으로 유지하고, 카드/투명 클릭 영역은 일반 버튼 스킨에서 제외한다.
저작 프롬프트와 적용 규약은 `docs/art/OwnerButtonSkin.md`를 따른다.

타이틀은 어느 모드에도 소속되지 않으므로 구단주 전용 프레임을 재사용하지 않고,
`TitleUiButtonSkin`과 `Resources/UI/TitleSkin`의 공용 ImageGen 프레임을 사용한다. 모드 선택 카드,
보조 동작, 주요 확인, 위험 동작의 위계를 분리하고 빈 기본 Label을 쓰는 모드 카드도 전체 영역을
클릭할 수 있어야 한다. 공용 `CareerUiSkin` 재적용 뒤에도 프레임과 입력 Graphic을 보존하며,
저작 프롬프트와 적용 규약은 `docs/art/TitleButtonSkin.md`를 따른다.

관리 UI는 `SharedGameShellView`의 Global Header, 상단 Navigation, Context Header, Workspace,
선택형 Right Inspector, Action Bar와 Overlay Layer를 공유한다. 화면이 닮았다는 이유만으로 상태와
Command를 합치지 않으며, `GameModeUiProfile`과 `UiCapabilitySet`이 모드별 메뉴·권한을 공급한다.
Player와 Owner Runtime이 함께 존재하거나 선택한 Runtime이 사라진 경우에는 한쪽을 임의로 열지 않고
타이틀의 모드 선택으로 돌아간다.

Navigation은 `Shared Game Shell → 최대 6개 Primary 업무 영역 → Local Route` 두 단계까지만
허용한다. 타격/투구, 정규/포스트시즌, 핵심/전체, ScoutType 같은 세부 선택은 Route가 아니라 화면
내부 Filter/Selector/View Mode로 둔다. Match Center는 Global 항목이 아닌 Context Navigation이며,
Home 또는 일정에서 들어온 원점을 보존해 Back 시 복귀한다. 기존 Deep Link는
`NavigationRouteMigrationMap`으로 새 Local/Context Target에 연결하고 즉시 삭제하지 않는다.
공용 UI의 Cancel 입력은 Popup 표시 스택을 먼저 한 단계 닫고, Popup이 없으면 Route 방문 스택을
역순으로 한 단계 복원한다. Home은 스택의 고정 바닥이라 더 뒤로 가지 않으며 Home에서 ESC를 누르면
설정 Popup을 열고, 열린 설정 Popup에서 ESC를 누르면 Popup을 닫는다.

선수 모드 Primary는 `홈/경기/선수/팀/리그/커리어`, 구단주 모드는
`홈/선수단/전력보강/덕아웃/구단/리그`다. Production Consumer가 없는 기능은 Global 슬롯을 차지하지
않고 해당 업무 영역의 Local 잠금 항목으로만 표시한다.

관리 화면의 시각 정본은 `docs/디자인/ref/` 이미지다. 밝은 회백색 업무 영역, 얇은 청회색 경계,
검정·은색 상단 상태 바, 작은 직사각형 탭과 조밀한 표·목록 비율을 우선한다. 파란 대형 카드 프레임,
야구공 장식이 본문을 침범하는 패널, SF/HUD 형태의 대형 버튼은 관리 화면 기본 Skin으로 사용하지 않는다.

구단주 대기실은 인벤 「첫화면 대기실! 하나하나 뜯어 보자」(news 86747)의 상단 중앙 아이콘 메뉴와
검정·은색 상태 바, 사무실 배경, 우측 하단의 밝은 구단 정보창을 기준으로 한다. 메뉴 Route·모드 권한은
위 계약을 유지하고, 정보창의 일정·구단·저장은 기존 Command로 연결한다. 레퍼런스에 있는 온라인 채팅과
연습경기는 추가하지 않는다. 생성 에셋·대응표·검증은
`docs/UI/ChangeRequests/OwnerLobbyInvenReference.md`에 기록한다.

선수 카드 표면은 사용자 지정 [인벤 선수 카드 가이드](https://www.inven.co.kr/webzine/news/?news=86752&site=bm)의
초기 프야매 디자인을 우선한다. 얇은 은색 테두리, 어두운 초상화 영역, 검정 명찰, 흰 능력치 막대와
하단 COST를 사용하며 구단주 상세 앞뒷면과 목록, 커리어 카드 앞면에 적용한다. 구단주 뒷면은 좌측
프로필·우측 구종/수비·하단 기록/스킬 영역으로 배치한다. 원작의 계약 잔여일·수비 등급·서포트카드처럼
현재 표시 데이터가 없는 값은 발명하지 않는다. 구단주 COST와 커리어 종합 능력은 각각 기존 의미를 유지한다.
실사 선수 사진은 이 작업에 포함하지 않으며 기존 초상화 자산을 사용한다. 검증 현황은
`docs/reports/선수카드_인벤레퍼런스_UI.md`를 따른다.

구단주 카드에는 Normal·AllStar·GoldenGlove·MVP·Rare·Ex·Legend·CareerHigh 8개 등급의
미니/전체 프레임 16종을 사용한다.
원본 레퍼런스의 얇은 테두리와 등급별 장식을 유지하고, 모든 등급에서 초상·명찰·연도·COST의
런타임 배치 좌표를 동일하게 유지한다. v4는 사용자 지정에 따라 `ALL STAR`, `Golden Glove`,
`MVP`, `RARE`, `EX`, `LEGEND`, `CAREER HIGH` 등급명을 프레임 장식에 포함한다. 이 문자열은 카드 데이터가
아니며, 선수명·구단명·연도·능력치·COST는 계속 런타임 UI가 표시한다. 일반은 v2를 유지하고
올스타·골든글러브·MVP와 개발용 디자인 갤러리의 Rare/EX/Legend/CareerHigh는 v4를 우선한다.
특수 영입의 Legend/CareerHigh 대상 프리뷰도 같은 공통 프레임 로더를 사용한다. 원작 참고와 최종
생성 프롬프트는 `docs/art/OwnerPlayerCardFrames-v4.md`를 따른다.
등급은 `PlayerCardEdition`으로 프레임에 전달하며 이름에 수상 명칭을 붙여 구분하지 않는다.
COST 별은 한 장의 4×2 마스터에서 자른 Normal/Rare/AllStar/GoldenGlove/MVP/Ex/Legend/CareerHigh
투명 Sprite 8종을 개별 배치하여 실제 비용만큼 밝게 표시한다. 마스터·프롬프트·분할 규칙은
`docs/art/PlayerCardCostStars-v4/README.md`를 따른다.
능력치·성적·성장판·구종 데이터와 계산은 그대로 유지한다. 생성 규약과 프롬프트는
`docs/art/OwnerPlayerCardFrames.md`를 따른다.

- 선수 모드는 기존 Career 성장·계약·감독 AI 기용을 유지한다. Team/Roster는 읽기 전용이고
  `OwnedPlayerCardState`, Scout, CardTraining, TeamColor 장착과 구단 재정 Command를 참조하지 않는다.
- 구단주 모드는 별도 `OwnerModeManager` Runtime과 Save를 사용한다. 현재 실제 화면은 Home, 25인
  선수단·라인업, 보유 선수 Collection, 재정, 시설, Staff Office, 선수 계약, Pregame,
  상대 라인업 조회와 경기 관전이다.
  같은 역할 그룹 슬롯 교환은 `UpsertLineupPreset`에 연결하지만, ActiveRoster 등록 변경 Command가
  없으므로 해당 Action은 비활성화한다.
- 선수단의 Local Tab은 레퍼런스 순서대로 `선수 오더 / 작전 카드 / 서포트 카드 / 팀 컬러`를 사용하고
  `docs/지침/Owner_Roster_UI_Guidelines_UPlayBall.md`를 정규 계약으로 삼는다. 선수 오더는 기존 25인
  선수단·타자/투수 역할 편성 View를 보존한다. 작전 카드와 팀 컬러는 기존 Production Query와
  `Preview → ValidateLineupPreset → 명시적 Confirm → UpsertLineupPreset`을 재사용한다. 서포트 카드는
  레퍼런스 편성 UI와 명시적 Locked 상태만 제공하며, Runtime 정의·효과·저장 Command를 발명하지 않는다.
- 선수 오더의 1군 등록 교체는 한 건마다 저장하지 않는다. 편집 세션의 불변 Preview에 여러 교체를
  순서대로 누적하고 매 변경 후 25인·선수 상태·프리셋을 전체 재검증하며, 유효한 최종 후보만 한 번의
  명시적 저장으로 적용한다. 상태 Strip에는 현재 누적 교체 건수를 표시한다.
- 선수 오더의 선발 카드 아래 포지션 버튼은 수비 위치 변경 전용 입력이다. 타순을 유지한 채 두 선수의
  수비 슬롯만 교환하고, 변경 전후 위치·주 포지션을 확인한 뒤 기존 Preview에 누적한다. 기존 Validator의
  비주포지션 경고와 실책 위험을 표시하며 최종 `배치 저장`으로 확정한다. 조사 근거와 검증은
  `docs/reports/owner-defensive-position-editing.md`를 따른다.
- 구단주 확장 전용 Condition·운영·Staff·Intel 조정값은
  `Assets/10.Datas/Resources/NewGame/OwnerExpansionBalance.json`에서 저작하고
  `LoadOwnerModeBalanceTable()`에서만 공통 경기 Balance에 합성한다. Config 누락·불완전 상태는
  Production 진입 시 실패하며, 선수 커리어의 `LoadConfiguration()`과 Balance hash/version은 바꾸지
  않는다.
- TeamColor와 Tactic은 실제 가용 후보 Query와 `ValidateLineupPreset`/`UpsertLineupPreset`을 통해
  선수단·라인업의 각 2슬롯을 변경한다. 전용 Snapshot과 상세 Route는 충족 인원·StackPolicy 및
  Trigger·Duration·Counter를 표시한다. TeamColor 상세 효과는 역할별 보너스 총합이 아니라 실제
  적용 선수 1명당 타자·투수 능력치 상승량을 표시한다. 과거 `Owner.Dugout.TeamColor`와 `Owner.Dugout.Tactics` Deep Link는
  각각 선수단의 `Owner.Roster.TeamColor`와 `Owner.Roster.TacticCards`로 이관한다.
- Scout, CardTraining, Enhancement, Sale은 `Owner.PowerUp.*` 전용 View State에서 실제
  Catalog/Query/Preview/검증/결정론적 Command를 소비한다. 세 Route는 Shop 또는 Collection 화면으로
  변환하지 않으며, Presentation은 후보군·확률·성장량·비용·판매가를 재계산하지 않는다. 레이아웃과
  상태·확정 흐름의 정규 계약은 `docs/지침/Owner_PowerUp_UI_Guidelines_UPlayBall.md`를 따른다.
- `Owner.Club.*` 여섯 Route는 `docs/지침/Owner_Club_UI_Guidelines_UPlayBall.md`를 정규 계약으로 삼는다.
  선수 계약은 Cost·Edition 기반 1~3년 계약과 시즌 급여를 구단주 SaveVersion 11에 저장한다.
  구단주 트레이드 Route와 Command는 제거하며, 선수단 변경은 보유 카드 기반 1군 등록 변경만 사용한다.
- 공용 `MatchHudView`는 같은 Match Event를 표시한다. Player 입력은 `PlayerMatchControls`만 통하고,
  Owner 경기는 `InternalAiOnly`로 한 번 확정한 이벤트를 타석 경계별로 재생하며 일시정지·배속·즉시
  결과만 허용한다. 설정의 경기 메뉴에서 전체 자동 진행/핵심 장면 자동 진행/결과만 보기와 1×/2×/5×
  기본값을 정한다. 결과만 보기는 중계 사건과 경기 사운드를 재생하지 않고 최종 결과·기록으로 바로
  이동한다. 존재하지 않는 실시간 교체·불펜·전술 Command를 UI에 만들지 않는다.
- Career 리그 요약은 `CompactRecordTableView`, 대량 리더보드와 시즌/역대 기록은 Viewport Row Pool
  기반 `RecordTableView`를 사용한다. 1,000행 전체를 GameObject로 생성하지 않으며 정렬 뒤에도 Stable
  Row ID 선택을 유지한다.
- Owner 일정은 날짜를 발명하지 않고 현재 Save의 Round·대진·완료 점수를 같은 `RecordTableView`로
  표시한다. Owner 역사 기록은 새 게임용 `WorldHistory`를 사용자에게 노출하지 않고, 현재 Save에서
  실제로 진행한 현재·완료 시즌의 구단 순위·승패·득실점과 팀 타격·투구 합계만 표시한다.
- 경기 준비 Local Tab은 `상대 분석 / 우리 라인업 / 상대 라인업 / 전술카드`다.
  컨디션·궁합 전용 UI를 제거하고 기존 링크는 상대 라인업으로 이관한다.
  리그 / 순위표의 구단 행을 선택하면 같은 읽기 전용 카드 보드를 열고, 닫기·ESC는 원래 순위표로 복귀한다.
  야수 9명·벤치 5명과 선발 5명·중계 4명·셋업·마무리를 두 줄로 배치하며,
  AI 기본 편성은 경기 서비스의 공개 로스터 역할 조회를 공유한다. 숨은 컨디션·궁합은 공개하지 않는다.
  개별 카드는 우클릭으로 같은 역할 그룹의 상세 카드를 연속 조회하며, 기본 능력치·공개 시즌 기록·
  투수 구종만 표시하고 상대 구단의 컨디션·훈련·스킬 블록은 노출하지 않는다.
  미술 프롬프트와 조회 경계는 `docs/UI/ChangeRequests/OwnerTeamLineup.md`를 따른다.

사용자 노출 게임 모드 명칭은 `구단주 모드`다. `ManagerTacticProfile`과 감독 AI·감독 판단은 실제
야구 감독 개념이므로 유지한다. `ManagerModeCoordinator`, `ManagerModeRuntimeState`, Save DTO의
`managerMode`는 기존 Save 호환을 위한 Legacy 내부 명칭이며, 추후 `OwnerMode*` 정식 타입으로 옮길
때에는 숫자 값과 JSON alias를 함께 보존해야 한다.

Game/Game.Unity/Presentation/Game.Tests/Presentation.Tests 보조 컴파일은 이전 대상 실행에서
경고·오류 없이 통과했다. 최신 Balance/시즌 lifecycle/UI 병합 전 구단주 Unity 대상 실행도 39/39
통과했다. 다만 최신 통합본의 집계 Unity Test Runner와 Player Build는 사용자 지시에 따라 생략했고,
1280×720·1920×1080·2560×1440 Screenshot 검증도 완료하지 않았다. 따라서 이 절은 구조·코드 연결
상태이며 UI 전체 Acceptance 완료 선언이 아니다. `TacticLab` 연구 Consumer, 임의 선수 편집, 전체
Tactic Inventory 선택과 승강·새 시즌 로스터 이월도 남아 있다. 세부 Gate는
`docs/todo/역사시뮬레이션_구단주모드/08_구현_로드맵_검증기준.md`와
`13_4종_통합_구현로드맵_Codex.md`를 따른다.

구단주 경기 관전의 표현 기준은 상공 2D GameCast다. 생성 이미지에는 구장 환경만 포함하고,
공·야수·주자와 스트라이크존·기록·감독 판단은 별도 코드 계층에서 공식 이벤트로 표시한다.
`MatchPlayVisualizer`는 공유 필드 좌표를 사용하고 `GameCastPresentation.json`이 이미지 정렬과
연출 시간을 소유한다. 투구 통과점은 `PitchPlayData`, 타구 위치는 `BallInPlayEventData`의 수비 구역을
사용하며 정밀한 물리 궤적을 발명하지 않는다. 연출 준비용 미리보기는 점수·기록의 공개 경계를
변경하지 않고, 사건 이동이 끝난 뒤 HUD를 공개한다. 모든 순간/중요 순간/즉시 결과는 동일하게
확정된 경기 스트림을 소비한다. 중요 순간은 매 이닝 종료나 결과 종류 자체가 아니라 타석 전후
Win Expectancy 변화, 동점·역전, 후반 접전, 시그니처 플레이와 마지막 타석으로 선별한다.
구체 임계값은 `GameCastPresentation.json`이 소유한다. 구현·미술 원본·검증 근거는
`docs/UI/ChangeRequests/OwnerGameCast.md`를 따른다.

### 43.1 구단주 상점·전술·승강 코드 연결 상태 (2026-09-06)

- Scout, 카드 강화·중복 판매를 공용 Shell에서 `OwnerModeManager`의 Production Command로 연결했다.
  구매 이력과 전술 보유 수량은 `ManagerHistoricalRuntimeState`가 소유하며 구단주 SaveVersion 5에 저장한다.
- 작전 카드 Catalog는 44장(타자 16, 투수 16, 분석 6, 공용 6)으로 확장됐고 Counter 조합, 연구 획득,
  경기별 최대 2장 지정과 경기 종료 시 수량 소모, 3연패 종료 Signature 해금, 리그 등급 기반 AI 선택을
  같은 데이터 정의로 처리한다. 상점은 전체 카탈로그에서 Roll하고 보유 수량은 별도 Save Aggregate에 쌓는다.
  `TacticLab`은 연구 Tier 가중치 소비자다.
- 작전 카드 일정 화면은 앞으로 열릴 최대 10경기에 서로 다른 두 장 구성을 미리 배치한다. 경기별 계획은
  `ScheduledGameState.GameId`에 연결해 구단주 SaveVersion 12로 저장하며, 준비 Snapshot과
  `DetailedMatchEngine`이 같은 계획을 소비한다. 보유 수량은 미리 배치한 모든 미완료 경기의 예약량까지
  검증한다. 카드 목록 클릭은 선택 슬롯에 즉시 장착되고 별도 장착 버튼은 두지 않는다. 일정과 슬롯은
  카드 계열 원본 아트를 표시하며, 경기 준비의 `전술카드` Context Tab도 같은 일정 화면을 연다.
- 구단주 시즌 종료는 플레이어 구단 승률을 `LeaguePromotionResolver`에 전달해 Rookie~Galaxy 등급을
  갱신한다. 참가 구단·로스터·카드 소유 상태는 유지하고 시즌 한도형 상점 구매 횟수만 초기화한다.
- Front Manager는 Owner Runtime 최초 관찰과 Scout 최초 진입 Fact를 생산한다. Scout 구매 결과는
  Reveal이 선수별 신규·중복 상태를 이미 공개하므로 일반 선수 획득 Fact를 중복 생산하지 않는다.
  `GuideRepeatState`는 Save/Load에 포함되어 `cooldownScope: Save`를 실제로 보존한다.
  튜토리얼과 기본 규칙 설명(지명타자의 수비 부담·전술 카드 소모 안내 포함)은 각 새 게임에서 한 번만
  표시한다. 화면 재진입·불러오기·GuideManager 지연 초기화에서는 노출 이력을 유지하고 새 게임에서
  초기화한다. 실제 경기 결과·선수단 오류 같은 상황 알림은 사건별 반복 정책을 유지한다.
  가이드 원문은 문장별 줄바꿈과 일관된 띄어쓰기를 사용하며, 이름표는 프레임 탭 내부 중앙에 정렬한다.
  스킬 블록 획득 가이드는 개별 획득 알림이 아닌 게임당 1회 사용법 안내다. 상점 구매 확인·처리·
  연출·최종 결과 화면에서는 가이드 표시와 자동 닫힘 시간을 보류하며, 결과 화면을 닫은 뒤 재개한다.
- 상점은 기존 Owner UI 문법의 탭·2열 상품 타일·확정 결과 공개 패널을 사용한다. `docs/디자인/ref`의
  단순 실루엣 문법을 따른 ImageGen 선수 Pack·스킬 Pack·Reveal 배경을 연결하고, 작전 상품은 기존
  `TacticCardArtwork`의 공격·투수·분석·공용 자산을 재사용한다. 재화는 공용 Global Header만 정본으로
  표시하고 상점 본문에서는 중복 노출하지 않는다. 카테고리 탭은 좌측 상품 목록 안에 두며 목록만
  세로 스크롤하고 우측 선택 상품 상세는 고정한다.
- 상점 상세는 `ScoutRoller`의 Joint Bucket과 `TacticResearchRoller`의 후보군 재정규화 확률,
  Skill Gacha 공개 확률을 Game 계층 `ShopProductDetails` Query로 확정한다. 구매는 상세 → 최신 Quote
  확인 → 처리 중 입력 차단 → 결과 공개 순서이며, Reveal의 `다시 구입`은 별도 확인 팝업 없이 최신 Quote와
  재화를 재검증한 뒤 같은 상품의 구매·연출로 바로 이어진다. 선수/스킬/작전 보관 화면으로 이동할 수 있고,
  선택 탭과 탭별 Scroll은 Route 복귀 후에도 유지한다.
- 선수 카드 상점은 현재 월드 카드에 실제 존재하는 모든 원 구단·원 연도를 Scout 대상으로 사용한다.
  선수 카드 탭의 구단·연도 필터로 각각의 집중 상품과 두 조건의 정밀 상품을 찾으며, 후보가 없는
  구단·연도 조합은 만들지 않는다. 전체 필터에서는 정밀 조합을 숨겨 목록이 조합 수만큼 폭증하지 않게 한다.
  조건 선택 시 정밀 일치, 단일 조건 일치, 공통 상품 순서로 진열하며 각 단계의 단품·묶음 순서는 유지한다.
  공개 확률표는 해당 상품의 상세 조회 시 계산하고 상점 서비스 안에서 재사용한다. 구매와 화면 갱신에서
  전체 구단·연도 상품의 확률표를 미리 계산하지 않는다.
- 상점 본문은 좌측 2열 상품 진열과 우측 선택 상품 Preview로 구성한다. Player/Skill/Tactic에
  10회 묶음을 제공하며 기존 단가와 5회 할인 계약만 사용한다. Player/Skill/Tactic 확정 결과는
  공통 카드 Flip 연출로 소비하고, 전체/희귀만/최소 설정과 건너뛰기를 지원한다. 선수는 기존 선수
  Mini Card, 스킬 블록과 작전 카드는 등급·이름·획득 상태를 표시하는 전용 앞면을 사용한다.
  10장은 5열 2행의 목표 위치에서 순서대로 뒤집혀 공개되고, 공개된 카드는 최종 결과까지 유지한다.
  Cost와 Edition은 카드 앞면에 각각 표시하며 희귀도에 따라 공개 대기와 카드 테두리를 강조한다.
  1장은 중앙, 5장은 1행에 배치하고 최소 연출·건너뛰기도 동일한 배치를 사용한다. 선수 공개가
  끝난 뒤 각 Mini Card를 클릭하면 보유 선수와 같은 카드 상세를 열어 실제 능력치와 성장 출처를 확인한다.
  스킬 블록 공개가 끝난 뒤 결과 카드를 클릭하면 DOTween Flip으로 뒷면의 실제 테트로미노 형상을
  확인하며, 다시 클릭하면 앞면으로 돌아온다. 공개 전 Cover에서는 형상을 노출하지 않는다.
- 선수 커리어의 기간 결장형 부상 시스템과 강제 재활·복귀 보호·시장 패널티·부상 뉴스·은퇴 회고를
  제거했다. 훈련 리스크는 0~100 Condition 추가 차감으로 통합했고, 구단주 모드는 기존 출장 소모와
  주간 회복 및 10단계 파생 표시를 유지한다. 구세이브 호환은 이번 정리 범위에서 보장하지 않는다.
- 위 항목은 Assembly 컴파일까지 확인한 **코드 연결** 상태다. 사용자 요청에 따라 UI 실행 검증과
  시뮬레이션·대량 통계 테스트는 수행하지 않았다. 상세 근거는
  `docs/reports/2026-09-06_한시_잔여작업_S1-S6_구현보고.md`에 기록한다.

### 43.2 모드 독립성 재검토와 진행 상태 보존

- 구단주 경기 서비스는 전술 정의 등록 여부와 실제 보유 수량을 경기 전 각각 검증한다.
  부족한 전술은 AI 경기·재무·일정·기록을 바꾸기 전에 거부한다. 전체 슬롯 소모와 경기 후
  장착 해제를 순수 Game 서비스가 소유하므로 Unity Manager를 거치지 않는 진행도 같은 규칙을 쓴다.
- 구단주 완료 시즌은 `ManagerModeRuntimeState.CompletedSeasons`에 당시 LeagueGrade와
  일정·Seed·점수·선수 누적을 보관한다. 현재 시즌을 교체하기 전에 모든 AI 경기도 완료됐는지
  확인한다. 구단주 SaveVersion 10은 현재 시즌과 완료 시즌 배열을 함께 저장하며, v1~v9의
  완료 이력은 빈 배열로 이행한다. 구버전에서 이미 사라진 기록은 생성하거나 추정하지 않는다.
  선수 기록 화면에서 현재 시즌과 저장된 완료 시즌을 선택할 수 있다.
- 구단주 홈의 `시즌 완료`는 남은 플레이어 경기와 같은 라운드의 AI 경기를 저장된 일정·Seed 순서로
  `DetailedMatchEngine`에서 모두 실행한다. 결과를 추첨하거나 별도 경량 공식을 사용하지 않는다.
  첫 확인은 실행 범위를 안내하고 두 번째 확인에서만 진행한다. 선수 모드의 안전 진행 구조만 참고해
  한 Frame에 플레이어 경기와 같은 라운드 AI 대진까지를 확정한다. 구단주 전용 Popup은 현재 라운드·대진,
  리그/내 구단 처리 경기 수, 시즌 성적을 표시하고 중단 시 완료한 라운드까지만 유지한다. 일정 완료 뒤 `다음 시즌`은 별도
  확인으로 계약·급여·승강 Transaction을 실행한다. 만료 임박 계약은 먼저 갱신하되, 시즌 종료 후
  갱신 계약금과 급여 부족액은 미지급금으로 이월하고 다음 시즌 수입에서 상환한다.
- 전술 수집을 도입하기 전인 v1~v4에만 초기 전술을 보충한다. 이후 SaveVersion 증가가
  반복적인 초기 카드 지급으로 이어지지 않도록 한다.
- 선수 월드의 오프시즌은 모든 AI의 은퇴를 먼저 확정하고 승격·신인 충원을 처리한다.
  출전·순위 근거는 방금 마친 소속 리그에서 읽으며 승격으로 은퇴 검사를 건너뛰지 않는다.
  대규모 동시 은퇴에도 후보군을 채울 수 있도록 아래 리그부터 공석을 복원하고 신인은 Rookie에서 생성한다.
- 이전 41.12절의 디스크 저장 미지원 설명은 당시 상태다. 현재 작업 폴더에는
  CareerSaveAdapter/GraphSerializer, CareerSaveJsonStore, CareerManager.Persistence와
  설정 화면의 저장·불러오기 연결이 추가되어 있다. 이번 수정에서 해당 저장 구현을 대체하지 않았다.
- 이 항목들은 코드 변경 상태다. 사용자의 지시에 따라 테스트·대량 시뮬레이션을 실행하지 않았다.
  이전 보고서의 테스트 결과를 수정 후 통과 근거로 재사용하지 않는다.

### 43.3 구단주 새 게임 메인 카드 선택 계약 (2026-09-06)

- 새 게임의 다섯 단계는 구단주 배경과 전용 버튼 스킨을 사용한다. 후보 목록은 선수단과 같은
  `PlayerMiniCardView`로 등급·초상·이름·연도·Cost·선택 여부를 표시하며, 좌클릭으로 선택/해제한다.
  후보와 최종 25인 미니 카드의 우클릭은 공용 선수 상세를 열고 해당 카드의 기본 능력치·구종·원본
  시즌 기록을 표시한다. 생성 전 후보에 기존 구단 Runtime의 성장·컨디션을 섞지 않는다.
- 메인 카드 10장의 Cost 상한은 `NewGameDefinition._ownerMaximumMainCost`가 소유하며 초기값은
  60이다. 6타자·4투수, 고Cost 카드 수, 같은 연도 Premium 수, 동일 선수 중복 금지 계약은 유지한다.
- Ability v10 / Cost v17 정식 아카이브에서도 Cost 1 카드가 역할별 충원 인원을 만족하지 않고
  동일 카드가 매 Draft에 고정되는 문제가 있으므로, 자동 보충 15장은 Cost 2 10장과 Cost 3 5장으로 뽑는다.
  기존 Cost 1·2·3 각 5장보다 총 Cost는 30에서 35로만 증가해 메인 선택의 비중을 유지한다. 타자 8명·투수 7명과
  동일 인물 중복 금지·외국인 상한·결정론적 리롤 계약은 유지한다. 생성 실패 시 Draft 단계를
  로스터 확인으로 넘기지 않으며 실제 실패 사유를 현재 입력 단계에 표시한다. 검증 결과는
  `docs/reports/owner-starter-roster-cost-v16-fix.md`를 따른다.
- 후보 Browser는 연도·포지션·Cost Dropdown과 선수 이름의 복합 Filter를 제공한다. 선택 카드는 Check 표시와
  이름 요약으로 Page·Filter가 바뀌어도 식별할 수 있고, 인원·Cost·중복 제약을 깨는 선택은 즉시
  거부한 뒤 이유를 표시해 완료 시점의 숨은 무효 상태를 만들지 않는다.
- 프런트 매니저 선택은 01 분석형 / 02 현장형 / 03 활력형 전용 초상화를 사용한다. 선택한 ID를
  구단주 프로필에 저장하고 인게임 안내 대화·구단 정보에도 해당 타입의 표정 8종을 사용한다.
  실패한 Resource 조회를 캐시하거나 다른 매니저
  초상화로 대체하지 않는다. 최종 25인 확인은 텍스트 나열 대신 공용 `PlayerMiniCardView` 카드로
  메인 10장과 자동 보충 15장을 구분해 표시한다.
- 구단 선택부터 홈·경기 준비·상대 분석·라인업·관전·일정·기록·계약·카드 원본 소속까지 모든 구단명은
  `원본연도 구단명`으로 표시한다. 실제 Identity가 켜지면 원본 `TeamSeason` 브랜드, 꺼지면 같은
  `TeamSeasonKey`에 대응하는 가상 프랜차이즈 이름을 쓰며, 엠블렘도 최종 표시 이름과 같은 모드로 고른다.
  이미 연도를 포함하는 특수 합성팀 이름은 연도를 중복해서 붙이지 않는다.
- 새 게임에서 입력한 구단명은 구단주 프로필 데이터로 보존하지만 역사 구단 Identity와 엠블렘을 갈라놓는
  Runtime 표시명으로 사용하지 않는다. 선수명도 실제 모드에서는 원본 `PlayerPerson`, 가상 모드에서는
  `WorldIdentityRegistry` 이름을 사용하며, UI가 시뮬레이션 입력에 저장된 문자열을 직접 표시하지 않는다.
- 변경 후 순수 C# 10,000경기 회귀 결과는 AVG .270, OBP .346, SLG .418, 팀 경기당 3.69득점,
  BB 9.0%, SO 18.2%, HR 1.53%, HBP 1.39%, 강팀 승률 54.6%(무승부 131경기)로 기존 허용 범위다.

### 43.4 전력보강의 스킬 배치·유학 진입점

- 전력보강 Local 메뉴는 `스카우트 / 카드훈련 / 카드 합성 / 스킬 블록 배치 / 유학`이다.
  `Owner.PowerUp.Skills`와 `Owner.PowerUp.Study`는 실제 보유 카드와 공유 인벤토리의 조회 결과를 표시한다.
- 카드 합성의 보유 선수 카드는 좌클릭 한 번으로 선택과 등록을 함께 처리한다. 등록취소·초기화는 유지하며,
  우클릭은 등록 상태를 바꾸지 않고 선수 상세만 연다.
- 스킬 화면은 왼쪽 선수 목록, 중앙 선택 선수와 4×4 성장판, 오른쪽 등급별 블록 목록으로 구성한다.
  블록 선택·회전 후 선수 모드와 같은 원점 역산 알고리즘으로 배치 가능한 칸을 표시하고, 블록 모양 중
  어느 칸을 눌러도 유효한 원점을 찾아 기존 `PlaceOwnedCardSkillBlock` 명령으로 즉시 장착하며,
  장착 블록을 선택하면 해당 인스턴스만 해제한다. 경계·겹침 미리보기는 `SkillBoardService`를 사용한다.
  다른 선수에게 장착된 공유 블록은 중복 배치할 수 없다. 스킬 상점의 사용처도 이 메뉴로 연결한다.
- 유학 화면은 세계지도형 과정 선택과 오른쪽 과정 정보·대상 선수·확정 버튼으로 구성한다.
  프로그램 이름·기간·육성 포인트 비용·성장 상한 적용 결과는 현재 `OwnerCardGrowth` 정의를 읽는다.
  1군 등록, 시즌 1회 제한, 정원, 성장 상한, 포인트 부족 사유를 표시하고 신청을 차단한다.
  지도 표식은 목적지 좌표·이동 경로·선택/잠금 상태를 가진 과정 선택 UI이며 실제 국가별 경제·리그 규칙을
  추가하지 않는다. 기본 과정은 즉시 개방하고 전문 과정은 `Minor` 리그 진출 또는 포스트시즌 우승 1회처럼
  저장된 구단 성취로 해금한다. 잠긴 표식도 선택해 조건과 효과를 비교할 수 있지만 Game 명령에서 신청을 다시 차단한다.
  목적지의 실제 입력 표식은 ImageGen으로 만든 비행기 아이콘이며, 불투명한 텍스트 버튼은 사용하지 않는다.
  목적지를 처음부터 자동 선택하지 않고 비행기를 누를 때만 상세 정보를 연다. 같은 비행기, 지도 빈 영역,
  정보 패널 빈 영역 또는 취소 입력을 누르면 선택과 확인 단계를 함께 닫아 비교 과정이 화면에 고착되지 않게 한다.
  유학 선수 선택 카드는 작은 슬롯에서도 선수 이름을 최상단에 고정하며, 2행 카드 높이를 확보해 이름이 프레임에
  가려지거나 축소되어 사라지지 않게 한다.
- 사용자가 제공한 레퍼런스의 은색 업무 배경·파란 지도·유학 비행기 아이콘은 내장 ImageGen으로 제작했다.
  비행기 생성 원본은 `Assets/10.Datas/ArtReferences/OwnerPowerUp/`, 투명 런타임 자산은
  `Assets/10.Datas/Resources/UI/OwnerPowerUp/`에 분리해 보존한다.
  한국어 텍스트·카드·버튼·블록은 실제 Unity UI이며 이미지에 고정하지 않는다.
  생성 경로와 최종 프롬프트는 `docs/reports/owner-growth-ui-imagegen.md`에 기록한다.
- 이 작업은 Presentation 진입점과 표시·명령 연결 범위다. 기존 성장 수치와 경기 확률 모델은 변경하지 않는다.

### 선수 오더 화면의 카드 중심 편성 보드

- 선수 오더는 기존 카드 원화·공통 Shell·Preview/Validate/Confirm 계약을 유지하고 차콜 본문과 상단 배치 배지로 편성 가독성을 높인다. 이름은 원화의 등급별 명찰 안에 배치하며 장식을 덮지 않는다. 선택은 블루 테두리와 상단 선택 라벨로 구분한다.
- 수비 경고와 위치 변경 행동을 분석의 첫 항목으로 두고, 컨디션은 기존 타순·투수 보직별 0~100 막대 그래프로 표시한다. 그래프는 저장 컨디션을 사용하며 배치·궁합 보정 전 값임을 화면에 명시한다.
- 이름 검색과 검색 조건 초기화, 교체 전후 카드 능력치 비교, 고정 하단의 배치 저장·변경 취소를 제공한다. 동일 선수의 다른 카드를 교체하는 기존 규칙은 유지한다.
- 구현 상세와 검수 절차는 `docs/지침/Owner_Roster_UI_Guidelines_UPlayBall.md`의 2026-09-11 개정을 따른다. 경기 계산·밸런스·저장 구조는 변경하지 않는다.

### 43.5 구단주 선수 카드 상세와 성장 출처 표시

- 계약 메뉴의 1~3년 선택은 기존 잔여 기간에 더하는 **연장 기간**이다. 잔여 1년에 +1년을 선택하면
  잔여 2년이 되며 새 연봉은 즉시 적용한다. 계약금은 추가 기간과 기존 연봉표·할인 계수로 계산한다.
  잔여 1년 이하의 만료 임박 선수는 대상 인원·총계약금·변경 후 선수단 연봉을 미리 확인하고
  일괄 연장할 수 있다. 비용 부족이나 유효하지 않은 계약이 있으면 전부 변경하지 않는다.
- 같은 CardId의 중복 수량은 하나의 계약을 공유한다. 동일 PlayerPersonId의 다른 연도·Edition 카드로
  1군을 교체할 때도 현재 계약의 잔여 기간·연봉·급여 정산 이력을 승계하며 별도 계약금을 받지 않는다.
  계약 연장 직후 목록과 상세 Snapshot을 다시 바인딩하여 잔여 기간과 자금을 즉시 표시한다.

- 선수 카드 상세는 전체 화면 모달이 아닌 우측 비모달 패널로 연다. 원본 목록·선수단을 보면서 다른 카드를 선택할 수 있고, ESC와 명시적 닫기 Button으로 닫는다.
- 투수 카드의 앞면·Mini Card·검색·새 게임 후보는 `Position`의 선발/구원 축약값 대신 시즌의
  Natural `PitcherRole`을 표시한다. `Setup`과 `Closer`는 각각 `셋업`과 `마무리`로 노출하고,
  라인업 슬롯의 현재 배정 보직과 카드 원본의 Natural Role은 서로 덮어쓰지 않는다.
- 앞면 능력 막대는 기본 카드와 훈련·스킬 블록·현재 장착 팀컬러·유학·합성 강화 기여분을 별도 색 구간으로 표시한다. 총합은 실제 경기 입력과 같은 HardCap을 사용하며, 비발동·미장착 팀컬러를 보너스로 발명하지 않는다.
- 유학 완료 성장은 일반 CardTraining 성장과 합계를 공유하되 출처를 별도로 저장한다. 구단주 SaveVersion 13은 `studyBonuses`를 추가하며, v1~v12의 기존 `trainingBonuses`는 손실 없이 일반 훈련 출처로 복원한다.
- 구단주 SaveVersion 14는 1군 등록 변경 시 현재 25인 `CardId`와 선수 계약을 같은 Aggregate에서 교체한다. v11~v13에서 계약 수는 25개지만 1군 `CardId`와 어긋난 알려진 상태만 기존 계약을 보존하고 새 등록 선수의 1년 계약을 생성해 복원한다. 재등록으로 장기 계약 갱신을 무료 우회하지 못하도록 초기 로스터의 2~3년 분산 계약은 새 1군 등록에 재사용하지 않는다.
- 이 변경은 기존 영구 능력치 공식과 경기 확률 모델을 변경하지 않고, 이미 적용되던 보너스의 출처를 설명 가능하게 노출한다.

### 43.6 개발용 실제 선수·구단 Identity 표시 (2026-09-07)

- Editor와 Development Build는 설정의 화면 탭에 `선수·구단 실명 + 실제 엠블렘`을 노출하고 최초값을
  ON으로 둔다. 일반 배포 Build는 기능을 강제로 OFF로 두며 해당 옵션을 그리지 않는다.
- 실제 이름은 정규화된 KBO Source ID를 기존 Runtime-safe `PlayerPersonId`/`FranchiseId`와
  `TeamSeasonKey`로 변환한 개발용 카탈로그에서 조회한다. 구단 표시는 최신 Franchise명이 아니라
  원본 연도의 브랜드를 사용한다. 예를 들어 2003년 `kbo-selector:SK`는 `SK 와이번스`, 2021년은
  `SSG 랜더스`로 표시한다. `WorldIdentityRegistry`가 Save에 소유한 가상 이름과 Stable ID는 수정하지
  않으며, 카탈로그에 없는 생성 선수만 기존 World Identity로 되돌아간다.
- 실제 엠블렘은 현행 KBO 10개 구단뿐 아니라 삼미·청보·태평양·현대·해태·MBC·OB·빙그레·쌍방울·
  SK·초기 히어로즈 계보의 원본 연도 브랜드에도 연결한다. 순위표가 구분용 연도 접두사를 붙여도 이를
  제거한 이름으로 같은 실제 엠블렘을 조회한다. 저장된 `EmblemId`의 양수·유일성 계약은 유지하고
  Presentation에서 실제 Identity가 켜졌을 때만 이미지를 덮어쓴다.
- 이 설정은 표시만 바꾸며 경기 입력, DetailedMatchEngine, 일정, 통계, 수상, 카드 경제와 세이브
  결과에는 관여하지 않는다. 선수 커리어 SaveVersion 18부터 `TeamState`와 `PlayerState`가 표시 복원용
  원본 ID를 보존하며, 표시 전환 이벤트는 Player/Owner/NewGame Snapshot을 즉시 갱신한다. 뉴스·경기 후
  서사·은퇴 회고도 저장 원문을 바꾸지 않고 Subject/Player/Team ID로 표시 시점에 같은 Identity를 적용한다.
  폭이 좁은 셀은 연도나 브랜드를 생략하지 않고 공백만 제거한 `2024LG트윈스` 형식을 사용한다.

### 43.7 선수 도감·수집 이력·위시리스트 (2026-09-07)

- 구단주 모드의 `선수` 메뉴에 `선수 도감`과 독립 `위시리스트` Route를 추가했다. 두 화면은
  `SharedGameShellView`, 기존 Owner Skin, `PlayerMiniCardView`, 카드 상세 Popup을 재사용한다.
  선수/카드/수집 현황 Tab, 표시 이름 검색, Origin 구단·연도·연대·Position·Role·Cost·Edition·
  보유 상태 Filter와 Stable ID 동점 정렬을 제공한다.
- 도감의 분모는 `HistoricalBakedContent.PlayerSeasons`와 해당 Save의 실제 `WorldCardCatalog.Cards`다.
  Core25와 현재 리그 참가 연도로 제한하지 않고, SpecialComposite를 Origin Franchise로 취급하지 않는다.
  World History 상세 기록은 전반기·포스트시즌·올스타 행과 구분된 정규시즌 전체 행만 사용한다.
- 구단주 SaveVersion 15는 `CardCollectionHistoryState`와 `WishlistState`를 저장한다. v1~v14는 현재
  Owned Card를 EverAcquired로 이행하고, 이미 사라진 과거 판매 기록은 추정하지 않는다. 현재 Catalog에
  없는 Stable ID는 향후 콘텐츠 복귀를 위해 보존하되 도감 분모와 화면에서는 제외한다.
- 카드 획득 Commit은 보유 갱신, EverAcquired 기록, 정확한 CardId 위시 자동 해제를 한 경계에서
  처리하고 `WasWishlisted` 결과를 공개 연출에 전달한다. 위시 상태는 Scout 후보 수 표시와 Navigation에만
  사용하며 Cost/Edition Weight, Pity, RNG Seed와 추첨 순서를 변경하지 않는다.
- 카드 행은 Virtualized Grid로 표시하며 큰 카드·능력치·기록·획득 경로는 선택된 행에서 한 번만 만든다.
  실제 Production Archive 19,044 Cards / 17,387 PlayerSeasons 계측에서 Index 420.80 ms,
  전체 카드+시즌 Query 432.38 ms, 구단·연도·Edition 복합 Filter 1.91 ms였다. 이 수치는 Editor 배치
  단일 실행값이며 Player Build 목표값으로 간주하지 않는다.
- 구단주 Production Route는 연결됐지만 전체 완료는 아니다. 선수 커리어는 Owner 상태를 숨기는
  읽기 전용 UI 계약만 있으며 동일 World History Seed를 Save/Load 뒤에도 복원하는 Production Route가
  아직 없다. 또한 현행 카드 판매는 중복 수량만 판매하므로 기본 보유 카드가 0장이 되는 정식 경로,
  16:9 실제 조작, Production E2E와 전체 Unity Test Runner 검증을 후속 Gate로 남긴다.

### 43.8 구단주 시즌 마감 연출과 포스트시즌 상태 (2026-09-08)

- **2026-09-11 포스트시즌 관전 연결:** 대진 화면에서 우리 구단의 다음 차전을 관전한다.
  월드의 기존 경기 순서에서 앞선 대진만 자동 진행하고, 우리 구단 경기 직전에 멈춰 정규시즌과 같은
  `OwnerMatchSpectatorSession`에 상세 경기의 Full 이벤트를 전달한다. 경기 확정 후 진행 세션은 닫히며,
  관전 종료 뒤 대진으로 복귀하여 다음 차전을 선택한다. 미진출·탈락 뒤에는 남은 리그를 마감한다.
  대진 카드·시리즈 전적·선승 조건·매치 포인트·탈락 위기를 표시하고 내부 조 ID와 계산 방식은 숨긴다.
  밸런스·경기 순서·저장 DTO는 바꾸지 않는다. 검증 범위는
  [포스트시즌 관전 개선 기록](docs/reports/owner-postseason-watch-20260911.md)을 따른다.
- **포스트시즌 단계별 연출:** 대진 카드는 순차 등장하며, 새로 확정된 우리 구단의 시리즈 승리와
  우승은 해당 경기의 중계 결과 공개 이후 한 번만 표현한다. 준결승 승리는 결승 진출 장면,
  우승은 트로피와 팀원·금색 색종이 장면을 사용한다. 기존 인게임 캐릭터의 2.5D 피규어 화풍을
  기준으로 ImageGen 삽화를 제작하고 팀명·상대·전적은 런타임 텍스트로 표시한다.
  연출 중에도 대진 확인·경기 기록 보기·건너뛰기가 가능하며, 공통 간소화·결과만 설정을 따른다.
  시간·아트 경로는 `UI/OwnerPostseason/Presentation.json`에서 관리한다. 연출은 경기 결과와
  저장 상태를 변경하지 않는다. 검증 범위는 [연출 개선 기록](docs/reports/owner-postseason-ceremony-20260911.md)을 따른다.
- 구단주 시즌 마감은 `페넌트레이스 최종 보고 → 포스트시즌 → 시즌 결산 → 다음 시즌`의 네 단계다.
  정규시즌 결과만 확정된 상태에서 다음 시즌으로 바로 넘어갈 수 없으며, 모든 조의 포스트시즌 우승팀이
  확정되고 플레이어가 결산을 확인한 뒤에만 홈 대표 행동을 `다음 시즌`으로 바꾼다.
- 각 조는 정규시즌 순위의 승률·득실차·영속 `TeamSeasonKey` 동점 규칙을 그대로 사용한다. 4팀 이상인
  조는 상위 4팀의 1-4/2-3 준결승 뒤 챔피언십을 진행하고, 2~3팀 조는 상위 2팀 챔피언십으로 축소한다.
  시리즈 경기 수와 홈 배정은 기존 `PostseasonBalance`와 `PostseasonBracket` 계약을 재사용한다.
- 포스트시즌 경기는 별도 결과 추첨이나 경량 공식을 만들지 않는다. 정규시즌과 같은 현재 25인 로스터,
  감독 방침, 팀컬러, 컨디션, 결정론적 RNG를 `DetailedMatchEngine`에 입력하고 무승부 없는 규칙만 적용한다.
  선수 기록은 `CompetitionScope.Postseason`에 분리해 누적한다.
- UI는 기존 Owner Shell의 색상·버튼·Popup Host를 유지한다. 하나의 시즌 리뷰 Popup에서 세 Tab을
  순서대로 공개하며, 정규시즌 순위·승무패·득실차, 시리즈별 승수, 우승 구단, 승격·잔류·강등 예정 등급을
  실제 저장 상태에서 읽는다. 포스트시즌 자동 진행은 메인 스레드 한 Frame당 상세 경기 한 건만 확정하고
  중단 시 완료 경기까지 유지한다.
- SaveVersion 18은 현재 및 완료된 모든 조의 포스트시즌 시드, 시리즈, 경기 Seed·점수·승수와
  포스트시즌 선수 기록을 저장한다. v1~v17은 정규시즌 상태를 보존한 채 포스트시즌을 아직 시작하지 않은
  상태로 복원하며, 완료된 결과를 추정하거나 합성하지 않는다.
- 이 변경은 경기 확률과 밸런스 수치를 바꾸지 않았다. 구현·검증 내역과 HTML/UI 시안 경로는
  `docs/design/owner-season-presentation/implementation.md`를 따른다.

### 덕아웃 작전 방침 선택 범위 (2026-09-06)

구단주 모드의 여섯 작전 방침은 감독 신뢰도와 관계없이 -2/-1/중립/+1/+2를 모두 허용한다.
감독 교체 직후에도 같은 범위를 유지하며 신뢰도 60의 ±2 해금 조건은 폐기한다.
확정한 방침은 기존 `DugoutTacticalProfileResolver`의 단계당 5 보정으로 합성해
`ManagerModeMatchService` → `MatchRosterSnapshot.ManagerProfile` → `DetailedMatchEngine`에 전달한다.
±2는 중립 대비 해당 감독 판단값을 ±10 조정하며 타격 접근·도루·번트·대타·선발 및 불펜 교체
판단에 적용한다. 선수 능력치를 직접 올리거나 특정 경기 결과를 보장하지 않는다.
검증 결과와 재현 명령은 `docs/reports/dugout-policy-range-validation.md`에 기록한다.

### 구단주 전체 구단 승강과 시즌별 조 재편성 (2026-09-07)

- 구단주 모드는 전체 Baked TeamSeason을 영속 구단으로 등록한다. 기존 첫 시즌의 플레이어 조는
  유지하고 나머지 연도별 정규 구단은 Rookie 조로 편성한다. 기존 조의 합성 참가팀도 구단 단위로
  유지하며, 추가 정규 구단의 로스터는 해당 TeamSeason의 Core25만 사용한다.
- 새 시즌에는 모든 조의 정규시즌 결과를 먼저 확정하고 아래의 등급별 순위표를 적용한다.
  Rookie 강등과 Galaxy 일반 승격은 없다. 플레이어와 AI에 같은 규칙을
  적용하며, 플레이어 승률 60%/40%에 따라 기존 참가팀 전체의 등급만 바꾸던 경로를 대체한다.
- 순위는 승률, 득실차, 영속 TeamSeasonKey 순으로 확정한다. 화면도 같은 Resolver를 사용한다.
  선수 커리어 모드의 별도 승강 결정전 계약은 변경하지 않는다.
- 승강 후 같은 등급의 전체 구단을 모아 World Seed·다음 시즌 번호·등급에서 파생한 RNG로
  재추첨한다. 조당 목표 10팀이며 남는 구단은 조 사이에 균등 배분한다. 구단의 ID·로스터·선수
  상태는 유지하고 상대 구단과 일정이 바뀐다. 추첨이므로 일부 이전 상대와 다시 만날 수 있다.
- 모든 조는 항상 목표 구단 수(10팀)로 시즌을 치른다. 추첨한 실제 구단이 모자라면 빈 자리를
  **CPU 임시 구단**으로 채운다(2026-09-11). 임시 구단은 순위표·일정·포스트시즌에 포함되지만 그 시즌에만
  존재하며 승강하지 않는다. 시즌이 끝나면 로스터·컨디션·친밀도를 버리고 다음 시즌 재편성 때 새로 만든다.
  지난 시즌 순위표·기록은 `FILLER:` TeamSeasonKey에서 이름을 복원한다. UI에는 CPU라는 사실을 표시하지 않고
  다른 참가 구단과 같은 형식으로 보여준다 — 연도 구단 덱은 원본 구단명, 특수 덱은 `레전드 선발팀` 같은
  덱 이름을 쓴다. 첫 시즌 플레이어 조의 특수 합성팀도
  같은 임시 참가팀으로 취급해 두 번째 시즌부터 월드에서 빠진다. 선수 ID 원장은 과거 기록이 참조하므로 유지한다.
- 승강은 **순위표에 보이는 순위 그대로** 판정한다. CPU 임시 구단이 승격 순위를 차지하면 그 승격 자리는
  사라지고 실제 구단에게 넘어가지 않는다. 한 구단뿐인 등급도 CPU로 채워 경기가 성립하므로
  기존의 "새 등급 단독 진입 시 잔류" 예외는 폐기했다.
- 임시 구단 덱은 등급별 `rankRules[].fillerDeck`에서 정한다. 기본값은 Rookie~Major 단일 연도 구단,
  World·AllStar EX, Classic·Winners 올스타, Champion MVP, Master 커리어하이, Galaxy 레전드다.
  단일 연도 구단 덱은 한 역사 구단의 Normal 카드를 Cost 순으로 뽑는다. 특수 카드 풀은 포지션이 편중되어
  있어(EX 포수 0명) 선발 야수는 원래 포지션 선수만 받고, 투수는 보직이 달라도 같은 Edition을 우선한다.
  구성 규칙은 `LeagueFillerDeckBuilder`에 있다.
- 25명을 고른 뒤 역할을 다시 배치한다. 스타를 예산 안에서 교체로 끼워 넣기만 하면 EX 선발 투수가 불펜에,
  강한 타자가 벤치에 남는다. 그래서 교체는 선발 라인업·로테이션 → 마무리·셋업 → 불펜 → 벤치 순으로 하고,
  마지막에 25명 안에서 중요한 자리부터 적합도·Cost 순으로 역할을 다시 정한다. 지명타자는 수비를 보지 않으므로
  전업 DH 우대 없이 남은 타자 중 가장 좋은 타자를 세운다.
- 조 크기는 `OwnerExpansionBalance.json`의 `leaguePromotion.groupTeamCount`, 등급별 승강 순위와
  목적지·임시 구단 덱은 `leaguePromotion.rankRules`에서 조정한다. 공통 상하위 2팀 규칙은 폐기한다.
- 구단주 SaveVersion 24는 조마다 `fillerTeamSeasonKeys`를 저장한다. v23 이하 저장본은 임시 구단 없는 조로
  복원하고, 다음 시즌 전환부터 새 규칙을 적용한다.
- 특수 덱은 해당 Edition만으로 채우면 평균 Cost가 8.3~9.9로, 실제 구단 Core25(중앙 5.84·최고 7.20)보다
  강해 상위 등급 순위표를 CPU가 독점한다. 그래서 등급마다 `rankRules[].fillerTargetCost`(25인 평균 Cost 상한)를
  두고, 목표보다 `fillerStarCostMargin`만큼 약한 연도 구단을 바탕으로 상한 안에서 해당 Edition 스타를
  약한 자리부터 채워 넣는다. 덱 이름값은 스타로 살리고 전체 강도는 등급 목표에 맞춘다.
- 목표 Cost는 상세 경기로 보정했다. 실제 구단 363개를 Cost 분포에서 고르게 뽑은 기준 구단 40개와 붙여
  강도 백분위를 만들고, CPU 덱의 기준풀 승률이 등급별 목표 백분위(World p50·AllStar p60·Classic p70·
  Winners p80·Champion p85·Master p90·Galaxy p95)와 같아지는 값을 골랐다. 확정값은 World 5.70·AllStar 5.75·
  Classic 5.85·Winners 6.10·Champion 6.20·Master 6.15·Galaxy 6.50이며, 커리어하이·레전드는 카드 능력치
  보정이 붙어 같은 Cost에서 더 강하므로 Master의 목표 Cost가 Champion보다 낮다. 독립 Seed 검증(등급별
  덱 200개 × 각 400경기)에서 승률은 목표 대비 ±0.012 안이다 — World 0.517/0.505, Champion 0.625/0.618,
  Galaxy 0.705/0.703.
  덱 하나하나는 바탕 구단 추첨 때문에 p10~p90이 약 ±0.07 흔들린다 — 조마다 CPU 강도가 다른 것은 의도다.
  재현: `Tools/SimulationDiagnostics`의 역사 전력 진단과 같은 경로를 쓰는 스크래치 러너(1982~2025 아카이브,
  올스타·MVP 카드는 원본 KBO 수상으로 근사).
  모든 조의 경기는 공통 경기 규칙과 AI 팀컬러·전술·상태 반영 경로를 사용하고,
  플레이어 조의 순위·개인 기록은 해당 조의 경기만 집계한다. 다른 조의 투구 계산 해상도는
  아래 간이 타석 계약을 따른다.

### 구단주 다른 조 간이 타석 시뮬레이션

- 사용자 승인으로 전체 역사 구단의 승강·강등은 유지하되, 플레이어가 속하지 않은 조에만
  `MatchExecutionProfile.AggregateBackground`를 적용한다. 같은 등급의 다른 조도 간이 대상이다.
  플레이어 조의 AI 대 AI 경기와 포스트시즌은 상세 경로를 유지하며, 다른 조의 포스트시즌은 간이 경로다.
- 간이 경로는 볼넷·삼진·사구·공정 타구와 추정 투구 소모량을 타석마다 직접 추첨한다.
  투구 선택·궤적·스윙 루프는 생략한다. 로스터·능력치·컨디션·팀컬러·전술 보정,
  투수 교체·수비·주루·이닝 종료·BoxScore·배터리/피로/시즌 기록 집계는 기존 경로를 공유한다.
- 간이 계수는 `OwnerExpansionBalance.json`의 `aggregateMatch`에서 저작하고 `BalanceTable`로 주입한다.
  상세 투구 모델의 밸런스는 바꾸지 않는다. 개별 구종 배합과 투구별 선택의 효과까지 재현하는 모델은 아니다.
- 결정론의 입력은 Seed·로스터·상태·밸런스·실행 프로필이다. 상세와 간이의 경기별 점수가 일치할 필요는
  없으며 각 프로필 내부의 재실행과 저장 후 재개가 같아야 한다. 간이 결과 입력에는 `AggregateV1`을 표시한다.
  완료 경기는 재계산하지 않고 다음 시즌 조 재편성 뒤 새 소속 조에 맞춰 해상도를 선택한다.
- 저장 구조를 바꾸지 않는다. 기존 완료 경기의 점수·기록은 보존하고 업데이트 이후 미완료 다른 조 경기부터
  간이 경로를 사용한다. 선수 커리어와 새 게임 이전 역사 베이크에는 이 정책을 적용하지 않는다.
- 시즌 진행은 한 경기 완료가 중단 안전 지점이다. 화면은 8ms 시간 예산 내에서 진행하며 최대 256 Step으로
  비정상 반복을 제한한다. 한 경기 내부를 선점하는 방식이 아니므로 단일 경기 지연의 엄격한 상한은 아니다.
- 대량 비교 및 전체 월드 실측과 남은 제한은 `docs/reports/owner-aggregate-simulation.md`에 기록한다.
- 경기 서비스는 동일 로스터·카드 카탈로그의 AI 기본 라인업과 팀컬러 보너스를 재사용한다.
  로스터 또는 카탈로그 교체 시 다시 계산하며 컨디션·피로·배터리·선수 능력치는 경기마다 반영한다.
  시즌 진행 표시의 내 구단 성적·다음 경기는 내 경기 완료 때 갱신한다. 포스트시즌도 정규시즌과
  같은 8ms·최대 256 Step 예산을 사용한다. 이 최적화의 속도 배율은 별도로 측정하지 않았다.

### 구단주 승강 저장 및 등급별 순위표

- SaveVersion 16은 다른 조의 일정·성적·로스터, 전체 조의 시즌별 마감 이력과 영속 선수 ID
  원장을 저장한다. v1~v15는 현재 진행 중인 플레이어 조·등급·성적을 유지하여 월드를 확장한다.
  선수 ID 원장은 재편성 뒤에도 이전 기록의 선수 식별자를 다시 배정하지 않는다.
- 사용자 지시에 따라 테스트 작성·실행과 대량 시뮬레이션은 생략한다. 컴파일 확인과 실제
  시즌 전환·결정론·저장 왕복·대량 진행 성능 검증을 구분하며, 후자는 미검증 상태다.

10팀 조 기준 사용자 확정 순위표:

| 등급 | 승격 | 잔류 | 강등 |
|---|---|---|---|
| 루키 | 1~6위 | 7~10위 | 없음 |
| 마이너 | 1~6위 | 7~8위 | 9~10위 |
| 메이저 | 1~6위 | 7~8위 | 9~10위 |
| 월드 | 1~4위 | 5~8위 | 9~10위 |
| 올스타 | 1~4위 | 5~7위 | 8~10위 |
| 클래식 | 1~4위 | 5~6위 | 7~10위 |
| 위너스 | 1~4위 | 5~6위 | 7~10위 |
| 챔피언 | 1~4위 → 마스터 | 5~6위 | 7~10위 → 위너스 |
| 마스터 | 1~4위 → 갤럭시 | 5~8위 | 9~10위 → 챔피언 |
| 갤럭시 | 일반 승격 없음 | 1~6위 | 7~10위 → 마스터 |

사용자 확정에 따라 챔피언 챌린지는 두지 않는다. 챔피언 1~4위는 별도 조건 없이 마스터로
자동 승격하며, 마스터 9~10위는 챔피언으로 강등된다.

### 공통 경기 성적 재현 검증 계약

실제 선수 기록은 카드 능력치·구종·로스터를 만드는 근거이며, 실제 구단 승률·득점·ERA를
경기 중 확률이나 전력에 직접 더하지 않는다. 구단명·연도별 분기와 목표 순위를 보장하는 보정을
금지한다. 실제 팀 성적은 경기 실행이 끝난 뒤 외부 검증기에서 비교하는 정답 데이터로만 사용한다.

공통 상세 투구·타격 계수와 도루 시도 빈도는 `Resources/NewGame/MiniGameBalance.json`에서
저작하며 선수·구단주 모드에 동일하게 주입하고 콘텐츠 해시에 포함한다. 3볼에서는 승부 회피를
제외한 기본 투구 접근법보다 카운트별 존 공략을 우선한다. 2아웃 도루도 기대값으로 평가하고,
다른 주자를 보존한 성공·실패 상태로 계산한다. 미세한 기대이득이 매번 도루를 일으키지 않도록
기대이득을 시도 빈도로 변환하며 주루 난수는 기존 주입 스트림을 사용한다.

통과 기준은 단일 능력치 반응, 공통 리그 통계, 상세/간이 비교, 반복 역사 시즌을 구분한다.
실제 선두의 평균 1~3위만 맞아도 완료가 아니다. 승률 차이와 투타 지표를 함께 확인한다.
추가 검증을 거친 구간별 경기 능력치 곡선과 투타 계수를 생산용 JSON·Core 기본값에 함께 반영한다.
저능력 구간의 동급 대결과 고능력 구간의 강화·성장 여유를 보존하며, 투구 품질의 타구 억제와
타자 교타의 타구 품질 기여를 분리한다. 구단·연도별 계수와 실제 승률 보정은 추가하지 않는다.
역사 시즌 타순은 수비 배치를 보존한 공통 감독 AI가 편성한다. 고정 5선발과 관측 카드 원본은 유지한다.
불펜 교체는 실제 다음 후보와 현재 투수의 피로 반영 구위를 비교하고 약한 불펜으로 바꾸는 비용도 평가한다.
2스트라이크 유인구 판단은 존 공략·땅볼 유도에서도 생략하지 않는다.
엔진 로직 변경도 역사 베이크 무효화 조건이며, 베이크 키에 엔진 버전을 포함한다.
일부 역사 팀의 과대·과소평가와 볼넷·삼진 성향 재현은 남아 있어 전체 재현 완료로 보고하지 않는다.
이전 검증은 `docs/reports/common-match-performance.md`, 추가 개선의 반복 시즌·독립 검증과
잔여 사항은 `docs/reports/historical-strength-continuation/README.md`를 따른다.

### 43.9 구단주 선수 수집 경제: 연도 구단 1군 25인 3~4시즌 (2026-09-11)

목표는 원하는 연도 구단의 1군 25인(Core25)을 정밀 Scout로 모아 덱을 돌리는 데 **중앙값 3~4시즌**이다.
강화(+5)·유학·스킬 블록·커리어하이·레전드는 이 목표에 넣지 않고 장기 목표로 둔다.
이전 규칙은 같은 측정에서 중앙값 32시즌(p90 61시즌)이라 사실상 도달할 수 없었다. 원인은 세 가지다.
정밀 풀 약 55명 중 비주전 예비 선수가 가중치 높은 Cost 2~4 버킷을 차지했고, 쿠폰 수집의 마지막 몇 장이
기간 대부분을 차지했으며, SP 수입이 시설 주간 생산(수동 결산)뿐이었다.

- **정밀 Scout(구단·연도)는 그 해 1군 25인만 후보로 둔다** (`ScoutRosterScope.ActiveRoster`).
  1군 명단은 `WorldCardCatalogBuilder`가 `TeamSeasonDefinition.Core25CardIds`로 만든다. 예비 선수는
  구단 집중·연도 집중·일반 Scout에서 계속 나온다. 가격 240 SP와 Cost·Edition 가중치는 그대로다.
- **Pity 게이지는 Scout에 쓴 SP만큼 찬다** (임계치 2,400 = 정밀 Scout 10회). 싼 일반 Scout로 게이지만
  채워 확정 영입하는 우회로가 정밀 Scout보다 싸지지 않게 하려는 규칙이다.
- **보장 영입**은 정밀 Scout 상품 옆에 게이지 재화(`ShopCurrency.ScoutPity`)로 진열한다. 미보유 Cost 7+
  → 미보유 1군 → Cost 7+ 순으로 처음 비지 않은 단계에서 균등 추첨한다. 문서에만 있고 소비 경로가 없던
  기존 `RollFocused`는 이 규칙(`ScoutRoller.RollGuaranteed`)으로 대체했다.
- **SP는 플레이어 구단 경기 완료마다 지급한다** (경기 15 SP + 승리 10 SP, 정규시즌·포스트시즌 공통).
  경기 완료와 같은 트랜잭션이라 반복 조작으로 늘릴 수 없다. 시작 SP는 10,000에서 3,000으로 줄였다.
  새 게임이 25인 로스터를 이미 지급하므로 시작 SP는 팀 구성 필수 재원이 아니다.
- 계수는 `OwnerExpansionBalance.json`의 `scoutEconomy` 구획에 있다(`ScoutEconomyBalance`).

실제 추첨 코드와 1982~2025 전 구단 연도 363팀 × 30회 측정
(`Tools/HistoricalSeasonDiagnostics --scout-collection`, 모든 SP를 목표 구단 정밀 Scout에 쓰는 가정):

| 조건 | p10 | 중앙값 | p90 | 가장 느린 팀 평균 |
|---|---|---|---|---|
| 새 규칙, 시설 SP 없음, 승률 .500 | 3.1 | 4.0 | 5.6 | 6.5 |
| 새 규칙, 시설 최단 업그레이드, 승률 .500 | 2.5 | 3.2 | 4.4 | 4.9 |
| 새 규칙, 시설 없음, 승률 .400 / .600 | 3.3 / 3.0 | 4.2 / 3.8 | 5.9 / 5.4 | 6.9 / 6.0 |
| 이전 규칙(시설 최단 업그레이드) | 17.9 | 32.4 | 60.7 | 85.6 |

정밀 Scout 필요 횟수는 중앙 60회, p90 80회다(이전 223회 / 393회). 시즌당 주차는 주 6경기 24주로 환산했다.
주간 결산은 여전히 경기 진행과 묶이지 않아 반복하면 시설 SP를 더 받을 수 있다. 이 문제는 별도로 다룬다.

### 43.10 타격·투수 균형과 역사 연도 재현 검증 (2026-09-12)

선수 Cost·원기록·카드 능력치는 유지하고 공통 경기 엔진을 v9로 올렸다. 늦은 스윙의 시간 상한
오류와 상세 Contact 보정 누락을 수정하고, Contact 중복 효과·구위의 스윙 위치 효과·승부회피 AI를
재조정했다. 역사 베이크와 구단주 경기는 원기록 기반 투수 용량·회복 배율 계산을 공유한다.
간이 경기는 기존 적용 범위를 유지하면서 Contact/Stuff 삼진 민감도를 상세 경로에 맞췄다.

44연도×8시드 212,636경기에서 연도별 평균 실제 대비 오차는 AVG .00950, ERA .484이며,
352시즌 규정타석 4할은 총 5명이었다. 구단주 실제 월드 26,640경기 및 상세/간이 각28,000경기도
검증했다. 일부 시대의 ERA·삼진·득점 재현과 플레이어 조의 상대적 고득점은 잔여 항목이며,
역사 모든 연도의 재현 완료를 뜻하지 않는다. 기존 누적 기록은 소급 수정하지 않고 베이크는 재실행한다.
최종 수치·테스트 범위·재현 명령은 `docs/reports/pitch-batting-balance-20260912.md`를 따른다.

### 43.11 불펜 휴식 판단과 역대 구단 순위의 잔여 문제 (2026-09-12)

경기 엔진 v14는 최근 투구량이 일률적 상한 미만이어도, 회복력과 투구 용량 대비 부담이 크면
회복된 대안을 먼저 기용한다. 불펜 후보 수와 보직별 선택·일반 선택이 같은 기준을 사용하며
전원 소진 시 비상 등판을 허용한다. 상세·간이는 공통 규칙을 사용한다.
카드 능력치·Cost·경기 밸런스 JSON·구위와 제구의 피로 계산은 유지한다.

44연도×16회 비교에서 역사 승률 오차는 4.237→4.082%p, 순위 오차는 1.149→1.077위로
줄고, 3계단 이상 차이 나는 구단은 47→44로 줄었다. **역사 순위 재현 완료로 간주하지 않는다.**
연습경기는 매 경기 회복 상태여서 v14 전량 재베이크 후 순위가 동일하다. 현재 편성에는 Rare 승격과
다른 연도 특수 카드가 포함되므로 원 시즌 성적과 평가 대상이 다르다. Normal만 사용한 별도 진단은
실제 승률 상관이 .556→.648로 높아졌으나 큰 괴리가 남아 있으며 게임에 적용하지 않았다.
검증 실패·잔여 문제·363구단 비교는 `docs/reports/historical-ranking-20260912.md`를 따른다.
